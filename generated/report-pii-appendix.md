## Appendix: Detailed PII Detection Results

*Generated on 2026-07-27 13:15:23*

This appendix lists all detected instances of potential personally identifiable information (PII) in the project files. Each entry shows the matched PII terms and, for data files, sample values to help verify whether the flagged content is indeed sensitive.

### Data Files

**/replication-package/replication_delivery/code/fortran_model/1_baseline/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/1_baseline/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/1_baseline/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/1_baseline/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/1_baseline/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11035276789142329, 0.117926869740084

**/replication-package/replication_delivery/code/fortran_model/2_rigid/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/2_rigid/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/2_rigid/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/2_rigid/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11036688834854852, 0.11794013541262278

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11036516759193687, 0.1179383808852798

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.1103663877348894, 0.1179396228650967

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11036702470283642, 0.11794036444411302

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11508216633584592, 0.11744876635277744

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.18227907355025896, 0.17816863391992896

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.18196582442331696, 0.17796108978960937

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.1103605627885056, 0.11838954263407736

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.12326677809973548

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13880510626657683

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13880510626657683

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13889279650416805

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13838352523704145

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13880360130153904

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.13880360130153904

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.18260161755845225

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.1843452817415868

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.10996804142521414, 0.11779588643567428, 0.18447737620707275

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11037395012783811, 0.1179467694247166

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11038101294093404, 0.1179534042205594

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.11039514166974834, 0.1179666761644634

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.17673034784290434, 0.17466083987169545

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.17672903069292095, 0.17465937373262808

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.1767299637159375, 0.1746604108780709

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/result/eqbm_dist_contribution.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/result/eqbm_dist_hires.txt**

- Variable: `capacity`
  - Matched terms: city
  - Sample values: 17.0, 16.0
- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/result/eqbm_dist_wages.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/result/eqbm_omegas.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/result/state_burden.txt**

- Variable: `rewarded_district`
  - Matched terms: district
  - Sample values: 0.17673049255648066, 0.17466106130938985

**/replication-package/replication_delivery/code/fortran_model/estimation_result/result/omega2_probs.txt**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/out_synth/tables/Table_6.csv**

- Variable: `subset_name`
  - Matched terms: name
  - Sample values: All Districts, 4th quartile, 3rd quartile

**/replication-package/replication_delivery/out_synth/tables/Table_B14.csv**

- Variable: `district`
  - Matched terms: district
  - Sample values: .%, 0.00%

**/replication-package/replication_delivery/out_synth/tables/Table_B22.csv**

- Variable: `district_group`
  - Matched terms: district
  - Sample values: lambda_d, budget_capacity, Urban

**/replication-package/replication_delivery/out_synth/tables/Table_B25.csv**

- Variable: `district_group`
  - Matched terms: district
  - Sample values: lambda_d, budget_capacity, Urban

**/replication-package/replication_delivery/real_data/cesa.dta**

- Variable: `district_code`
  - Matched terms: district
  - Sample values: 7.0, 14.0, 63.0
- Variable: `district_name`
  - Matched terms: district, name
  - Sample values: Abbotsford, Adams-Friendship Area, Albany

**/replication-package/replication_delivery/real_data/crosswalk_census_state_id.csv**

- Variable: `District Name`
  - Matched terms: district, name
  - Sample values: 21st Century Preparatory School Agency, Abbotsford School District, Adams-Friendship Area School District
- Variable: `district_code`
  - Matched terms: district
  - Sample values: WI-8110, WI-0007, WI-0014

**/replication-package/replication_delivery/real_data/crosswalk_district_county.dta**

- Variable: `district_code` (label: *Code of working agency (contract-specific)*)
  - Matched terms: district
  - Sample values: 14.0, 2205.0, 4543.0

**/replication-package/replication_delivery/real_data/cz_id.csv**

- Variable: `district_id`
  - Matched terms: district
  - Sample values: 1, 2, 3

**/replication-package/replication_delivery/real_data/district_county_cz.dta**

- Variable: `district_code` (label: *State District ID*)
  - Matched terms: district
  - Sample values: 7.0, 14.0, 63.0

**/replication-package/replication_delivery/real_data/district_id_tag.dta**

- Variable: `district_code` (label: *District ID*)
  - Matched terms: district
  - Sample values: 7.0, 14.0, 63.0

**/replication-package/replication_delivery/real_data/district_id_tag_validation.dta**

- Variable: `district_code` (label: *group(group_by_value)*)
  - Matched terms: district
  - Sample values: 7.0, 14.0, 63.0

**/replication-package/replication_delivery/real_data/district_shapefiles/WI_School_Districts.dbf**

- Variable: `DISTRICT`
  - Matched terms: district
  - Sample values: Abbotsford, Adams-Friendship Area, Albany

**/replication-package/replication_delivery/real_data/district_shapefiles/WI_School_Districts.dta**

- Variable: `DISTRICT` (label: *DISTRICT*)
  - Matched terms: district
  - Sample values: Abbotsford, Adams-Friendship Area, Albany
- Variable: `_CX` (label: *x-coordinate of area centroid*)
  - Matched terms: coord
  - Sample values: 496985.765441264, 537080.3866100273, 565148.5526962501
- Variable: `_CY` (label: *y-coordinate of area centroid*)
  - Matched terms: coord
  - Sample values: 498673.79409441777, 389046.5681601551, 250543.04439153816

**/replication-package/replication_delivery/real_data/nces_acs_2013-2017/EDGE_Export_101244429626/ACS-ED_2013-2017_RecordLayouts.xlsx**

- Variable: `Variable name`
  - Matched terms: name
  - Sample values: GeoId, Geography, LEAID

**/replication-package/replication_delivery/real_data/nces_acs_2013-2017/EDGE_Export_101244639981/ACS-ED_2013-2017_RecordLayouts.xlsx**

- Variable: `Variable name`
  - Matched terms: name
  - Sample values: GeoId, Geography, LEAID

**/replication-package/replication_delivery/real_data/nces_acs_2013-2017/EDGE_Export_1012447445/ACS-ED_2013-2017_RecordLayouts.xlsx**

- Variable: `Variable name`
  - Matched terms: name
  - Sample values: GeoId, Geography, LEAID

**/replication-package/replication_delivery/real_data/nces_acs_2013-2017/EDGE_Export_101253245819/ACS-ED_2013-2017_RecordLayouts.xlsx**

- Variable: `Variable name`
  - Matched terms: name
  - Sample values: GeoId, Geography, LEAID

**/replication-package/replication_delivery/synthetic_data/documentation/synthetic_data_manifest.csv**

- Variable: `relative_file_path`
  - Matched terms: lat
  - Sample values: raw/staff/06staff.txt, raw/staff/07staff.txt, raw/staff/08staff.txt

**/replication-package/replication_delivery/synthetic_data/raw/staff/15staff.csv**

- Variable: `AdminName`
  - Matched terms: name
  - Sample values: Synthetic Admin
- Variable: `BirthYear`
  - Matched terms: birth
  - Sample values: 1960, 1961, 1962
- Variable: `CntyName`
  - Matched terms: name
  - Sample values: Synthetic County 03, Synthetic County 04, Synthetic County 05
- Variable: `FirstName`
  - Matched terms: name
  - Sample values: P0002-Giv X, P0003-Giv X, P0004-Giv X
- Variable: `HighDegree`
  - Matched terms: degree
  - Sample values: X, 7, 4
- Variable: `LastName`
  - Matched terms: name
  - Sample values: S0002-Unit X, S0003-Unit X, S0004-Unit X
- Variable: `LocalExp`
  - Matched terms: loc
  - Sample values: 10, 14, 18
- Variable: `MailCity`
  - Matched terms: city
  - Sample values: Synthetic City
- Variable: `MailZipCd`
  - Matched terms: zip
  - Sample values: ZIP, 53102, 53103
- Variable: `Phone`
  - Matched terms: phone
  - Sample values: 000-000-0000
- Variable: `SchoolCd`
  - Matched terms: school
  - Sample values: 1902, 1903, 1904
- Variable: `SchoolMailingAddress1`
  - Matched terms: address, school
  - Sample values: Synthetic Address
- Variable: `SchoolMailingAddress2`
  - Matched terms: address, school
- Variable: `SchoolName`
  - Matched terms: lname, name, school
  - Sample values: Synthetic School 1902, Synthetic School 1903, Synthetic School 1904
- Variable: `SchoolShippingAddress1`
  - Matched terms: address, school
  - Sample values: Synthetic Address
- Variable: `SchoolShippingAddress2`
  - Matched terms: address, school
- Variable: `ShipCity`
  - Matched terms: city
  - Sample values: Synthetic City
- Variable: `ShipZipCd`
  - Matched terms: zip
  - Sample values: ZIP, 53102, 53103
- Variable: `WorkLocationName`
  - Matched terms: loc, location, name
  - Sample values: Synthetic District 0422, Synthetic District 0665, Synthetic District 2793

**/replication-package/replication_delivery/synthetic_data/raw/staff/16staff.csv**

- Variable: `AdminName`
  - Matched terms: name
  - Sample values: Synthetic Admin
- Variable: `BirthYear`
  - Matched terms: birth
  - Sample values: 1960, 1961, 1962
- Variable: `CntyName`
  - Matched terms: name
  - Sample values: Synthetic County 03, Synthetic County 04, Synthetic County 05
- Variable: `FirstName`
  - Matched terms: name
  - Sample values: P0002-Giv X, P0003-Giv X, P0004-Giv X
- Variable: `HighDegree`
  - Matched terms: degree
  - Sample values: X, 7, 4
- Variable: `LastName`
  - Matched terms: name
  - Sample values: S0002-Unit X, S0003-Unit X, S0004-Unit X
- Variable: `LocalExp`
  - Matched terms: loc
  - Sample values: 11, 15, 19
- Variable: `MailCity`
  - Matched terms: city
  - Sample values: Synthetic City
- Variable: `MailZipCd`
  - Matched terms: zip
  - Sample values: ZIP, 53102, 53103
- Variable: `Phone`
  - Matched terms: phone
  - Sample values: 000-000-0000
- Variable: `SchoolCd`
  - Matched terms: school
  - Sample values: 1902, 1903, 1904
- Variable: `SchoolMailingAddress1`
  - Matched terms: address, school
  - Sample values: Synthetic Address
- Variable: `SchoolMailingAddress2`
  - Matched terms: address, school
- Variable: `SchoolName`
  - Matched terms: lname, name, school
  - Sample values: Synthetic School 1902, Synthetic School 1903, Synthetic School 1904
- Variable: `SchoolShippingAddress1`
  - Matched terms: address, school
  - Sample values: Synthetic Address
- Variable: `SchoolShippingAddress2`
  - Matched terms: address, school
- Variable: `ShipCity`
  - Matched terms: city
  - Sample values: Synthetic City
- Variable: `ShipZipCd`
  - Matched terms: zip
  - Sample values: ZIP, 53102, 53103
- Variable: `WorkLocationName`
  - Matched terms: loc, location, name
  - Sample values: Synthetic District 0422, Synthetic District 0665, Synthetic District 2793

**/replication-package/replication_delivery/synthetic_data/raw/staff/temp.dta**

- Variable: `birth`
  - Matched terms: birth
  - Sample values: 1960.0, 1961.0, 1962.0
- Variable: `district_code`
  - Matched terms: district
  - Sample values: 413.0, 422.0, 665.0
- Variable: `firstname`
  - Matched terms: name
  - Sample values: P1001-Giv X, P0002-Giv X, P0003-Giv X
- Variable: `gender`
  - Matched terms: gender
  - Sample values: M, F
- Variable: `lastname`
  - Matched terms: name
  - Sample values: S11001-Unit X, S0002-Unit X, S0003-Unit X

**/replication-package/replication_delivery/synthetic_data/raw/teacher_data_linked/Linked_Staffing_Data_10202015.csv**

- Variable: `birth_year`
  - Matched terms: birth
  - Sample values: NA, 1960, 1961
- Variable: `first_name`
  - Matched terms: first_name, name
  - Sample values: P1001-Giv X, P0002-Giv X, P0003-Giv X
- Variable: `last_name`
  - Matched terms: last_name, name
  - Sample values: S11001-Unit X, S0002-Unit X, S0003-Unit X
- Variable: `sex`
  - Matched terms: sex
  - Sample values: M, F

**/replication-package/replication_delivery/synthetic_data/raw/testscores_indlev/student_demographics.csv**

- Variable: `GENDER`
  - Matched terms: gender
  - Sample values: M, F
- Variable: `RACE_ETHNICITY`
  - Matched terms: city
  - Sample values: Black, Hispanic, Asian
- Variable: `SCHOOL_YEAR`
  - Matched terms: school
  - Sample values: 2005-2006, 2006-2007, 2007-2008

**/replication-package/replication_delivery/synthetic_data/raw/testscores_indlev/testscores.csv**

- Variable: `SCHOOL_YEAR`
  - Matched terms: school
  - Sample values: 2005-2006, 2006-2007, 2007-2008
- Variable: `TEST_NAME`
  - Matched terms: name
  - Sample values: SYNTEST

### Code Files

**/replication-package/replication_delivery/code/Figure_B9/Figure_B9.do**

- Line 10: district
  ```
  sepscatter c2 c1, separate(d_dist1_1) scheme(plotplain) ytitle("C2") xtitle("C1") title("District Ma
  ```
- Line 14: district
  ```
  sepscatter c2 c1, separate(d_dist1_2) scheme(plotplain) ytitle("C2") xtitle("C1") title("District Ma
  ```
- Line 18: district
  ```
  sepscatter c2 c1, separate(d_dist1_3) scheme(plotplain) ytitle("C2") xtitle("C1") title("District Ma
  ```

**/replication-package/replication_delivery/code/Figure_B9/cutoff_prefs_id_example_graph.R**

- Line 5: lat
  ```
  # This is a simulation exercise to illustrate part of the ID argument
  ```
- Line 12: loc
  ```
  # Locate the replication root without changing the caller's working directory.
  ```
- Line 13: loc, location
  ```
  # Under Rscript, use this file's location. When sourced interactively, accept
  ```
- Line 24: name
  ```
  file.path(dirname(script_path), "..", ".."),
  ```
- Line 46: loc
  ```
  "Could not locate the replication root. Run this script with Rscript ",
  ```
- Line 72: name
  ```
  if (!requireNamespace(pkg, quietly = TRUE)) {
  ```
- Line 91: name
  ```
  names(df) <- c('c1','c2')
  ```
- Line 93: district
  ```
  #  Generate district 1 prefs over teachers:
  ```
- Line 99: district
  ```
  ## Districts have no shock, just a cutoff, teachers have shock   -----------
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/aux_model_teachers_wages.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 22: district
  ```
  * Share of districts at each support point of omega1 / alpha0.
  ```
- Line 24: district
  ```
  keep District alpha0_
  ```
- Line 27: district, loc
  ```
  local ndistricts = r(N)
  ```
- Line 28: district
  ```
  collapse (count) n = District, by(alpha0_)
  ```
- Line 41: district
  ```
  gen share = n / `ndistricts'
  ```
- Line 42: district
  ```
  gen var = share * (1 - share) * `ndistricts' / (`ndistricts' - 1)
  ```
- Line 47: district
  ```
  * Share of districts at each support point of omega2 / alpha1.
  ```
- Line 49: district
  ```
  keep District alpha1_
  ```
- Line 52: district, loc
  ```
  local ndistricts = r(N)
  ```
- Line 53: district
  ```
  collapse (count) n = District, by(alpha1_)
  ```
- Line 68: district
  ```
  gen share = n / `ndistricts'
  ```
- Line 69: district
  ```
  gen var = share * (1 - share) * `ndistricts' / (`ndistricts' - 1)
  ```
- Line 78: district
  ```
  * Merge in district-to-district distances.
  ```
- Line 80: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 88: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 89: district
  ```
  gen d2 = District
  ```
- Line 95: district
  ```
  * Check that all districts are covered.
  ```
- Line 110: district, name
  ```
  rename d2 district_code
  ```
- Line 111: district
  ```
  sort district_code
  ```
- Line 115: district
  ```
  replace cesa = 6 if cesa == . // this should only be district_code = 2570
  ```
- Line 120: district
  ```
  sort id District
  ```
- Line 132: district
  ```
  gen milw = District == 3619
  ```
- Line 156: name
  ```
  rename A1 A
  ```
- Line 166: name
  ```
  rename B1 B
  ```
- Line 177: district
  ```
  * Merge in district-to-district distances.
  ```
- Line 179: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 188: district, name
  ```
  rename District district_code
  ```
- Line 189: district
  ```
  sort district_code
  ```
- Line 193: district
  ```
  replace cesa = 6 if cesa == . // this should only be district_code = 2570
  ```
- Line 194: district, name
  ```
  rename district_code District
  ```
- Line 196: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 197: district
  ```
  gen d2 = District
  ```
- Line 203: district
  ```
  * Check that all districts are covered.
  ```
- Line 216: district
  ```
  sort id District
  ```
- Line 229: district
  ```
  sort id District
  ```
- Line 245: district
  ```
  gen milw = District == 3619
  ```
- Line 269: name
  ```
  rename A1 A
  ```
- Line 279: name
  ```
  rename B1 B
  ```
- Line 295: city
  ```
  gen budget_pp = budget / capacity
  ```
- Line 297: city
  ```
  reg alpha0_ Exp3 Exp4 Exp5 Exp6 masterup d tc tc_tenured budget_pp capacity ///
  ```
- Line 305: name
  ```
  rename A1 A
  ```
- Line 315: name
  ```
  rename B1 B
  ```
- Line 320: city
  ```
  reg alpha1_ Exp3 Exp4 Exp5 Exp6 masterup d tc tc_tenured budget_pp capacity ///
  ```
- Line 328: name
  ```
  rename A1 A
  ```
- Line 338: name
  ```
  rename B1 B
  ```
- Line 356: loc
  ```
  local a0 = r(mean)
  ```
- Line 357: loc
  ```
  local Va0 = r(Var)
  ```
- Line 359: loc
  ```
  local a1 = r(mean)
  ```
- Line 360: loc
  ```
  local Va1 = r(Var)
  ```
- Line 362: loc
  ```
  local a01 = r(mean)
  ```
- Line 363: loc
  ```
  local Va01 = r(mean)
  ```
- Line 365: loc
  ```
  local a0sq = r(mean)
  ```
- Line 366: loc
  ```
  local Va0sq = r(Var)
  ```
- Line 368: loc
  ```
  local a1sq = r(mean)
  ```
- Line 369: loc
  ```
  local Va1sq = r(Var)
  ```
- Line 371: loc
  ```
  local a01sq = r(mean)
  ```
- Line 372: loc
  ```
  local Va01sq = r(Var)
  ```
- Line 394: loc
  ```
  local share = r(mean)
  ```
- Line 395: loc
  ```
  local varshare = r(sd)^2
  ```
- Line 409: loc
  ```
  local alpha0_dem = `r(mean)'
  ```
- Line 411: loc
  ```
  local alpha0_demV = `r(sd)'
  ```
- Line 413: loc
  ```
  local alpha1_dem = `r(mean)'
  ```
- Line 415: loc
  ```
  local alpha1_demV = `r(sd)'
  ```
- Line 418: loc
  ```
  local alpha0_highdem = `r(mean)'
  ```
- Line 421: loc
  ```
  local alpha0_highdemV = `r(sd)'
  ```
- Line 424: loc
  ```
  local alpha1_highdem = `r(mean)'
  ```
- Line 427: loc
  ```
  local alpha1_highdemV = `r(sd)'
  ```
- Line 432: name
  ```
  gen name = ""
  ```
- Line 433: name
  ```
  replace name = "E(omega1*dem)" in 1
  ```
- Line 434: name
  ```
  replace name = "E(omega2*dem)" in 2
  ```
- Line 435: name
  ```
  replace name = "E(omega1|dem>=median)" in 3
  ```
- Line 436: name
  ```
  replace name = "E(omega2|dem>=median)" in 4
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/aux_other_moments.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 19: loc
  ```
  local base "$code"
  ```
- Line 34: name
  ```
  syntax varname [if], POST(name)
  ```
- Line 41: name
  ```
  tempname V
  ```
- Line 49: district
  ```
  * Matched Teacher-District Data
  ```
- Line 58: district
  ```
  bysort District: gen NN = _N
  ```
- Line 62: name
  ```
  rename totalexp2 exper
  ```
- Line 63: name
  ```
  rename C1 C2
  ```
- Line 64: name
  ```
  rename C0 C1
  ```
- Line 65: name
  ```
  rename masterup master
  ```
- Line 67: name
  ```
  tempname aux_post
  ```
- Line 68: city
  ```
  tempfile aux_variances excess_capacity
  ```
- Line 73: district
  ```
  * Moments 1-4: Teacher Characteristics by District Groups
  ```
- Line 77: district
  ```
  collapse exper C1 C2 master d urban suburban budget_teacher, by(District)
  ```
- Line 115: district
  ```
  * Moments 5-10: Districts with Only Tenured Incumbents
  ```
- Line 122: district
  ```
  collapse (sum) Match incten, by(District)
  ```
- Line 123: city
  ```
  gen excess_capacity = Match > incten
  ```
- Line 124: city, district
  ```
  keep District excess_capacity
  ```
- Line 125: city
  ```
  save `excess_capacity', replace
  ```
- Line 130: district
  ```
  collapse untenured urban suburban d budget_teacher, by(District)
  ```
- Line 163: city
  ```
  tabstat onlyten, by(excess_capacity) statistics(mean)
  ```
- Line 165: city
  ```
  _post_mean_var onlyten if excess_capacity == `status', post(`aux_post')
  ```
- Line 177: district
  ```
  collapse newhire, by(District)
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxiliary_models_districts.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 5: district
  ```
  * File:    auxiliary_models_districts.do
  ```
- Line 6: district
  ```
  * Purpose: Estimate district-side auxiliary models for model moments.
  ```
- Line 19: district
  ```
  * District Regressions
  ```
- Line 25: district
  ```
  * Standardize district contribution measures before forming district moments.
  ```
- Line 33: city
  ```
  ** residual capacity
  ```
- Line 35: district
  ```
  bysort District: egen x = sum(stay)
  ```
- Line 36: city
  ```
  gen capacity_left = capacity - x
  ```
- Line 40: district
  ```
  bysort District: egen xx = sum(x)
  ```
- Line 44: city
  ```
  gen budget_left_pp = budget_left / capacity_left
  ```
- Line 45: city
  ```
  replace budget_left_pp = 0 if capacity_left == 0
  ```
- Line 54: city
  ```
  *replace capacity_left = capacity_left / capacity
  ```
- Line 56: city
  ```
  gen cap_d = capacity_left * d
  ```
- Line 60: district
  ```
  gen milwaukee = District == 3619
  ```
- Line 62: district
  ```
  tempfile district_base
  ```
- Line 63: district
  ```
  save `district_base', replace
  ```
- Line 71: city, district
  ```
  collapse capacity_left budget_left budget_left_pp cap_d masterup totalexp2 V0 V1 xb0 xb1 d avgcont a
  ```
- Line 81: city
  ```
  label var capacity_left "free capacity"
  ```
- Line 93: city
  ```
  eststo r1: reg totalexp2 budget_left capacity_left d , rob
  ```
- Line 98: name
  ```
  rename A1 A
  ```
- Line 108: name
  ```
  rename B1 B
  ```
- Line 114: city
  ```
  eststo r2: reg totalexp2 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 119: city
  ```
  eststo r5: reg masterup budget_left capacity_left d , rob
  ```
- Line 124: name
  ```
  rename A1 A
  ```
- Line 134: name
  ```
  rename B1 B
  ```
- Line 139: city
  ```
  eststo r6: reg masterup budget_left capacity_left d budgetpt_d, rob
  ```
- Line 144: city
  ```
  eststo r9: reg C0 budget_left capacity_left d , rob
  ```
- Line 149: name
  ```
  rename A1 A
  ```
- Line 159: name
  ```
  rename B1 B
  ```
- Line 164: city
  ```
  eststo r10: reg C0 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 169: city
  ```
  eststo r13: reg C1 budget_left capacity_left d , rob
  ```
- Line 174: name
  ```
  rename A1 A
  ```
- Line 184: name
  ```
  rename B1 B
  ```
- Line 189: city
  ```
  eststo r14: reg C1 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 194: city
  ```
  eststo r17: reg avgcont budget_left capacity_left d , rob
  ```
- Line 195: city
  ```
  eststo r18: reg avgcont budget_left capacity_left d budgetpt_d, rob
  ```
- Line 207: city
  ```
  keep(budget_left budget_left_pp capacity_left d budgetpt_d urban suburban milwaukee) mgroups("experi
  ```
- Line 217: city
  ```
  keep(budget_left budget_left_pp capacity_left d budgetpt_d urban suburban milwaukee) mgroups("$ c_{1
  ```
- Line 225: city
  ```
  keep(budget_left budget_left_pp capacity_left d budgetpt_d urban suburban milwaukee) mgroups("avg co
  ```
- Line 246: district
  ```
  use `district_base', clear
  ```
- Line 257: district, loc
  ```
  qui levelsof District, local(Dist)
  ```
- Line 258: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 260: district
  ```
  qui sum `var' if District == `d', det
  ```
- Line 261: district
  ```
  qui sum `var' if District == `d' & `var' <= r(p10), det
  ```
- Line 262: district
  ```
  replace mean_`var' = r(mean) if District == `d'
  ```
- Line 268: city, district
  ```
  collapse capacity_left budget_left* cap_d mean_* d alpha* C0 C1  urban suburban milwaukee (sum) Matc
  ```
- Line 269: city
  ```
  *drop capacity_left
  ```
- Line 270: city, name
  ```
  *rename Match capacity_left
  ```
- Line 277: city
  ```
  label var capacity_left "capacity"
  ```
- Line 289: city
  ```
  eststo r1: reg mean_totalexp2 budget_left capacity_left d, rob
  ```
- Line 294: name
  ```
  rename A1 A
  ```
- Line 304: name
  ```
  rename B1 B
  ```
- Line 310: city
  ```
  eststo r2: reg mean_totalexp2 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 317: city
  ```
  eststo r5: reg mean_masterup budget_left capacity_left d, rob
  ```
- Line 322: name
  ```
  rename A1 A
  ```
- Line 332: name
  ```
  rename B1 B
  ```
- Line 337: city
  ```
  eststo r6: reg mean_masterup budget_left capacity_left d budgetpt_d, rob
  ```
- Line 342: city
  ```
  eststo r9: reg mean_C0 budget_left capacity_left d, rob
  ```
- Line 347: name
  ```
  rename A1 A
  ```
- Line 357: name
  ```
  rename B1 B
  ```
- Line 362: city
  ```
  eststo r10: reg mean_C0 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 367: city
  ```
  eststo r13: reg mean_C1 budget_left capacity_left d, rob
  ```
- Line 372: name
  ```
  rename A1 A
  ```
- Line 382: name
  ```
  rename B1 B
  ```
- Line 387: city
  ```
  eststo r14: reg mean_C1 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 392: city
  ```
  eststo r17: reg mean_avgcont budget_left capacity_left d , rob
  ```
- Line 393: city
  ```
  eststo r18: reg mean_avgcont budget_left capacity_left d budgetpt_d, rob
  ```
- Line 407: city
  ```
  keep(budget_left budget_left_pp capacity_left d budgetpt_d urban suburban milwaukee) mgroups("experi
  ```
- Line 416: city
  ```
  keep(budget_left budget_left_pp d capacity_left budgetpt_d urban suburban milwaukee) mgroups("$ c_{1
  ```
- Line 425: city
  ```
  keep(budget_left budget_left_pp d capacity_left budgetpt_d urban suburban milwaukee) mgroups("avg. c
  ```
- Line 439: district
  ```
  use `district_base', clear
  ```
- Line 445: city, district
  ```
  collapse capacity_left budget_left* cap_d masterup totalexp2 C0 C1 d avgcont alpha*  urban suburban 
  ```
- Line 446: city
  ```
  *drop capacity_left
  ```
- Line 447: city, name
  ```
  *rename Match capacity_left
  ```
- Line 454: city
  ```
  label var capacity_left "capacity"
  ```
- Line 465: city
  ```
  eststo r1: reg totalexp2 budget_left capacity_left d, rob
  ```
- Line 470: name
  ```
  rename A1 A
  ```
- Line 480: name
  ```
  rename B1 B
  ```
- Line 485: city
  ```
  eststo r2: reg totalexp2 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 492: city
  ```
  eststo r5: reg masterup budget_left capacity_left d, rob
  ```
- Line 497: name
  ```
  rename A1 A
  ```
- Line 507: name
  ```
  rename B1 B
  ```
- Line 512: city
  ```
  eststo r6: reg masterup budget_left capacity_left d budgetpt_d, rob
  ```
- Line 517: city
  ```
  eststo r9: reg C0 budget_left capacity_left d, rob
  ```
- Line 522: name
  ```
  rename A1 A
  ```
- Line 532: name
  ```
  rename B1 B
  ```
- Line 537: city
  ```
  eststo r10: reg C0 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 542: city
  ```
  eststo r13: reg C1 budget_left capacity_left d, rob
  ```
- Line 547: name
  ```
  rename A1 A
  ```
- Line 557: name
  ```
  rename B1 B
  ```
- Line 562: city
  ```
  eststo r14: reg C1 budget_left capacity_left d budgetpt_d, rob
  ```
- Line 567: city
  ```
  eststo r17: reg avgcont budget_left capacity_left d , rob
  ```
- Line 568: city
  ```
  eststo r18: reg avgcont budget_left capacity_left d budgetpt_d, rob
  ```
- Line 582: city
  ```
  keep(budget_left budget_left_pp capacity_left d budgetpt_d urban suburban milwaukee) mgroups("experi
  ```
- Line 591: city
  ```
  keep(budget_left budget_left_pp d capacity_left budgetpt_d urban suburban milwaukee) mgroups("$ c_{1
  ```
- Line 600: city
  ```
  keep(budget_left budget_left_pp d capacity_left budgetpt_d urban suburban milwaukee) mgroups("avg. c
  ```
- Line 615: district
  ```
  use `district_base', clear
  ```
- Line 618: city
  ```
  collapse d urban suburban alpha0_ alpha1_ budget budget_left capacity ///
  ```
- Line 619: district
  ```
  totalexp2 masterup C0 C1 avgcont (sum) Match (max) relevant, by(District)
  ```
- Line 633: loc
  ```
  levelsof relevant, local(relevant_groups)
  ```
- Line 634: loc
  ```
  local n_relevant_groups : word count `relevant_groups'
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxiliary_models_teachers.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 15: loc
  ```
  local inc_mean = r(mean)
  ```
- Line 16: loc
  ```
  local inc_var = r(sd)^2
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxiliary_models_teachers_info.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 24: district
  ```
  * Assumption 1: Teachers and districts both make mistakes on C0, C1
  ```
- Line 44: district
  ```
  gen milw = District == 3619
  ```
- Line 65: district
  ```
  collapse Match wage d C0 C1 urban suburban, by(id District)
  ```
- Line 79: district
  ```
  gen milw = District == 3619
  ```
- Line 100: district
  ```
  collapse Match wage d C0 C1 urban suburban, by(id District)
  ```
- Line 114: district
  ```
  gen milw = District == 3619
  ```
- Line 135: district
  ```
  collapse Match wage d C0 C1 urban suburban, by(id District)
  ```
- Line 149: district
  ```
  gen milw = District == 3619
  ```
- Line 170: district
  ```
  collapse Match wage d C0 C1 urban suburban, by(id District)
  ```
- Line 184: district
  ```
  gen milw = District == 3619
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxiliary_models_teachers_nobottom.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 25: district
  ```
  sort id District
  ```
- Line 36: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 44: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 45: district
  ```
  gen d2 = District
  ```
- Line 51: district
  ```
  * Check that we have all districts.
  ```
- Line 101: district
  ```
  gen milw = District == 3619
  ```
- Line 104: district
  ```
  gen mad = District == 3269
  ```
- Line 105: son
  ```
  label var mad "Madison"
  ```
- Line 130: loc
  ```
  local b0 = `r(p10)'
  ```
- Line 132: loc
  ```
  local b1 = `r(p10)'
  ```
- Line 144: district
  ```
  keep if ((Exp2 == 1 & Match == 1) | (Inc == 0 & Match == 1 & Exp1 == 0 & Exp2 == 0 & INC == 1) | (In
  ```
- Line 145: district
  ```
  /* non-tenured 				tenured in new district		tenured in incumbent districts	BUT ONLY IF d0 != 0*/
  ```
- Line 146: district
  ```
  keep id District C0 C1 exp wage Inc samecz d Match urban suburban masterup
  ```
- Line 157: district
  ```
  collapse (mean) District C0 C1 exp wage masterup, by(id)
  ```
- Line 159: name
  ```
  rename (C0 C1 exp wage masterup) ///
  ```
- Line 178: district
  ```
  keep id District v0 v1 exp wage Inc samecz C0 C1 d Match urban suburban masterup
  ```
- Line 195: district
  ```
  keep id District v0 v1 exp wage Inc samecz C0 C1 d Match urban suburban masterup
  ```
- Line 202: district
  ```
  sort id District
  ```
- Line 207: district
  ```
  sort id District
  ```
- Line 218: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 226: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 227: district
  ```
  gen d2 = District
  ```
- Line 233: district
  ```
  * Check that we have all districts.
  ```
- Line 283: district
  ```
  gen milw = District == 3619
  ```
- Line 286: district
  ```
  gen mad = District == 3269
  ```
- Line 287: son
  ```
  label var mad "Madison"
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxiliary_models_teachers_richprefs.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 25: district
  ```
  sort id District
  ```
- Line 44: son
  ```
  * Run comparison.
  ```
- Line 84: district
  ```
  sort id District
  ```
- Line 95: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 103: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 104: district
  ```
  gen d2 = District
  ```
- Line 110: district
  ```
  * Check that we have all districts.
  ```
- Line 156: district
  ```
  gen milw = District == 3619
  ```
- Line 159: district
  ```
  gen mad = District == 3269
  ```
- Line 160: son
  ```
  label var mad "Madison"
  ```
- Line 174: district
  ```
  tsset id District
  ```
- Line 203: loc, location
  ```
  * Interact experience with the location controls.
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/auxmodels_teachers_varobust.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 19: name
  ```
  rename v0 c0_class
  ```
- Line 20: name
  ```
  rename v1 c1_class
  ```
- Line 33: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 61: degree
  ```
  * experience-by-degree cell.
  ```
- Line 86: district
  ```
  * Merge in district-to-district distances.
  ```
- Line 87: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 88: district
  ```
  gen d2 = District
  ```
- Line 89: district
  ```
  tempvar home_district
  ```
- Line 90: district
  ```
  bysort id: egen `home_district' = max(d1)
  ```
- Line 91: district
  ```
  replace d1 = `home_district'
  ```
- Line 93: district
  ```
  * Check that all districts are covered.
  ```
- Line 108: district, name
  ```
  rename d2 district_code
  ```
- Line 109: district
  ```
  sort district_code
  ```
- Line 113: district
  ```
  replace cesa = 6 if cesa == . // this should only be district_code = 2570
  ```
- Line 118: district
  ```
  sort id District
  ```
- Line 130: district
  ```
  gen milw = District == 3619
  ```
- Line 158: loc
  ```
  local b0 = `r(p10)'
  ```
- Line 160: loc
  ```
  local b1 = `r(p10)'
  ```
- Line 173: district
  ```
  * the new district, and tenured teachers in incumbent districts.
  ```
- Line 179: district
  ```
  keep id District C0 C1 exp wage Inc samecz d Match urban suburban masterup
  ```
- Line 186: lat
  ```
  * Create an empty dataset with the same variables for accumulating choices.
  ```
- Line 193: loc
  ```
  qui levelsof id, local(ID)
  ```
- Line 194: loc
  ```
  local t = 1
  ```
- Line 195: loc
  ```
  qui foreach teacher_id of local ID {
  ```
- Line 198: district
  ```
  sum District if id == `teacher_id' & Match == 1
  ```
- Line 199: loc
  ```
  local dist = r(mean)
  ```
- Line 201: loc
  ```
  local C0 = r(mean) + 0.00000000000000001
  ```
- Line 203: loc
  ```
  local C1 = r(mean) + 0.00000000000000001
  ```
- Line 205: loc
  ```
  local exp = r(mean) + 0.00000000000000001
  ```
- Line 207: loc
  ```
  local wage = r(mean) + 0.00001
  ```
- Line 209: loc
  ```
  local masterup = r(mean)
  ```
- Line 222: district
  ```
  District == `dist' & Match == 0
  ```
- Line 228: district
  ```
  keep id District v0 v1 exp wage Inc samecz C0 C1 d Match urban ///
  ```
- Line 235: loc
  ```
  local t = `t' + 1
  ```
- Line 243: district
  ```
  sort id District
  ```
- Line 254: district, name
  ```
  rename District district_code
  ```
- Line 255: district
  ```
  sort district_code
  ```
- Line 259: district
  ```
  replace cesa = 6 if cesa == . // this should only be district_code = 2570
  ```
- Line 260: district, name
  ```
  rename district_code District
  ```
- Line 262: district
  ```
  * Merge in district-to-district distances.
  ```
- Line 263: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 264: district
  ```
  gen d2 = District
  ```
- Line 265: district
  ```
  tempvar home_district2
  ```
- Line 266: district
  ```
  bysort id: egen `home_district2' = max(d1)
  ```
- Line 267: district
  ```
  replace d1 = `home_district2'
  ```
- Line 269: district
  ```
  * Check that all districts are covered.
  ```
- Line 282: district
  ```
  sort id District
  ```
- Line 287: district
  ```
  keep id District
  ```
- Line 291: district
  ```
  sort id District
  ```
- Line 307: district
  ```
  gen milw = District == 3619
  ```

**/replication-package/replication_delivery/code/auxiliary_regressions/master.do**

- Line 3: school
  ```
  *			Equilibrium in the Market for Public School Teachers	(Biasi, Fu, Stromme JPE)			*
  ```
- Line 26: loc
  ```
  local launch_dir "`c(pwd)'"
  ```
- Line 32: loc
  ```
  local launch_dir "`c(pwd)'"
  ```
- Line 37: loc
  ```
  di as error "Could not locate the replication root."
  ```
- Line 48: lat
  ```
  global sim = "$rootpath/simulation_data/"
  ```
- Line 71: lon
  ```
  + va_teacherside_math.dta - contains list of teachers and grade assignments, in long form
  ```
- Line 72: lat
  ```
  + va_highlow_math_expdisc.dta - contains main teacher contribution measures. Constructed in valueadd
  ```
- Line 73: lat
  ```
  + va_nocomparativeadv.dta - contains teacher contribution w/o comparative advantage. Constructed in 
  ```
- Line 75: district
  ```
  + alphagrid_8groups_lambdad.dta - generates the omegas for each district. Constructed in gen_omegas.
  ```
- Line 84: lon
  ```
  + alphagrid_12.dta, budget_past_long.dta, budget_past.dta, budget_long.dta, budget.dta, info_all.dta
  ```
- Line 86: lat
  ```
  *---> Simulation data
  ```
- Line 87: lon
  ```
  + budget_past_long_pre_NEW.dta, budget_past_pre_NEW.dta, budget_long_pre_NEW.dta, budget_NEW.dta, in
  ```
- Line 109: lat
  ```
  do $code/build/valueadded_calculation.do // Model 1
  ```
- Line 110: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do // Model 2
  ```
- Line 111: lat
  ```
  do $code/build/valueadded_calculation_stockstaiger_nocomparativeadv.do // No comparative advantage
  ```
- Line 124: district
  ```
  do $code/auxiliary_regressions/auxiliary_models_districts.do
  ```
- Line 136: lat
  ```
  * Generate model simulation data files (2011)
  ```
- Line 138: lat
  ```
  do $code/build/build_simulation_lndist.do
  ```
- Line 171: lat
  ```
  //do $code/build/valueadded_calculation.do
  ```
- Line 180: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do
  ```
- Line 214: lat
  ```
  do $code/descriptives/summary_simulation.do
  ```
- Line 242: lat
  ```
  do $code/Figure_B9/Figure_B9.do // see R code in Figure_B9 folder for generation of the simulation d
  ```

**/replication-package/replication_delivery/code/build/_synthetic_model_helpers.do**

- Line 5: district
  ```
  capture program drop _synthetic_model_districts
  ```
- Line 6: district
  ```
  program define _synthetic_model_districts
  ```
- Line 12: district, school
  ```
  use "$raw/district_shapefiles/WI_School_Districts.dta", clear
  ```
- Line 13: district, lon
  ```
  gen long district_code = real(SDID)
  ```
- Line 14: district
  ```
  keep district_code
  ```
- Line 15: district
  ```
  drop if district_code == .
  ```
- Line 19: district
  ```
  use "$raw/district_county_cz.dta", clear
  ```
- Line 20: district
  ```
  keep district_code
  ```
- Line 21: district
  ```
  drop if district_code == .
  ```
- Line 29: district
  ```
  keep district_code
  ```
- Line 30: district
  ```
  drop if district_code == .
  ```
- Line 37: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 42: district, name
  ```
  rename d1 district_code
  ```
- Line 47: district, name
  ```
  rename d2 district_code
  ```
- Line 56: district
  ```
  sort district_code
  ```
- Line 60: district, lon
  ```
  gen long _synthetic_district_rank = _n
  ```
- Line 61: district, name
  ```
  rename district_code _synthetic_district_code
  ```
- Line 68: district
  ```
  syntax, Years(numlist integer) Target(integer) Districts(string)
  ```
- Line 73: loc
  ```
  local base_obs = r(N)
  ```
- Line 74: loc
  ```
  local copies = max(1, ceil(`target' / `base_obs'))
  ```
- Line 76: loc
  ```
  local max_id = r(max)
  ```
- Line 81: lon
  ```
  gen long _synthetic_source_order = _n
  ```
- Line 83: lon
  ```
  bysort _synthetic_source_order: gen long _synthetic_copy = _n
  ```
- Line 85: lon
  ```
  gen long _synthetic_rank = _n
  ```
- Line 88: district, lon
  ```
  gen long _synthetic_district_rank = mod(_synthetic_rank - 1, 411) + 1
  ```
- Line 91: district
  ```
  replace district_code = _synthetic_district_code
  ```
- Line 92: district
  ```
  _synth_apply_district_type
  ```
- Line 93: district, school
  ```
  replace schoolcode = 100000 + _synthetic_district_rank ///
  ```
- Line 94: school
  ```
  if schoolcode < .
  ```
- Line 96: district
  ```
  _synthetic_district_rank _synthetic_district_code
  ```
- Line 105: district
  ```
  egen byte _synthetic_district_tag = tag(district_code) ///
  ```
- Line 107: district
  ```
  quietly count if _synthetic_district_tag == 1
  ```
- Line 109: district
  ```
  drop _synthetic_district_tag
  ```
- Line 114: district
  ```
  capture program drop _synth_apply_district_type
  ```
- Line 115: district
  ```
  program define _synth_apply_district_type
  ```
- Line 120: district
  ```
  replace type = 3 if district_code < .
  ```
- Line 129: district
  ```
  replace type = 2 if district_code == `n'
  ```
- Line 136: district
  ```
  replace type = 1 if district_code == `n'
  ```
- Line 140: district
  ```
  capture program drop _synthetic_pad_current_districts
  ```
- Line 141: district
  ```
  program define _synthetic_pad_current_districts
  ```
- Line 142: district
  ```
  syntax, Districts(string)
  ```
- Line 145: district
  ```
  use "`districts'", clear
  ```
- Line 146: district, name
  ```
  rename _synthetic_district_code district_code
  ```
- Line 147: district
  ```
  drop _synthetic_district_rank
  ```
- Line 148: district
  ```
  tempfile synthetic_district_codes
  ```
- Line 149: district
  ```
  save `synthetic_district_codes', replace
  ```
- Line 154: district
  ```
  ds district_code _merge, not
  ```
- Line 155: loc
  ```
  local fillvars `r(varlist)'
  ```
- Line 156: loc
  ```
  foreach v of local fillvars {
  ```
- Line 174: district
  ```
  sort district_code
  ```
- Line 196: district
  ```
  sort district_code
  ```
- Line 197: lon
  ```
  gen long _synthetic_alpha_rank = _n
  ```

**/replication-package/replication_delivery/code/build/auxiliary_models_teachers_info_matches.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 19: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 20: loc
  ```
  local synthetic_mover_id_cap = 250
  ```
- Line 42: name
  ```
  rename v0 v0tilde
  ```
- Line 43: name
  ```
  rename v1 v1tilde
  ```
- Line 44: name
  ```
  rename V0 V0tilde
  ```
- Line 45: name
  ```
  rename V1 V1tilde
  ```
- Line 77: city, district, lon
  ```
  reshape long wage Match Inc samecz d N budget capacity budget_past capacity_past urban suburban alph
  ```
- Line 79: district
  ```
  sort id District
  ```
- Line 107: district
  ```
  * the new district, and tenured teachers in incumbent districts.
  ```
- Line 113: district
  ```
  keep id District C0 C1 exp wage Inc samecz d Match urban suburban masterup
  ```
- Line 116: district
  ```
  * The public synthetic data now carry the full 411-district choice set.
  ```
- Line 121: lon
  ```
  gen long _synthetic_id_rank = sum(_synthetic_id_tag)
  ```
- Line 129: lat
  ```
  * Create an empty dataset with the same variables for accumulating choices.
  ```
- Line 136: loc
  ```
  qui levelsof id, local(ID)
  ```
- Line 137: loc
  ```
  local t = 1
  ```
- Line 138: loc
  ```
  qui foreach teacher_id of local ID {
  ```
- Line 141: district
  ```
  sum District if id == `teacher_id' & Match == 1
  ```
- Line 142: loc
  ```
  local dist = r(mean)
  ```
- Line 144: loc
  ```
  local C0 = r(mean)
  ```
- Line 146: loc
  ```
  local C1 = r(mean)
  ```
- Line 148: loc
  ```
  local exp = r(mean)
  ```
- Line 150: loc
  ```
  local wage = r(mean)
  ```
- Line 152: loc
  ```
  local masterup = r(mean)
  ```
- Line 165: district
  ```
  District == `dist' & Match == 0
  ```
- Line 171: district
  ```
  keep id District v0 v1 exp wage Inc samecz C0 C1 d Match urban ///
  ```
- Line 178: loc
  ```
  local t = `t' + 1
  ```
- Line 186: district
  ```
  sort id District
  ```

**/replication-package/replication_delivery/code/build/budget_overtime.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 6: district
  ```
  * Purpose: generates district panel containing budget per teacher
  ```
- Line 16: district
  ```
  collapse salary_round , by(year district_code)
  ```
- Line 17: district
  ```
  sort district_code year
  ```
- Line 18: name
  ```
  rename salary_round budget_pp
  ```

**/replication-package/replication_delivery/code/build/build_distance_matrix.do**

- Line 5: district
  ```
  * This file creates log-distance matrices between all districts
  ```
- Line 10: district
  ```
  * Create District-to-district log-distance matrix
  ```
- Line 14: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 22: district
  ```
  use "$raw/district_id_tag.dta", clear
  ```
- Line 23: district
  ```
  mata: tags = st_data(., "district_code")
  ```
- Line 53: district
  ```
  unlink("$est/logdist_btwn_districts.csv")
  ```
- Line 54: district
  ```
  fh = fopen("$est/logdist_btwn_districts.csv", "w")
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: district
  ```
  use "$raw/crosswalk_district_county.dta", clear
  ```
- Line 17: loc
  ```
  local new = _N + 4
  ```
- Line 19: loc
  ```
  local z = _N - 3
  ```
- Line 20: district
  ```
  replace district_code = 2527 in `z'
  ```
- Line 22: loc
  ```
  local z = _N - 2
  ```
- Line 23: district
  ```
  replace district_code = 3689 in `z'
  ```
- Line 25: loc
  ```
  local z = _N - 1
  ```
- Line 26: district
  ```
  replace district_code = 3913 in `z'
  ```
- Line 28: loc
  ```
  local z = _N
  ```
- Line 29: district
  ```
  replace district_code = 4998 in `z'
  ```
- Line 43: district
  ```
  collapse democratic, by(district_code)
  ```
- Line 48: district
  ```
  * Share low achievers by district.
  ```
- Line 51: school
  ```
  drop if schoolcode == 8888
  ```
- Line 54: district
  ```
  collapse d, by(district_code)
  ```
- Line 55: district
  ```
  sort district_code
  ```
- Line 60: district
  ```
  sort district_code
  ```
- Line 68: birth
  ```
  * Retain a stable birth year for the forward repeated-game age input. This
  ```
- Line 70: birth
  ```
  * reported birth year for each teacher before matching to the model sample.
  ```
- Line 71: birth
  ```
  gen birth_estsample = birth
  ```
- Line 72: birth
  ```
  replace birth_estsample = . if birth_estsample > 1996
  ```
- Line 73: birth
  ```
  bysort id: egen birth_estsample_min = min(birth_estsample)
  ```
- Line 74: birth
  ```
  drop birth_estsample
  ```
- Line 75: birth, name
  ```
  rename birth_estsample_min birth_estsample
  ```
- Line 79: loc
  ```
  local a = r(r50)
  ```
- Line 93: district
  ```
  * Resolve past district assignment.
  ```
- Line 94: district
  ```
  gen Dpast = district_past2011
  ```
- Line 95: district
  ```
  replace Dpast = district_past2012 if ///
  ```
- Line 96: district
  ```
  (district_past2012 != district_code & district_past2011 == .) & ///
  ```
- Line 97: district
  ```
  district_past2012 != .
  ```
- Line 98: district
  ```
  replace Dpast = district_past2013 if ///
  ```
- Line 99: district
  ```
  (district_past2013 != district_code & district_past2011 == .) & ///
  ```
- Line 100: district
  ```
  district_past2013 != .
  ```
- Line 107: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 109: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 112: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 118: loc
  ```
  local model_target_obs = 6600
  ```
- Line 119: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 122: district
  ```
  tempfile synthetic_districts
  ```
- Line 131: lon
  ```
  gen long _model_rank = _n
  ```
- Line 149: district
  ```
  * rows across the 411-district model universe only in synthetic runs.
  ```
- Line 152: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 156: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 157: district
  ```
  qui tab district_code if year == 2014
  ```
- Line 158: district
  ```
  disp "Nr districts ="
  ```
- Line 161: district
  ```
  * Tag districts in this sample.
  ```
- Line 164: district
  ```
  keep district_code
  ```
- Line 166: district
  ```
  sort district_code
  ```
- Line 178: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 181: district
  ```
  sort district_code
  ```
- Line 186: district
  ```
  reshape wide democratic, i(n) j(district_code)
  ```
- Line 195: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 205: district
  ```
  sort district_code
  ```
- Line 209: district
  ```
  sort district_code
  ```
- Line 210: name
  ```
  rename *_ba *_0
  ```
- Line 211: name
  ```
  rename *_ma *_1
  ```
- Line 212: district
  ```
  sort district_code
  ```
- Line 213: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 214: district
  ```
  i(district_code) j(masterup)
  ```
- Line 215: name
  ```
  rename *_ *
  ```
- Line 216: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 217: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 220: name
  ```
  rename CExp mode
  ```
- Line 221: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 231: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 233: district
  ```
  sort district_code
  ```
- Line 237: name
  ```
  rename S_* *_
  ```
- Line 243: district
  ```
  reshape wide alpha0_ alpha1_, i(n) j(district_code)
  ```
- Line 302: district
  ```
  * Tag only relevant districts.
  ```
- Line 303: district
  ```
  sort district_code
  ```
- Line 324: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 325: loc
  ```
  foreach d of local Dist {
  ```
- Line 326: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 327: loc
  ```
  local z = r(mean)
  ```
- Line 328: loc
  ```
  local y = 0
  ```
- Line 329: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 339: district
  ```
  if district_code == `d'
  ```
- Line 349: city
  ```
  gen capacity_past = 1
  ```
- Line 350: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 351: district
  ```
  sort district_code
  ```
- Line 352: lon
  ```
  save "$est/budget_past_long.dta", replace
  ```
- Line 354: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 355: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 364: city
  ```
  gen capacity = 1
  ```
- Line 365: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 366: district
  ```
  sort district_code
  ```
- Line 367: lon
  ```
  save "$est/budget_long.dta", replace
  ```
- Line 369: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 370: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 400: lat
  ```
  * original generator, calculate age in 2014, impute a missing age with the
  ```
- Line 405: birth
  ```
  keep id birth_estsample totalexp2
  ```
- Line 406: birth
  ```
  gen age = 2014 - birth_estsample
  ```
- Line 407: loc
  ```
  quietly levelsof totalexp2 if missing(age), local(age_exp_levels)
  ```
- Line 408: loc
  ```
  foreach e of local age_exp_levels {
  ```
- Line 419: birth
  ```
  drop birth_estsample
  ```
- Line 424: district
  ```
  keep id district_code Dpast
  ```
- Line 427: district, name
  ```
  rename district_code Dist_all
  ```
- Line 480: degree
  ```
  * Export master's degree indicator.
  ```
- Line 505: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 506: loc
  ```
  foreach d of local Dist {
  ```
- Line 507: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 521: district, name
  ```
  rename district_code D
  ```
- Line 522: district, name
  ```
  rename Dpast district_code
  ```
- Line 523: district
  ```
  sort district_code
  ```
- Line 525: district
  ```
  sum cz if district_code == 2525
  ```
- Line 530: name
  ```
  rename cz cz_past
  ```
- Line 531: district, name
  ```
  rename district_code Dpast
  ```
- Line 532: district, name
  ```
  rename D district_code
  ```
- Line 533: district
  ```
  sort district_code
  ```
- Line 535: district
  ```
  sum cz if district_code == 2525
  ```
- Line 540: district
  ```
  qui tab district_code
  ```
- Line 542: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 543: loc
  ```
  foreach d of local Dist {
  ```
- Line 544: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 551: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 552: loc
  ```
  foreach d of local Dist {
  ```
- Line 580: district
  ```
  * District Characteristics
  ```
- Line 586: school
  ```
  drop if schoolcode == 8888
  ```
- Line 589: district
  ```
  collapse d, by(district_code)
  ```
- Line 591: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 593: district
  ```
  sort district_code
  ```
- Line 598: district
  ```
  keep n district_code d
  ```
- Line 599: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 608: school
  ```
  drop if schoolcode == 8888
  ```
- Line 611: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 613: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 615: district
  ```
  sort district_code
  ```
- Line 620: district
  ```
  keep n N district_code
  ```
- Line 621: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 630: district
  ```
  keep if district_code != .
  ```
- Line 631: district
  ```
  collapse urban, by(district_code)
  ```
- Line 633: district
  ```
  keep n district_code urban
  ```
- Line 634: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 643: district
  ```
  keep if district_code != .
  ```
- Line 644: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 646: district
  ```
  keep n district_code suburban
  ```
- Line 647: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```
- Line 656: district, lon
  ```
  reshape long urban, i(n) j(district_code)
  ```
- Line 666: school
  ```
  drop if schoolcode == 8888
  ```
- Line 671: district
  ```
  collapse econ_disadv econ_low (sum) enrl, by(district_code)
  ```
- Line 673: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 675: district
  ```
  sort district_code
  ```
- Line 680: district
  ```
  drop district_code
  ```
- Line 696: district, lon
  ```
  reshape long wage, i(id) j(district)
  ```
- Line 699: district
  ```
  reshape wide wage, i(id) j(district)
  ```
- Line 739: city, district, lon
  ```
  reshape long wage Match Inc samecz d N budget capacity budget_past capacity_past urban suburban alph
  ```
- Line 741: district
  ```
  sort id District
  ```
- Line 764: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 772: district
  ```
  gen d1 = District if Inc == 1
  ```
- Line 773: district
  ```
  gen d2 = District
  ```
- Line 779: district
  ```
  * Check that all districts are covered.
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset_info0.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 9: loc
  ```
  local info_version 0
  ```
- Line 16: district
  ```
  * Share low achievers by district.
  ```
- Line 18: school
  ```
  drop if schoolcode == 8888
  ```
- Line 21: district
  ```
  collapse d, by(district_code)
  ```
- Line 22: district
  ```
  sort district_code
  ```
- Line 27: district
  ```
  sort district_code
  ```
- Line 36: loc
  ```
  local a = r(r50)
  ```
- Line 52: name
  ```
  rename v0_`info_version' v0
  ```
- Line 53: name
  ```
  rename v1_`info_version' v1
  ```
- Line 61: district
  ```
  * Resolve past district assignment.
  ```
- Line 62: district
  ```
  gen Dpast = district_past2011
  ```
- Line 63: district
  ```
  replace Dpast = district_past2012 if ///
  ```
- Line 64: district
  ```
  (district_past2012 != district_code & district_past2011 == .) & ///
  ```
- Line 65: district
  ```
  district_past2012 != .
  ```
- Line 66: district
  ```
  replace Dpast = district_past2013 if ///
  ```
- Line 67: district
  ```
  (district_past2013 != district_code & district_past2011 == .) & ///
  ```
- Line 68: district
  ```
  district_past2013 != .
  ```
- Line 75: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 77: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 80: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 86: loc
  ```
  local model_target_obs = 6600
  ```
- Line 87: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 90: district
  ```
  tempfile synthetic_districts
  ```
- Line 99: lon
  ```
  gen long _model_rank = _n
  ```
- Line 117: district
  ```
  * rows across the 411-district model universe only in synthetic runs.
  ```
- Line 120: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 124: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 125: district
  ```
  qui tab district_code if year == 2014
  ```
- Line 126: district
  ```
  disp "Nr districts ="
  ```
- Line 129: district
  ```
  * Tag districts in this sample.
  ```
- Line 132: district
  ```
  keep district_code
  ```
- Line 134: district
  ```
  sort district_code
  ```
- Line 147: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 157: district
  ```
  sort district_code
  ```
- Line 161: district
  ```
  sort district_code
  ```
- Line 162: name
  ```
  rename *_ba *_0
  ```
- Line 163: name
  ```
  rename *_ma *_1
  ```
- Line 164: district
  ```
  sort district_code
  ```
- Line 165: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 166: district
  ```
  i(district_code) j(masterup)
  ```
- Line 167: name
  ```
  rename *_ *
  ```
- Line 168: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 169: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 172: name
  ```
  rename CExp mode
  ```
- Line 173: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 184: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 186: district
  ```
  sort district_code
  ```
- Line 190: name
  ```
  rename S_* *_
  ```
- Line 196: district
  ```
  reshape wide alpha0_ alpha1_, i(n) j(district_code)
  ```
- Line 215: district
  ```
  * Tag only relevant districts.
  ```
- Line 216: district
  ```
  sort district_code
  ```
- Line 237: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 238: loc
  ```
  foreach d of local Dist {
  ```
- Line 239: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 240: loc
  ```
  local z = r(mean)
  ```
- Line 241: loc
  ```
  local y = 0
  ```
- Line 242: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 252: district
  ```
  if district_code == `d'
  ```
- Line 262: city
  ```
  gen capacity_past = 1
  ```
- Line 263: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 264: district
  ```
  sort district_code
  ```
- Line 265: lon
  ```
  save "$est/budget_past_long_info`info_version'.dta", replace
  ```
- Line 267: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 268: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 275: city
  ```
  gen capacity = 1
  ```
- Line 276: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 277: district
  ```
  sort district_code
  ```
- Line 278: lon
  ```
  save "$est/budget_long_info`info_version'.dta", replace
  ```
- Line 280: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 281: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 310: district
  ```
  keep id district_code Dpast
  ```
- Line 313: district, name
  ```
  rename district_code Dist_all
  ```
- Line 358: degree
  ```
  * Export master's degree indicator.
  ```
- Line 379: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 380: loc
  ```
  foreach d of local Dist {
  ```
- Line 381: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 393: district, name
  ```
  rename district_code D
  ```
- Line 394: district, name
  ```
  rename Dpast district_code
  ```
- Line 395: district
  ```
  sort district_code
  ```
- Line 397: district
  ```
  sum cz if district_code == 2525
  ```
- Line 402: name
  ```
  rename cz cz_past
  ```
- Line 403: district, name
  ```
  rename district_code Dpast
  ```
- Line 404: district, name
  ```
  rename D district_code
  ```
- Line 405: district
  ```
  sort district_code
  ```
- Line 407: district
  ```
  sum cz if district_code == 2525
  ```
- Line 412: district
  ```
  qui tab district_code
  ```
- Line 414: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 415: loc
  ```
  foreach d of local Dist {
  ```
- Line 416: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 423: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 424: loc
  ```
  foreach d of local Dist {
  ```
- Line 448: district
  ```
  * District Characteristics
  ```
- Line 454: school
  ```
  drop if schoolcode == 8888
  ```
- Line 457: district
  ```
  collapse d, by(district_code)
  ```
- Line 459: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 461: district
  ```
  sort district_code
  ```
- Line 466: district
  ```
  keep n district_code d
  ```
- Line 467: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 474: school
  ```
  drop if schoolcode == 8888
  ```
- Line 477: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 479: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 481: district
  ```
  sort district_code
  ```
- Line 486: district
  ```
  keep n N district_code
  ```
- Line 487: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 494: district
  ```
  keep if district_code != .
  ```
- Line 495: district
  ```
  collapse urban, by(district_code)
  ```
- Line 497: district
  ```
  keep n district_code urban
  ```
- Line 498: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 505: district
  ```
  keep if district_code != .
  ```
- Line 506: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 508: district
  ```
  keep n district_code suburban
  ```
- Line 509: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```
- Line 516: district, lon
  ```
  reshape long urban, i(n) j(district_code)
  ```
- Line 518: district
  ```
  sort district_code
  ```
- Line 520: district
  ```
  sort district_code
  ```
- Line 527: school
  ```
  drop if schoolcode == 8888
  ```
- Line 532: district
  ```
  collapse econ_disadv econ_low (sum) enrl, by(district_code)
  ```
- Line 534: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 536: district
  ```
  sort district_code
  ```
- Line 540: district
  ```
  drop district_code
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset_info1.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 9: loc
  ```
  local info_version 1
  ```
- Line 16: district
  ```
  * Share low achievers by district.
  ```
- Line 18: school
  ```
  drop if schoolcode == 8888
  ```
- Line 21: district
  ```
  collapse d, by(district_code)
  ```
- Line 22: district
  ```
  sort district_code
  ```
- Line 27: district
  ```
  sort district_code
  ```
- Line 36: loc
  ```
  local a = r(r50)
  ```
- Line 53: name
  ```
  rename v0_`info_version' v0
  ```
- Line 54: name
  ```
  rename v1_`info_version' v1
  ```
- Line 62: district
  ```
  * Resolve past district assignment.
  ```
- Line 63: district
  ```
  gen Dpast = district_past2011
  ```
- Line 64: district
  ```
  replace Dpast = district_past2012 if ///
  ```
- Line 65: district
  ```
  (district_past2012 != district_code & district_past2011 == .) & ///
  ```
- Line 66: district
  ```
  district_past2012 != .
  ```
- Line 67: district
  ```
  replace Dpast = district_past2013 if ///
  ```
- Line 68: district
  ```
  (district_past2013 != district_code & district_past2011 == .) & ///
  ```
- Line 69: district
  ```
  district_past2013 != .
  ```
- Line 76: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 78: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 81: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 87: loc
  ```
  local model_target_obs = 6600
  ```
- Line 88: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 91: district
  ```
  tempfile synthetic_districts
  ```
- Line 100: lon
  ```
  gen long _model_rank = _n
  ```
- Line 118: district
  ```
  * rows across the 411-district model universe only in synthetic runs.
  ```
- Line 121: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 125: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 126: district
  ```
  qui tab district_code if year == 2014
  ```
- Line 127: district
  ```
  disp "Nr districts ="
  ```
- Line 130: district
  ```
  * Tag districts in this sample.
  ```
- Line 133: district
  ```
  keep district_code
  ```
- Line 135: district
  ```
  sort district_code
  ```
- Line 148: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 158: district
  ```
  sort district_code
  ```
- Line 162: district
  ```
  sort district_code
  ```
- Line 163: name
  ```
  rename *_ba *_0
  ```
- Line 164: name
  ```
  rename *_ma *_1
  ```
- Line 165: district
  ```
  sort district_code
  ```
- Line 166: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 167: district
  ```
  i(district_code) j(masterup)
  ```
- Line 168: name
  ```
  rename *_ *
  ```
- Line 169: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 170: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 173: name
  ```
  rename CExp mode
  ```
- Line 174: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 185: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 187: district
  ```
  sort district_code
  ```
- Line 191: name
  ```
  rename S_* *_
  ```
- Line 197: district
  ```
  reshape wide alpha0_ alpha1_, i(n) j(district_code)
  ```
- Line 216: district
  ```
  * Tag only relevant districts.
  ```
- Line 217: district
  ```
  sort district_code
  ```
- Line 238: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 239: loc
  ```
  foreach d of local Dist {
  ```
- Line 240: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 241: loc
  ```
  local z = r(mean)
  ```
- Line 242: loc
  ```
  local y = 0
  ```
- Line 243: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 253: district
  ```
  if district_code == `d'
  ```
- Line 263: city
  ```
  gen capacity_past = 1
  ```
- Line 264: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 265: district
  ```
  sort district_code
  ```
- Line 266: lon
  ```
  save "$est/budget_past_long_info`info_version'.dta", replace
  ```
- Line 268: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 269: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 276: city
  ```
  gen capacity = 1
  ```
- Line 277: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 278: district
  ```
  sort district_code
  ```
- Line 279: lon
  ```
  save "$est/budget_long_info`info_version'.dta", replace
  ```
- Line 281: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 282: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 311: district
  ```
  keep id district_code Dpast
  ```
- Line 314: district, name
  ```
  rename district_code Dist_all
  ```
- Line 359: degree
  ```
  * Export master's degree indicator.
  ```
- Line 380: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 381: loc
  ```
  foreach d of local Dist {
  ```
- Line 382: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 394: district, name
  ```
  rename district_code D
  ```
- Line 395: district, name
  ```
  rename Dpast district_code
  ```
- Line 396: district
  ```
  sort district_code
  ```
- Line 398: district
  ```
  sum cz if district_code == 2525
  ```
- Line 403: name
  ```
  rename cz cz_past
  ```
- Line 404: district, name
  ```
  rename district_code Dpast
  ```
- Line 405: district, name
  ```
  rename D district_code
  ```
- Line 406: district
  ```
  sort district_code
  ```
- Line 408: district
  ```
  sum cz if district_code == 2525
  ```
- Line 413: district
  ```
  qui tab district_code
  ```
- Line 415: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 416: loc
  ```
  foreach d of local Dist {
  ```
- Line 417: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 424: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 425: loc
  ```
  foreach d of local Dist {
  ```
- Line 449: district
  ```
  * District Characteristics
  ```
- Line 455: school
  ```
  drop if schoolcode == 8888
  ```
- Line 458: district
  ```
  collapse d, by(district_code)
  ```
- Line 460: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 462: district
  ```
  sort district_code
  ```
- Line 467: district
  ```
  keep n district_code d
  ```
- Line 468: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 475: school
  ```
  drop if schoolcode == 8888
  ```
- Line 478: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 480: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 482: district
  ```
  sort district_code
  ```
- Line 487: district
  ```
  keep n N district_code
  ```
- Line 488: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 495: district
  ```
  keep if district_code != .
  ```
- Line 496: district
  ```
  collapse urban, by(district_code)
  ```
- Line 498: district
  ```
  keep n district_code urban
  ```
- Line 499: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 506: district
  ```
  keep if district_code != .
  ```
- Line 507: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 509: district
  ```
  keep n district_code suburban
  ```
- Line 510: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```
- Line 517: district, lon
  ```
  reshape long urban, i(n) j(district_code)
  ```
- Line 519: district
  ```
  sort district_code
  ```
- Line 521: district
  ```
  sort district_code
  ```
- Line 528: school
  ```
  drop if schoolcode == 8888
  ```
- Line 533: district
  ```
  collapse econ_disadv econ_low (sum) enrl, by(district_code)
  ```
- Line 535: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 537: district
  ```
  sort district_code
  ```
- Line 541: district
  ```
  drop district_code
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset_info2.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 9: loc
  ```
  local info_version 2
  ```
- Line 16: district
  ```
  * Share low achievers by district.
  ```
- Line 18: school
  ```
  drop if schoolcode == 8888
  ```
- Line 21: district
  ```
  collapse d, by(district_code)
  ```
- Line 22: district
  ```
  sort district_code
  ```
- Line 27: district
  ```
  sort district_code
  ```
- Line 36: loc
  ```
  local a = r(r50)
  ```
- Line 53: name
  ```
  rename v0_`info_version' v0
  ```
- Line 54: name
  ```
  rename v1_`info_version' v1
  ```
- Line 62: district
  ```
  * Resolve past district assignment.
  ```
- Line 63: district
  ```
  gen Dpast = district_past2011
  ```
- Line 64: district
  ```
  replace Dpast = district_past2012 if ///
  ```
- Line 65: district
  ```
  (district_past2012 != district_code & district_past2011 == .) & ///
  ```
- Line 66: district
  ```
  district_past2012 != .
  ```
- Line 67: district
  ```
  replace Dpast = district_past2013 if ///
  ```
- Line 68: district
  ```
  (district_past2013 != district_code & district_past2011 == .) & ///
  ```
- Line 69: district
  ```
  district_past2013 != .
  ```
- Line 76: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 78: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 81: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 87: loc
  ```
  local model_target_obs = 6600
  ```
- Line 88: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 91: district
  ```
  tempfile synthetic_districts
  ```
- Line 100: lon
  ```
  gen long _model_rank = _n
  ```
- Line 118: district
  ```
  * rows across the 411-district model universe only in synthetic runs.
  ```
- Line 121: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 125: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 126: district
  ```
  qui tab district_code if year == 2014
  ```
- Line 127: district
  ```
  disp "Nr districts ="
  ```
- Line 130: district
  ```
  * Tag districts in this sample.
  ```
- Line 133: district
  ```
  keep district_code
  ```
- Line 135: district
  ```
  sort district_code
  ```
- Line 148: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 158: district
  ```
  sort district_code
  ```
- Line 162: district
  ```
  sort district_code
  ```
- Line 163: name
  ```
  rename *_ba *_0
  ```
- Line 164: name
  ```
  rename *_ma *_1
  ```
- Line 165: district
  ```
  sort district_code
  ```
- Line 166: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 167: district
  ```
  i(district_code) j(masterup)
  ```
- Line 168: name
  ```
  rename *_ *
  ```
- Line 169: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 170: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 173: name
  ```
  rename CExp mode
  ```
- Line 174: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 185: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 187: district
  ```
  sort district_code
  ```
- Line 191: name
  ```
  rename S_* *_
  ```
- Line 197: district
  ```
  reshape wide alpha0_ alpha1_, i(n) j(district_code)
  ```
- Line 216: district
  ```
  * Tag only relevant districts.
  ```
- Line 217: district
  ```
  sort district_code
  ```
- Line 238: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 239: loc
  ```
  foreach d of local Dist {
  ```
- Line 240: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 241: loc
  ```
  local z = r(mean)
  ```
- Line 242: loc
  ```
  local y = 0
  ```
- Line 243: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 253: district
  ```
  if district_code == `d'
  ```
- Line 263: city
  ```
  gen capacity_past = 1
  ```
- Line 264: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 265: district
  ```
  sort district_code
  ```
- Line 266: lon
  ```
  save "$est/budget_past_long_info`info_version'.dta", replace
  ```
- Line 268: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 269: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 276: city
  ```
  gen capacity = 1
  ```
- Line 277: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 278: district
  ```
  sort district_code
  ```
- Line 279: lon
  ```
  save "$est/budget_long_info`info_version'.dta", replace
  ```
- Line 281: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 282: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 311: district
  ```
  keep id district_code Dpast
  ```
- Line 314: district, name
  ```
  rename district_code Dist_all
  ```
- Line 359: degree
  ```
  * Export master's degree indicator.
  ```
- Line 380: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 381: loc
  ```
  foreach d of local Dist {
  ```
- Line 382: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 394: district, name
  ```
  rename district_code D
  ```
- Line 395: district, name
  ```
  rename Dpast district_code
  ```
- Line 396: district
  ```
  sort district_code
  ```
- Line 398: district
  ```
  sum cz if district_code == 2525
  ```
- Line 403: name
  ```
  rename cz cz_past
  ```
- Line 404: district, name
  ```
  rename district_code Dpast
  ```
- Line 405: district, name
  ```
  rename D district_code
  ```
- Line 406: district
  ```
  sort district_code
  ```
- Line 408: district
  ```
  sum cz if district_code == 2525
  ```
- Line 413: district
  ```
  qui tab district_code
  ```
- Line 415: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 416: loc
  ```
  foreach d of local Dist {
  ```
- Line 417: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 424: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 425: loc
  ```
  foreach d of local Dist {
  ```
- Line 449: district
  ```
  * District Characteristics
  ```
- Line 455: school
  ```
  drop if schoolcode == 8888
  ```
- Line 458: district
  ```
  collapse d, by(district_code)
  ```
- Line 460: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 462: district
  ```
  sort district_code
  ```
- Line 467: district
  ```
  keep n district_code d
  ```
- Line 468: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 475: school
  ```
  drop if schoolcode == 8888
  ```
- Line 478: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 480: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 482: district
  ```
  sort district_code
  ```
- Line 487: district
  ```
  keep n N district_code
  ```
- Line 488: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 495: district
  ```
  keep if district_code != .
  ```
- Line 496: district
  ```
  collapse urban, by(district_code)
  ```
- Line 498: district
  ```
  keep n district_code urban
  ```
- Line 499: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 506: district
  ```
  keep if district_code != .
  ```
- Line 507: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 509: district
  ```
  keep n district_code suburban
  ```
- Line 510: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```
- Line 517: district, lon
  ```
  reshape long urban, i(n) j(district_code)
  ```
- Line 519: district
  ```
  sort district_code
  ```
- Line 521: district
  ```
  sort district_code
  ```
- Line 528: school
  ```
  drop if schoolcode == 8888
  ```
- Line 533: district
  ```
  collapse econ_disadv econ_low (sum) enrl, by(district_code)
  ```
- Line 535: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 537: district
  ```
  sort district_code
  ```
- Line 541: district
  ```
  drop district_code
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset_info3.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 10: loc
  ```
  local info_version 3
  ```
- Line 17: district
  ```
  * Share low achievers by district.
  ```
- Line 19: school
  ```
  drop if schoolcode == 8888
  ```
- Line 22: district
  ```
  collapse d, by(district_code)
  ```
- Line 23: district
  ```
  sort district_code
  ```
- Line 28: district
  ```
  sort district_code
  ```
- Line 37: loc
  ```
  local a = r(r50)
  ```
- Line 54: name
  ```
  rename v0_`info_version' v0
  ```
- Line 55: name
  ```
  rename v1_`info_version' v1
  ```
- Line 63: district
  ```
  * Resolve past district assignment.
  ```
- Line 64: district
  ```
  gen Dpast = district_past2011
  ```
- Line 65: district
  ```
  replace Dpast = district_past2012 if ///
  ```
- Line 66: district
  ```
  (district_past2012 != district_code & district_past2011 == .) & ///
  ```
- Line 67: district
  ```
  district_past2012 != .
  ```
- Line 68: district
  ```
  replace Dpast = district_past2013 if ///
  ```
- Line 69: district
  ```
  (district_past2013 != district_code & district_past2011 == .) & ///
  ```
- Line 70: district
  ```
  district_past2013 != .
  ```
- Line 77: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 79: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 82: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup ///
  ```
- Line 88: loc
  ```
  local model_target_obs = 6600
  ```
- Line 89: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 92: district
  ```
  tempfile synthetic_districts
  ```
- Line 101: lon
  ```
  gen long _model_rank = _n
  ```
- Line 119: district
  ```
  * rows across the 411-district model universe only in synthetic runs.
  ```
- Line 122: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 126: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 127: district
  ```
  qui tab district_code if year == 2014
  ```
- Line 128: district
  ```
  disp "Nr districts ="
  ```
- Line 131: district
  ```
  * Tag districts in this sample.
  ```
- Line 134: district
  ```
  keep district_code
  ```
- Line 136: district
  ```
  sort district_code
  ```
- Line 149: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 159: district
  ```
  sort district_code
  ```
- Line 163: district
  ```
  sort district_code
  ```
- Line 164: name
  ```
  rename *_ba *_0
  ```
- Line 165: name
  ```
  rename *_ma *_1
  ```
- Line 166: district
  ```
  sort district_code
  ```
- Line 167: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 168: district
  ```
  i(district_code) j(masterup)
  ```
- Line 169: name
  ```
  rename *_ *
  ```
- Line 170: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 171: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 174: name
  ```
  rename CExp mode
  ```
- Line 175: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 186: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 188: district
  ```
  sort district_code
  ```
- Line 192: name
  ```
  rename S_* *_
  ```
- Line 198: district
  ```
  reshape wide alpha0_ alpha1_, i(n) j(district_code)
  ```
- Line 217: district
  ```
  * Tag only relevant districts.
  ```
- Line 218: district
  ```
  sort district_code
  ```
- Line 239: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 240: loc
  ```
  foreach d of local Dist {
  ```
- Line 241: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 242: loc
  ```
  local z = r(mean)
  ```
- Line 243: loc
  ```
  local y = 0
  ```
- Line 244: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 254: district
  ```
  if district_code == `d'
  ```
- Line 264: city
  ```
  gen capacity_past = 1
  ```
- Line 265: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 266: district
  ```
  sort district_code
  ```
- Line 267: lon
  ```
  save "$est/budget_past_long_info`info_version'.dta", replace
  ```
- Line 269: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 270: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 277: city
  ```
  gen capacity = 1
  ```
- Line 278: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 279: district
  ```
  sort district_code
  ```
- Line 280: lon
  ```
  save "$est/budget_long_info`info_version'.dta", replace
  ```
- Line 282: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 283: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 312: district
  ```
  keep id district_code Dpast
  ```
- Line 315: district, name
  ```
  rename district_code Dist_all
  ```
- Line 360: degree
  ```
  * Export master's degree indicator.
  ```
- Line 381: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 382: loc
  ```
  foreach d of local Dist {
  ```
- Line 383: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 395: district, name
  ```
  rename district_code D
  ```
- Line 396: district, name
  ```
  rename Dpast district_code
  ```
- Line 397: district
  ```
  sort district_code
  ```
- Line 399: district
  ```
  sum cz if district_code == 2525
  ```
- Line 404: name
  ```
  rename cz cz_past
  ```
- Line 405: district, name
  ```
  rename district_code Dpast
  ```
- Line 406: district, name
  ```
  rename D district_code
  ```
- Line 407: district
  ```
  sort district_code
  ```
- Line 409: district
  ```
  sum cz if district_code == 2525
  ```
- Line 414: district
  ```
  qui tab district_code
  ```
- Line 416: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 417: loc
  ```
  foreach d of local Dist {
  ```
- Line 418: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 425: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 426: loc
  ```
  foreach d of local Dist {
  ```
- Line 450: district
  ```
  * District Characteristics
  ```
- Line 456: school
  ```
  drop if schoolcode == 8888
  ```
- Line 459: district
  ```
  collapse d, by(district_code)
  ```
- Line 461: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 463: district
  ```
  sort district_code
  ```
- Line 468: district
  ```
  keep n district_code d
  ```
- Line 469: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 476: school
  ```
  drop if schoolcode == 8888
  ```
- Line 479: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 481: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 483: district
  ```
  sort district_code
  ```
- Line 488: district
  ```
  keep n N district_code
  ```
- Line 489: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 496: district
  ```
  keep if district_code != .
  ```
- Line 497: district
  ```
  collapse urban, by(district_code)
  ```
- Line 499: district
  ```
  keep n district_code urban
  ```
- Line 500: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 507: district
  ```
  keep if district_code != .
  ```
- Line 508: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 510: district
  ```
  keep n district_code suburban
  ```
- Line 511: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```
- Line 518: district, lon
  ```
  reshape long urban, i(n) j(district_code)
  ```
- Line 520: district
  ```
  sort district_code
  ```
- Line 522: district
  ```
  sort district_code
  ```
- Line 529: school
  ```
  drop if schoolcode == 8888
  ```
- Line 534: district
  ```
  collapse econ_disadv econ_low (sum) enrl, by(district_code)
  ```
- Line 536: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 538: district
  ```
  sort district_code
  ```
- Line 542: district
  ```
  drop district_code
  ```

**/replication-package/replication_delivery/code/build/build_model_dataset_pre2011.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: school
  ```
  * Merge in share low achievers by school.
  ```
- Line 19: school
  ```
  drop if schoolcode == 8888
  ```
- Line 22: district
  ```
  collapse d, by(district_code)
  ```
- Line 23: district
  ```
  sort district_code
  ```
- Line 26: district
  ```
  sort district_code
  ```
- Line 32: school
  ```
  * Merge in share low achievers by school, in 2007-2009.
  ```
- Line 35: school
  ```
  drop if schoolcode == 8888
  ```
- Line 38: district
  ```
  collapse dold, by(district_code)
  ```
- Line 39: district
  ```
  sort district_code
  ```
- Line 42: district
  ```
  sort district_code
  ```
- Line 52: loc
  ```
  local a = r(r50)
  ```
- Line 60: loc
  ```
  local a = r(r50)
  ```
- Line 74: district
  ```
  * Resolve past district assignment.
  ```
- Line 75: district
  ```
  gen Dpast = district_past2007
  ```
- Line 76: district
  ```
  replace Dpast = district_past2008 if ///
  ```
- Line 77: district
  ```
  (district_past2008 != district_code & district_past2007 == .) & ///
  ```
- Line 78: district
  ```
  district_past2008 != .
  ```
- Line 79: district
  ```
  replace Dpast = district_past2009 if ///
  ```
- Line 80: district
  ```
  (district_past2009 != district_code & district_past2007 == .) & ///
  ```
- Line 81: district
  ```
  district_past2009 != .
  ```
- Line 88: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 90: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 95: district, lat
  ```
  * simulation rows across the 411-district model universe only in synthetic runs.
  ```
- Line 96: loc
  ```
  local model_target_obs = 6741
  ```
- Line 97: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 100: district
  ```
  tempfile synthetic_districts
  ```
- Line 103: district
  ```
  target(`model_target_obs') districts("`synthetic_districts'")
  ```
- Line 106: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 107: district
  ```
  qui tab district_code if year == 2010
  ```
- Line 108: district
  ```
  disp "Nr districts ="
  ```
- Line 111: district
  ```
  * Tag districts in this sample.
  ```
- Line 114: district
  ```
  keep district_code
  ```
- Line 116: district
  ```
  sort district_code
  ```
- Line 128: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 138: district
  ```
  sort district_code
  ```
- Line 142: district
  ```
  sort district_code
  ```
- Line 143: name
  ```
  rename *_ba *_0
  ```
- Line 144: name
  ```
  rename *_ma *_1
  ```
- Line 145: district
  ```
  sort district_code
  ```
- Line 146: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 147: district
  ```
  i(district_code) j(masterup)
  ```
- Line 148: name
  ```
  rename *_ *
  ```
- Line 149: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 150: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 153: name
  ```
  rename CExp mode
  ```
- Line 154: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 169: district
  ```
  * Tag only relevant districts.
  ```
- Line 170: district
  ```
  sort district_code
  ```
- Line 191: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 192: loc
  ```
  foreach d of local Dist {
  ```
- Line 193: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 194: loc
  ```
  local z = r(mean)
  ```
- Line 195: loc
  ```
  local y = 0
  ```
- Line 196: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 204: district
  ```
  replace Wage = mode`d' if district_code == `d'
  ```
- Line 215: city
  ```
  gen capacity_past = 1
  ```
- Line 216: city, district
  ```
  collapse (sum) capacity_past budget_past = Wage, by(district_code)
  ```
- Line 217: district
  ```
  sort district_code
  ```
- Line 218: lon
  ```
  save "$sim/budget_past_long_pre_NEW.dta", replace
  ```
- Line 220: city, district
  ```
  keep n district_code capacity_past budget_past
  ```
- Line 221: city, district
  ```
  reshape wide capacity_past budget_past, i(n) j(district_code)
  ```
- Line 230: city
  ```
  gen capacity = 1
  ```
- Line 231: city, district
  ```
  collapse (sum) capacity budget = Wage, by(district_code)
  ```
- Line 232: district
  ```
  sort district_code
  ```
- Line 233: lon
  ```
  save "$sim/budget_long_pre_NEW.dta", replace
  ```
- Line 235: city, district
  ```
  keep n district_code capacity budget
  ```
- Line 236: city, district
  ```
  reshape wide capacity budget, i(n) j(district_code)
  ```
- Line 263: district
  ```
  keep id district_code Dpast
  ```
- Line 266: district, name
  ```
  rename district_code Dist_all
  ```
- Line 319: degree
  ```
  * Export master's degree indicator.
  ```
- Line 344: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 345: loc
  ```
  foreach d of local Dist {
  ```
- Line 346: district
  ```
  gen Match`d' = district_code == `d'
  ```
- Line 360: district, name
  ```
  rename district_code D
  ```
- Line 361: district, name
  ```
  rename Dpast district_code
  ```
- Line 362: district
  ```
  sort district_code
  ```
- Line 364: district
  ```
  sum cz if district_code == 2525
  ```
- Line 369: name
  ```
  rename cz cz_past
  ```
- Line 370: district, name
  ```
  rename district_code Dpast
  ```
- Line 371: district, name
  ```
  rename D district_code
  ```
- Line 372: district
  ```
  sort district_code
  ```
- Line 374: district
  ```
  sum cz if district_code == 2525
  ```
- Line 379: district
  ```
  qui tab district_code
  ```
- Line 381: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 382: loc
  ```
  foreach d of local Dist {
  ```
- Line 383: district
  ```
  qui sum cz if district_code == `d'
  ```
- Line 390: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 391: loc
  ```
  foreach d of local Dist {
  ```
- Line 419: district
  ```
  * District Characteristics
  ```
- Line 425: school
  ```
  drop if schoolcode == 8888
  ```
- Line 428: district
  ```
  collapse d, by(district_code)
  ```
- Line 430: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 432: district
  ```
  sort district_code
  ```
- Line 437: district
  ```
  keep n district_code d
  ```
- Line 438: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 447: district, lon
  ```
  reshape long d, i(n) j(district_code)
  ```
- Line 448: district
  ```
  sort district_code
  ```
- Line 456: school
  ```
  drop if schoolcode == 8888
  ```
- Line 459: district
  ```
  collapse dold, by(district_code)
  ```
- Line 460: district
  ```
  sort district_code
  ```
- Line 465: district
  ```
  keep n district_code d
  ```
- Line 466: district
  ```
  reshape wide d, i(n) j(district_code)
  ```
- Line 475: school
  ```
  drop if schoolcode == 8888
  ```
- Line 478: district
  ```
  collapse (sum) N, by(district_code)
  ```
- Line 480: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 482: district
  ```
  sort district_code
  ```
- Line 487: district
  ```
  keep n N district_code
  ```
- Line 488: district
  ```
  reshape wide N, i(n) j(district_code)
  ```
- Line 497: district
  ```
  keep if district_code != .
  ```
- Line 498: district
  ```
  collapse urban, by(district_code)
  ```
- Line 500: district
  ```
  keep n district_code urban
  ```
- Line 501: district
  ```
  reshape wide urban, i(n) j(district_code)
  ```
- Line 510: district
  ```
  keep if district_code != .
  ```
- Line 511: district
  ```
  collapse suburban, by(district_code)
  ```
- Line 513: district
  ```
  keep n district_code suburban
  ```
- Line 514: district
  ```
  reshape wide suburban, i(n) j(district_code)
  ```

**/replication-package/replication_delivery/code/build/build_simulation_lndist.do**

- Line 1: lat
  ```
  * build_simulation_lndist.do
  ```
- Line 5: lat
  ```
  * This file creates log-distances for the simulation/validation set teachers
  ```
- Line 10: lat
  ```
  * Build the Pre/validation/simulation version of lndist
  ```
- Line 14: district
  ```
  use "$raw/district_distance_sdid.dta", clear
  ```
- Line 22: district
  ```
  use "$raw/district_id_tag_validation.dta", clear
  ```
- Line 23: district
  ```
  mata: tags_val = st_data(., "district_code")
  ```

**/replication-package/replication_delivery/code/build/build_teachers_offer_set.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 29: district
  ```
  keep id District C0 C1 exp wage Inc samecz d Match urban suburban masterup
  ```
- Line 41: loc
  ```
  qui levelsof id, local(ID)
  ```
- Line 42: loc
  ```
  local t = 1
  ```
- Line 43: loc
  ```
  qui foreach id of local ID {
  ```
- Line 45: district
  ```
  sum District if id == `id' & Match == 1
  ```
- Line 46: loc
  ```
  local dist = r(mean)
  ```
- Line 48: loc
  ```
  local C0 = r(mean) + 0.00000000000000001
  ```
- Line 50: loc
  ```
  local C1 = r(mean) + 0.00000000000000001
  ```
- Line 52: loc
  ```
  local exp = r(mean) + 0.00000000000000001
  ```
- Line 54: loc
  ```
  local wage = r(mean) + 0.00001
  ```
- Line 56: loc
  ```
  local masterup = r(mean)
  ```
- Line 66: district
  ```
  wage <= `wage' & masterup >= `masterup' & District == `dist' & ///
  ```
- Line 71: district
  ```
  keep id District v0 v1 exp wage Inc samecz C0 C1 d Match urban ///
  ```
- Line 77: loc
  ```
  local t = `t' + 1
  ```
- Line 87: district
  ```
  sort id District
  ```

**/replication-package/replication_delivery/code/build/build_wageschedule_exitentry.do**

- Line 12: district
  ```
  * Use the "tag" file as a district filter, if needed
  ```
- Line 14: district
  ```
  *tempfile tmp_districts
  ```
- Line 15: district
  ```
  *use "$raw/district_id_tag.dta", clear
  ```
- Line 16: district
  ```
  *save `tmp_districts', replace
  ```
- Line 21: district, name
  ```
  rename district_code District
  ```
- Line 25: district
  ```
  keep District CExp1_ba CExp2_ba CExp3_ba CExp4_ba CExp5_ba CExp6_ba
  ```
- Line 34: district
  ```
  keep District CExp1_ma CExp2_ma CExp3_ma CExp4_ma CExp5_ma CExp6_ma
  ```

**/replication-package/replication_delivery/code/build/gen_omegas.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 15: district
  ```
  * This district is excluded only when working with wages.
  ```
- Line 16: district
  ```
  drop if district_code == 2793 & year == 2010
  ```
- Line 19: district
  ```
  sort district_code
  ```
- Line 26: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 29: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 48: school
  ```
  * Merge in share low achievers by school.
  ```
- Line 51: school
  ```
  drop if schoolcode == 8888
  ```
- Line 54: district
  ```
  collapse d, by(district_code)
  ```
- Line 55: district
  ```
  sort district_code
  ```
- Line 58: district
  ```
  sort district_code
  ```
- Line 68: loc
  ```
  local a = r(r50)
  ```
- Line 113: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 114: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 115: district
  ```
  noisily disp "District n. `d'"
  ```
- Line 117: loc
  ```
  local A0 = 1
  ```
- Line 118: loc
  ```
  local A1 = 1
  ```
- Line 121: loc
  ```
  local A1 = 1
  ```
- Line 125: district
  ```
  if district_code == `d'
  ```
- Line 128: district
  ```
  gen dev = (salary_round - wage) ^ 2 if district_code == `d'
  ```
- Line 129: district
  ```
  qui sum dev if district_code == `d', det
  ```
- Line 130: district
  ```
  replace SS_`A0'_`A1' = r(sum) if district_code == `d'
  ```
- Line 132: loc
  ```
  local A1 = `A1' + 1
  ```
- Line 134: loc
  ```
  local A0 = `A0' + 1
  ```
- Line 138: district
  ```
  replace Min = x if district_code == `d'
  ```
- Line 141: district
  ```
  qui sum Min if district_code == `d', det
  ```
- Line 142: loc
  ```
  local M = r(mean)
  ```
- Line 146: district
  ```
  qui sum SS_`A0'_`A1' if district_code == `d', det
  ```
- Line 147: loc
  ```
  local S = r(mean)
  ```
- Line 152: district
  ```
  replace S_alpha0 = `A0' if district_code == `d'
  ```
- Line 153: district
  ```
  replace S_alpha1 = `A1' if district_code == `d'
  ```
- Line 175: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 178: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 190: district
  ```
  collapse S_alpha*, by(district_code)
  ```
- Line 191: district
  ```
  sort district_code
  ```

**/replication-package/replication_delivery/code/build/gen_omegas_info.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 18: district
  ```
  * This district is excluded only when working with wages.
  ```
- Line 19: district
  ```
  drop if district_code == 2793 & year == 2010
  ```
- Line 22: district
  ```
  sort district_code
  ```
- Line 29: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 32: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 51: district
  ```
  * Merge in share low achievers by district.
  ```
- Line 54: school
  ```
  drop if schoolcode == 8888
  ```
- Line 57: district
  ```
  collapse d, by(district_code)
  ```
- Line 58: district
  ```
  sort district_code
  ```
- Line 63: district
  ```
  sort district_code
  ```
- Line 72: loc
  ```
  local a = r(r50)
  ```
- Line 88: loc
  ```
  local multiplier = 0.5
  ```
- Line 90: loc
  ```
  local multiplier = 1
  ```
- Line 93: loc
  ```
  local multiplier = 2
  ```
- Line 96: loc
  ```
  local multiplier = 4
  ```
- Line 136: loc
  ```
  local title "0.5 * sigma"
  ```
- Line 138: loc
  ```
  local title "1 * sigma"
  ```
- Line 141: loc
  ```
  local title "2 * sigma"
  ```
- Line 144: loc
  ```
  local title "4 * sigma"
  ```
- Line 150: district
  ```
  keep id year district_code exp salary_round mode lambda c0_`info' c1_`info'
  ```
- Line 151: name
  ```
  rename c0_`info' c0
  ```
- Line 152: name
  ```
  rename c1_`info' c1
  ```
- Line 185: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 186: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 187: district
  ```
  noisily disp "District n. `d'"
  ```
- Line 189: loc
  ```
  local A0 = 1
  ```
- Line 190: loc
  ```
  local A1 = 1
  ```
- Line 193: loc
  ```
  local A1 = 1
  ```
- Line 197: district
  ```
  if district_code == `d'
  ```
- Line 200: district
  ```
  gen dev = (salary_round - wage)^2 if district_code == `d'
  ```
- Line 201: district
  ```
  qui sum dev if district_code == `d', det
  ```
- Line 202: district
  ```
  replace SS_`A0'_`A1' = r(sum) if district_code == `d'
  ```
- Line 204: loc
  ```
  local A1 = `A1' + 1
  ```
- Line 206: loc
  ```
  local A0 = `A0' + 1
  ```
- Line 210: district
  ```
  replace Min = x if district_code == `d'
  ```
- Line 213: district
  ```
  qui sum Min if district_code == `d', det
  ```
- Line 214: loc
  ```
  local M = r(mean)
  ```
- Line 218: district
  ```
  qui sum SS_`A0'_`A1' if district_code == `d', det
  ```
- Line 219: loc
  ```
  local S = r(mean)
  ```
- Line 224: district
  ```
  replace S_alpha0 = `A0' if district_code == `d'
  ```
- Line 225: district
  ```
  replace S_alpha1 = `A1' if district_code == `d'
  ```
- Line 247: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 250: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 261: district
  ```
  collapse S_alpha*, by(district_code)
  ```
- Line 262: district
  ```
  sort district_code
  ```

**/replication-package/replication_delivery/code/build/oldteacher_roster_build.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 14: birth, first_name, last_name, name, sex
  ```
  keep last_name first_name file_num year race birth sex distidworksite
  ```
- Line 19: gender, sex
  ```
  gen gender = "F" if sex == "F"
  ```
- Line 20: gender, sex
  ```
  replace gender = "M" if sex == "M"
  ```
- Line 21: sex
  ```
  drop sex
  ```
- Line 23: birth
  ```
  destring birth_year, gen(birth) force
  ```
- Line 24: birth
  ```
  drop birth_year
  ```
- Line 28: first_name, name
  ```
  rename first_name firstname
  ```
- Line 29: last_name, name
  ```
  rename last_name lastname
  ```
- Line 31: district
  ```
  destring distidworksite, gen(district_code) force
  ```
- Line 35: name
  ```
  * Standardize Names
  ```
- Line 38: name
  ```
  * Keep only the first element of multi-word last names.
  ```
- Line 39: name
  ```
  split lastname, gen(Last)
  ```
- Line 40: name
  ```
  replace lastname = Last1 if Last2 != "" & Last1 != "Van" & ///
  ```
- Line 44: name
  ```
  replace lastname = proper(lastname)
  ```
- Line 46: name
  ```
  split lastname, gen(Last) parse("-")
  ```
- Line 47: name
  ```
  replace lastname = Last1 if Last2 != ""
  ```
- Line 48: name
  ```
  replace lastname = Last2 if Last1 == "A"
  ```
- Line 52: name
  ```
  * Keep only the first element of multi-word first names.
  ```
- Line 53: name
  ```
  split firstname, gen(First)
  ```
- Line 54: name
  ```
  replace firstname = First1 if First2 != ""
  ```
- Line 57: name
  ```
  replace firstname = proper(firstname)
  ```
- Line 59: name
  ```
  split firstname, gen(First) parse("-")
  ```
- Line 60: name
  ```
  replace firstname = First1 if First2 != ""
  ```
- Line 61: name
  ```
  replace firstname = First2 if First1 == "A"
  ```
- Line 73: district, name
  ```
  * Carry forward file numbers and demographics within name-district histories.
  ```
- Line 75: birth, district, gender, name
  ```
  gsort lastname firstname birth gender district_code -year
  ```
- Line 76: birth, district, gender, name
  ```
  bysort lastname firstname birth gender district_code: ///
  ```
- Line 77: gender
  ```
  carryforward filenum race gender, replace
  ```
- Line 78: birth, district, gender, name
  ```
  bysort lastname firstname birth gender district_code (year): ///
  ```
- Line 79: gender
  ```
  carryforward filenum race gender, replace
  ```
- Line 80: name
  ```
  rename filenum filenum2
  ```
- Line 82: birth, district, gender
  ```
  sort last first birth gender district_code
  ```
- Line 86: name
  ```
  * Manual Name Corrections
  ```
- Line 89: name
  ```
  replace firstname = "Katherine" if lastname == "Allred" & firstname == "Kate"
  ```
- Line 90: name
  ```
  replace firstname = "Kathryn" if lastname == "Alcorta" & firstname == "Kathy"
  ```
- Line 91: name
  ```
  replace firstname = "Kathryn" if lastname == "Debeir" & firstname == "Kathy"
  ```
- Line 92: name
  ```
  replace firstname = "Kathryn" if lastname == "Semon" & firstname == "Kathy"
  ```
- Line 93: name
  ```
  replace firstname = "Kathryn" if lastname == "Westfahl" & firstname == "Kathy"
  ```
- Line 94: name
  ```
  replace firstname = "Catherine" if lastname == "Wickler" & firstname == "Cathy"
  ```
- Line 95: name
  ```
  replace firstname = "Guadelupe" if lastname == "Ancel" & firstname == "Guadalupe"
  ```
- Line 96: name
  ```
  replace firstname = "Andrzejewski" if lastname == "Andrzejews" & ///
  ```
- Line 97: name
  ```
  firstname == "Katherine"
  ```
- Line 98: name, son
  ```
  replace firstname = "Maria" if lastname == "Anderson" & firstname == "Mara"
  ```
- Line 99: name, son
  ```
  replace firstname = "Kristine" if firstname == "Kris" & lastname == "Arenson"
  ```
- Line 100: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Baumgart"
  ```
- Line 101: name
  ```
  replace firstname = "Erika" if firstname == "Ericka" & lastname == "Beaumont"
  ```
- Line 102: name
  ```
  replace firstname = "Joseph" if firstname == "Joaeph"
  ```
- Line 103: name
  ```
  replace firstname = "Guarinia" if firstname == "Guarina"
  ```
- Line 104: name
  ```
  replace firstname = "Gretchen" if firstname == "Grechen"
  ```
- Line 105: name
  ```
  replace firstname = "Norbert" if firstname == "Nobert"
  ```
- Line 106: name
  ```
  replace firstname = "Judith" if firstname == "Judy"
  ```
- Line 107: name
  ```
  replace firstname = "Judith" if firstname == "Judi"
  ```
- Line 108: name
  ```
  replace firstname = "Nicole" if firstname == "Nichole"
  ```
- Line 109: name
  ```
  replace firstname = "Susan" if firstname == "Sue"
  ```
- Line 110: name
  ```
  replace firstname = "Juliet" if firstname == "Julie" & lastname == "Draxler"
  ```
- Line 111: name
  ```
  replace firstname = "Rosemarie" if firstname == "Rose" & lastname == "Cortenbach"
  ```
- Line 112: name
  ```
  replace firstname = "Katherine" if lastname == "Cowan" & firstname == "Kate"
  ```
- Line 113: name
  ```
  replace firstname = "Theresa" if firstname == "Therese" & lastname == "Fuller"
  ```
- Line 114: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Gardner"
  ```
- Line 115: name, son
  ```
  replace firstname = "Johnette" if firstname == "J" & lastname == "Gunderson"
  ```
- Line 116: name
  ```
  replace firstname = "Janet" if firstname == "Jan" & lastname == "Hansford"
  ```
- Line 117: name
  ```
  replace firstname = "Melissa" if firstname == "Melisa"
  ```
- Line 118: name
  ```
  replace firstname = "Sally" if firstname == "Sallyjoyce"
  ```
- Line 119: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "Hegewald"
  ```
- Line 120: name
  ```
  replace firstname = "Mary" if firstname == "Marylu" & lastname == "Heldt"
  ```
- Line 121: name
  ```
  replace firstname = "Jessica" if firstname == "Jesica"
  ```
- Line 122: name
  ```
  replace firstname = "Kathleen" if firstname == "Kathy" & lastname == "Hennings"
  ```
- Line 123: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Hinds"
  ```
- Line 124: name
  ```
  replace firstname = "Dolores" if firstname == "Delores"
  ```
- Line 125: name
  ```
  replace firstname = "Charisse" if firstname == "Charise"
  ```
- Line 126: name
  ```
  replace firstname = "Carl" if firstname == "C" & lastname == "Huth"
  ```
- Line 127: name
  ```
  replace firstname = "Kristine" if firstname == "Kris" & lastname == "Jack"
  ```
- Line 128: name, son
  ```
  replace firstname = "Luann" if firstname == "Lu" & lastname == "Jacobson"
  ```
- Line 129: name, son
  ```
  replace firstname = "Patricia" if firstname == "Patsy" & lastname == "Johnson"
  ```
- Line 130: name, son
  ```
  replace firstname = "Joann" if firstname == "Jo" & lastname == "Johnson"
  ```
- Line 131: name
  ```
  replace firstname = "Kate" if firstname == "Kathryn" & lastname == "Jones"
  ```
- Line 132: name
  ```
  replace firstname = "Patrick" if firstname == "Pat" & lastname == "Keehner"
  ```
- Line 133: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "King"
  ```
- Line 134: name
  ```
  replace firstname = "Vicki" if firstname == "Vikki"
  ```
- Line 135: name
  ```
  replace firstname = "Geralyn" if firstname == "Geri" & lastname == "Knautz"
  ```
- Line 136: name
  ```
  replace firstname = "Mary" if firstname == "Maryellen" & lastname == "Lachance"
  ```
- Line 137: name
  ```
  replace firstname = "Katherine" if firstname == "Katie" & lastname == "Lachance"
  ```
- Line 138: name
  ```
  replace firstname = "Sarah" if firstname == "Sara" & lastname == "Lafriske"
  ```
- Line 139: name
  ```
  replace firstname = "Christopher" if firstname == "Christoper" & lastname == "Lambert"
  ```
- Line 140: name
  ```
  replace firstname = "Jo" if firstname == "Joann" & lastname == "Leary"
  ```
- Line 141: name
  ```
  replace firstname = "Kathryn" if firstname == "Lee" & lastname == "Kathy"
  ```
- Line 142: name
  ```
  replace firstname = "Frederick" if firstname == "Fred" & lastname == "Leece"
  ```
- Line 143: name
  ```
  replace firstname = "Jacqueline" if firstname == "Jacquline" & lastname == "Leis"
  ```
- Line 144: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Lewis"
  ```
- Line 145: name
  ```
  replace firstname = "Dianne" if firstname == "Diane" & lastname == "Luba"
  ```
- Line 146: name
  ```
  replace firstname = "Edwin" if firstname == "Ed" & lastname == "Lucas"
  ```
- Line 147: name
  ```
  replace firstname = "Allan" if firstname == "Al" & lastname == "Lundquist"
  ```
- Line 148: name
  ```
  replace firstname = "Bradely" if firstname == "Bradley"
  ```
- Line 149: name
  ```
  replace firstname = "Kathleen" if lastname == "Mclellan" & firstname == "Kathy"
  ```
- Line 150: name
  ```
  replace firstname = "Ann" if firstname == "Annmarie" & lastname == "Meissner"
  ```
- Line 151: name
  ```
  replace firstname = "Darlene" if lastname == "D" & lastname == "Mell"
  ```
- Line 152: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "Miller"
  ```
- Line 153: name
  ```
  replace firstname = "Jackie" if firstname == "Jacqulynn" & lastname == "Mitchell"
  ```
- Line 154: name
  ```
  replace firstname = "Bonnie" if firstname == "Bonnita" & lastname == "Modjeski"
  ```
- Line 155: name
  ```
  replace firstname = "Frederick" if firstname == "Fred" & lastname == "Morley"
  ```
- Line 156: name, son
  ```
  replace firstname = "Anna" if firstname == "Anne" & lastname == "Morrison"
  ```
- Line 157: name
  ```
  replace firstname = "Kimberly" if firstname == "Kimberley" & lastname == "Muonio"
  ```
- Line 158: name, son
  ```
  replace firstname = "Linda" if firstname == "Lynda" & lastname == "Nelson"
  ```
- Line 159: name
  ```
  replace firstname = "Samuel" if firstname == "Sam" & lastname == "Northey"
  ```
- Line 160: name
  ```
  replace firstname = "Thomas" if firstname == "Tom" & lastname == "Ojanpera"
  ```
- Line 161: name
  ```
  replace firstname = "Angie" if firstname == "Angela"
  ```
- Line 162: name
  ```
  replace firstname = "Wilfred" if firstname == "Welfred" & lastname == "Ortiz"
  ```
- Line 163: name
  ```
  replace firstname = "Mary" if firstname == "May" & lastname == "Palzkill"
  ```
- Line 164: name
  ```
  replace firstname = "Kenneth" if firstname == "Ken" & lastname == "Paris"
  ```
- Line 165: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Penglase"
  ```
- Line 166: name
  ```
  replace firstname = "Laura" if firstname == "Laurajo" & lastname == "Peters"
  ```
- Line 167: name
  ```
  replace firstname = "Myragene" if firstname == "Myrgene" & lastname == "Pettit"
  ```
- Line 168: name
  ```
  replace firstname = "Donna" if firstname == "D" & lastname == "Polzin"
  ```
- Line 169: name
  ```
  replace firstname = "Patricia" if firstname == "Patti"
  ```
- Line 170: name
  ```
  replace firstname = "Jo" if firstname == "Joanne" & lastname == "Revels"
  ```
- Line 171: name
  ```
  replace firstname = "Marissa" if firstname == "Marisa" & lastname == "Romanelli"
  ```
- Line 172: name, son
  ```
  replace firstname = "Allison" if firstname == "Allyson" & lastname == "Ryan"
  ```
- Line 173: name
  ```
  replace firstname = "Gregory" if firstname == "Greg" & lastname == "Sauve"
  ```
- Line 174: name
  ```
  replace firstname = "Jeffrey" if firstname == "Jeffery"
  ```
- Line 175: name
  ```
  replace firstname = "Debra" if firstname == "Deb" & lastname == "Schroeder"
  ```
- Line 176: name
  ```
  replace firstname = "James" if firstname == "Jim" & lastname == "Schuchardt"
  ```
- Line 177: name
  ```
  replace firstname = "Janet" if firstname == "Jan" & lastname == "Schuldt"
  ```
- Line 178: name
  ```
  replace firstname = "Kimberly" if firstname == "Kim" & lastname == "Serene"
  ```
- Line 179: name
  ```
  replace firstname = "Theresa" if firstname == "Teresa" & lastname == "Shelton"
  ```
- Line 180: name, son
  ```
  replace firstname = "Julia" if firstname == "Jubie" & lastname == "Simonson"
  ```
- Line 181: name
  ```
  replace firstname = "Naomi" if firstname == "Namoi"
  ```
- Line 182: name
  ```
  replace firstname = "Elizabeth" if firstname == "E" & lastname == "Spear"
  ```
- Line 183: name
  ```
  replace firstname = "Margaret" if firstname == "Peg" & lastname == "Stachowiak"
  ```
- Line 184: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Stellick"
  ```
- Line 185: name
  ```
  replace firstname = "Douglas" if firstname == "Doug" & lastname == "Straus"
  ```
- Line 186: name
  ```
  replace firstname = "Jenelle" if firstname == "Jenell"
  ```
- Line 187: name
  ```
  replace firstname = "Jane" if firstname == "Jan" & lastname == "Wagner"
  ```
- Line 188: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Wall"
  ```
- Line 189: name
  ```
  replace firstname = "Jessie" if firstname == "Jesse" & lastname == "Warner"
  ```
- Line 190: name
  ```
  replace firstname = "Catherine" if firstname == "Catharine" & lastname == "Wellman"
  ```
- Line 191: name
  ```
  replace firstname = "Allan" if firstname == "Allen" & lastname == "Wendland"
  ```
- Line 192: name
  ```
  replace firstname = "Eric" if firstname == "Erica" & lastname == "Wenninger"
  ```
- Line 193: name
  ```
  replace firstname = "Dustin" if firstname == "Dustian"
  ```
- Line 194: name
  ```
  replace firstname = "Keshum" if firstname == "Keushum"
  ```
- Line 195: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Zander"
  ```
- Line 198: name, son
  ```
  replace filenum = 646059 if (lastname == "Johnson" & ///
  ```
- Line 199: birth, name
  ```
  firstname == "Christine" & birth == 1979)
  ```
- Line 200: name
  ```
  replace firstname = "Jonathan" if (lastname == "Jonathon" & ///
  ```
- Line 201: birth, name, son
  ```
  firstname == "Thompson" & birth == 1976 & filenum == 661623)
  ```
- Line 202: name, son
  ```
  replace lastname = "Thompson" if (lastname == "Jonathon" & ///
  ```
- Line 203: birth, name
  ```
  firstname == "Jonathan" & birth == 1976 & filenum == 661623)
  ```
- Line 204: name
  ```
  replace firstname = "Billie" if (lastname == "Billie" & ///
  ```
- Line 205: birth, name
  ```
  firstname == "Rau" & birth == 1977 & filenum == 637687)
  ```
- Line 206: name
  ```
  replace lastname = "Rau" if (lastname == "Billie" & ///
  ```
- Line 207: birth, name
  ```
  firstname == "Billie" & birth == 1977 & filenum == 637687)
  ```
- Line 208: name
  ```
  replace filenum = 707967 if (lastname == "Worzella" & firstname == "Jeff")
  ```
- Line 209: name
  ```
  replace lastname = subinstr(lastname," Jr","",1)
  ```
- Line 210: name
  ```
  replace lastname = subinstr(lastname," Iii","",1)
  ```
- Line 211: name
  ```
  replace lastname = subinstr(lastname," Ii","",1)
  ```

**/replication-package/replication_delivery/code/build/omega_databuild.do**

- Line 4: district
  ```
  * Purpose: Build district-level data for the analysis of omega choices.
  ```
- Line 13: census
  ```
  * Crosswalk state id-census id
  ```
- Line 15: district
  ```
  replace district_code = substr(district_code,4,4)
  ```
- Line 16: district
  ```
  destring district_code, replace
  ```
- Line 17: district
  ```
  keep district_code id
  ```
- Line 18: loc
  ```
  local new = _N + 2
  ```
- Line 20: loc
  ```
  local z = _N
  ```
- Line 21: district
  ```
  replace district_code = 4998 in `z'
  ```
- Line 23: loc
  ```
  local z = _N - 1
  ```
- Line 24: district
  ```
  replace district_code = 3913 in `z'
  ```
- Line 26: census, name
  ```
  rename id censusid
  ```
- Line 27: census
  ```
  sort censusid
  ```
- Line 33: district, school
  ```
  "$raw/nces_acs_2013-2017/EDGE_Export_101244429626/DP03.6_001_USSchoolDistrictAll_101244423682.txt", 
  ```
- Line 34: name
  ```
  names clear delimiter("|")
  ```
- Line 37: name
  ```
  rename dp036_62est medianinc
  ```
- Line 38: name
  ```
  rename dp036_63est meaninc
  ```
- Line 39: name
  ```
  rename dp036_61pct share_inc_above_200k
  ```
- Line 40: census, name
  ```
  rename leaid censusid
  ```
- Line 42: census
  ```
  drop if censusid == 5599999
  ```
- Line 43: census
  ```
  sort censusid
  ```
- Line 47: census
  ```
  sort censusid
  ```
- Line 53: district, school
  ```
  "$raw/nces_acs_2013-2017/EDGE_Export_101244639981/DP03.8_001_USSchoolDistrictAll_101244637329.txt", 
  ```
- Line 54: name
  ```
  names clear delimiter("|")
  ```
- Line 57: name
  ```
  rename dp038_119pct share_poverty
  ```
- Line 58: census, name
  ```
  rename leaid censusid
  ```
- Line 60: census
  ```
  drop if censusid == 5599999
  ```
- Line 61: census
  ```
  sort censusid
  ```
- Line 65: census
  ```
  sort censusid
  ```
- Line 71: district, school
  ```
  "$raw/nces_acs_2013-2017/EDGE_Export_1012447445/DP02.7_001_USSchoolDistrictAll_101244740697.txt", //
  ```
- Line 72: name
  ```
  names clear delimiter("|")
  ```
- Line 75: name, school
  ```
  rename dp027_61pct highschool
  ```
- Line 76: name
  ```
  rename dp027_67pct ba_or_higher
  ```
- Line 77: census, name
  ```
  rename leaid censusid
  ```
- Line 79: census
  ```
  drop if censusid == 5599999
  ```
- Line 80: census
  ```
  sort censusid
  ```
- Line 84: census
  ```
  sort censusid
  ```
- Line 90: district, school
  ```
  "$raw/nces_acs_2013-2017/EDGE_Export_101253245819/DP05.1_001_USSchoolDistrictAll_101253240998.txt", 
  ```
- Line 91: name
  ```
  names clear delimiter("|")
  ```
- Line 103: census, name
  ```
  rename leaid censusid
  ```
- Line 104: census
  ```
  drop if censusid == 5599999
  ```
- Line 105: census
  ```
  sort censusid
  ```
- Line 109: census
  ```
  sort censusid
  ```
- Line 118: district
  ```
  use "$raw/crosswalk_district_county.dta", clear
  ```
- Line 119: loc
  ```
  local new = _N + 4
  ```
- Line 121: loc
  ```
  local z = _N - 3
  ```
- Line 122: district
  ```
  replace district_code = 2527 in `z'
  ```
- Line 124: loc
  ```
  local z = _N - 2
  ```
- Line 125: district
  ```
  replace district_code = 3689 in `z'
  ```
- Line 127: loc
  ```
  local z = _N - 1
  ```
- Line 128: district
  ```
  replace district_code = 3913 in `z'
  ```
- Line 130: loc
  ```
  local z = _N
  ```
- Line 131: district
  ```
  replace district_code = 4998 in `z'
  ```
- Line 145: district
  ```
  collapse democratic, by(district_code)
  ```
- Line 147: district
  ```
  sort district_code
  ```
- Line 150: district
  ```
  sort district_code
  ```
- Line 155: district
  ```
  * Characteristics of Teachers Districts Would Like to Hire
  ```
- Line 160: district, lon
  ```
  reshape long Inc, i(id) j(District)
  ```
- Line 165: district
  ```
  keep id District exp d
  ```
- Line 167: district
  ```
  sort id District
  ```
- Line 172: district, name
  ```
  rename District district_code
  ```
- Line 173: district
  ```
  keep id district_code d
  ```
- Line 184: district
  ```
  (p10) exp_10pct = totalexp2 (p10) c_10pct = c, by(district_code)
  ```
- Line 185: name
  ```
  rename * attr_*
  ```
- Line 186: name
  ```
  rename attr_d* d*
  ```
- Line 198: district, lon
  ```
  reshape long Match, i(id) j(district_code)
  ```
- Line 206: district, lon
  ```
  reshape long d, i(n) j(district_code)
  ```
- Line 209: district
  ```
  sort district_code
  ```
- Line 216: district
  ```
  sort district_code
  ```
- Line 220: district
  ```
  collapse tc_hired, by(district_code)
  ```
- Line 232: school
  ```
  drop if schoolcode == 8888
  ```
- Line 236: district
  ```
  collapse d econ_disadvantage (sum) Nr_students, by(district_code)
  ```
- Line 240: district
  ```
  sort district_code
  ```
- Line 251: district
  ```
  * Generate variables used in district-level summaries.
  ```
- Line 255: district
  ```
  gen milwaukee = District == 3619
  ```
- Line 259: district
  ```
  use "$raw/district_county_cz.dta", clear
  ```
- Line 260: district
  ```
  keep district_code cz
  ```
- Line 261: district, name
  ```
  rename district District
  ```
- Line 266: district
  ```
  sort District
  ```
- Line 268: district
  ```
  sum cz if District == 2525
  ```
- Line 282: district
  ```
  collapse Exp* tc* tenured d (sum) nr, by(District)
  ```
- Line 283: name
  ```
  rename * CZ_*
  ```
- Line 284: district, name
  ```
  rename CZ_District District
  ```
- Line 285: district
  ```
  sort District
  ```
- Line 293: district
  ```
  * Summarize characteristics of each district's incumbent teachers.
  ```
- Line 295: district
  ```
  bysort District: egen `X' = sd(tc) if Exp1 == 0 & Inc == 1
  ```
- Line 296: district
  ```
  bysort District: egen tc_sd = max(`X')
  ```
- Line 303: district
  ```
  bysort District: egen `X' = mean(`var') if Inc == 1
  ```
- Line 304: district
  ```
  bysort District: egen `Y' = max(`X')
  ```
- Line 309: city
  ```
  keep Exp* tenured tc* master *alpha* capacity budget milwaukee urban suburban ///
  ```
- Line 310: district
  ```
  d cz District wage C0 C1
  ```
- Line 314: district
  ```
  sort District
  ```
- Line 319: lat
  ```
  * Calculate leave-one-out zone characteristics.
  ```
- Line 320: city
  ```
  gen budget_pc = budget / (capacity)
  ```
- Line 322: city, district
  ```
  keep urban suburban budget_pc d capacity District cz
  ```
- Line 323: city
  ```
  foreach var in urban suburban budget_pc d capacity {
  ```
- Line 325: district, loc
  ```
  qui levelsof District, local(D)
  ```
- Line 326: loc
  ```
  foreach d of local D {
  ```
- Line 327: district
  ```
  qui sum cz if District == `d'
  ```
- Line 328: loc
  ```
  local cz = `r(mean)'
  ```
- Line 329: district
  ```
  qui sum `var' if cz == `cz' & District != `d'
  ```
- Line 330: district
  ```
  replace CZc_`var' = `r(mean)' if cz == `cz' & District == `d'
  ```
- Line 334: district
  ```
  sort District
  ```
- Line 344: district, name
  ```
  rename District district_code
  ```
- Line 345: district
  ```
  sort district_code
  ```
- Line 354: city
  ```
  replace CZ_nr = CZ_nr / capacity
  ```
- Line 356: district
  ```
  sort district_code
  ```
- Line 361: district
  ```
  sort district_code
  ```
- Line 365: district
  ```
  sort district_code
  ```
- Line 368: district
  ```
  sort district_code
  ```
- Line 371: district
  ```
  sort district_code
  ```

**/replication-package/replication_delivery/code/build/schedule_new.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 6: district
  ```
  * Purpose: Build and analyze district-specific wage schedules.
  ```
- Line 21: district
  ```
  * Estimate pre-Act 10 wage schedules, fully district-specific.
  ```
- Line 24: district
  ```
  * Generate fictitious teachers for district-by-experience cells that need them.
  ```
- Line 25: district, loc
  ```
  levelsof district_code, local(D)
  ```
- Line 26: loc
  ```
  foreach d of local D {
  ```
- Line 30: district
  ```
  district_code == `d'
  ```
- Line 32: district
  ```
  disp "district = `d'"
  ```
- Line 37: district
  ```
  replace district_code = `d' if id == _N + 500000000000
  ```
- Line 43: district
  ```
  * Estimate district-specific wage schedule cells.
  ```
- Line 44: district
  ```
  sort district_code
  ```
- Line 45: district, loc
  ```
  levelsof district_code, local(D)
  ```
- Line 46: loc
  ```
  foreach d of local D {
  ```
- Line 47: district
  ```
  gen E1_`d' = district_code == `d' & exp == 1
  ```
- Line 48: district
  ```
  gen E2_`d' = district_code == `d' & exp == 2
  ```
- Line 49: district
  ```
  gen E3_`d' = district_code == `d' & exp == 3
  ```
- Line 50: district
  ```
  gen E4_`d' = district_code == `d' & exp == 4
  ```
- Line 51: district
  ```
  gen E5_`d' = district_code == `d' & exp == 5
  ```
- Line 52: district
  ```
  gen E6_`d' = district_code == `d' & exp == 6
  ```
- Line 54: district
  ```
  gen M1_`d' = district_code == `d' & masterup == 1
  ```
- Line 60: district, loc
  ```
  qui levelsof district_code, local(D)
  ```
- Line 61: loc
  ```
  foreach d of local D {
  ```
- Line 63: district
  ```
  replace mode = _b[E`e'_`d'] if district_code == `d' & ///
  ```
- Line 66: district
  ```
  district_code == `d' & exp == `e' & masterup == 1
  ```
- Line 75: district
  ```
  * Collapse to one wage-schedule row per district.
  ```
- Line 76: district
  ```
  collapse (mean) C*, by(district_code)
  ```
- Line 81: district
  ```
  sort district_code
  ```
- Line 89: district, loc
  ```
  levelsof district_code if `Var' == ., local(D)
  ```
- Line 92: loc
  ```
  foreach d of local D {
  ```
- Line 93: district
  ```
  qui tab `Var' if district_code == `d'
  ```
- Line 95: district
  ```
  qui tab `var' if district_code == `d'
  ```
- Line 97: district
  ```
  sum `var' if district_code == `d'
  ```
- Line 98: loc
  ```
  local z = `r(mean)'
  ```
- Line 99: district
  ```
  disp "district = `d'"
  ```
- Line 100: district
  ```
  qui levelsof district_code if `var' != . & ///
  ```
- Line 101: district, loc
  ```
  `Var' != . & district_code != `d', local(DD)
  ```
- Line 102: loc
  ```
  local gap = 1000000
  ```
- Line 103: loc
  ```
  foreach dd of local DD {
  ```
- Line 104: district
  ```
  qui sum `var' if district_code == `dd'
  ```
- Line 105: loc
  ```
  local dev = abs(`z' - `r(mean)')
  ```
- Line 106: loc
  ```
  local y = `r(mean)'
  ```
- Line 109: district
  ```
  qui sum `Var' if district_code == `dd'
  ```
- Line 110: district
  ```
  replace `Var' = `y' if district_code == `d'
  ```
- Line 111: loc
  ```
  local gap = `dev'
  ```
- Line 121: district, loc
  ```
  levelsof district_code if `Var' == ., local(D)
  ```
- Line 124: loc
  ```
  foreach d of local D {
  ```
- Line 125: district
  ```
  qui tab `Var' if district_code == `d'
  ```
- Line 127: district
  ```
  qui tab `var' if district_code == `d'
  ```
- Line 129: district
  ```
  sum `var' if district_code == `d'
  ```
- Line 130: loc
  ```
  local z = `r(mean)'
  ```
- Line 131: district
  ```
  disp "district = `d'"
  ```
- Line 132: district
  ```
  qui levelsof district_code if `var' != . & ///
  ```
- Line 133: district, loc
  ```
  `Var' != . & district_code != `d', local(DD)
  ```
- Line 134: loc
  ```
  local gap = 1000000
  ```
- Line 135: loc
  ```
  foreach dd of local DD {
  ```
- Line 136: district
  ```
  qui sum `var' if district_code == `dd'
  ```
- Line 137: loc
  ```
  local dev = abs(`z' - `r(mean)')
  ```
- Line 138: loc
  ```
  local y = `r(mean)'
  ```
- Line 141: district
  ```
  qui sum `Var' if district_code == `dd'
  ```
- Line 142: district
  ```
  replace `Var' = `y' if district_code == `d'
  ```
- Line 143: loc
  ```
  local gap = `dev'
  ```
- Line 153: district, loc
  ```
  levelsof district_code if `Var' == ., local(D)
  ```
- Line 155: loc
  ```
  foreach d of local D {
  ```
- Line 156: district
  ```
  qui tab `Var' if district_code == `d'
  ```
- Line 158: district
  ```
  qui tab `var' if district_code == `d'
  ```
- Line 160: district
  ```
  sum `var' if district_code == `d'
  ```
- Line 161: loc
  ```
  local z = `r(mean)'
  ```
- Line 162: district
  ```
  disp "district = `d'"
  ```
- Line 163: district
  ```
  qui levelsof district_code if `var' != . & ///
  ```
- Line 164: district, loc
  ```
  `Var' != . & district_code != `d', local(DD)
  ```
- Line 165: loc
  ```
  local gap = 1000000
  ```
- Line 166: loc
  ```
  foreach dd of local DD {
  ```
- Line 167: district
  ```
  qui sum `var' if district_code == `dd'
  ```
- Line 168: loc
  ```
  local dev = abs(`z' - `r(mean)')
  ```
- Line 169: loc
  ```
  local y = `r(mean)'
  ```
- Line 172: district
  ```
  qui sum `Var' if district_code == `dd'
  ```
- Line 173: district
  ```
  replace `Var' = `y' if district_code == `d'
  ```
- Line 174: loc
  ```
  local gap = `dev'
  ```
- Line 184: district, loc
  ```
  levelsof district_code if `Var' == ., local(D)
  ```
- Line 186: loc
  ```
  foreach d of local D {
  ```
- Line 187: district
  ```
  qui tab `Var' if district_code == `d'
  ```
- Line 189: district
  ```
  qui tab `var' if district_code == `d'
  ```
- Line 191: district
  ```
  sum `var' if district_code == `d'
  ```
- Line 192: loc
  ```
  local z = `r(mean)'
  ```
- Line 193: district
  ```
  disp "district = `d'"
  ```
- Line 194: district
  ```
  qui levelsof district_code if `var' != . & ///
  ```
- Line 195: district, loc
  ```
  `Var' != . & district_code != `d', local(DD)
  ```
- Line 196: loc
  ```
  local gap = 1000000
  ```
- Line 197: loc
  ```
  foreach dd of local DD {
  ```
- Line 198: district
  ```
  qui sum `var' if district_code == `dd'
  ```
- Line 199: loc
  ```
  local dev = abs(`z' - `r(mean)')
  ```
- Line 200: loc
  ```
  local y = `r(mean)'
  ```
- Line 203: district
  ```
  qui sum `Var' if district_code == `dd'
  ```
- Line 204: district
  ```
  replace `Var' = `y' if district_code == `d'
  ```
- Line 205: loc
  ```
  local gap = `dev'
  ```
- Line 215: district, loc
  ```
  levelsof district_code if `Var' == ., local(D)
  ```
- Line 217: loc
  ```
  foreach d of local D {
  ```
- Line 218: district
  ```
  qui tab `Var' if district_code == `d'
  ```
- Line 220: district
  ```
  qui tab `var' if district_code == `d'
  ```
- Line 222: district
  ```
  sum `var' if district_code == `d'
  ```
- Line 223: loc
  ```
  local z = `r(mean)'
  ```
- Line 224: district
  ```
  disp "district = `d'"
  ```
- Line 225: district
  ```
  qui levelsof district_code if `var' != . & ///
  ```
- Line 226: district, loc
  ```
  `Var' != . & district_code != `d', local(DD)
  ```
- Line 227: loc
  ```
  local gap = 1000000
  ```
- Line 228: loc
  ```
  foreach dd of local DD {
  ```
- Line 229: district
  ```
  qui sum `var' if district_code == `dd'
  ```
- Line 230: loc
  ```
  local dev = abs(`z' - `r(mean)')
  ```
- Line 231: loc
  ```
  local y = `r(mean)'
  ```
- Line 234: district
  ```
  qui sum `Var' if district_code == `dd'
  ```
- Line 235: district
  ```
  replace `Var' = `y' if district_code == `d'
  ```
- Line 236: loc
  ```
  local gap = `dev'
  ```
- Line 246: district
  ```
  * a small subset of districts. The model and forward repeated-game exercises,
  ```
- Line 247: district
  ```
  * however, use the same 411-district choice set. Only for synthetic runs, add
  ```
- Line 248: district
  ```
  * the missing canonical model districts and fill their schedule cells with the
  ```
- Line 249: district
  ```
  * corresponding means among observed synthetic districts. Restricted-data
  ```
- Line 250: district
  ```
  * runs retain their original district coverage and estimated schedules.
  ```
- Line 251: loc
  ```
  local using_synthetic = strpos(lower("$confdata"), "synthetic_data") > 0
  ```
- Line 254: district
  ```
  tempfile synthetic_districts
  ```
- Line 256: district
  ```
  _synthetic_pad_current_districts, districts("`synthetic_districts'")
  ```
- Line 259: district
  ```
  isid district_code
  ```
- Line 269: lon
  ```
  * The forward repeated-game code expects the completed wage schedule alongside
  ```
- Line 271: second
  ```
  * files remain exactly identical rather than maintaining a second build path.
  ```

**/replication-package/replication_delivery/code/build/staff_build.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 20: name
  ```
  str lastname        10  -   29  ///
  ```
- Line 21: name
  ```
  str firstname       30  -   45  ///
  ```
- Line 22: gender
  ```
  str gender          46  -   46  ///
  ```
- Line 24: birth
  ```
  birth           48  -   51  ///
  ```
- Line 27: loc
  ```
  localexp        66  -   68  ///
  ```
- Line 35: school
  ```
  schoolcode      111 -   114 ///
  ```
- Line 44: name
  ```
  str workagencyname  147 -   176 ///
  ```
- Line 45: lname, name, school
  ```
  str schoolname      177 -   206 ///
  ```
- Line 50: zip
  ```
  str zipcode         443 -   452 ///
  ```
- Line 73: name
  ```
  str lastname        11-30   ///
  ```
- Line 74: name
  ```
  str firstname       31-46   ///
  ```
- Line 76: gender
  ```
  str gender          53-53   ///
  ```
- Line 78: birth
  ```
  birth           55-58   ///
  ```
- Line 81: loc
  ```
  localexp        72-75   ///
  ```
- Line 89: school
  ```
  schoolcode      118-121 ///
  ```
- Line 98: name
  ```
  str workagencyname  140-183 ///
  ```
- Line 99: lname, name, school
  ```
  str schoolname      184-213 ///
  ```
- Line 104: zip
  ```
  str zipcode         450-459 ///
  ```
- Line 121: name
  ```
  rename idnbr id
  ```
- Line 122: name
  ```
  rename lastname lastname
  ```
- Line 123: name
  ```
  rename firstname firstname
  ```
- Line 124: name
  ```
  rename filenumber filenumber
  ```
- Line 125: gender, name
  ```
  rename gndr gender
  ```
- Line 126: name
  ```
  rename raceethn race
  ```
- Line 127: birth, name
  ```
  rename birthyear birth
  ```
- Line 128: degree, name
  ```
  rename highdegree highedu
  ```
- Line 129: name
  ```
  rename cntrctdays dayscontract
  ```
- Line 130: loc, name
  ```
  rename localexp localexp
  ```
- Line 131: name
  ```
  rename totalexp totalexp
  ```
- Line 132: name
  ```
  rename totsalary salary
  ```
- Line 133: name
  ```
  rename totfringe fringe
  ```
- Line 134: name
  ```
  rename staffcat staffcat
  ```
- Line 135: name
  ```
  rename hireagncycd hireagencycode
  ```
- Line 136: name
  ```
  rename workagncycd workagencycode
  ```
- Line 137: name
  ```
  rename hireagncytyp hireagencytype
  ```
- Line 138: name, school
  ```
  rename schoolcd schoolcode
  ```
- Line 139: name
  ```
  rename positioncd position
  ```
- Line 140: name
  ```
  rename assgnareacd area
  ```
- Line 141: name
  ```
  rename lowgrd lowgrade
  ```
- Line 142: name
  ```
  rename highgrd highgrade
  ```
- Line 143: name
  ```
  rename lgsortcd lowgradecode
  ```
- Line 144: name
  ```
  rename hgsortcd highgradecode
  ```
- Line 145: name
  ```
  rename bilingual bilingual
  ```
- Line 146: name
  ```
  rename assgnfte fte
  ```
- Line 147: loc, location, name
  ```
  rename worklocationname workagencyname
  ```
- Line 148: lname, name, school
  ```
  rename schoolname schoolname
  ```
- Line 149: name
  ```
  rename grdlevel gradelevel
  ```
- Line 150: name
  ```
  rename cesanumber cesa
  ```
- Line 151: name
  ```
  rename cntynbr county
  ```
- Line 152: name
  ```
  rename workagncytyp workagencytype
  ```
- Line 153: name, zip
  ```
  rename mailzipcd zipcode
  ```
- Line 154: name
  ```
  rename ltsub lt_substitute
  ```
- Line 155: name
  ```
  rename subcntrctd subcontracted
  ```
- Line 156: school
  ```
  destring schoolcode, replace
  ```
- Line 162: birth, gender, name
  ```
  keep id lastname firstname filenumber gender race birth highedu ///
  ```
- Line 163: loc
  ```
  dayscontract localexp totalexp salary fringe staffcat ///
  ```
- Line 164: school
  ```
  hireagencycode workagencycode hireagencytype schoolcode position ///
  ```
- Line 166: lname, name, school
  ```
  workagencyname schoolname gradelevel cesa county workagencytype ///
  ```
- Line 167: zip
  ```
  zipcode lt_substitute subcontracted
  ```
- Line 192: name
  ```
  * Prepare Staff Names and File Numbers
  ```
- Line 198: district, name
  ```
  rename workagencycode district_code
  ```
- Line 199: birth, district, gender
  ```
  keep id last first gender birth year filenum race district_code
  ```
- Line 200: birth, gender
  ```
  sort last first gender birth year
  ```
- Line 202: name
  ```
  * Keep only the first element of multi-word last names.
  ```
- Line 203: name
  ```
  split lastname, gen(Last)
  ```
- Line 204: name
  ```
  replace lastname = Last1 if Last2 != "" & Last1 != "Van" & ///
  ```
- Line 208: name
  ```
  replace lastname = proper(lastname)
  ```
- Line 210: name
  ```
  split lastname, gen(Last) parse("-")
  ```
- Line 211: name
  ```
  replace lastname = Last1 if Last2 != ""
  ```
- Line 212: name
  ```
  replace lastname = Last2 if Last1 == "A"
  ```
- Line 216: name
  ```
  * Keep only the first element of multi-word first names.
  ```
- Line 217: name
  ```
  split firstname, gen(First)
  ```
- Line 218: name
  ```
  replace firstname = First1 if First2 != ""
  ```
- Line 221: name
  ```
  replace firstname = proper(firstname)
  ```
- Line 223: name
  ```
  split firstname, gen(First) parse("-")
  ```
- Line 224: name
  ```
  replace firstname = First1 if First2 != ""
  ```
- Line 225: name
  ```
  replace firstname = First2 if First1 == "A"
  ```
- Line 229: name
  ```
  * Manual name corrections.
  ```
- Line 230: name
  ```
  replace firstname = "Katherine" if lastname == "Allred" & firstname == "Kate"
  ```
- Line 231: name
  ```
  replace firstname = "Kathryn" if lastname == "Alcorta" & firstname == "Kathy"
  ```
- Line 232: name
  ```
  replace firstname = "Kathryn" if lastname == "Debeir" & firstname == "Kathy"
  ```
- Line 233: name
  ```
  replace firstname = "Kathryn" if lastname == "Semon" & firstname == "Kathy"
  ```
- Line 234: name
  ```
  replace firstname = "Kathryn" if lastname == "Westfahl" & firstname == "Kathy"
  ```
- Line 235: name
  ```
  replace firstname = "Catherine" if lastname == "Wickler" & firstname == "Cathy"
  ```
- Line 236: name
  ```
  replace firstname = "Guadelupe" if lastname == "Ancel" & firstname == "Guadalupe"
  ```
- Line 237: name
  ```
  replace firstname = "Andrzejewski" if lastname == "Andrzejews" & ///
  ```
- Line 238: name
  ```
  firstname == "Katherine"
  ```
- Line 239: name, son
  ```
  replace firstname = "Maria" if lastname == "Anderson" & firstname == "Mara"
  ```
- Line 240: name, son
  ```
  replace firstname = "Kristine" if firstname == "Kris" & lastname == "Arenson"
  ```
- Line 241: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Baumgart"
  ```
- Line 242: name
  ```
  replace firstname = "Erika" if firstname == "Ericka" & lastname == "Beaumont"
  ```
- Line 243: name
  ```
  replace firstname = "Joseph" if firstname == "Joaeph"
  ```
- Line 244: name
  ```
  replace firstname = "Guarinia" if firstname == "Guarina"
  ```
- Line 245: name
  ```
  replace firstname = "Gretchen" if firstname == "Grechen"
  ```
- Line 246: name
  ```
  replace firstname = "Norbert" if firstname == "Nobert"
  ```
- Line 247: name
  ```
  replace firstname = "Judith" if firstname == "Judy"
  ```
- Line 248: name
  ```
  replace firstname = "Judith" if firstname == "Judi"
  ```
- Line 249: name
  ```
  replace firstname = "Nicole" if firstname == "Nichole"
  ```
- Line 250: name
  ```
  replace firstname = "Susan" if firstname == "Sue"
  ```
- Line 251: name
  ```
  replace firstname = "Juliet" if firstname == "Julie" & lastname == "Draxler"
  ```
- Line 252: name
  ```
  replace firstname = "Rosemarie" if firstname == "Rose" & lastname == "Cortenbach"
  ```
- Line 253: name
  ```
  replace firstname = "Katherine" if lastname == "Cowan" & firstname == "Kate"
  ```
- Line 254: name
  ```
  replace firstname = "Theresa" if firstname == "Therese" & lastname == "Fuller"
  ```
- Line 255: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Gardner"
  ```
- Line 256: name, son
  ```
  replace firstname = "Johnette" if firstname == "J" & lastname == "Gunderson"
  ```
- Line 257: name
  ```
  replace firstname = "Janet" if firstname == "Jan" & lastname == "Hansford"
  ```
- Line 258: name
  ```
  replace firstname = "Melissa" if firstname == "Melisa"
  ```
- Line 259: name
  ```
  replace firstname = "Sally" if firstname == "Sallyjoyce"
  ```
- Line 260: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "Hegewald"
  ```
- Line 261: name
  ```
  replace firstname = "Mary" if firstname == "Marylu" & lastname == "Heldt"
  ```
- Line 262: name
  ```
  replace firstname = "Jessica" if firstname == "Jesica"
  ```
- Line 263: name
  ```
  replace firstname = "Kathleen" if firstname == "Kathy" & lastname == "Hennings"
  ```
- Line 264: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Hinds"
  ```
- Line 265: name
  ```
  replace firstname = "Dolores" if firstname == "Delores"
  ```
- Line 266: name
  ```
  replace firstname = "Charisse" if firstname == "Charise"
  ```
- Line 267: name
  ```
  replace firstname = "Carl" if firstname == "C" & lastname == "Huth"
  ```
- Line 268: name
  ```
  replace firstname = "Kristine" if firstname == "Kris" & lastname == "Jack"
  ```
- Line 269: name, son
  ```
  replace firstname = "Luann" if firstname == "Lu" & lastname == "Jacobson"
  ```
- Line 270: name, son
  ```
  replace firstname = "Patricia" if firstname == "Patsy" & lastname == "Johnson"
  ```
- Line 271: name, son
  ```
  replace firstname = "Joann" if firstname == "Jo" & lastname == "Johnson"
  ```
- Line 272: name
  ```
  replace firstname = "Kate" if firstname == "Kathryn" & lastname == "Jones"
  ```
- Line 273: name
  ```
  replace firstname = "Patrick" if firstname == "Pat" & lastname == "Keehner"
  ```
- Line 274: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "King"
  ```
- Line 275: name
  ```
  replace firstname = "Vicki" if firstname == "Vikki"
  ```
- Line 276: name
  ```
  replace firstname = "Geralyn" if firstname == "Geri" & lastname == "Knautz"
  ```
- Line 277: name
  ```
  replace firstname = "Mary" if firstname == "Maryellen" & lastname == "Lachance"
  ```
- Line 278: name
  ```
  replace firstname = "Katherine" if firstname == "Katie" & lastname == "Lachance"
  ```
- Line 279: name
  ```
  replace firstname = "Sarah" if firstname == "Sara" & lastname == "Lafriske"
  ```
- Line 280: name
  ```
  replace firstname = "Christopher" if firstname == "Christoper" & lastname == "Lambert"
  ```
- Line 281: name
  ```
  replace firstname = "Jo" if firstname == "Joann" & lastname == "Leary"
  ```
- Line 282: name
  ```
  replace firstname = "Kathryn" if firstname == "Lee" & lastname == "Kathy"
  ```
- Line 283: name
  ```
  replace firstname = "Frederick" if firstname == "Fred" & lastname == "Leece"
  ```
- Line 284: name
  ```
  replace firstname = "Jacqueline" if firstname == "Jacquline" & lastname == "Leis"
  ```
- Line 285: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Lewis"
  ```
- Line 286: name
  ```
  replace firstname = "Dianne" if firstname == "Diane" & lastname == "Luba"
  ```
- Line 287: name
  ```
  replace firstname = "Edwin" if firstname == "Ed" & lastname == "Lucas"
  ```
- Line 288: name
  ```
  replace firstname = "Allan" if firstname == "Al" & lastname == "Lundquist"
  ```
- Line 289: name
  ```
  replace firstname = "Bradely" if firstname == "Bradley"
  ```
- Line 290: name
  ```
  replace firstname = "Kathleen" if lastname == "Mclellan" & firstname == "Kathy"
  ```
- Line 291: name
  ```
  replace firstname = "Ann" if firstname == "Annmarie" & lastname == "Meissner"
  ```
- Line 292: name
  ```
  replace firstname = "Darlene" if lastname == "D" & lastname == "Mell"
  ```
- Line 293: name
  ```
  replace firstname = "Daniel" if firstname == "Dan" & lastname == "Miller"
  ```
- Line 294: name
  ```
  replace firstname = "Jackie" if firstname == "Jacqulynn" & lastname == "Mitchell"
  ```
- Line 295: name
  ```
  replace firstname = "Bonnie" if firstname == "Bonnita" & lastname == "Modjeski"
  ```
- Line 296: name
  ```
  replace firstname = "Frederick" if firstname == "Fred" & lastname == "Morley"
  ```
- Line 297: name, son
  ```
  replace firstname = "Anna" if firstname == "Anne" & lastname == "Morrison"
  ```
- Line 298: name
  ```
  replace firstname = "Kimberly" if firstname == "Kimberley" & lastname == "Muonio"
  ```
- Line 299: name, son
  ```
  replace firstname = "Linda" if firstname == "Lynda" & lastname == "Nelson"
  ```
- Line 300: name
  ```
  replace firstname = "Samuel" if firstname == "Sam" & lastname == "Northey"
  ```
- Line 301: name
  ```
  replace firstname = "Thomas" if firstname == "Tom" & lastname == "Ojanpera"
  ```
- Line 302: name
  ```
  replace firstname = "Angie" if firstname == "Angela"
  ```
- Line 303: name
  ```
  replace firstname = "Wilfred" if firstname == "Welfred" & lastname == "Ortiz"
  ```
- Line 304: name
  ```
  replace firstname = "Mary" if firstname == "May" & lastname == "Palzkill"
  ```
- Line 305: name
  ```
  replace firstname = "Kenneth" if firstname == "Ken" & lastname == "Paris"
  ```
- Line 306: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Penglase"
  ```
- Line 307: name
  ```
  replace firstname = "Laura" if firstname == "Laurajo" & lastname == "Peters"
  ```
- Line 308: name
  ```
  replace firstname = "Myragene" if firstname == "Myrgene" & lastname == "Pettit"
  ```
- Line 309: name
  ```
  replace firstname = "Donna" if firstname == "D" & lastname == "Polzin"
  ```
- Line 310: name
  ```
  replace firstname = "Patricia" if firstname == "Patti"
  ```
- Line 311: name
  ```
  replace firstname = "Jo" if firstname == "Joanne" & lastname == "Revels"
  ```
- Line 312: name
  ```
  replace firstname = "Marissa" if firstname == "Marisa" & lastname == "Romanelli"
  ```
- Line 313: name, son
  ```
  replace firstname = "Allison" if firstname == "Allyson" & lastname == "Ryan"
  ```
- Line 314: name
  ```
  replace firstname = "Gregory" if firstname == "Greg" & lastname == "Sauve"
  ```
- Line 315: name
  ```
  replace firstname = "Jeffrey" if firstname == "Jeffery"
  ```
- Line 316: name
  ```
  replace firstname = "Debra" if firstname == "Deb" & lastname == "Schroeder"
  ```
- Line 317: name
  ```
  replace firstname = "James" if firstname == "Jim" & lastname == "Schuchardt"
  ```
- Line 318: name
  ```
  replace firstname = "Janet" if firstname == "Jan" & lastname == "Schuldt"
  ```
- Line 319: name
  ```
  replace firstname = "Kimberly" if firstname == "Kim" & lastname == "Serene"
  ```
- Line 320: name
  ```
  replace firstname = "Theresa" if firstname == "Teresa" & lastname == "Shelton"
  ```
- Line 321: name, son
  ```
  replace firstname = "Julia" if firstname == "Jubie" & lastname == "Simonson"
  ```
- Line 322: name
  ```
  replace firstname = "Naomi" if firstname == "Namoi"
  ```
- Line 323: name
  ```
  replace firstname = "Elizabeth" if firstname == "E" & lastname == "Spear"
  ```
- Line 324: name
  ```
  replace firstname = "Margaret" if firstname == "Peg" & lastname == "Stachowiak"
  ```
- Line 325: name
  ```
  replace firstname = "Mary" if firstname == "Maryann" & lastname == "Stellick"
  ```
- Line 326: name
  ```
  replace firstname = "Douglas" if firstname == "Doug" & lastname == "Straus"
  ```
- Line 327: name
  ```
  replace firstname = "Jenelle" if firstname == "Jenell"
  ```
- Line 328: name
  ```
  replace firstname = "Jane" if firstname == "Jan" & lastname == "Wagner"
  ```
- Line 329: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Wall"
  ```
- Line 330: name
  ```
  replace firstname = "Jessie" if firstname == "Jesse" & lastname == "Warner"
  ```
- Line 331: name
  ```
  replace firstname = "Catherine" if firstname == "Catharine" & lastname == "Wellman"
  ```
- Line 332: name
  ```
  replace firstname = "Allan" if firstname == "Allen" & lastname == "Wendland"
  ```
- Line 333: name
  ```
  replace firstname = "Eric" if firstname == "Erica" & lastname == "Wenninger"
  ```
- Line 334: name
  ```
  replace firstname = "Dustin" if firstname == "Dustian"
  ```
- Line 335: name
  ```
  replace firstname = "Keshum" if firstname == "Keushum"
  ```
- Line 336: name
  ```
  replace firstname = "Timothy" if firstname == "Tim" & lastname == "Zander"
  ```
- Line 339: name, son
  ```
  replace filenum = 646059 if (lastname == "Johnson" & ///
  ```
- Line 340: birth, name
  ```
  firstname == "Christine" & birth == 1979)
  ```
- Line 341: name
  ```
  replace firstname = "Jonathan" if (lastname == "Jonathon" & ///
  ```
- Line 342: birth, name, son
  ```
  firstname == "Thompson" & birth == 1976 & filenum == 661623)
  ```
- Line 343: name, son
  ```
  replace lastname = "Thompson" if (lastname == "Jonathon" & ///
  ```
- Line 344: birth, name
  ```
  firstname == "Jonathan" & birth == 1976 & filenum == 661623)
  ```
- Line 345: name
  ```
  replace firstname = "Billie" if (lastname == "Billie" & ///
  ```
- Line 346: birth, name
  ```
  firstname == "Rau" & birth == 1977 & filenum == 637687)
  ```
- Line 347: name
  ```
  replace lastname = "Rau" if (lastname == "Billie" & ///
  ```
- Line 348: birth, name
  ```
  firstname == "Billie" & birth == 1977 & filenum == 637687)
  ```
- Line 349: name
  ```
  replace filenum = 707967 if (lastname == "Worzella" & firstname == "Jeff")
  ```
- Line 350: name
  ```
  replace lastname = subinstr(lastname," Jr","",1)
  ```
- Line 351: name
  ```
  replace lastname = subinstr(lastname," Iii","",1)
  ```
- Line 352: name
  ```
  replace lastname = subinstr(lastname," Ii","",1)
  ```
- Line 355: birth, gender
  ```
  gsort last first birth gender race -year
  ```
- Line 356: birth, gender
  ```
  bysort last first birth gender race: carryforward filenum, replace
  ```
- Line 370: birth, district, gender
  ```
  bysort last first gender birth district_code (year): ///
  ```
- Line 374: birth, gender
  ```
  gsort last first birth gender race -year
  ```
- Line 375: birth, gender
  ```
  bysort last first birth gender race: carryforward filenum2, replace
  ```
- Line 376: birth, gender
  ```
  bysort last first birth gender race (year): carryforward filenum2, replace
  ```
- Line 378: name
  ```
  rename filenum2 filenumber
  ```
- Line 380: birth, gender
  ```
  gsort last first birth gender race -year
  ```
- Line 381: birth, gender
  ```
  bysort last first birth gender race: carryforward filenum, replace
  ```
- Line 382: birth, gender
  ```
  bysort last first birth gender race (year): carryforward filenum, replace
  ```
- Line 385: birth, gender
  ```
  egen group = group(last first birth race gender)
  ```
- Line 387: district
  ```
  drop group two* district_code
  ```

**/replication-package/replication_delivery/code/build/teachers_build.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 25: lon
  ```
  * Flag long-term substitutes.
  ```
- Line 26: name
  ```
  rename lt_substitute LT
  ```
- Line 30: district, school
  ```
  * Keep public school district or school employees hired by districts or schools.
  ```
- Line 34: school
  ```
  * Drop CESAs, charters, special schools, and records without school codes.
  ```
- Line 36: school
  ```
  drop if schoolcode == .
  ```
- Line 44: name
  ```
  rename id id_old
  ```
- Line 45: name
  ```
  rename filenum id
  ```
- Line 52: gender
  ```
  * Education and gender.
  ```
- Line 53: gender
  ```
  gen female = gender == "F"
  ```
- Line 54: gender
  ```
  replace female = . if gender == "NA" | gender == ""
  ```
- Line 55: gender
  ```
  drop gender
  ```
- Line 67: city
  ```
  * Race and ethnicity.
  ```
- Line 115: district, school
  ```
  * Total FTE, district FTE, school FTE, and subject-specific FTE.
  ```
- Line 117: district
  ```
  bysort id year workagencycode: egen fte_district = sum(fte)
  ```
- Line 118: school
  ```
  bysort id year schoolcode workagencycode: egen fte_school = sum(fte)
  ```
- Line 147: school
  ```
  * Total number of schools.
  ```
- Line 148: school
  ```
  bysort id year workagencycode schoolcode: gen nr_schools = _n == 1
  ```
- Line 149: school
  ```
  bysort id year: replace nr_schools = sum(nr_schools)
  ```
- Line 150: school
  ```
  bysort id year: replace nr_schools = nr_schools[_N]
  ```
- Line 152: district
  ```
  * Total number of districts.
  ```
- Line 153: district
  ```
  bysort id year workagencycode: gen nr_districts = _n == 1
  ```
- Line 154: district
  ```
  bysort id year: replace nr_districts = sum(nr_districts)
  ```
- Line 155: district
  ```
  bysort id year: replace nr_districts = nr_districts[_N]
  ```
- Line 162: district
  ```
  * Prepare Teacher-District-Year Panel
  ```
- Line 165: birth, loc, name
  ```
  keep id id_old lastname firstname female birth year dayscontract localexp ///
  ```
- Line 166: lname, name, school
  ```
  totalexp salary fringe workagencyname workagencycode schoolcode schoolname ///
  ```
- Line 168: school
  ```
  english art science reading title1 all_elem crosscat fte_tot fte_school ///
  ```
- Line 169: district
  ```
  fte_district fte_math fte_eng fte_read fte_art fte_science fte_title1 ///
  ```
- Line 170: district, school
  ```
  fte_crosscat fte_all_elem nr_assignments_total nr_schools nr_districts ///
  ```
- Line 173: district
  ```
  * Adjust one duplicate where district-level contract days differ within teacher-year.
  ```
- Line 186: loc
  ```
  bysort id year workagencycode (localexp): replace localexp = localexp[_N]
  ```
- Line 191: birth
  ```
  foreach var in female birth bachelor master specialist phd black hispanic asian {
  ```
- Line 194: name
  ```
  rename `var'2 `var'
  ```
- Line 197: name
  ```
  foreach var in lastname firstname {
  ```
- Line 202: district, school
  ```
  * Reshape so that one school is one teacher-district-year.
  ```
- Line 203: school
  ```
  egen school_id_unique = group(workagencycode schoolcode)
  ```
- Line 206: school
  ```
  * Rank schools by FTE and break exact FTE ties with stable school attributes.
  ```
- Line 207: school
  ```
  * The official school code is the substantive tie-breaker; the remaining
  ```
- Line 208: school
  ```
  * fields only order duplicate records for the same school deterministically.
  ```
- Line 209: school
  ```
  tempvar max_school_fte min_tied_schoolcode
  ```
- Line 210: school
  ```
  bysort id year workagencycode: egen `max_school_fte' = max(fte_school)
  ```
- Line 211: school
  ```
  bysort id year workagencycode: egen `min_tied_schoolcode' = ///
  ```
- Line 212: school
  ```
  min(cond(fte_school == `max_school_fte', schoolcode, .))
  ```
- Line 213: lname, name, school
  ```
  gsort id year workagencycode -fte_school schoolcode schoolname county ///
  ```
- Line 216: school
  ```
  schoolcode[1] == `min_tied_schoolcode'
  ```
- Line 218: school
  ```
  drop `max_school_fte' `min_tied_schoolcode'
  ```
- Line 220: lname, name, school
  ```
  reshape wide fte_school school_id_unique schoolcode schoolname county ///
  ```
- Line 226: school
  ```
  * Cross-School Mobility
  ```
- Line 231: school
  ```
  gen mover_school = school_id_unique1 != l.school_id_unique1
  ```
- Line 232: school
  ```
  replace mover_school = . if l.school_id_unique1 == .
  ```
- Line 233: school
  ```
  replace mover_school = . if school_id_unique1 == .
  ```
- Line 237: school
  ```
  replace mover_school = 0 if ///
  ```
- Line 238: school
  ```
  (school_id_unique`n' == l.school_id_unique`m') & ///
  ```
- Line 239: school
  ```
  school_id_unique`n' != . & l.school_id_unique`m' != .
  ```
- Line 243: school
  ```
  bysort id year (mover_school): replace mover_school = mover_school[_N]
  ```
- Line 247: district
  ```
  * Teacher-Year Panel and Cross-District Mobility
  ```
- Line 250: birth, loc, name
  ```
  keep id lastname firstname female birth year dayscontract localexp totalexp ///
  ```
- Line 251: lname, name, school
  ```
  salary fringe workagencyname workagencycode schoolcode1 schoolname1 ///
  ```
- Line 253: school
  ```
  english art science reading title1 all_elem crosscat fte_tot fte_school1 ///
  ```
- Line 254: district
  ```
  fte_district fte_math fte_eng fte_read fte_art fte_science fte_title1 ///
  ```
- Line 255: district, school
  ```
  fte_crosscat fte_all_elem nr_assignments_total nr_schools nr_districts ///
  ```
- Line 256: school
  ```
  mover_school lowgrade1 highgrade1
  ```
- Line 258: name, school
  ```
  rename fte_school1 fte_school
  ```
- Line 262: district
  ```
  * Preserve the existing ascending district-FTE rank and break exact ties by
  ```
- Line 263: district
  ```
  * official district code. This makes suffix assignment independent of Stata
  ```
- Line 265: district
  ```
  tempvar min_district_fte min_tied_districtcode
  ```
- Line 266: district
  ```
  bysort fakeid: egen `min_district_fte' = min(fte_district)
  ```
- Line 267: district
  ```
  bysort fakeid: egen `min_tied_districtcode' = ///
  ```
- Line 268: district
  ```
  min(cond(fte_district == `min_district_fte', workagencycode, .))
  ```
- Line 269: district
  ```
  sort fakeid fte_district workagencycode
  ```
- Line 270: district
  ```
  by fakeid: assert workagencycode[1] == `min_tied_districtcode'
  ```
- Line 272: district
  ```
  drop `min_district_fte' `min_tied_districtcode'
  ```
- Line 273: district, loc, school
  ```
  reshape wide dayscontract localexp fte_district fte_school schoolcode1 ///
  ```
- Line 274: lname, name, school
  ```
  schoolname1 lowgrade1 highgrade1 workagencyname workagencycode county, ///
  ```
- Line 278: district
  ```
  * Cross-district mover indicator.
  ```
- Line 303: name
  ```
  * Labels, Variable Names, and Experience Adjustment
  ```
- Line 306: birth, name
  ```
  order id lastname firstname year birth totalexp salary fringe bachelor master ///
  ```
- Line 309: school
  ```
  fte_title1 fte_crosscat fte_all_elem nr_assignments_total nr_schools ///
  ```
- Line 310: district, school
  ```
  nr_districts mover_d mover_school exit exit_temp, first
  ```
- Line 322: district, school
  ```
  label variable fte_math "Math FTE (across all schools and districts)"
  ```
- Line 323: district, school
  ```
  label variable fte_eng "English FTE (across all schools and districts)"
  ```
- Line 324: district, school
  ```
  label variable fte_read "Reading FTE (across all schools and districts)"
  ```
- Line 325: district, school
  ```
  label variable fte_art "Art FTE (across all schools and districts)"
  ```
- Line 326: district, school
  ```
  label variable fte_science "Science FTE (across all schools and districts)"
  ```
- Line 327: district, school
  ```
  label variable fte_crosscat "Cross-category FTE (across all schools and districts)"
  ```
- Line 328: district, school
  ```
  label variable fte_title1 "Title 1 FTE (across all schools and districts)"
  ```
- Line 329: district, school
  ```
  label variable fte_all_elem "All subjects - Elementary FTE (across all schools and districts)"
  ```
- Line 331: school
  ```
  label variable nr_schools "Nr. schools taught"
  ```
- Line 332: district
  ```
  label variable nr_districts "Nr. districts taught"
  ```
- Line 333: district
  ```
  label variable mover_d "Cross-district mover"
  ```
- Line 334: school
  ```
  label variable mover_school "Cross-school mover"
  ```
- Line 337: district, lname, name, school
  ```
  label variable schoolname11 "School name (or district where applicable)"
  ```
- Line 338: school
  ```
  label variable schoolcode11 "School ID (Wisconsin system)"
  ```
- Line 339: school
  ```
  label variable fte_school1 "FTE at school"
  ```
- Line 340: school
  ```
  label variable county11 "County of school"
  ```
- Line 341: district
  ```
  label variable dayscontract1 "Contract days at district"
  ```
- Line 342: district, loc
  ```
  label variable localexp1 "Experience with district"
  ```
- Line 343: district
  ```
  label variable fte_district1 "FTE at district"
  ```
- Line 344: district, name
  ```
  label variable workagencyname1 "District name"
  ```
- Line 345: district
  ```
  label variable workagencycode1 "District ID"
  ```
- Line 347: district, lname, name, school
  ```
  label variable schoolname12 "School name, district 2 (or district where applicable)"
  ```
- Line 348: district, school
  ```
  label variable schoolcode12 "School ID, district 2 (Wisconsin system)"
  ```
- Line 349: district, school
  ```
  label variable fte_school2 "FTE at school, district 2"
  ```
- Line 350: school
  ```
  label variable county12 "County of school 2"
  ```
- Line 351: district
  ```
  label variable dayscontract2 "Contract days at district, district 2"
  ```
- Line 352: district, loc
  ```
  label variable localexp2 "Experience with district, district 2"
  ```
- Line 353: district
  ```
  label variable fte_district2 "FTE at district 2"
  ```
- Line 354: district, name
  ```
  label variable workagencyname2 "District 2 name"
  ```
- Line 355: district
  ```
  label variable workagencycode2 "District 2 ID"
  ```
- Line 357: district, lname, name, school
  ```
  label variable schoolname13 "School name, district 3 (or district where applicable)"
  ```
- Line 358: district, school
  ```
  label variable schoolcode13 "School ID, district 3 (Wisconsin system)"
  ```
- Line 359: district, school
  ```
  label variable fte_school3 "FTE at school, district 3"
  ```
- Line 360: school
  ```
  label variable county13 "County of school 3"
  ```
- Line 361: district
  ```
  label variable dayscontract3 "Contract days at district, district 3"
  ```
- Line 362: district, loc
  ```
  label variable localexp3 "Experience with district, district 3"
  ```
- Line 363: district
  ```
  label variable fte_district3 "FTE at district 3"
  ```
- Line 364: district, name
  ```
  label variable workagencyname3 "District 3 name"
  ```
- Line 365: district
  ```
  label variable workagencycode3 "District 3 ID"
  ```
- Line 367: lname, name, school
  ```
  foreach var in schoolname schoolcode county lowgrade highgrade {
  ```
- Line 368: name
  ```
  rename `var'11 `var'
  ```
- Line 369: name
  ```
  rename `var'12 `var'2
  ```
- Line 370: name
  ```
  rename `var'13 `var'3
  ```
- Line 373: district, loc, name, school
  ```
  foreach var in dayscontract localexp fte_district fte_school workagencyname ///
  ```
- Line 375: name
  ```
  rename `var'1 `var'
  ```
- Line 378: district, name
  ```
  rename workagencycode district_code
  ```
- Line 382: loc
  ```
  replace localexp = round(localexp/10) if year <= 2014
  ```
- Line 383: birth
  ```
  gen exp = year - birth - 22
  ```
- Line 384: loc
  ```
  replace totalexp = localexp if totalexp > 50 & totalexp > exp
  ```
- Line 386: lat
  ```
  bysort id: ipolate totalexp year, gen(expi)
  ```
- Line 389: loc
  ```
  replace totalexp = localexp if totalexp == .
  ```

**/replication-package/replication_delivery/code/build/teachers_panel.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: birth
  ```
  keep id year totalexp birth salary fringe bachelor master specialist ///
  ```
- Line 18: lname, name, school
  ```
  mover_school exit exit_temp schoolcode lowgrade highgrade schoolname ///
  ```
- Line 19: birth, district, name
  ```
  district_code workagencyname black hispanic asian birth
  ```
- Line 23: district
  ```
  * Define previous district as described in the paper.
  ```
- Line 25: district
  ```
  gen district_past = l.district_code
  ```
- Line 26: district
  ```
  gen district_past2007 = l.district_code if year == 2008
  ```
- Line 27: district
  ```
  replace district_past2007 = l2.district_code if year == 2009
  ```
- Line 28: district
  ```
  replace district_past2007 = l3.district_code if year == 2010
  ```
- Line 29: district
  ```
  replace district_past2007 = l4.district_code if year == 2011
  ```
- Line 30: district
  ```
  gen district_past2008 = l.district_code if year == 2009
  ```
- Line 31: district
  ```
  replace district_past2008 = l2.district_code if year == 2010
  ```
- Line 32: district
  ```
  replace district_past2008 = l3.district_code if year == 2011
  ```
- Line 33: district
  ```
  replace district_past2008 = l4.district_code if year == 2012
  ```
- Line 34: district
  ```
  gen district_past2009 = l.district_code if year == 2010
  ```
- Line 35: district
  ```
  replace district_past2009 = l2.district_code if year == 2011
  ```
- Line 36: district
  ```
  replace district_past2009 = l3.district_code if year == 2012
  ```
- Line 37: district
  ```
  replace district_past2009 = l4.district_code if year == 2013
  ```
- Line 38: district
  ```
  gen district_past2010 = l.district_code if year == 2011
  ```
- Line 39: district
  ```
  replace district_past2010 = l2.district_code if year == 2012
  ```
- Line 40: district
  ```
  replace district_past2010 = l3.district_code if year == 2013
  ```
- Line 41: district
  ```
  replace district_past2010 = l4.district_code if year == 2014
  ```
- Line 42: district
  ```
  gen district_past2011 = l.district_code if year == 2012
  ```
- Line 43: district
  ```
  replace district_past2011 = l2.district_code if year == 2013
  ```
- Line 44: district
  ```
  replace district_past2011 = l3.district_code if year == 2014
  ```
- Line 45: district
  ```
  replace district_past2011 = l4.district_code if year == 2015
  ```
- Line 46: district
  ```
  gen district_past2012 = l.district_code if year == 2013
  ```
- Line 47: district
  ```
  replace district_past2012 = l2.district_code if year == 2014
  ```
- Line 48: district
  ```
  replace district_past2012 = l3.district_code if year == 2015
  ```
- Line 49: district
  ```
  gen district_past2013 = l.district_code if year == 2014
  ```
- Line 50: district
  ```
  replace district_past2013 = l2.district_code if year == 2015
  ```
- Line 51: district
  ```
  gen district_past2014 = l.district_code if year == 2015
  ```
- Line 52: district
  ```
  replace district_past2014 = l2.district_code if year == 2016
  ```
- Line 53: district
  ```
  gen district_past2015 = l.district_code if year == 2016
  ```
- Line 65: loc
  ```
  qui levelsof year, local(Year)
  ```
- Line 66: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 69: loc
  ```
  foreach y of local Year {
  ```
- Line 81: address
  ```
  * Correct obvious mistakes and address backward jumps in experience.
  ```
- Line 84: birth
  ```
  gen tooyoung = (year - birth - totalexp) < 20
  ```
- Line 104: lat
  ```
  bysort id: ipolate totalexp3 t, epolate gen(totalexp4)
  ```
- Line 106: name
  ```
  rename totalexp4 totalexp2
  ```
- Line 114: district
  ```
  drop if year == 2015 & district_code == 3619
  ```
- Line 116: school
  ```
  drop if schoolcode == 8888
  ```
- Line 121: district
  ```
  * Add district classifications
  ```
- Line 125: district
  ```
  sort district_code
  ```
- Line 130: district
  ```
  * District type: urban = 1, suburban = 2, rural = 3.
  ```
- Line 139: district
  ```
  replace type = 2 if district_code == `n'
  ```
- Line 145: district
  ```
  replace type = 1 if district_code == `n'
  ```
- Line 148: district
  ```
  * Fix district codes that changed because of consolidation.
  ```
- Line 149: district
  ```
  replace cesa = 11 if district_code == 6410
  ```
- Line 150: district
  ```
  replace cesa = 2 if district_code == 5075
  ```
- Line 151: district
  ```
  replace cesa = 2 if district_code == 5061
  ```
- Line 152: district
  ```
  replace cesa = 6 if district_code == 4998
  ```
- Line 153: district
  ```
  replace cesa = 6 if district_code == 3913
  ```
- Line 154: district
  ```
  replace cesa = 6 if district_code == 2523
  ```
- Line 155: district
  ```
  replace cesa = 11 if district_code == 1078
  ```
- Line 237: district, school
  ```
  keep id year district_code schoolcode district_past* ///
  ```
- Line 239: birth
  ```
  english highgrade lowgrade fte_tot mover_d exit entry birth
  ```
- Line 240: district
  ```
  order id year district_code district_past* exp masterup salary_round cesa ///
  ```

**/replication-package/replication_delivery/code/build/teachers_with_allva.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 19: name
  ```
  * Construct weighted averages and rename teacher-type-specific estimates.
  ```
- Line 22: name
  ```
  rename mu0 v0_u
  ```
- Line 23: name
  ```
  rename mu1 v1_u
  ```
- Line 24: name
  ```
  rename v0 v0
  ```
- Line 25: name
  ```
  rename v1 v1
  ```
- Line 26: name
  ```
  rename nr0 nr0
  ```
- Line 27: name
  ```
  rename nr1 nr1
  ```
- Line 28: name
  ```
  rename nr nr
  ```
- Line 67: district, school
  ```
  keep id year district_code schoolcode district_past* M* R* v0 v1 g*_* ///
  ```
- Line 69: birth
  ```
  english highgrade lowgrade fte_tot mover_d exit entry birth
  ```
- Line 70: district
  ```
  order id year district_code district_past* exp masterup salary_round cesa ///
  ```

**/replication-package/replication_delivery/code/build/testscores_build.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 18: name
  ```
  comma names clear
  ```
- Line 21: school
  ```
  gen year = substr(school_year,1,4)
  ```
- Line 23: school
  ```
  drop school_year
  ```
- Line 25: gender
  ```
  gen Sfemale = gender == "F"
  ```
- Line 26: gender
  ```
  replace Sfemale = . if gender == ""
  ```
- Line 27: gender
  ```
  drop gender
  ```
- Line 29: city
  ```
  gen Sblack = race_ethnicity == "Black"
  ```
- Line 30: city
  ```
  gen Shispanic = race_ethnicity == "Hispanic"
  ```
- Line 31: city
  ```
  gen Sasian = race_ethnicity == "Asian"
  ```
- Line 32: city
  ```
  gen Sam_ind = race_ethnicity == "Amer Indian"
  ```
- Line 48: school
  ```
  gen year = substr(school_year,1,4)
  ```
- Line 50: school
  ```
  drop school_year
  ```
- Line 52: school
  ```
  gen schoolcode = substr(sch_acctbl_code,6,4)
  ```
- Line 53: school
  ```
  destring schoolcode, replace
  ```
- Line 56: district, name
  ```
  rename dist_acctbl_code district_code
  ```
- Line 111: district, school
  ```
  keep student_key district_code schoolcode year grade test_subject std_score
  ```
- Line 112: district, school
  ```
  order student_key district_code schoolcode year grade, first
  ```
- Line 131: district, school
  ```
  sort district_code schoolcode year grade student_key
  ```
- Line 135: school
  ```
  gen schoolcode2 = l.schoolcode
  ```
- Line 136: school
  ```
  replace schoolcode = schoolcode2 if schoolcode2 != . & year <= 2013
  ```
- Line 137: district
  ```
  gen dist2 = l.district_code
  ```
- Line 138: district
  ```
  replace district_code = dist2 if dist2 != . & year <= 2013
  ```
- Line 139: school
  ```
  drop schoolcode2 dist2
  ```
- Line 156: school
  ```
  gen year = substr(school_year,6,4)
  ```
- Line 158: school
  ```
  drop school_year
  ```
- Line 160: school
  ```
  gen schoolcode = substr(sch_acctbl_code,6,4)
  ```
- Line 161: school
  ```
  destring schoolcode, replace
  ```
- Line 164: district, name
  ```
  rename dist_acctbl_code district_code
  ```
- Line 219: district, school
  ```
  keep student_key district_code schoolcode year grade test_subject std_score
  ```
- Line 220: district, school
  ```
  order student_key district_code schoolcode year grade, first
  ```
- Line 253: name
  ```
  rename std_score std_score_new
  ```
- Line 254: name
  ```
  rename std_score_past std_score_past_new
  ```
- Line 260: name
  ```
  rename std_score std_score_old
  ```
- Line 261: name
  ```
  rename std_score_past std_score_past_old
  ```

**/replication-package/replication_delivery/code/build/va_teacherside_build.do**

- Line 9: school
  ```
  drop if schoolcode == .
  ```
- Line 11: district, school
  ```
  keep id schoolcode district_code year highgrade lowgrade
  ```
- Line 12: district
  ```
  drop if district_code == .
  ```
- Line 31: lon
  ```
  reshape long grade, i(fakeid) j(n)
  ```
- Line 37: district, school
  ```
  bysort fakeid: carryforward id year schoolcode district_code highgrade ///
  ```
- Line 39: district, school
  ```
  bysort fakeid (grade): carryforward id year schoolcode district_code highgrade ///
  ```
- Line 45: district, school
  ```
  sort district_code schoolcode year grade
  ```
- Line 47: school
  ```
  * Reshape to one school-grade-year observation with teacher IDs in wide form.
  ```
- Line 48: district, school
  ```
  egen fakeid = group(district_code schoolcode year grade)
  ```
- Line 52: district, school
  ```
  sort district_code schoolcode year grade
  ```

**/replication-package/replication_delivery/code/build/valueadded_calculation.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 5: lat
  ```
  * File:    valueadded_calculation.do
  ```
- Line 39: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 40: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 43: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 46: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 61: loc
  ```
  local z = `x' + 1
  ```
- Line 67: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 76: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 101: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 125: school
  ```
  if D == 0, a(schoolid)
  ```
- Line 144: school
  ```
  if D == 1, a(schoolid)
  ```
- Line 158: district, school
  ```
  keep student_key year district_code schoolcode grade v id* D
  ```

**/replication-package/replication_delivery/code/build/valueadded_calculation_classmodel.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 5: lat
  ```
  * File:    valueadded_calculation_classmodel.do
  ```
- Line 7: school
  ```
  *          assume equal teacher contributions within a grade-school cell.
  ```
- Line 31: district, school
  ```
  egen fakeid = group(district_code schoolcode grade)
  ```
- Line 43: district, school
  ```
  keep district_code schoolcode grade year overlap
  ```
- Line 44: district, school
  ```
  sort district_code schoolcode grade year
  ```
- Line 66: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 67: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 70: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 74: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 92: loc
  ```
  local z = `x' + 1
  ```
- Line 99: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 109: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 138: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 161: school
  ```
  MShispanic MSasian MSam_ind Msize if D == 0, a(schoolid)
  ```
- Line 172: school
  ```
  MShispanic MSasian MSam_ind Msize if D == 1, a(schoolid)
  ```
- Line 252: name
  ```
  rename v1 v1_class
  ```
- Line 253: name
  ```
  rename v0 v0_class
  ```
- Line 254: name
  ```
  rename mu1 mu1_class
  ```
- Line 255: name
  ```
  rename mu0 mu0_class
  ```

**/replication-package/replication_delivery/code/build/valueadded_calculation_stockstaiger_nocomparativeadv.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 5: lat
  ```
  * File:    valueadded_calculation_stockstaiger_nocomparativeadv.do
  ```
- Line 39: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 40: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 43: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 46: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 61: loc
  ```
  local z = `x' + 1
  ```
- Line 67: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 76: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 103: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 127: school
  ```
  a(schoolid)
  ```
- Line 140: district, school
  ```
  keep student_key year district_code schoolcode grade v id* D
  ```

**/replication-package/replication_delivery/code/descriptives/Figure_4.do**

- Line 14: lon
  ```
  reshape long v, i(id) j(col)
  ```
- Line 16: name
  ```
  rename v lambda
  ```
- Line 27: lon
  ```
  reshape long v, i(id) j(col)
  ```
- Line 29: name
  ```
  rename v omega1
  ```
- Line 40: lon
  ```
  reshape long v, i(id) j(col)
  ```
- Line 42: name
  ```
  rename v omega2
  ```
- Line 59: loc
  ```
  local med = r(p50)
  ```

**/replication-package/replication_delivery/code/descriptives/Table_5_Table_A1.do**

- Line 7: name
  ```
  tempname fh
  ```
- Line 10: loc
  ```
  local nlines = 0
  ```
- Line 17: loc
  ```
  local ++nlines
  ```
- Line 18: loc
  ```
  local mparam_`nlines' = word(`"`line'"', 4)
  ```
- Line 19: loc
  ```
  local desc_`nlines' ""
  ```
- Line 20: loc
  ```
  local w = 6
  ```
- Line 21: loc
  ```
  local wd = word(`"`line'"', `w')
  ```
- Line 23: loc
  ```
  local desc_`nlines' `"`desc_`nlines'' `wd'"'
  ```
- Line 24: loc
  ```
  local ++w
  ```
- Line 25: loc
  ```
  local wd = word(`"`line'"', `w')
  ```
- Line 27: loc
  ```
  local desc_`nlines' = strtrim(`"`desc_`nlines''"')
  ```
- Line 44: loc
  ```
  local se_all ""
  ```
- Line 45: name
  ```
  tempname fh2
  ```
- Line 49: loc
  ```
  local se_all `"`se_all' `line'"'
  ```
- Line 53: loc
  ```
  local se_all = strtrim(`"`se_all'"')
  ```
- Line 55: loc
  ```
  local nwords = wordcount(`"`se_all'"')
  ```
- Line 56: loc
  ```
  local npairs = `nwords' / 2
  ```
- Line 61: loc
  ```
  local pos_est = (`p' - 1) * 2 + 1
  ```
- Line 62: loc
  ```
  local pos_se  = `pos_est' + 1
  ```
- Line 63: loc
  ```
  local est_val = word(`"`se_all'"', `pos_est')
  ```
- Line 64: loc
  ```
  local sev = word(`"`se_all'"', `pos_se')
  ```
- Line 66: loc
  ```
  local est_num = real("`est_val'")
  ```
- Line 67: loc
  ```
  local se_num = real("`sev'")
  ```
- Line 152: lon
  ```
  replace parameter = "sigma_epsilon" if panel == 1 & side == 1 & order == 6
  ```
- Line 180: district
  ```
  *--- STEP 5: Negate same district/CZ parameters for I(d!=d0) presentation ---
  ```
- Line 182: district
  ```
  (strpos(description, "same district") | strpos(description, "same CZ"))
  ```
- Line 215: name
  ```
  rename est_fmt estimate
  ```
- Line 216: name
  ```
  rename se_fmt se
  ```

**/replication-package/replication_delivery/code/descriptives/Table_6.do**

- Line 9: loc
  ```
  *** local directory of the results
  ```
- Line 10: loc
  ```
  local baseline_dir "$fortran_code/2_rigid/result"
  ```
- Line 11: loc
  ```
  local cf_dir "$fortran_code/1_baseline/result"
  ```
- Line 22: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 23: name
  ```
  rename avg_c1 c1
  ```
- Line 24: name
  ```
  rename avg_c2 c2
  ```
- Line 25: name
  ```
  rename avg_expect_exp exp
  ```
- Line 26: name
  ```
  rename avg_expect_ma ma
  ```
- Line 27: name
  ```
  rename budg_per_teach budg_per_teach
  ```
- Line 28: name
  ```
  rename budget budget
  ```
- Line 38: district
  ```
  sort econ district_id
  ```
- Line 41: name
  ```
  rename omega1 omega1
  ```
- Line 42: name
  ```
  rename omega2 omega2
  ```
- Line 43: district
  ```
  sort econ district_id
  ```
- Line 46: city
  ```
  * Capacity/hires info
  ```
- Line 50: city
  ```
  drop capacity
  ```
- Line 51: name
  ```
  rename expected_hires expected_hires
  ```
- Line 52: name
  ```
  rename belief_expected_hires belief_expected_hires
  ```
- Line 56: district
  ```
  sort econ district_id
  ```
- Line 59: district
  ```
  sort econ district_id
  ```
- Line 73: name
  ```
  rename avg_expect_cont contributenew
  ```
- Line 74: name
  ```
  rename avg_c1 c1new
  ```
- Line 75: name
  ```
  rename avg_c2 c2new
  ```
- Line 76: name
  ```
  rename avg_expect_exp expnew
  ```
- Line 77: name
  ```
  rename avg_expect_ma manew
  ```
- Line 78: name
  ```
  rename budg_per_teach budg_per_teachnew
  ```
- Line 79: name
  ```
  rename budget budgetnew
  ```
- Line 89: district
  ```
  sort econ district_id
  ```
- Line 92: name
  ```
  rename omega1 omega1new
  ```
- Line 93: name
  ```
  rename omega2 omega2new
  ```
- Line 94: district
  ```
  sort econ district_id
  ```
- Line 97: city
  ```
  * Capacity/hires info
  ```
- Line 101: city
  ```
  drop capacity
  ```
- Line 102: name
  ```
  rename expected_hires expected_hiresnew
  ```
- Line 103: name
  ```
  rename belief_expected belief_expected_hiresnew
  ```
- Line 107: district
  ```
  sort econ district_id
  ```
- Line 110: district
  ```
  sort econ district_id
  ```
- Line 112: loc
  ```
  levelsof economy_id, local(econs)
  ```
- Line 120: district
  ```
  sort econ district_id
  ```
- Line 130: district
  ```
  * Merge in district characteristics (cz, lambda, etc.)
  ```
- Line 136: district
  ```
  keep district_id lambda
  ```
- Line 137: district
  ```
  sort district_id
  ```
- Line 140: district
  ```
  sort district_id
  ```
- Line 166: city
  ```
  gen capacity_c1 = capacity * lambda
  ```
- Line 167: city
  ```
  gen capacity_c2 = capacity * (1 - lambda)
  ```
- Line 177: district
  ```
  * Collapse across economies to get district-level means
  ```
- Line 178: city, district
  ```
  collapse (mean) totalcontr totalcontrnew totalc1 totalc2 totalc1new totalc2new capacity capacity_c1 
  ```
- Line 182: district, loc, name
  ```
  local subsetname = "All Districts"
  ```
- Line 186: loc, name
  ```
  local subsetname = "4th quartile"
  ```
- Line 190: loc, name
  ```
  local subsetname = "3rd quartile"
  ```
- Line 194: loc, name
  ```
  local subsetname = "2nd quartile"
  ```
- Line 198: loc, name
  ```
  local subsetname = "1st quartile"
  ```
- Line 203: loc
  ```
  local mean_lambda = r(mean)
  ```
- Line 204: loc
  ```
  local sd_lambda   = r(sd)
  ```
- Line 206: district
  ```
  * Collapse across districts to get totals
  ```
- Line 223: name
  ```
  gen subset_name = "`subsetname'"
  ```
- Line 227: name
  ```
  keep subset_name order pct_difftotalcont pct_difftotalc1 pct_difftotalc2 lambda_mean lambda_sd
  ```
- Line 228: name
  ```
  order subset_name order pct_difftotalcont pct_difftotalc1 pct_difftotalc2 lambda_mean lambda_sd
  ```
- Line 257: name
  ```
  * Rename for clarity
  ```
- Line 258: name
  ```
  rename pct_difftotalcont pct_diff_TC
  ```
- Line 259: name
  ```
  rename pct_difftotalc1   pct_diff_C1
  ```
- Line 260: name
  ```
  rename pct_difftotalc2   pct_diff_C2
  ```
- Line 266: name
  ```
  keep order subset_name pct_diff_TC pct_diff_C1 pct_diff_C2 lambda_display
  ```
- Line 267: name
  ```
  order order subset_name pct_diff_TC pct_diff_C1 pct_diff_C2 lambda_display
  ```

**/replication-package/replication_delivery/code/descriptives/Table_7.do**

- Line 9: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 11: loc
  ```
  local baseline_dir "$fortran_code/1_baseline/result"
  ```
- Line 14: loc
  ```
  local cf_dir_1 "$fortran_code/3_basicCfs/new1/result"
  ```
- Line 15: loc
  ```
  local cf_dir_2 "$fortran_code/3_basicCfs/new2/result"
  ```
- Line 16: loc
  ```
  local cf_dir_3 "$fortran_code/3_basicCfs/new3/result"
  ```
- Line 34: loc
  ```
  local cf_dir "`cf_dir_`col''"
  ```
- Line 42: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 43: name
  ```
  rename avg_c1 c1
  ```
- Line 44: name
  ```
  rename avg_c2 c2
  ```
- Line 45: name
  ```
  rename avg_expect_exp exp
  ```
- Line 46: district
  ```
  sort econ district_id
  ```
- Line 52: city
  ```
  drop capacity
  ```
- Line 53: name
  ```
  rename expected_hires expected_hires
  ```
- Line 61: district
  ```
  sort econ district_id
  ```
- Line 70: name
  ```
  rename avg_expect_cont contributenew
  ```
- Line 71: name
  ```
  rename avg_c1 c1new
  ```
- Line 72: name
  ```
  rename avg_c2 c2new
  ```
- Line 73: name
  ```
  rename avg_expect_exp expnew
  ```
- Line 74: district
  ```
  sort econ district_id
  ```
- Line 80: city
  ```
  drop capacity
  ```
- Line 81: name
  ```
  rename expected_hires expected_hiresnew
  ```
- Line 82: name
  ```
  rename belief_expected belief_expected_hiresnew
  ```
- Line 90: loc
  ```
  levelsof economy_id, local(econs)
  ```
- Line 91: district
  ```
  sort econ district_id
  ```
- Line 106: district
  ```
  * Merge in lambda/district info
  ```
- Line 112: district
  ```
  keep district_id lambda
  ```
- Line 113: district
  ```
  sort district_id
  ```
- Line 117: district
  ```
  sort district_id
  ```
- Line 140: district
  ```
  * Row 1-3: All districts (collapse across economies first, then sum)
  ```
- Line 141: district
  ```
  collapse (mean) totalcontr totalcontrnew totalc1 totalc2 totalc1new totalc2new, by(district_id)
  ```
- Line 145: loc
  ```
  local pct_tc = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 149: loc
  ```
  local pct_c1 = (totalc1new[1] - totalc1[1]) / totalc1[1]
  ```
- Line 153: loc
  ```
  local pct_c2 = (totalc2new[1] - totalc2[1]) / totalc2[1]
  ```
- Line 161: district
  ```
  collapse (mean) totalcontr totalcontrnew, by(district_id quartlam)
  ```
- Line 164: loc
  ```
  local pct_q = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 165: loc
  ```
  local row = 4 + (4 - `q')
  ```
- Line 204: district
  ```
  replace row_label = "TC in 4th quartile lambda_d districts" in 4
  ```
- Line 205: district
  ```
  replace row_label = "TC in 3rd quartile lambda_d districts" in 5
  ```
- Line 206: district
  ```
  replace row_label = "TC in 2nd quartile lambda_d districts" in 6
  ```
- Line 207: district
  ```
  replace row_label = "TC in 1st quartile lambda_d districts" in 7
  ```
- Line 220: loc
  ```
  local val = table7[`row', `col'] * 100
  ```
- Line 221: loc
  ```
  local val_fmt : display %5.2f `val'
  ```
- Line 225: loc
  ```
  local val = table7[9, `col']
  ```
- Line 226: loc
  ```
  local val_fmt : display %4.1f `val'
  ```
- Line 230: loc
  ```
  local val = table7[10, `col']
  ```
- Line 231: loc
  ```
  local val_fmt : display %4.2f `val'
  ```
- Line 243: name
  ```
  * Rename columns for clarity
  ```
- Line 244: name
  ```
  rename new1 pct_change_new1
  ```
- Line 245: name
  ```
  rename new2 pct_change_new2
  ```
- Line 246: name
  ```
  rename new3 pct_change_new3
  ```

**/replication-package/replication_delivery/code/descriptives/Table_8.do**

- Line 9: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 12: loc
  ```
  local baseline_dir "$fortran_code/4_nomovecost/case_baseline/result"
  ```
- Line 13: loc
  ```
  local case1_dir "$fortran_code/4_nomovecost/case1/result"
  ```
- Line 14: loc
  ```
  local case2_dir "$fortran_code/4_nomovecost/case2/result"
  ```
- Line 15: loc
  ```
  local case2_new2_dir "$fortran_code/4_nomovecost/case2_new2/result"
  ```
- Line 41: name
  ```
  rename avg_expect_cont contribute`suffix'
  ```
- Line 42: name
  ```
  rename avg_c1 c1`suffix'
  ```
- Line 43: name
  ```
  rename avg_c2 c2`suffix'
  ```
- Line 44: name
  ```
  rename avg_expect_exp exp`suffix'
  ```
- Line 45: district
  ```
  sort econ district_id
  ```
- Line 51: city
  ```
  drop capacity
  ```
- Line 52: name
  ```
  rename expected_hires expected_hires`suffix'
  ```
- Line 60: district
  ```
  sort econ district_id
  ```
- Line 83: district
  ```
  * Merge in lambda/district info
  ```
- Line 89: district
  ```
  keep district_id lambda
  ```
- Line 90: district
  ```
  sort district_id
  ```
- Line 94: district
  ```
  sort district_id
  ```
- Line 123: district
  ```
  collapse (mean) totalcontr* totalc1* totalc2*, by(district_id)
  ```
- Line 162: loc
  ```
  if `col' == 1 local cvar "col1"
  ```
- Line 163: loc
  ```
  if `col' == 2 local cvar "col2"
  ```
- Line 164: loc
  ```
  if `col' == 3 local cvar "col3"
  ```
- Line 167: loc
  ```
  local val = table8[`row', `col'] * 100
  ```
- Line 168: loc
  ```
  local val_fmt : display %5.2f `val'
  ```
- Line 173: name
  ```
  * Rename columns for clarity
  ```
- Line 174: name
  ```
  rename col1 Case1_minus_Base
  ```
- Line 175: name
  ```
  rename col2 Case2_minus_Base
  ```
- Line 176: name
  ```
  rename col3 Case2New2_minus_Case2
  ```

**/replication-package/replication_delivery/code/descriptives/Table_A2.do**

- Line 2: district
  ```
  * Table A2 (Model Fit: OLS of Teacher-District Match)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/estimation_result/result/aux_and_omega_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 34: loc
  ```
  local small_start = r(min)
  ```
- Line 37: loc
  ```
  local big_start = r(min)
  ```
- Line 39: loc
  ```
  * Extract values into locals
  ```
- Line 43: loc
  ```
  local linepos = `small_start' + 1 + `j'
  ```
- Line 44: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 45: loc
  ```
  local sd`j' : word 1 of `thisline'
  ```
- Line 46: loc
  ```
  local sm`j' : word 2 of `thisline'
  ```
- Line 47: loc
  ```
  local nw : word count `thisline'
  ```
- Line 48: loc
  ```
  local sl`j' ""
  ```
- Line 50: loc
  ```
  local wk : word `k' of `thisline'
  ```
- Line 51: loc
  ```
  local sl`j' "`sl`j'' `wk'"
  ```
- Line 53: loc
  ```
  local sl`j' = strtrim("`sl`j''")
  ```
- Line 57: loc
  ```
  local linepos = `big_start' + 1 + `j'
  ```
- Line 58: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 59: loc
  ```
  local bd`j' : word 1 of `thisline'
  ```
- Line 60: loc
  ```
  local bm`j' : word 2 of `thisline'
  ```
- Line 79: loc
  ```
  local order "1 2 3 4 5 6 7 8 9 12 10 11 13"
  ```
- Line 81: loc
  ```
  local row = 1
  ```
- Line 82: loc
  ```
  foreach s of local order {
  ```
- Line 88: loc
  ```
  local row = `row' + 1
  ```
- Line 91: name
  ```
  * Rename labels to match Table A2
  ```
- Line 104: name
  ```
  * Rename columns
  ```
- Line 105: name
  ```
  rename small_data inferred_data
  ```
- Line 106: name
  ```
  rename small_model inferred_model
  ```
- Line 107: name
  ```
  rename big_data all_data
  ```
- Line 108: name
  ```
  rename big_model all_model
  ```

**/replication-package/replication_delivery/code/descriptives/Table_A3_PanelA.do**

- Line 2: district
  ```
  * Table A3, Panel A (Model Fit: District Wage Schedules)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/estimation_result/result/aux_and_omega_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 35: loc
  ```
  local om_start = r(min)
  ```
- Line 38: loc
  ```
  local om_old_line = r(min)
  ```
- Line 41: loc
  ```
  local pr_o2_1_line = r(min)
  ```
- Line 45: loc
  ```
  local linepos = `om_start' + `j'
  ```
- Line 46: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 47: loc
  ```
  local omv1_`j' : word 1 of `thisline'
  ```
- Line 48: loc
  ```
  local omv2_`j' : word 2 of `thisline'
  ```
- Line 52: loc
  ```
  local linepos = `om_old_line' + 1
  ```
- Line 53: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 54: loc
  ```
  local old_d : word 1 of `thisline'
  ```
- Line 55: loc
  ```
  local old_m : word 2 of `thisline'
  ```
- Line 58: loc
  ```
  local thisline = rawline[`pr_o2_1_line']
  ```
- Line 59: loc
  ```
  local pr2_d : word 1 of `thisline'
  ```
- Line 60: loc
  ```
  local pr2_m : word 2 of `thisline'
  ```

**/replication-package/replication_delivery/code/descriptives/Table_A3_PanelB.do**

- Line 2: district
  ```
  * Table A3, Panel B (Model Fit: District Wage Schedules)
  ```
- Line 12: city, district
  ```
  keep District alpha0_ alpha1_ urban suburban d budget capacity
  ```
- Line 16: city
  ```
  gen budget_per_teacher = budget / capacity
  ```
- Line 21: district
  ```
  gen district_id = _n
  ```
- Line 29: name
  ```
  rename o2_0 o2zeroprob
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B14.do**

- Line 12: name
  ```
  foreach vname of varlist v3-v29 {
  ```
- Line 13: name
  ```
  *replace `vname' = `vname' - v2
  ```
- Line 14: name
  ```
  replace `vname' = (`vname' - v2 ) / v2
  ```
- Line 25: name
  ```
  rename v2 weight
  ```
- Line 26: name
  ```
  *rename v1 weight
  ```
- Line 32: name
  ```
  foreach vname of varlist v3-v29 {
  ```
- Line 33: name
  ```
  *replace `vname' = abs(`vname' * weight)
  ```
- Line 34: name
  ```
  replace `vname' = abs(`vname')
  ```
- Line 39: lat
  ```
  *  (for easy checking of numbers, see objective.f90 lines 575+, and the latest_aux_param.txt
  ```
- Line 47: district
  ```
  *aux 2 (district level teacher caracteristics (x,c1,c2) by district groups, quintiles of lambda/budg
  ```
- Line 49: district
  ```
  *aux 3 (regressions of wage schedule on district conditions)
  ```
- Line 51: district
  ```
  *aux 4 (cross district wage moments, average and variance/cov of omega1,omega2)
  ```
- Line 68: name
  ```
  * rename so we know what is what
  ```
- Line 69: name
  ```
  *rename v2   unperturbed_aux
  ```
- Line 70: name
  ```
  rename v3   alpha1_urban
  ```
- Line 71: name
  ```
  rename v4   alpha2_suburban
  ```
- Line 72: name
  ```
  rename v5   alpha3_milwaukee
  ```
- Line 73: name
  ```
  rename v6   alpha4_exp_lambda
  ```
- Line 74: name
  ```
  rename v7   alpha5_c1_lambda
  ```
- Line 75: name
  ```
  rename v8   alpha7_move_exp2
  ```
- Line 76: name
  ```
  rename v9   alpha8_move_exp3
  ```
- Line 77: name
  ```
  rename v10  alpha9_move_exp4
  ```
- Line 78: name
  ```
  rename v11  alpha10_move_exp5
  ```
- Line 79: name
  ```
  rename v12  alpha11_move_exp6
  ```
- Line 80: name
  ```
  rename v13  alpha12_move_logdist
  ```
- Line 81: name
  ```
  rename v14  alpha13_samecz
  ```
- Line 82: name
  ```
  rename v15  beta2_exp2
  ```
- Line 83: name
  ```
  rename v16  beta3_exp3
  ```
- Line 84: name
  ```
  rename v17  beta4_exp4
  ```
- Line 85: name
  ```
  rename v18  beta5_exp5
  ```
- Line 86: name
  ```
  rename v19  beta6_exp6
  ```
- Line 87: name
  ```
  rename v20  beta7_lambda_c2
  ```
- Line 88: name
  ```
  rename v21  beta8_master
  ```
- Line 89: name
  ```
  rename v22  sigma_teacher
  ```
- Line 90: name
  ```
  rename v23  gamma1_fc
  ```
- Line 91: name
  ```
  rename v24  gamma2_dem
  ```
- Line 92: name
  ```
  rename v25  gamma3_o1
  ```
- Line 93: name
  ```
  rename v26  gamma4_dem_o1
  ```
- Line 94: name
  ```
  rename v27  gamma5_o2
  ```
- Line 95: name
  ```
  rename v28  gamma6_dem_o2
  ```
- Line 96: name
  ```
  rename v29  sigma_wage
  ```
- Line 109: district
  ```
  egen district = rowmean(beta*)
  ```
- Line 113: district
  ```
  keep group teacher district gamma_wage
  ```
- Line 116: district
  ```
  foreach var of varlist teacher district gamma_wage {
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B15.do**

- Line 21: name
  ```
  * Rename d to lambda
  ```
- Line 22: name
  ```
  rename d lambda
  ```
- Line 25: loc
  ```
  local N = _N
  ```
- Line 27: district
  ```
  * Save teacher and district vars
  ```
- Line 30: lat
  ```
  * --- Baseline calculation ---
  ```
- Line 36: loc
  ```
  local bl_tc = r(sum)
  ```
- Line 38: loc
  ```
  local bl_ec = r(sum)
  ```
- Line 40: loc
  ```
  local bl_c2 = r(sum)
  ```
- Line 44: district
  ```
  * --- Efficiency focused (beta=1): sort districts by -lambda, teachers by -(c0 - 1*c1) ---
  ```
- Line 47: district
  ```
  * Sort districts by descending lambda, keep rank
  ```
- Line 49: district
  ```
  gen district_rank = _n
  ```
- Line 58: district
  ```
  * Merge back district lambda by rank
  ```
- Line 59: name
  ```
  rename lambda lambda_old
  ```
- Line 60: district
  ```
  drop district_rank
  ```
- Line 61: district
  ```
  gen district_rank = teacher_rank
  ```
- Line 69: loc
  ```
  local eff_tc = r(sum)
  ```
- Line 71: loc
  ```
  local eff_ec = r(sum)
  ```
- Line 73: loc
  ```
  local eff_c2 = r(sum)
  ```
- Line 77: district
  ```
  * --- Equity focused (beta=0): sort districts by -lambda, teachers by -(c0 - 0*c1) = -c0 ---
  ```
- Line 81: district
  ```
  gen district_rank = _n
  ```
- Line 90: name
  ```
  rename lambda lambda_old
  ```
- Line 91: district
  ```
  drop district_rank
  ```
- Line 92: district
  ```
  gen district_rank = teacher_rank
  ```
- Line 100: loc
  ```
  local eq_tc = r(sum)
  ```
- Line 102: loc
  ```
  local eq_ec = r(sum)
  ```
- Line 104: loc
  ```
  local eq_c2 = r(sum)
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B16.do**

- Line 10: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 12: loc
  ```
  local baseline "$fortran_code/5_exitentry/baseline/result/"
  ```
- Line 13: loc
  ```
  local cf1 "$fortran_code/5_exitentry/new1_05/result/"
  ```
- Line 14: loc
  ```
  local cf2 "$fortran_code/5_exitentry/new1_1/result/"
  ```
- Line 15: loc
  ```
  local cf3 "$fortran_code/5_exitentry/new1_15/result/"
  ```
- Line 16: loc
  ```
  local cf4 "$fortran_code/5_exitentry/new2_05/result/"
  ```
- Line 17: loc
  ```
  local cf5 "$fortran_code/5_exitentry/new2_1/result/"
  ```
- Line 18: loc
  ```
  local cf6 "$fortran_code/5_exitentry/new2_15/result/"
  ```
- Line 20: loc
  ```
  local tp = 2
  ```
- Line 34: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 35: district
  ```
  keep time_period economy_id district_id avg_expect_cont avg_c1 avg_c2 lambda
  ```
- Line 36: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 37: name
  ```
  rename avg_c1 c1
  ```
- Line 38: name
  ```
  rename avg_c2 c2
  ```
- Line 44: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 45: district
  ```
  keep time_period economy_id district_id
  ```
- Line 51: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 52: district
  ```
  keep time_period economy_id district_id expected_hires
  ```
- Line 65: district
  ```
  keep time_period economy_id district_id expected_hires lambda ///
  ```
- Line 72: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 73: district
  ```
  keep time_period economy_id district_id avg_expect_cont avg_c1 avg_c2
  ```
- Line 74: name
  ```
  rename avg_expect_cont cf_contribute
  ```
- Line 75: name
  ```
  rename avg_c1 cf_c1
  ```
- Line 76: name
  ```
  rename avg_c2 cf_c2
  ```
- Line 82: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 83: district
  ```
  keep time_period economy_id district_id
  ```
- Line 89: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 90: district
  ```
  keep time_period economy_id district_id expected_hires
  ```
- Line 91: name
  ```
  rename expected_hires cf_expected_hires
  ```
- Line 102: district
  ```
  keep time_period economy_id district_id cf_expected_hires ///
  ```
- Line 118: district
  ```
  * Step 1: Average across economies (by district)
  ```
- Line 120: district
  ```
  totalc1 cf_totalc1 totalc2 cf_totalc2, by(district_id)
  ```
- Line 131: district
  ```
  * Step 2: Sum across all districts
  ```
- Line 184: name
  ```
  rename s_`v' `v'
  ```
- Line 188: loc
  ```
  local newobs = _N + 1
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B17.do**

- Line 10: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 12: loc
  ```
  local baseline "$fortran_code/6_forward/forward_baseline/result/"
  ```
- Line 13: loc
  ```
  local cf1 "$fortran_code/6_forward/forward_new1/result/"
  ```
- Line 14: loc
  ```
  local cf2 "$fortran_code/6_forward/forward_new2/result/"
  ```
- Line 15: lat
  ```
  * forward simulation goes to t=5  (i.e., t0-t5 is 6 items)
  ```
- Line 16: loc
  ```
  local tp = 6
  ```
- Line 30: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 31: district
  ```
  keep time_period economy_id district_id avg_expect_cont avg_c1 avg_c2 lambda
  ```
- Line 32: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 33: name
  ```
  rename avg_c1 c1
  ```
- Line 34: name
  ```
  rename avg_c2 c2
  ```
- Line 40: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 41: district
  ```
  keep time_period economy_id district_id
  ```
- Line 47: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 48: district
  ```
  keep time_period economy_id district_id expected_hires
  ```
- Line 61: district
  ```
  keep time_period economy_id district_id expected_hires lambda ///
  ```
- Line 68: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 69: district
  ```
  keep time_period economy_id district_id avg_expect_cont avg_c1 avg_c2
  ```
- Line 70: name
  ```
  rename avg_expect_cont cf_contribute
  ```
- Line 71: name
  ```
  rename avg_c1 cf_c1
  ```
- Line 72: name
  ```
  rename avg_c2 cf_c2
  ```
- Line 78: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 79: district
  ```
  keep time_period economy_id district_id
  ```
- Line 85: name
  ```
  delim(" ", collapse) varnames(1) clear
  ```
- Line 86: district
  ```
  keep time_period economy_id district_id expected_hires
  ```
- Line 87: name
  ```
  rename expected_hires cf_expected_hires
  ```
- Line 98: district
  ```
  keep time_period economy_id district_id cf_expected_hires ///
  ```
- Line 114: district
  ```
  * Step 1: Average across economies (by district)
  ```
- Line 116: district
  ```
  totalc1 cf_totalc1 totalc2 cf_totalc2, by(district_id)
  ```
- Line 127: district
  ```
  * Step 2: Sum across all districts
  ```
- Line 168: name
  ```
  rename s_`v' `v'
  ```
- Line 172: loc
  ```
  local newobs = _N + 1
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B18.do**

- Line 8: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 11: loc
  ```
  local baseline_dir "$fortran_code/1_baseline/result"
  ```
- Line 14: loc
  ```
  local cf_dir_1 "$fortran_code/3_basicCfs/new1/result"
  ```
- Line 15: loc
  ```
  local cf_dir_2 "$fortran_code/3_basicCfs/new2/result"
  ```
- Line 16: loc
  ```
  local cf_dir_3 "$fortran_code/3_basicCfs/new3/result"
  ```
- Line 17: loc
  ```
  local cf_dir_4 "$fortran_code/3_basicCfs/new4/result"
  ```
- Line 36: loc
  ```
  local cf_dir "`cf_dir_`col''"
  ```
- Line 44: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 45: name
  ```
  rename avg_c1 c1
  ```
- Line 46: name
  ```
  rename avg_c2 c2
  ```
- Line 47: name
  ```
  rename avg_expect_exp exp
  ```
- Line 48: district
  ```
  sort econ district_id
  ```
- Line 54: city
  ```
  capture drop capacity
  ```
- Line 55: name
  ```
  rename expected_hires expected_hires
  ```
- Line 63: district
  ```
  sort econ district_id
  ```
- Line 72: name
  ```
  rename avg_expect_cont contributenew
  ```
- Line 73: name
  ```
  rename avg_c1 c1new
  ```
- Line 74: name
  ```
  rename avg_c2 c2new
  ```
- Line 75: name
  ```
  rename avg_expect_exp expnew
  ```
- Line 76: district
  ```
  sort econ district_id
  ```
- Line 82: city
  ```
  capture drop capacity
  ```
- Line 83: name
  ```
  rename expected_hires expected_hiresnew
  ```
- Line 84: name
  ```
  rename belief_expected belief_expected_hiresnew
  ```
- Line 92: loc
  ```
  levelsof economy_id, local(econs)
  ```
- Line 93: district
  ```
  sort econ district_id
  ```
- Line 108: district
  ```
  * Merge in lambda/district info
  ```
- Line 114: district
  ```
  keep district_id lambda
  ```
- Line 115: district
  ```
  sort district_id
  ```
- Line 119: district
  ```
  sort district_id
  ```
- Line 144: district
  ```
  * State-level (all districts)
  ```
- Line 145: district
  ```
  collapse (mean) totalcontr totalcontrnew totalc1 totalc2 totalc1new totalc2new quartlam, by(district
  ```
- Line 159: loc
  ```
  local base_row = 3 + (4 - `q') * 3 + 1
  ```
- Line 187: district
  ```
  replace row_label = "4th quartile lambda_d districts" in 4
  ```
- Line 189: district
  ```
  replace row_label = "4th quartile lambda_d districts" in 5
  ```
- Line 191: district
  ```
  replace row_label = "4th quartile lambda_d districts" in 6
  ```
- Line 195: district
  ```
  replace row_label = "3rd quartile lambda_d districts" in 7
  ```
- Line 197: district
  ```
  replace row_label = "3rd quartile lambda_d districts" in 8
  ```
- Line 199: district
  ```
  replace row_label = "3rd quartile lambda_d districts" in 9
  ```
- Line 203: district
  ```
  replace row_label = "2nd quartile lambda_d districts" in 10
  ```
- Line 205: district
  ```
  replace row_label = "2nd quartile lambda_d districts" in 11
  ```
- Line 207: district
  ```
  replace row_label = "2nd quartile lambda_d districts" in 12
  ```
- Line 211: district
  ```
  replace row_label = "1st quartile lambda_d districts" in 13
  ```
- Line 213: district
  ```
  replace row_label = "1st quartile lambda_d districts" in 14
  ```
- Line 215: district
  ```
  replace row_label = "1st quartile lambda_d districts" in 15
  ```
- Line 232: loc
  ```
  local val = table_detail[`row', `col']
  ```
- Line 233: loc
  ```
  local val_fmt : display %5.2f `val'
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B19.do**

- Line 9: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 12: loc
  ```
  local baseline_dir "$fortran_code/1_baseline/result"
  ```
- Line 15: loc
  ```
  local cf_dir_1 "$fortran_code/3_basicCfs/new1/result"
  ```
- Line 16: loc
  ```
  local cf_dir_2 "$fortran_code/7_higherbonus/new1_15/result"
  ```
- Line 17: loc
  ```
  local cf_dir_3 "$fortran_code/7_higherbonus/new1_2/result"
  ```
- Line 18: loc
  ```
  local cf_dir_4 "$fortran_code/7_higherbonus/new1_25/result"
  ```
- Line 30: loc
  ```
  local cf_dir "`cf_dir_`col''"
  ```
- Line 40: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 41: name
  ```
  rename avg_c1 c1
  ```
- Line 42: name
  ```
  rename avg_c2 c2
  ```
- Line 43: name
  ```
  rename avg_expect_exp exp
  ```
- Line 44: district
  ```
  sort econ district_id
  ```
- Line 50: city
  ```
  drop capacity
  ```
- Line 51: name
  ```
  rename expected_hires expected_hires
  ```
- Line 59: district
  ```
  sort econ district_id
  ```
- Line 68: name
  ```
  rename avg_expect_cont contributenew
  ```
- Line 69: name
  ```
  rename avg_c1 c1new
  ```
- Line 70: name
  ```
  rename avg_c2 c2new
  ```
- Line 71: name
  ```
  rename avg_expect_exp expnew
  ```
- Line 72: district
  ```
  sort econ district_id
  ```
- Line 78: city
  ```
  drop capacity
  ```
- Line 79: name
  ```
  rename expected_hires expected_hiresnew
  ```
- Line 80: name
  ```
  rename belief_expected belief_expected_hiresnew
  ```
- Line 88: loc
  ```
  levelsof economy_id, local(econs)
  ```
- Line 89: district
  ```
  sort econ district_id
  ```
- Line 104: district
  ```
  * Merge in lambda/district info
  ```
- Line 110: district
  ```
  keep district_id lambda
  ```
- Line 111: district
  ```
  sort district_id
  ```
- Line 115: district
  ```
  sort district_id
  ```
- Line 138: district
  ```
  * Row 1-3: All districts
  ```
- Line 139: district
  ```
  collapse (mean) totalcontr totalcontrnew totalc1 totalc2 totalc1new totalc2new, by(district_id)
  ```
- Line 143: loc
  ```
  local pct_tc = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 147: loc
  ```
  local pct_c1 = (totalc1new[1] - totalc1[1]) / totalc1[1]
  ```
- Line 151: loc
  ```
  local pct_c2 = (totalc2new[1] - totalc2[1]) / totalc2[1]
  ```
- Line 159: district
  ```
  collapse (mean) totalcontr totalcontrnew, by(district_id quartlam)
  ```
- Line 162: loc
  ```
  local pct_q = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 163: loc
  ```
  local row = 4 + (4 - `q')
  ```
- Line 195: district
  ```
  replace row_label = "TC in 4th quartile lambda_d districts" in 4
  ```
- Line 196: district
  ```
  replace row_label = "TC in 3rd quartile lambda_d districts" in 5
  ```
- Line 197: district
  ```
  replace row_label = "TC in 2nd quartile lambda_d districts" in 6
  ```
- Line 198: district
  ```
  replace row_label = "TC in 1st quartile lambda_d districts" in 7
  ```
- Line 209: name
  ```
  * Determine column variable name
  ```
- Line 210: loc
  ```
  if `col' == 1 local cvar "new1"
  ```
- Line 211: loc
  ```
  if `col' == 2 local cvar "new1_5"
  ```
- Line 212: loc
  ```
  if `col' == 3 local cvar "new2"
  ```
- Line 213: loc
  ```
  if `col' == 4 local cvar "new2_5"
  ```
- Line 217: loc
  ```
  local val = tableB19[`row', `col'] * 100
  ```
- Line 218: loc
  ```
  local val_fmt : display %5.2f `val'
  ```
- Line 223: loc
  ```
  local val = tableB19[9, `col']
  ```
- Line 224: loc
  ```
  local val_fmt : display %4.2f `val'
  ```
- Line 234: name
  ```
  * Rename columns for clarity
  ```
- Line 235: name
  ```
  rename new1 pct_change_New1
  ```
- Line 236: name
  ```
  rename new1_5 pct_change_1_5New1
  ```
- Line 237: name
  ```
  rename new2 pct_change_2New1
  ```
- Line 238: name
  ```
  rename new2_5 pct_change_2_5New1
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B20.do**

- Line 9: loc
  ```
  * Define some extra local paths for where to find results
  ```
- Line 12: loc
  ```
  local baseline_dir "$fortran_code/1_baseline/result"
  ```
- Line 15: loc
  ```
  local cf_dir_1 "$fortran_code/8_noRcost/new1/result"
  ```
- Line 16: loc
  ```
  local cf_dir_2 "$fortran_code/8_noRcost/new2/result"
  ```
- Line 17: loc
  ```
  local cf_dir_3 "$fortran_code/8_noRcost/new3/result"
  ```
- Line 18: loc
  ```
  local cf_dir_4 "$fortran_code/8_noRcost/new4/result"
  ```
- Line 30: loc
  ```
  local cf_dir "`cf_dir_`col''"
  ```
- Line 40: name
  ```
  rename avg_expect_cont contribute
  ```
- Line 41: name
  ```
  rename avg_c1 c1
  ```
- Line 42: name
  ```
  rename avg_c2 c2
  ```
- Line 43: name
  ```
  rename avg_expect_exp exp
  ```
- Line 44: district
  ```
  sort econ district_id
  ```
- Line 50: city
  ```
  drop capacity
  ```
- Line 51: name
  ```
  rename expected_hires expected_hires
  ```
- Line 59: district
  ```
  sort econ district_id
  ```
- Line 68: name
  ```
  rename avg_expect_cont contributenew
  ```
- Line 69: name
  ```
  rename avg_c1 c1new
  ```
- Line 70: name
  ```
  rename avg_c2 c2new
  ```
- Line 71: name
  ```
  rename avg_expect_exp expnew
  ```
- Line 72: district
  ```
  sort econ district_id
  ```
- Line 78: city
  ```
  drop capacity
  ```
- Line 79: name
  ```
  rename expected_hires expected_hiresnew
  ```
- Line 80: name
  ```
  rename belief_expected belief_expected_hiresnew
  ```
- Line 88: loc
  ```
  levelsof economy_id, local(econs)
  ```
- Line 89: district
  ```
  sort econ district_id
  ```
- Line 104: district
  ```
  * Merge in lambda/district info
  ```
- Line 110: district
  ```
  keep district_id lambda
  ```
- Line 111: district
  ```
  sort district_id
  ```
- Line 115: district
  ```
  sort district_id
  ```
- Line 138: district
  ```
  * Row 1-3: All districts
  ```
- Line 139: district
  ```
  collapse (mean) totalcontr totalcontrnew totalc1 totalc2 totalc1new totalc2new, by(district_id)
  ```
- Line 143: loc
  ```
  local pct_tc = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 147: loc
  ```
  local pct_c1 = (totalc1new[1] - totalc1[1]) / totalc1[1]
  ```
- Line 151: loc
  ```
  local pct_c2 = (totalc2new[1] - totalc2[1]) / totalc2[1]
  ```
- Line 159: district
  ```
  collapse (mean) totalcontr totalcontrnew, by(district_id quartlam)
  ```
- Line 162: loc
  ```
  local pct_q = (totalcontrnew[1] - totalcontr[1]) / totalcontr[1]
  ```
- Line 163: loc
  ```
  local row = 4 + (4 - `q')
  ```
- Line 195: district
  ```
  replace row_label = "TC in 4th quartile lambda_d districts" in 4
  ```
- Line 196: district
  ```
  replace row_label = "TC in 3rd quartile lambda_d districts" in 5
  ```
- Line 197: district
  ```
  replace row_label = "TC in 2nd quartile lambda_d districts" in 6
  ```
- Line 198: district
  ```
  replace row_label = "TC in 1st quartile lambda_d districts" in 7
  ```
- Line 209: name
  ```
  * Determine column variable name
  ```
- Line 210: loc
  ```
  if `col' == 1 local cvar "new1"
  ```
- Line 211: loc
  ```
  if `col' == 2 local cvar "new2"
  ```
- Line 212: loc
  ```
  if `col' == 3 local cvar "new3"
  ```
- Line 213: loc
  ```
  if `col' == 4 local cvar "new4"
  ```
- Line 217: loc
  ```
  local val = tableB19[`row', `col'] * 100
  ```
- Line 218: loc
  ```
  local val_fmt : display %5.2f `val'
  ```
- Line 223: loc
  ```
  local val = tableB19[9, `col']
  ```
- Line 224: loc
  ```
  local val_fmt : display %4.2f `val'
  ```
- Line 234: name
  ```
  * Rename columns for clarity
  ```
- Line 235: name
  ```
  rename new1 New1_norcost
  ```
- Line 236: name
  ```
  rename new2 New2_norcost
  ```
- Line 237: name
  ```
  rename new3 New3_norcost
  ```
- Line 238: name
  ```
  rename new4 New4_norcost
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B22.do**

- Line 2: district
  ```
  * Table B22 (Model Fit: Average District Employee Characteristics)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/estimation_result/result/other_aux_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 35: district
  ```
  gen is_fraction = regexm(rawline, `"Fraction of districts who only"')
  ```
- Line 38: loc
  ```
  local lambda_start = r(min)
  ```
- Line 41: loc
  ```
  local budget_start = r(min)
  ```
- Line 44: loc
  ```
  local suburban_start = r(min)
  ```
- Line 47: loc
  ```
  local urban_start = r(min)
  ```
- Line 52: loc
  ```
  local linepos = `lambda_start' + 1 + `j'
  ```
- Line 53: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 54: loc
  ```
  local lam_exp_d`j' : word 1 of `thisline'
  ```
- Line 55: loc
  ```
  local lam_exp_m`j' : word 2 of `thisline'
  ```
- Line 56: loc
  ```
  local lam_v1_d`j'  : word 3 of `thisline'
  ```
- Line 57: loc
  ```
  local lam_v1_m`j'  : word 4 of `thisline'
  ```
- Line 58: loc
  ```
  local lam_v2_d`j'  : word 5 of `thisline'
  ```
- Line 59: loc
  ```
  local lam_v2_m`j'  : word 6 of `thisline'
  ```
- Line 60: loc
  ```
  local lam_deg_d`j' : word 7 of `thisline'
  ```
- Line 61: loc
  ```
  local lam_deg_m`j' : word 8 of `thisline'
  ```
- Line 66: loc
  ```
  local linepos = `budget_start' + 1 + `j'
  ```
- Line 67: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 68: loc
  ```
  local bud_exp_d`j' : word 1 of `thisline'
  ```
- Line 69: loc
  ```
  local bud_exp_m`j' : word 2 of `thisline'
  ```
- Line 70: loc
  ```
  local bud_v1_d`j'  : word 3 of `thisline'
  ```
- Line 71: loc
  ```
  local bud_v1_m`j'  : word 4 of `thisline'
  ```
- Line 72: loc
  ```
  local bud_v2_d`j'  : word 5 of `thisline'
  ```
- Line 73: loc
  ```
  local bud_v2_m`j'  : word 6 of `thisline'
  ```
- Line 74: loc
  ```
  local bud_deg_d`j' : word 7 of `thisline'
  ```
- Line 75: loc
  ```
  local bud_deg_m`j' : word 8 of `thisline'
  ```
- Line 79: loc
  ```
  local linepos = `suburban_start' + 1 + 1
  ```
- Line 80: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 81: loc
  ```
  local sub_exp_d : word 1 of `thisline'
  ```
- Line 82: loc
  ```
  local sub_exp_m : word 2 of `thisline'
  ```
- Line 83: loc
  ```
  local sub_v1_d  : word 3 of `thisline'
  ```
- Line 84: loc
  ```
  local sub_v1_m  : word 4 of `thisline'
  ```
- Line 85: loc
  ```
  local sub_v2_d  : word 5 of `thisline'
  ```
- Line 86: loc
  ```
  local sub_v2_m  : word 6 of `thisline'
  ```
- Line 87: loc
  ```
  local sub_deg_d : word 7 of `thisline'
  ```
- Line 88: loc
  ```
  local sub_deg_m : word 8 of `thisline'
  ```
- Line 91: loc
  ```
  local linepos = `urban_start' + 1 + 1
  ```
- Line 92: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 93: loc
  ```
  local urb_exp_d : word 1 of `thisline'
  ```
- Line 94: loc
  ```
  local urb_exp_m : word 2 of `thisline'
  ```
- Line 95: loc
  ```
  local urb_v1_d  : word 3 of `thisline'
  ```
- Line 96: loc
  ```
  local urb_v1_m  : word 4 of `thisline'
  ```
- Line 97: loc
  ```
  local urb_v2_d  : word 5 of `thisline'
  ```
- Line 98: loc
  ```
  local urb_v2_m  : word 6 of `thisline'
  ```
- Line 99: loc
  ```
  local urb_deg_d : word 7 of `thisline'
  ```
- Line 100: loc
  ```
  local urb_deg_m : word 8 of `thisline'
  ```
- Line 106: district
  ```
  gen str40 district_group = ""
  ```
- Line 119: district
  ```
  replace district_group = "lambda_d" in `j'
  ```
- Line 133: loc
  ```
  local row = 4 + `j'
  ```
- Line 134: city, district
  ```
  replace district_group = "budget_capacity" in `row'
  ```
- Line 147: district
  ```
  replace district_group = "Urban" in 9
  ```
- Line 159: district
  ```
  replace district_group = "Suburban" in 10
  ```
- Line 171: district
  ```
  export delimited district_group subgroup exp_data exp_model deg_data deg_model ///
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B23.do**

- Line 2: district
  ```
  * Table B23 (Model Fit: OLS of District Wage Schedule)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/estimation_result/result/aux_and_omega_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 34: loc
  ```
  local omega1_start = r(min)
  ```
- Line 37: loc
  ```
  local omega2_start = r(min)
  ```
- Line 41: loc
  ```
  local linepos = `omega1_start' + 1 + `j'
  ```
- Line 42: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 43: loc
  ```
  local o1d`j' : word 1 of `thisline'
  ```
- Line 44: loc
  ```
  local o1m`j' : word 2 of `thisline'
  ```
- Line 46: loc
  ```
  local nw : word count `thisline'
  ```
- Line 47: loc
  ```
  local o1l`j' ""
  ```
- Line 49: loc
  ```
  local wk : word `k' of `thisline'
  ```
- Line 50: loc
  ```
  local o1l`j' "`o1l`j'' `wk'"
  ```
- Line 52: loc
  ```
  local o1l`j' = strtrim("`o1l`j''")
  ```
- Line 57: loc
  ```
  local linepos = `omega2_start' + 1 + `j'
  ```
- Line 58: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 59: loc
  ```
  local o2d`j' : word 1 of `thisline'
  ```
- Line 60: loc
  ```
  local o2m`j' : word 2 of `thisline'
  ```
- Line 65: city
  ```
  *   7=tc, 8=tc_tenured, 9=budget_pc, 10=capacity, 11=urban, 12=suburban,
  ```
- Line 70: city
  ```
  *   lambda, budget_pc, capacity, urban, suburban, milwaukee, share dem vote,
  ```
- Line 83: loc
  ```
  local order "1 2 3 4 5 7 8 6 9 10 11 12 13 16 14 15"
  ```
- Line 85: loc
  ```
  local row = 1
  ```
- Line 86: loc
  ```
  foreach s of local order {
  ```
- Line 91: loc
  ```
  local row = `row' + 1
  ```
- Line 104: city
  ```
  replace label = "capacity" in 10
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B24.do**

- Line 2: district
  ```
  * Table B24 (Pre-Act 10 Model Validation: OLS of Teacher-District Match)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/validation_result/result/aux_and_omega_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 34: loc
  ```
  local small_start = r(min)
  ```
- Line 37: loc
  ```
  local big_start = r(min)
  ```
- Line 39: loc
  ```
  * Extract values into locals
  ```
- Line 43: loc
  ```
  local linepos = `small_start' + 1 + `j'
  ```
- Line 44: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 45: loc
  ```
  local sd`j' : word 1 of `thisline'
  ```
- Line 46: loc
  ```
  local sm`j' : word 2 of `thisline'
  ```
- Line 47: loc
  ```
  local nw : word count `thisline'
  ```
- Line 48: loc
  ```
  local sl`j' ""
  ```
- Line 50: loc
  ```
  local wk : word `k' of `thisline'
  ```
- Line 51: loc
  ```
  local sl`j' "`sl`j'' `wk'"
  ```
- Line 53: loc
  ```
  local sl`j' = strtrim("`sl`j''")
  ```
- Line 57: loc
  ```
  local linepos = `big_start' + 1 + `j'
  ```
- Line 58: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 59: loc
  ```
  local bd`j' : word 1 of `thisline'
  ```
- Line 60: loc
  ```
  local bm`j' : word 2 of `thisline'
  ```
- Line 79: loc
  ```
  local order "1 2 3 4 5 6 7 8 9 12 10 11 13"
  ```
- Line 81: loc
  ```
  local row = 1
  ```
- Line 82: loc
  ```
  foreach s of local order {
  ```
- Line 88: loc
  ```
  local row = `row' + 1
  ```
- Line 91: name
  ```
  * Rename labels to match Table A2
  ```
- Line 104: name
  ```
  * Rename columns
  ```
- Line 105: name
  ```
  rename small_data inferred_data
  ```
- Line 106: name
  ```
  rename small_model inferred_model
  ```
- Line 107: name
  ```
  rename big_data all_data
  ```
- Line 108: name
  ```
  rename big_model all_model
  ```

**/replication-package/replication_delivery/code/descriptives/Table_B25.do**

- Line 2: district
  ```
  * Table B25 (Pre Act 10 Model Fit: Average District Employee Characteristics)
  ```
- Line 9: loc
  ```
  local fn "$fortran_code/validation_result/result/other_aux_params.txt"
  ```
- Line 14: loc
  ```
  local nlines = 500
  ```
- Line 18: loc
  ```
  local i = 0
  ```
- Line 20: loc
  ```
  local i = `i' + 1
  ```
- Line 35: district
  ```
  gen is_fraction = regexm(rawline, `"Fraction of districts who only"')
  ```
- Line 38: loc
  ```
  local lambda_start = r(min)
  ```
- Line 41: loc
  ```
  local budget_start = r(min)
  ```
- Line 44: loc
  ```
  local suburban_start = r(min)
  ```
- Line 47: loc
  ```
  local urban_start = r(min)
  ```
- Line 52: loc
  ```
  local linepos = `lambda_start' + 1 + `j'
  ```
- Line 53: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 54: loc
  ```
  local lam_exp_d`j' : word 1 of `thisline'
  ```
- Line 55: loc
  ```
  local lam_exp_m`j' : word 2 of `thisline'
  ```
- Line 56: loc
  ```
  local lam_v1_d`j'  : word 3 of `thisline'
  ```
- Line 57: loc
  ```
  local lam_v1_m`j'  : word 4 of `thisline'
  ```
- Line 58: loc
  ```
  local lam_v2_d`j'  : word 5 of `thisline'
  ```
- Line 59: loc
  ```
  local lam_v2_m`j'  : word 6 of `thisline'
  ```
- Line 60: loc
  ```
  local lam_deg_d`j' : word 7 of `thisline'
  ```
- Line 61: loc
  ```
  local lam_deg_m`j' : word 8 of `thisline'
  ```
- Line 66: loc
  ```
  local linepos = `budget_start' + 1 + `j'
  ```
- Line 67: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 68: loc
  ```
  local bud_exp_d`j' : word 1 of `thisline'
  ```
- Line 69: loc
  ```
  local bud_exp_m`j' : word 2 of `thisline'
  ```
- Line 70: loc
  ```
  local bud_v1_d`j'  : word 3 of `thisline'
  ```
- Line 71: loc
  ```
  local bud_v1_m`j'  : word 4 of `thisline'
  ```
- Line 72: loc
  ```
  local bud_v2_d`j'  : word 5 of `thisline'
  ```
- Line 73: loc
  ```
  local bud_v2_m`j'  : word 6 of `thisline'
  ```
- Line 74: loc
  ```
  local bud_deg_d`j' : word 7 of `thisline'
  ```
- Line 75: loc
  ```
  local bud_deg_m`j' : word 8 of `thisline'
  ```
- Line 79: loc
  ```
  local linepos = `suburban_start' + 1 + 1
  ```
- Line 80: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 81: loc
  ```
  local sub_exp_d : word 1 of `thisline'
  ```
- Line 82: loc
  ```
  local sub_exp_m : word 2 of `thisline'
  ```
- Line 83: loc
  ```
  local sub_v1_d  : word 3 of `thisline'
  ```
- Line 84: loc
  ```
  local sub_v1_m  : word 4 of `thisline'
  ```
- Line 85: loc
  ```
  local sub_v2_d  : word 5 of `thisline'
  ```
- Line 86: loc
  ```
  local sub_v2_m  : word 6 of `thisline'
  ```
- Line 87: loc
  ```
  local sub_deg_d : word 7 of `thisline'
  ```
- Line 88: loc
  ```
  local sub_deg_m : word 8 of `thisline'
  ```
- Line 91: loc
  ```
  local linepos = `urban_start' + 1 + 1
  ```
- Line 92: loc
  ```
  local thisline = rawline[`linepos']
  ```
- Line 93: loc
  ```
  local urb_exp_d : word 1 of `thisline'
  ```
- Line 94: loc
  ```
  local urb_exp_m : word 2 of `thisline'
  ```
- Line 95: loc
  ```
  local urb_v1_d  : word 3 of `thisline'
  ```
- Line 96: loc
  ```
  local urb_v1_m  : word 4 of `thisline'
  ```
- Line 97: loc
  ```
  local urb_v2_d  : word 5 of `thisline'
  ```
- Line 98: loc
  ```
  local urb_v2_m  : word 6 of `thisline'
  ```
- Line 99: loc
  ```
  local urb_deg_d : word 7 of `thisline'
  ```
- Line 100: loc
  ```
  local urb_deg_m : word 8 of `thisline'
  ```
- Line 106: district
  ```
  gen str40 district_group = ""
  ```
- Line 119: district
  ```
  replace district_group = "lambda_d" in `j'
  ```
- Line 133: loc
  ```
  local row = 4 + `j'
  ```
- Line 134: city, district
  ```
  replace district_group = "budget_capacity" in `row'
  ```
- Line 147: district
  ```
  replace district_group = "Urban" in 9
  ```
- Line 159: district
  ```
  replace district_group = "Suburban" in 10
  ```
- Line 171: district
  ```
  export delimited district_group subgroup exp_data exp_model deg_data deg_model ///
  ```

**/replication-package/replication_delivery/code/descriptives/achievement_race.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 7: city
  ```
  *          race/ethnicity interactions and construct race-adjusted VA measures.
  ```
- Line 61: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 62: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 65: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 69: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 84: loc
  ```
  local z = `x' + 1
  ```
- Line 91: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 100: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 123: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 134: city
  ```
  * Residualize Outcomes with Race/Ethnicity Interactions
  ```
- Line 153: school
  ```
  egen schgr = group(schoolid grade)
  ```
- Line 162: school
  ```
  a(schoolid) cluster(schgr)
  ```
- Line 183: school
  ```
  a(schoolid) cluster(schgr)
  ```

**/replication-package/replication_delivery/code/descriptives/appendix_figures.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: district
  ```
  drop if year == 2010 & district_code == 2793
  ```
- Line 18: school
  ```
  * Merge school-level low-achievement shares.
  ```
- Line 21: school
  ```
  drop if schoolcode == 8888
  ```
- Line 24: district
  ```
  collapse d, by(district_code)
  ```
- Line 25: district
  ```
  sort district_code
  ```
- Line 29: district
  ```
  sort district_code
  ```
- Line 39: loc
  ```
  local a = r(r50)
  ```
- Line 53: district
  ```
  * Resolve prior-district variables.
  ```
- Line 54: district
  ```
  gen Dpast = district_past2009
  ```
- Line 55: district
  ```
  replace Dpast = district_past2008 if (district_past2009 == .) & ///
  ```
- Line 56: district
  ```
  district_past2012 != .
  ```
- Line 63: district
  ```
  keep id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round type ///
  ```
- Line 64: school
  ```
  totalexp year schoolcode
  ```
- Line 65: district
  ```
  qui reg id district_code Dpast v0 v1 xb0 xb1 d exp masterup salary_round ///
  ```
- Line 69: district
  ```
  * Check the number of districts in the sample.
  ```
- Line 70: district
  ```
  qui tab district_code if year == 2010
  ```
- Line 71: district
  ```
  disp "Nr districts ="
  ```
- Line 74: district
  ```
  * Tag districts in this sample.
  ```
- Line 77: district
  ```
  keep district_code
  ```
- Line 79: district
  ```
  sort district_code
  ```
- Line 91: district
  ```
  sort district_code
  ```
- Line 95: district
  ```
  sort district_code
  ```
- Line 96: name
  ```
  rename *_ba *_0
  ```
- Line 97: name
  ```
  rename *_ma *_1
  ```
- Line 98: district
  ```
  sort district_code
  ```
- Line 99: lon
  ```
  reshape long CExp1_ CExp2_ CExp3_ CExp4_ CExp5_ CExp6_, ///
  ```
- Line 100: district
  ```
  i(district_code) j(masterup)
  ```
- Line 101: name
  ```
  rename *_ *
  ```
- Line 102: district
  ```
  egen fake = group(district_code masterup)
  ```
- Line 103: lon
  ```
  reshape long CExp, i(fake) j(exp)
  ```
- Line 106: name
  ```
  rename CExp mode
  ```
- Line 107: district
  ```
  reshape wide mode, i(fake) j(district_code)
  ```
- Line 119: district
  ```
  * Keep relevant districts.
  ```
- Line 120: district
  ```
  sort district_code
  ```
- Line 141: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 142: loc
  ```
  foreach d of local Dist {
  ```
- Line 143: district
  ```
  qui sum d if district_code == `d'
  ```
- Line 144: loc
  ```
  local z = r(mean)
  ```
- Line 145: loc
  ```
  local y = 0
  ```
- Line 146: district
  ```
  qui count if district_code == `d' & year == 2014 & exp == 1 & V0 != .
  ```
- Line 154: district
  ```
  replace Wage = mode`d' if district_code == `d'
  ```
- Line 168: loc
  ```
  local corr = round(`r(rho)',0.01)
  ```
- Line 173: lat
  ```
  text(45 70 "correlation = `corr'", )
  ```
- Line 196: lat
  ```
  * Correlation between c1 and c2 (Figure B2)
  ```
- Line 207: loc
  ```
  local corr: di %6.2f `r(rho)'
  ```
- Line 208: lat
  ```
  twoway (scatter c0 c1) (lfit c0 c1), ytitle("c{subscript:2}") xtitle("c{subscript:1}") legend(off) t
  ```

**/replication-package/replication_delivery/code/descriptives/budget_shifts.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 68: district
  ```
  collapse (sum) salary sal_*, by(district_code year)
  ```

**/replication-package/replication_delivery/code/descriptives/c1c2_variation_studentlevel.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 37: school
  ```
  * the school-grade-year teacher-side file.
  ```
- Line 50: name
  ```
  rename id id`n'
  ```
- Line 51: name
  ```
  rename v c_`n'
  ```
- Line 66: name
  ```
  rename id id`n'
  ```
- Line 67: name
  ```
  rename c0 c0_`n'
  ```
- Line 68: name
  ```
  rename c1 c1_`n'
  ```
- Line 80: district, school
  ```
  sort district_code schoolcode grade year
  ```
- Line 95: district, school
  ```
  sort district_code schoolcode grade year
  ```
- Line 104: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 107: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 111: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 132: school
  ```
  a(schoolid) resid
  ```
- Line 143: school
  ```
  a(schoolid) resid
  ```
- Line 153: school
  ```
  MShispanic MSasian MSam_ind Msize, a(schoolid) resid
  ```
- Line 175: district, school
  ```
  bysort district_code schoolcode grade year: egen lambda_share = mean(lambda)
  ```
- Line 178: district, school
  ```
  by(district_code schoolcode grade year lambda)
  ```
- Line 180: district, school
  ```
  sort district_code schoolcode grade year
  ```
- Line 195: loc
  ```
  local mc: di %9.4f r(mean)
  ```
- Line 198: loc
  ```
  local mc0: di %9.4f r(mean)
  ```
- Line 200: loc
  ```
  local diff: disp %3.1f 100*((`mc'/`mc0') - 1)
  ```
- Line 206: loc
  ```
  local mc: di %9.4f r(mean)
  ```
- Line 209: loc
  ```
  local mc0: di %9.4f r(mean)
  ```
- Line 211: loc
  ```
  local diff: disp %3.1f 100*((`mc'/`mc0') - 1)
  ```
- Line 218: loc
  ```
  local mc: di %9.4f r(mean)
  ```
- Line 221: loc
  ```
  local mc0: di %9.4f r(mean)
  ```
- Line 223: loc
  ```
  local diff: disp %3.1f 100*((`mc'/`mc0') - 1)
  ```

**/replication-package/replication_delivery/code/descriptives/data_patterns.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 20: school
  ```
  drop if schoolcode == 8888
  ```
- Line 22: district
  ```
  replace salary_r = . if year == 2010 & district_code == 2793
  ```
- Line 24: school
  ```
  * Merge school-level low-achievement shares.
  ```
- Line 27: school
  ```
  drop if schoolcode == 8888
  ```
- Line 31: district
  ```
  collapse d std_score econ_disadv, by(district_code)
  ```
- Line 32: district
  ```
  sort district_code
  ```
- Line 36: district
  ```
  sort district_code
  ```
- Line 53: district
  ```
  drop if year == 2010 & district_code == 2793
  ```
- Line 54: district
  ```
  reghdfe salary_r, a(district_code##year exp##masterup) resid
  ```
- Line 58: district
  ```
  by(district_code year)
  ```
- Line 66: loc
  ```
  local z = `n' + 2008
  ```
- Line 79: district
  ```
  * Movements of Teachers Across Districts (Figure 2a)
  ```
- Line 86: loc
  ```
  local z = `n' + 2008
  ```
- Line 89: district
  ```
  eststo r1: reg mover yr1-yr2 yr3 yr4-yr6, noco cluster(district_code)
  ```
- Line 126: loc
  ```
  local a = `r(mean)'
  ```
- Line 128: loc
  ```
  local b = `r(mean)'
  ```
- Line 135: district
  ```
  if year > 2008  & mover_d == 1, cluster(district_code) a(id)
  ```
- Line 146: district
  ```
  if year > 2008  & mover_d == 0, cluster(district_code) a(id)
  ```
- Line 169: district
  ```
  * Sorting Across Districts by Lambda, Budget, and Student Disadvantage (Figure 3)
  ```
- Line 174: school
  ```
  drop if schoolcode == 8888
  ```
- Line 175: district
  ```
  collapse econ_disad, by(district_code year)
  ```
- Line 176: district
  ```
  sort district_code
  ```
- Line 184: district
  ```
  sort district_code
  ```
- Line 204: district
  ```
  if year >= 2009 & year <= 2014, by(district_code year post)
  ```
- Line 222: lat
  ```
  ytitle("correlation w/average experience", size(large)) ///
  ```

**/replication-package/replication_delivery/code/descriptives/gen_omegas_3omegas.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: district
  ```
  * Exclude this district only for wage analyses.
  ```
- Line 17: district
  ```
  drop if district_code == 2793 & year == 2010
  ```
- Line 20: district
  ```
  sort district_code
  ```
- Line 27: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 30: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 54: school
  ```
  * Merge school-level low-achievement shares.
  ```
- Line 57: school
  ```
  drop if schoolcode == 8888
  ```
- Line 60: district
  ```
  collapse d, by(district_code)
  ```
- Line 61: district
  ```
  sort district_code
  ```
- Line 65: district
  ```
  sort district_code
  ```
- Line 75: loc
  ```
  local a = r(r50)
  ```
- Line 134: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 135: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 136: district
  ```
  noisily disp "District n. `d'"
  ```
- Line 138: loc
  ```
  local A0 = 1
  ```
- Line 139: loc
  ```
  local A1 = 1
  ```
- Line 140: loc
  ```
  local A2 = 1
  ```
- Line 143: loc
  ```
  local A1 = 1
  ```
- Line 144: loc
  ```
  local A2 = 1
  ```
- Line 146: loc
  ```
  local A2 = 1
  ```
- Line 150: district
  ```
  (`Alpha2' * V1d) if district_code == `d'
  ```
- Line 153: district
  ```
  gen dev = (salary_round - wage) ^ 2 if district_code == `d'
  ```
- Line 154: district
  ```
  qui sum dev if district_code == `d', det
  ```
- Line 156: district
  ```
  if district_code == `d'
  ```
- Line 158: loc
  ```
  local A2 = `A2' + 1
  ```
- Line 160: loc
  ```
  local A1 = `A1' + 1
  ```
- Line 162: loc
  ```
  local A0 = `A0' + 1
  ```
- Line 166: district
  ```
  replace Min = x if district_code == `d'
  ```
- Line 169: district
  ```
  qui sum Min if district_code == `d', det
  ```
- Line 170: loc
  ```
  local M = r(mean)
  ```
- Line 175: district
  ```
  qui sum SS_`A0'_`A1'_`A2' if district_code == `d', det
  ```
- Line 176: loc
  ```
  local S = r(mean)
  ```
- Line 182: district
  ```
  replace S_alpha0 = `A0' if district_code == `d'
  ```
- Line 183: district
  ```
  replace S_alpha1 = `A1' if district_code == `d'
  ```
- Line 184: district
  ```
  replace S_alpha2 = `A2' if district_code == `d'
  ```
- Line 215: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 218: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 236: name
  ```
  rename S_dev* SS_dev*
  ```

**/replication-package/replication_delivery/code/descriptives/gen_omegas_exp1cutoff.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 16: district
  ```
  * Exclude this district only for wage analyses.
  ```
- Line 17: district
  ```
  drop if district_code == 2793 & year == 2010
  ```
- Line 20: district
  ```
  sort district_code
  ```
- Line 27: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 30: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 54: school
  ```
  * Merge school-level low-achievement shares.
  ```
- Line 57: school
  ```
  drop if schoolcode == 8888
  ```
- Line 60: district
  ```
  collapse d, by(district_code)
  ```
- Line 61: district
  ```
  sort district_code
  ```
- Line 65: district
  ```
  sort district_code
  ```
- Line 75: loc
  ```
  local a = r(r50)
  ```
- Line 132: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 133: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 134: district
  ```
  noisily disp "District n. `d'"
  ```
- Line 136: loc
  ```
  local A0 = 1
  ```
- Line 137: loc
  ```
  local A1 = 1
  ```
- Line 138: loc
  ```
  local A2 = 1
  ```
- Line 141: loc
  ```
  local A1 = 1
  ```
- Line 142: loc
  ```
  local A2 = 1
  ```
- Line 144: loc
  ```
  local A2 = 1
  ```
- Line 149: district
  ```
  (`Alpha2' * Comb_tenured) if district_code == `d'
  ```
- Line 152: district
  ```
  gen dev = (salary_round - wage) ^ 2 if district_code == `d'
  ```
- Line 153: district
  ```
  qui sum dev if district_code == `d', det
  ```
- Line 155: district
  ```
  if district_code == `d'
  ```
- Line 157: loc
  ```
  local A2 = `A2' + 1
  ```
- Line 159: loc
  ```
  local A1 = `A1' + 1
  ```
- Line 161: loc
  ```
  local A0 = `A0' + 1
  ```
- Line 165: district
  ```
  replace Min = x if district_code == `d'
  ```
- Line 168: district
  ```
  qui sum Min if district_code == `d', det
  ```
- Line 169: loc
  ```
  local M = r(mean)
  ```
- Line 174: district
  ```
  qui sum SS_`A0'_`A1'_`A2' if district_code == `d', det
  ```
- Line 175: loc
  ```
  local S = r(mean)
  ```
- Line 181: district
  ```
  replace S_alpha0 = `A0' if district_code == `d'
  ```
- Line 182: district
  ```
  replace S_alpha1 = `A1' if district_code == `d'
  ```
- Line 183: district
  ```
  replace S_alpha2 = `A2' if district_code == `d'
  ```
- Line 219: name
  ```
  rename S_* T_*
  ```
- Line 220: district
  ```
  sort district_code
  ```
- Line 225: district
  ```
  sort district_code
  ```
- Line 229: district
  ```
  bysort district_code: egen tot_lowexp = sum(lowexp)
  ```
- Line 251: name
  ```
  rename S_* SS_*
  ```

**/replication-package/replication_delivery/code/descriptives/gen_omegas_tenured.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 15: district
  ```
  * Exclude this district only for wage analyses.
  ```
- Line 16: district
  ```
  drop if district_code == 2793 & year == 2010
  ```
- Line 19: district
  ```
  sort district_code
  ```
- Line 26: loc
  ```
  local low = r(p1) - (0.3*r(sd))
  ```
- Line 29: loc
  ```
  local high = r(max) + (0.2*r(sd))
  ```
- Line 53: school
  ```
  * Merge school-level low-achievement shares.
  ```
- Line 56: school
  ```
  drop if schoolcode == 8888
  ```
- Line 59: district
  ```
  collapse d, by(district_code)
  ```
- Line 60: district
  ```
  sort district_code
  ```
- Line 64: district
  ```
  sort district_code
  ```
- Line 74: loc
  ```
  local a = r(r50)
  ```
- Line 131: district, loc
  ```
  qui levelsof district_code, local(Dist)
  ```
- Line 132: loc
  ```
  qui foreach d of local Dist {
  ```
- Line 133: district
  ```
  noisily disp "District n. `d'"
  ```
- Line 135: loc
  ```
  local A0 = 1
  ```
- Line 136: loc
  ```
  local A1 = 1
  ```
- Line 137: loc
  ```
  local A2 = 1
  ```
- Line 140: loc
  ```
  local A1 = 1
  ```
- Line 141: loc
  ```
  local A2 = 1
  ```
- Line 143: loc
  ```
  local A2 = 1
  ```
- Line 148: district
  ```
  (`Alpha2' * Comb_tenured) if district_code == `d'
  ```
- Line 151: district
  ```
  gen dev = (salary_round - wage) ^ 2 if district_code == `d'
  ```
- Line 152: district
  ```
  qui sum dev if district_code == `d', det
  ```
- Line 154: district
  ```
  if district_code == `d'
  ```
- Line 156: loc
  ```
  local A2 = `A2' + 1
  ```
- Line 158: loc
  ```
  local A1 = `A1' + 1
  ```
- Line 160: loc
  ```
  local A0 = `A0' + 1
  ```
- Line 164: district
  ```
  replace Min = x if district_code == `d'
  ```
- Line 167: district
  ```
  qui sum Min if district_code == `d', det
  ```
- Line 168: loc
  ```
  local M = r(mean)
  ```
- Line 173: district
  ```
  qui sum SS_`A0'_`A1'_`A2' if district_code == `d', det
  ```
- Line 174: loc
  ```
  local S = r(mean)
  ```
- Line 180: district
  ```
  replace S_alpha0 = `A0' if district_code == `d'
  ```
- Line 181: district
  ```
  replace S_alpha1 = `A1' if district_code == `d'
  ```
- Line 182: district
  ```
  replace S_alpha2 = `A2' if district_code == `d'
  ```
- Line 218: name
  ```
  rename S_* T_*
  ```
- Line 219: district
  ```
  sort district_code
  ```
- Line 224: district
  ```
  sort district_code
  ```
- Line 228: district
  ```
  bysort district_code: egen tot_unten = sum(untenured)
  ```
- Line 251: name
  ```
  rename S_* SS_*
  ```

**/replication-package/replication_delivery/code/descriptives/lambda_map.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 6: district
  ```
  * Purpose: Generates map of lambda across districts.
  ```
- Line 10: district
  ```
  cd "$raw/district_shapefiles"
  ```
- Line 11: district, school
  ```
  spshape2dta "WI_School_Districts.shp", replace
  ```
- Line 13: district
  ```
  * Get lambda for missing districts
  ```
- Line 15: school
  ```
  drop if schoolcode == 8888
  ```
- Line 16: district
  ```
  drop if district_code >= 8000
  ```
- Line 20: district
  ```
  collapse d_, by(district_code)
  ```
- Line 21: district
  ```
  sort district_code
  ```
- Line 27: district, lon
  ```
  reshape long d, i(n) j(district_code)
  ```
- Line 28: district
  ```
  keep district_code d
  ```
- Line 29: district
  ```
  sort district_code
  ```
- Line 33: district
  ```
  gen SDID = string(district_code,"%04.0f")
  ```
- Line 35: district
  ```
  * adjustments to have old map match to new (accounts for district consolidation over time)
  ```

**/replication-package/replication_delivery/code/descriptives/omega_analysis.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 6: district
  ```
  * Purpose: Analyze district omega choices.
  ```
- Line 19: city
  ```
  gen budget_pp = budget / capacity
  ```
- Line 52: degree, school
  ```
  label var highschool "Share pop w/ HS degree"
  ```
- Line 59: city
  ```
  label var capacity "Capacity"
  ```
- Line 86: city
  ```
  global base = "capacity budget_pp d tc tc_tenured Exp3 Exp4 Exp5 Exp6 master CZ_tc CZ_tenured urban 
  ```
- Line 90: name
  ```
  rename alpha0 omega1
  ```
- Line 91: name
  ```
  rename alpha1 omega2
  ```
- Line 92: name
  ```
  rename absalpha0 absdev_omega1
  ```
- Line 94: loc
  ```
  local var = "omega2"
  ```

**/replication-package/replication_delivery/code/descriptives/rothstein_test.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 23: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 24: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 27: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 31: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 46: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 49: school
  ```
  egen schgr = group(schoolid grade)
  ```
- Line 56: school
  ```
  a(schoolid) cluster(schgr)
  ```
- Line 65: school
  ```
  a(schoolid) cluster(schgr)
  ```
- Line 110: district, school
  ```
  * Count the number of schools per district in 2014.
  ```
- Line 112: district
  ```
  keep if district_code < 8000
  ```
- Line 114: district, school
  ```
  egen schid = group(district_code schoolcode)
  ```
- Line 116: district
  ```
  keep schid district_code
  ```
- Line 119: district
  ```
  collapse (sum) nrsch, by(district_code)
  ```
- Line 121: district
  ```
  sort district_code
  ```
- Line 122: school
  ```
  tempfile school
  ```
- Line 123: school
  ```
  save `school', replace
  ```
- Line 126: school
  ```
  * Generate school-grade-year means for student characteristics.
  ```
- Line 127: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 130: district, school
  ```
  bysort district_code schoolcode grade year: egen M`var' = mean(`var')
  ```
- Line 134: district, school
  ```
  bysort district_code schoolcode grade year: egen Msize = sum(t)
  ```
- Line 149: loc
  ```
  local z = `x' + 1
  ```
- Line 156: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 165: lon
  ```
  reshape long id, i(fakeid) j(n)
  ```
- Line 186: district, school
  ```
  bysort district_code schoolcode: egen d = mean(D)
  ```
- Line 202: district, school
  ```
  egen tagT = tag(id schoolcode district_code grade year)
  ```
- Line 210: district, school
  ```
  collapse I* (sum) tag*, by(D grade schoolcode district_code year)
  ```
- Line 211: name
  ```
  rename I* *
  ```
- Line 213: district, school
  ```
  egen id = group(grade schoolcode district_code)
  ```
- Line 214: district, school
  ```
  egen idD = group(D grade schoolcode district_code)
  ```
- Line 215: district, school
  ```
  egen schyr = group(schoolcode district_code year)
  ```
- Line 243: school
  ```
  indicate("School-by-year FE = Y") obslast ///
  ```
- Line 248: school
  ```
  stats(N N_clust, fmt(0 0) labels(`"N"' `"\# school-grades"')) ///
  ```

**/replication-package/replication_delivery/code/descriptives/salary_variation.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 27: district, school
  ```
  egen schoolid = group(district_code schoolcode)
  ```
- Line 39: district
  ```
  a(totalexp2 masterup district_code)
  ```
- Line 41: district, school
  ```
  a(totalexp2 masterup district_code schoolid)
  ```

**/replication-package/replication_delivery/code/descriptives/schedule_summary.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 15: district, lon
  ```
  reshape long d, i(n) j(district_code)
  ```
- Line 22: district
  ```
  sort district_code
  ```

**/replication-package/replication_delivery/code/descriptives/summary.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 6: district
  ```
  * Purpose: Produce descriptive summary statistics for teachers and districts.
  ```
- Line 26: degree
  ```
  * Master's degree.
  ```
- Line 51: district
  ```
  * District Summary Statistics (Table 1, panel B)
  ```
- Line 58: district, lon
  ```
  reshape long d, i(n) j(district)
  ```
- Line 59: district
  ```
  sort district
  ```
- Line 63: district
  ```
  * Urban districts.
  ```
- Line 65: district, lon
  ```
  reshape long urban, i(n) j(district)
  ```
- Line 76: district
  ```
  * Suburban districts.
  ```
- Line 78: district, lon
  ```
  reshape long suburban, i(n) j(district)
  ```
- Line 93: district
  ```
  * Average teacher experience by incumbent district.
  ```
- Line 99: loc
  ```
  local n = substr("`v'",4,4)
  ```
- Line 105: district, name
  ```
  rename DD district
  ```
- Line 115: degree, district
  ```
  * Master's degree by incumbent district.
  ```
- Line 121: loc
  ```
  local n = substr("`v'",4,4)
  ```
- Line 127: district, name
  ```
  rename DD district
  ```
- Line 137: district
  ```
  * Teacher contributions by incumbent district.
  ```
- Line 145: loc
  ```
  local n = substr("`v'",4,4)
  ```
- Line 151: district, name
  ```
  rename DD district
  ```
- Line 165: district
  ```
  * District budgets.
  ```
- Line 167: city, district, lon
  ```
  reshape long budget capacity, i(n) j(district)
  ```
- Line 171: city
  ```
  su capacity
  ```
- Line 175: city
  ```
  su budget_pc capacity
  ```
- Line 177: city
  ```
  su budget_pc capacity if d <= `r(p25)'
  ```
- Line 179: city
  ```
  su budget_pc capacity if d > `r(p75)'
  ```
- Line 183: district
  ```
  * Summary Statistics for District Wage Schedules (Table 3)
  ```
- Line 186: district
  ```
  * Merge district characteristics.
  ```
- Line 198: loc
  ```
  local n = substr("`v'",4,4)
  ```
- Line 204: district, name
  ```
  rename DD district
  ```
- Line 205: district
  ```
  sort district
  ```
- Line 209: district, lon
  ```
  reshape long urban, i(n) j(district)
  ```
- Line 210: district
  ```
  sort district
  ```
- Line 213: district
  ```
  sort district
  ```
- Line 217: district, lon
  ```
  reshape long suburban, i(n) j(district)
  ```
- Line 218: district
  ```
  sort district
  ```
- Line 221: district
  ```
  sort district
  ```
- Line 225: district, lon
  ```
  reshape long d, i(n) j(district)
  ```
- Line 226: district
  ```
  sort district
  ```
- Line 229: district
  ```
  sort district
  ```
- Line 233: city, district, lon
  ```
  reshape long budget capacity, i(n) j(district)
  ```
- Line 234: city
  ```
  gen budget_pc = budget/capacity
  ```
- Line 235: district
  ```
  sort district
  ```
- Line 238: district
  ```
  sort district
  ```
- Line 241: district
  ```
  * Number of other districts in the same commuting zone.
  ```
- Line 242: district
  ```
  use "$raw/district_county_cz.dta", clear
  ```
- Line 243: district, name
  ```
  rename district district
  ```
- Line 244: district
  ```
  keep district cz
  ```
- Line 247: district
  ```
  sort district
  ```
- Line 249: district
  ```
  sum cz if district == 2525
  ```
- Line 251: district
  ```
  sum Nrd_samecz if district == 2525
  ```
- Line 255: district
  ```
  drop if district == .
  ```
- Line 256: district
  ```
  sort district
  ```
- Line 266: district
  ```
  gen district = .
  ```
- Line 268: loc
  ```
  local n = substr("`v'",4,4)
  ```
- Line 269: district
  ```
  replace district = `n' if `v' == 1
  ```
- Line 272: district
  ```
  sort district
  ```
- Line 273: district, name
  ```
  rename dist district_code
  ```
- Line 275: district
  ```
  sum cz if district == 2525
  ```
- Line 279: district, name
  ```
  rename district_code district
  ```
- Line 280: district, loc
  ```
  qui levelsof district, local(Dist)
  ```
- Line 283: loc
  ```
  foreach d of local Dist {
  ```
- Line 284: district
  ```
  qui sum cz if district == `d'
  ```
- Line 285: district
  ```
  sum c0 if district != `d' & cz == `r(mean)'
  ```
- Line 286: district
  ```
  replace c0_cz = `r(mean)' if district == `d'
  ```
- Line 287: district
  ```
  qui sum cz if district == `d'
  ```
- Line 288: district
  ```
  sum c1 if district != `d' & cz == `r(mean)'
  ```
- Line 289: district
  ```
  replace c1_cz = `r(mean)' if district == `d'
  ```
- Line 291: district
  ```
  keep district c0_ c1_
  ```
- Line 293: district
  ```
  sort district
  ```
- Line 296: district
  ```
  sort district
  ```
- Line 302: district, lon
  ```
  reshape long alpha0_ alpha1_, i(n) j(district)
  ```
- Line 303: district
  ```
  sort district
  ```
- Line 306: district
  ```
  sort district
  ```
- Line 316: city
  ```
  qui sum capacity, det
  ```
- Line 317: city
  ```
  gen capmed = capacity > `r(p50)'
  ```
- Line 321: district
  ```
  drop if district == .
  ```

**/replication-package/replication_delivery/code/descriptives/summary_simulation.do**

- Line 2: school
  ```
  * Equilibrium in the Market for Public School Teachers
  ```
- Line 5: lat
  ```
  * File:    summary_simulation.do
  ```
- Line 6: district
  ```
  * Purpose: Produce 2010 teacher and district summary statistics for Table B21.
  ```
- Line 12: lat
  ```
  display as error "Global sim must point to simulation_data."
  ```
- Line 42: degree
  ```
  * Master's degree.
  ```
- Line 44: name
  ```
  rename masterup master
  ```
- Line 68: district
  ```
  * District Summary Statistics (Table B21, panel B)
  ```
- Line 77: district, lon
  ```
  reshape long d, i(n) j(district)
  ```
- Line 79: district
  ```
  sort district
  ```
- Line 82: district
  ```
  * Urban districts.
  ```
- Line 84: district, lon
  ```
  reshape long urban, i(n) j(district)
  ```
- Line 96: district
  ```
  * Suburban districts.
  ```
- Line 98: district, lon
  ```
  reshape long suburban, i(n) j(district)
  ```
- Line 114: district
  ```
  * Average teacher experience by incumbent district.
  ```
- Line 118: district
  ```
  gen district = .
  ```
- Line 121: district, loc
  ```
  local district_id = substr("`var'", 4, .)
  ```
- Line 122: district
  ```
  replace district = `district_id' if `var' == 1
  ```
- Line 126: district
  ```
  collapse totalexp2, by(district)
  ```
- Line 137: degree, district
  ```
  * Master's degree by incumbent district.
  ```
- Line 139: name
  ```
  rename masterup master
  ```
- Line 142: district
  ```
  gen district = .
  ```
- Line 145: district, loc
  ```
  local district_id = substr("`var'", 4, .)
  ```
- Line 146: district
  ```
  replace district = `district_id' if `var' == 1
  ```
- Line 150: district
  ```
  collapse master, by(district)
  ```
- Line 161: district
  ```
  * Teacher contributions by incumbent district.
  ```
- Line 167: district
  ```
  gen district = .
  ```
- Line 170: district, loc
  ```
  local district_id = substr("`var'", 4, .)
  ```
- Line 171: district
  ```
  replace district = `district_id' if `var' == 1
  ```
- Line 175: district
  ```
  collapse c0 c1, by(district)
  ```
- Line 190: district
  ```
  * District budgets.
  ```
- Line 192: city, district, lon
  ```
  reshape long budget capacity, i(n) j(district)
  ```
- Line 195: city
  ```
  gen budget_pc = budget / capacity
  ```
- Line 197: city
  ```
  summarize capacity
  ```
- Line 201: city
  ```
  summarize budget_pc capacity
  ```
- Line 203: city
  ```
  summarize budget_pc capacity if d <= `r(p25)'
  ```
- Line 205: city
  ```
  summarize budget_pc capacity if d > `r(p75)'
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/1_baseline/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/2_rigid/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new3/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/3_basicCfs/new4/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 764: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 766: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 769: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 772: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 783: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 789: lat
  ```
  ! Late addition
  ```
- Line 797: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 799: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 813: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 898: lon
  ```
  if (abs(theta(j)) < epsilon(theta(j))) then
  ```
- Line 900: lon
  ```
  if (abs(theta_prev(j)) > epsilon(theta_prev(j))) then
  ```
- Line 1036: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1059: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1062: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1081: loc
  ```
  call deallocate_variables()
  ```
- Line 1086: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1094: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1136: lat
  ```
  !   and fix if need be later
  ```
- Line 1260: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1371: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1456: district
  ```
  ! teachers rewarded: district
  ```
- Line 1483: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1508: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1522: district
  ```
  !figure out index of district j
  ```
- Line 1622: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1665: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1675: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1692: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1792: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1797: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1799: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1800: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1801: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1803: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 153: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 161: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 166: lat
  ```
  ! Calculate our moments
  ```
- Line 182: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 230: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 281: lat
  ```
  !call write_latest_theta()
  ```
- Line 283: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 764: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 766: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 769: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 772: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 783: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 789: lat
  ```
  ! Late addition
  ```
- Line 797: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 799: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 813: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 898: lon
  ```
  if (abs(theta(j)) < epsilon(theta(j))) then
  ```
- Line 900: lon
  ```
  if (abs(theta_prev(j)) > epsilon(theta_prev(j))) then
  ```
- Line 1036: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1059: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1062: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1081: loc
  ```
  call deallocate_variables()
  ```
- Line 1086: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1094: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1136: lat
  ```
  !   and fix if need be later
  ```
- Line 1260: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1371: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1456: district
  ```
  ! teachers rewarded: district
  ```
- Line 1483: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1508: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1522: district
  ```
  !figure out index of district j
  ```
- Line 1622: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1665: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1675: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1692: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1792: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1797: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1799: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1800: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1801: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1803: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 153: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 161: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 166: lat
  ```
  ! Calculate our moments
  ```
- Line 182: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 230: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 281: lat
  ```
  !call write_latest_theta()
  ```
- Line 283: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 763: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 765: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 768: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 771: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 782: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 788: lat
  ```
  ! Late addition
  ```
- Line 796: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 798: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 812: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 898: lon
  ```
  if (abs(theta(j)) < epsilon(theta(j))) then
  ```
- Line 900: lon
  ```
  if (abs(theta_prev(j)) > epsilon(theta_prev(j))) then
  ```
- Line 1036: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1059: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1062: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1081: loc
  ```
  call deallocate_variables()
  ```
- Line 1086: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1094: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1136: lat
  ```
  !   and fix if need be later
  ```
- Line 1260: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1371: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1456: district
  ```
  ! teachers rewarded: district
  ```
- Line 1483: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1508: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1522: district
  ```
  !figure out index of district j
  ```
- Line 1622: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1665: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1675: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1692: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1792: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1797: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1799: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1800: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1801: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1803: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 153: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 161: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 166: lat
  ```
  ! Calculate our moments
  ```
- Line 182: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 230: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 281: lat
  ```
  !call write_latest_theta()
  ```
- Line 283: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case2_new2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 764: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 766: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 769: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 772: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 783: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 789: lat
  ```
  ! Late addition
  ```
- Line 797: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 799: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 813: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 898: lon
  ```
  if (abs(theta(j)) < epsilon(theta(j))) then
  ```
- Line 900: lon
  ```
  if (abs(theta_prev(j)) > epsilon(theta_prev(j))) then
  ```
- Line 1036: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1059: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1062: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1081: loc
  ```
  call deallocate_variables()
  ```
- Line 1086: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1094: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1136: lat
  ```
  !   and fix if need be later
  ```
- Line 1260: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1371: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1456: district
  ```
  ! teachers rewarded: district
  ```
- Line 1483: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1508: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1522: district
  ```
  !figure out index of district j
  ```
- Line 1622: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1665: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1675: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1692: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1792: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1797: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1799: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1800: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1801: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1803: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/4_nomovecost/case_baseline/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1077: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1242: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1266: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1269: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1291: loc
  ```
  call deallocate_variables()
  ```
- Line 1298: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1310: loc
  ```
  !dealloc nteach vars
  ```
- Line 1311: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1312: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1313: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1314: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1316: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1326: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1339: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1341: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1346: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1348: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1350: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1363: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1365: loc
  ```
  !allocate nteach vars
  ```
- Line 1366: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1367: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1368: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1369: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1370: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1371: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1374: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1375: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1388: lat
  ```
  !   and fix if need be later
  ```
- Line 1502: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1525: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1538: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1643: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1730: district
  ```
  ! teachers rewarded: district
  ```
- Line 1782: loc
  ```
  !deallocate
  ```
- Line 1783: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1784: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1785: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1786: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1787: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1788: loc
  ```
  deallocate(H_target)
  ```
- Line 1801: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1803: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1805: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1807: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1809: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1816: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1820: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1822: loc
  ```
  !allocate nteach vars
  ```
- Line 1823: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1824: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1825: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1826: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1840: district
  ```
  !figure out index of district j
  ```
- Line 1993: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2003: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2021: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2040: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2042: loc
  ```
  !dealloc nteach vars
  ```
- Line 2043: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2044: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2054: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2058: loc
  ```
  !allocate nteach vars
  ```
- Line 2059: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2130: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2132: loc
  ```
  !dealloc nteach vars
  ```
- Line 2133: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2139: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2141: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2142: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2143: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2145: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2217: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 458: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 488: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 489: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 492: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 493: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 494: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 504: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 506: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 507: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 533: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 538: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 643: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 645: lon
  ```
  ! no longer need these:
  ```
- Line 646: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 647: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 650: loc
  ```
  deallocate(tshock)
  ```
- Line 651: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 666: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 668: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 669: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 679: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 727: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 728: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 730: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 735: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 739: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 740: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 741: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 757: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 764: loc
  ```
  deallocate(param0)
  ```
- Line 765: loc
  ```
  deallocate(tshock)
  ```
- Line 767: loc
  ```
  deallocate(dshock)
  ```
- Line 768: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 771: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 784: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 790: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 792: loc
  ```
  !allocate
  ```
- Line 793: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 794: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 805: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 815: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 826: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 827: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 828: loc
  ```
  deallocate(var_wage)
  ```
- Line 839: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 842: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 937: loc
  ```
  deallocate(subsidy)
  ```
- Line 948: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 951: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1046: loc
  ```
  deallocate(subsidy)
  ```
- Line 1105: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1107: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1114: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1130: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1154: loc
  ```
  deallocate(temp_prob)
  ```
- Line 1159: lat
  ```
  ! note that wstat and read_data needs to be calculated before this is run...
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/baseline/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_05/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new1_15/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_05/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1020: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1033: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1080: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1245: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1269: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1272: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1294: loc
  ```
  call deallocate_variables()
  ```
- Line 1301: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1313: loc
  ```
  !dealloc nteach vars
  ```
- Line 1314: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1315: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1316: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1317: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1319: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1329: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1332: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1335: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1342: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1344: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1349: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1351: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1353: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1366: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1368: loc
  ```
  !allocate nteach vars
  ```
- Line 1369: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1370: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1371: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1372: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1373: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1374: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1375: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1376: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1377: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1378: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1379: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1391: lat
  ```
  !   and fix if need be later
  ```
- Line 1505: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1528: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1541: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1646: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1733: district
  ```
  ! teachers rewarded: district
  ```
- Line 1785: loc
  ```
  !deallocate
  ```
- Line 1786: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1787: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1788: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1789: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1790: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1791: loc
  ```
  deallocate(H_target)
  ```
- Line 1804: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1806: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1808: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1810: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1812: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1819: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1823: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1825: loc
  ```
  !allocate nteach vars
  ```
- Line 1826: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1827: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1828: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1829: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1830: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1831: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1843: district
  ```
  !figure out index of district j
  ```
- Line 1996: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2006: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2024: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2043: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2045: loc
  ```
  !dealloc nteach vars
  ```
- Line 2046: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2047: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2057: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2061: loc
  ```
  !allocate nteach vars
  ```
- Line 2062: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2133: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2135: loc
  ```
  !dealloc nteach vars
  ```
- Line 2136: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2142: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2144: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2145: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2146: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2148: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2220: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/5_exitentry/new2_15/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 873: loc
  ```
  !allocate nteach vars
  ```
- Line 874: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 875: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 878: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 879: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 895: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 897: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 901: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 905: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 919: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 930: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 933: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 935: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 949: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1050: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1063: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1110: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1275: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1299: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1302: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1324: loc
  ```
  call deallocate_variables()
  ```
- Line 1331: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1343: loc
  ```
  !dealloc nteach vars
  ```
- Line 1344: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1345: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1346: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1347: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1349: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1359: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1362: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1365: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1372: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1374: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1379: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1383: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1396: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1398: loc
  ```
  !allocate nteach vars
  ```
- Line 1399: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1400: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1401: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1402: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1403: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1404: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1405: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1406: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1407: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1408: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1409: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1421: lat
  ```
  !   and fix if need be later
  ```
- Line 1535: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1558: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1571: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1676: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1763: district
  ```
  ! teachers rewarded: district
  ```
- Line 1815: loc
  ```
  !deallocate
  ```
- Line 1816: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1817: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1818: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1819: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1820: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1821: loc
  ```
  deallocate(H_target)
  ```
- Line 1834: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1836: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1838: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1840: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1842: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1849: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1853: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1855: loc
  ```
  !allocate nteach vars
  ```
- Line 1856: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1857: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1858: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1859: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1860: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1861: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1873: district
  ```
  !figure out index of district j
  ```
- Line 2026: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2036: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2054: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2073: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2075: loc
  ```
  !dealloc nteach vars
  ```
- Line 2076: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2077: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2087: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2091: loc
  ```
  !allocate nteach vars
  ```
- Line 2092: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2163: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2165: loc
  ```
  !dealloc nteach vars
  ```
- Line 2166: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2172: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2174: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2175: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2176: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2178: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2250: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```
- Line 1157: lat
  ```
  ! note that wstat and read_data needs to be calculated before this is run...
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_baseline/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 874: loc
  ```
  !allocate nteach vars
  ```
- Line 875: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 878: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 879: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 882: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 896: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 898: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 902: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 906: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 920: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 931: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 934: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 936: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 950: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1061: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1074: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1124: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1293: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1317: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1320: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1342: loc
  ```
  call deallocate_variables()
  ```
- Line 1349: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1361: loc
  ```
  !dealloc nteach vars
  ```
- Line 1362: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1363: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1364: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1365: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1367: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1377: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1383: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1390: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1392: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1397: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1399: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1401: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1414: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1416: loc
  ```
  !allocate nteach vars
  ```
- Line 1417: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1418: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1419: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1420: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1421: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1422: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1423: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1424: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1425: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1426: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1427: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1439: lat
  ```
  !   and fix if need be later
  ```
- Line 1553: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1576: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1589: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1694: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1781: district
  ```
  ! teachers rewarded: district
  ```
- Line 1833: loc
  ```
  !deallocate
  ```
- Line 1834: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1835: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1836: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1837: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1838: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1839: loc
  ```
  deallocate(H_target)
  ```
- Line 1852: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1854: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1856: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1858: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1860: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1867: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1871: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1873: loc
  ```
  !allocate nteach vars
  ```
- Line 1874: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1875: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1876: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1877: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1878: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1879: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1891: district
  ```
  !figure out index of district j
  ```
- Line 2044: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2054: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2072: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2091: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2093: loc
  ```
  !dealloc nteach vars
  ```
- Line 2094: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2095: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2105: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2109: loc
  ```
  !allocate nteach vars
  ```
- Line 2110: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2181: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2183: loc
  ```
  !dealloc nteach vars
  ```
- Line 2184: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2190: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2192: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2193: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2194: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2196: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2268: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```
- Line 1157: lat
  ```
  ! note that wstat and read_data needs to be calculated before this is run...
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 35: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer_all, seed, model_offer_sub
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: ten_inc_stay, new_teachers
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, 
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 44: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 46: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 47: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 48: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 59: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp
  ```
- Line 60: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach_ndist
  ```
- Line 63: loc, location
  ```
  !some allocations
  ```
- Line 64: loc
  ```
  allocate(model_offer_all(nteach,ndist), seed(nteach,ndist), model_offer_sub(nteach,ndist))
  ```
- Line 65: loc
  ```
  allocate(ten_inc_stay(nteach,ndist), new_teachers(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(x1(nteach,ndist), x2(nteach,ndist), x3(nteach,ndist), x4(nteach,ndist), x5(nteach,ndist), x
  ```
- Line 67: loc
  ```
  allocate(x7(nteach,ndist), x8(nteach,ndist), x9(nteach,ndist), x10(nteach,ndist), x11(nteach,ndist),
  ```
- Line 68: loc
  ```
  allocate(x13(nteach,ndist), y(nteach,ndist), w(nteach,ndist))
  ```
- Line 69: loc
  ```
  allocate(wage_tmp(nteach,ndist))
  ```
- Line 87: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 97: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 98: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 104: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 137: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 140: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 147: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 184: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 185: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 203: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 204: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 206: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 217: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 218: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 221: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 222: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 223: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 224: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 228: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 234: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 267: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 273: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 288: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 290: loc
  ```
  deallocate(X_big)
  ```
- Line 293: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 294: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 297: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 300: loc
  ```
  deallocate(y_big)
  ```
- Line 301: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 303: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 309: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 329: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 337: loc
  ```
  allocate(ones_nteach_ndist(nteach,ndist))
  ```
- Line 340: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 350: loc
  ```
  deallocate(ones_nteach_ndist)
  ```
- Line 355: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 357: city
  ```
  X(:,2) = free_capacity
  ```
- Line 363: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 368: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 369: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 370: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 416: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 421: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 422: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 423: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 440: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 457: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 462: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 463: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 464: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 481: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: loc, location
  ```
  !some deallocations
  ```
- Line 500: loc
  ```
  deallocate(model_offer_all, seed, model_offer_sub)
  ```
- Line 501: loc
  ```
  deallocate(ten_inc_stay, new_teachers)
  ```
- Line 502: loc
  ```
  deallocate(x1, x2, x3, x4, x5, x6)
  ```
- Line 503: loc
  ```
  deallocate(x7, x8, x9, x10, x11, x12)
  ```
- Line 504: loc
  ```
  deallocate(x13, y, w)
  ```
- Line 505: loc
  ```
  deallocate(wage_tmp)
  ```
- Line 525: loc
  ```
  double precision, allocatable, dimension(:,:) :: tc, tc_mean
  ```
- Line 536: loc
  ```
  allocate(tc(nteach,ndist), tc_mean(nteach,ndist))
  ```
- Line 541: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 566: second
  ```
  !second set of vars
  ```
- Line 569: city
  ```
  !capacity
  ```
- Line 590: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 621: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 629: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 698: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 739: loc
  ```
  deallocate(tc, tc_mean)
  ```
- Line 751: loc
  ```
  double precision, allocatable, dimension(:,:) :: new_hires, C1_mat, C2_mat
  ```
- Line 753: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 769: loc
  ```
  allocate(new_hires(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 772: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 775: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 790: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 807: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 809: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 819: district
  ```
  !across all district
  ```
- Line 833: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 834: city
  ```
  excess_capacity = 0
  ```
- Line 837: city
  ```
  excess_capacity(j) = 1
  ```
- Line 840: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 841: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 843: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 844: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 850: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 858: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 909: loc
  ```
  deallocate(new_hires, C1_mat, C2_mat)
  ```
- Line 926: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 930: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 938: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 960: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 982: loc
  ```
  deallocate(sortedvec)
  ```
- Line 997: loc
  ```
  double precision, allocatable, dimension(:,:) :: mat2
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 24: loc
  ```
  double precision, allocatable, dimension(:,:) :: f, c_ave, C1_mat, C2_mat
  ```
- Line 26: loc
  ```
  allocate(f(nteach,ndist), c_ave(nteach,ndist), C1_mat(nteach,ndist), C2_mat(nteach,ndist))
  ```
- Line 76: loc
  ```
  deallocate(f, c_ave, C1_mat, C2_mat)
  ```
- Line 82: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```
- Line 89: loc
  ```
  double precision, allocatable, dimension(:) :: f
  ```
- Line 92: loc
  ```
  double precision, allocatable, dimension(:,:) :: c_ave
  ```
- Line 94: loc
  ```
  allocate(f(nteach), c_ave(nteach,1))
  ```
- Line 159: loc
  ```
  deallocate(f, c_ave)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/global_variables.f90**

- Line 33: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 48: loc
  ```
  double precision, allocatable :: wage(:,:), wageschedule(:,:), contribution(:,:)
  ```
- Line 53: loc
  ```
  double precision, allocatable :: meanW(:), sdW(:)
  ```
- Line 54: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 55: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 56: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 58: loc
  ```
  integer,  allocatable, dimension(:,:) :: match, inc, entrant, milwauk, samecz
  ```
- Line 60: loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist
  ```
- Line 61: district, loc
  ```
  double precision, allocatable, dimension(:,:) :: logdist_btwn_districts
  ```
- Line 62: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 64: loc
  ```
  integer, allocatable, dimension(:,:) :: master, exp_con
  ```
- Line 65: district
  ```
  !double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 66: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1, C2
  ```
- Line 73: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum, exp_gr, exp_dum1, exp_dum2, exp_dum3
  ```
- Line 74: loc
  ```
  integer, allocatable, dimension(:,:) :: exp_dum4, exp_dum5, exp_dum6, tenured
  ```
- Line 75: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 77: loc
  ```
  double precision, allocatable, dimension(:,:) :: share_low, urban, suburban
  ```
- Line 81: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 84: loc
  ```
  integer, allocatable, dimension(:) :: subsidized, subsidized_C1, subsidized_C2
  ```
- Line 86: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 87: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 90: loc
  ```
  double precision, allocatable, dimension(:,:) :: wstat
  ```
- Line 94: loc
  ```
  integer, allocatable, dimension(:) :: age
  ```
- Line 97: city
  ```
  double precision :: wage_elas !assumption on wage elasticity
  ```
- Line 98: loc
  ```
  double precision, allocatable, dimension(:)   :: baseline_prob_entry
  ```
- Line 99: loc
  ```
  integer,          allocatable, dimension(:)   :: age_entrant, exp_entrant, master_entrant
  ```
- Line 100: loc
  ```
  double precision, allocatable, dimension(:,:) :: wageschedule_entrant, contribution_entrant, C1_entr
  ```
- Line 102: loc
  ```
  !double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 105: loc
  ```
  double precision, allocatable, dimension(:,:) :: C1_period, C2_period
  ```
- Line 106: loc
  ```
  integer,          allocatable, dimension(:,:) :: age_period, exp_period, master_period
  ```
- Line 107: loc
  ```
  integer,          allocatable, dimension(:,:) :: inc_period
  ```
- Line 108: loc
  ```
  double precision, allocatable, dimension(:,:) :: theta_period, wage_theta_period
  ```
- Line 130: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  !integer, allocatable, dimension(:) :: noninc_offert  moved to local to functions
  ```
- Line 31: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 33: loc
  ```
  integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 38: loc
  ```
  double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 39: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 51: loc
  ```
  integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 54: loc
  ```
  double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 56: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 59: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 79: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 84: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: v!uteach!, H_target !exp_uteach,
  ```
- Line 88: loc
  ```
  allocate(v(ncopyT,nteach, ndist))
  ```
- Line 89: loc
  ```
  allocate(model_offer(nteach,ndist), best_model_offer(nteach,ndist))
  ```
- Line 90: loc
  ```
  allocate(H(nteach,ndist),udist(nteach,ndist), uteach(nteach,ndist),exp_uteach(nteach,ndist))
  ```
- Line 93: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 94: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 104: district
  ```
  !udist  = utility_district(beta_in)
  ```
- Line 105: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 125: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 192: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 213: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 217: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 227: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 240: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 256: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 258: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 261: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 274: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 327: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 331: loc
  ```
  call deallocate_variables()
  ```
- Line 333: loc
  ```
  deallocate(v)
  ```
- Line 334: loc
  ```
  deallocate(model_offer, best_model_offer)
  ```
- Line 335: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 346: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 347: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 363: loc
  ```
  double precision, allocatable, dimension(:,:) :: absdiff
  ```
- Line 366: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 372: loc
  ```
  integer, allocatable, dimension(:) :: nooffers
  ```
- Line 374: loc
  ```
  double precision, allocatable, dimension(:,:) :: indicator
  ```
- Line 376: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 378: loc
  ```
  !allocating nteach vars
  ```
- Line 379: loc
  ```
  allocate(absdiff(nteach,ndist))
  ```
- Line 380: loc
  ```
  allocate(denom(nteach))
  ```
- Line 381: loc
  ```
  allocate(nooffers(nteach))
  ```
- Line 382: loc
  ```
  allocate(indicator(nteach,ndist))
  ```
- Line 383: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 407: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 410: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 414: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 428: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 461: loc
  ```
  !dealloc nteach vars
  ```
- Line 462: loc
  ```
  deallocate(absdiff, denom, nooffers, indicator)
  ```
- Line 463: loc
  ```
  deallocate(H_target)
  ```
- Line 476: loc
  ```
  !integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 477: loc
  ```
  !integer, allocatable, dimension(:,:) :: model_offer, best_model_offer
  ```
- Line 478: loc
  ```
  !double precision, allocatable, dimension(:,:) :: H, udist, uteach
  ```
- Line 479: loc
  ```
  !double precision, allocatable, dimension(:,:) :: exp_uteach!, H_target moved H_target to local fn
  ```
- Line 480: loc
  ```
  !double precision, allocatable, dimension(:,:) :: wage_eqm_glob, H_eqm_glob
  ```
- Line 481: loc
  ```
  !integer, allocatable, dimension(:,:)       :: offer_eqm_glob
  ```
- Line 482: loc
  ```
  !double precision, allocatable, dimension(:) :: mean_eqm_glob, sd_eqm_glob
  ```
- Line 485: loc
  ```
  allocate(udist(nteach,ndist))
  ```
- Line 486: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 487: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist), wage_eqm_glob(nteach,ndist), H_eqm_glob(nteach,ndist))
  ```
- Line 500: district
  ```
  !solve knapsack for given districts
  ```
- Line 532: district
  ```
  !solve knapsack for given districts
  ```
- Line 542: loc
  ```
  call deallocate_variables()
  ```
- Line 544: loc
  ```
  deallocate(udist)
  ```
- Line 545: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 546: loc
  ```
  deallocate(offer_eqm_glob, wage_eqm_glob, H_eqm_glob)
  ```
- Line 558: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 577: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 581: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 593: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 600: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 604: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 608: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 617: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 618: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 619: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 620: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 628: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 631: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 657: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 662: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 665: district
  ```
  !    which district nums each thread is working on
  ```
- Line 671: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht_vary, waget_vary
  ```
- Line 673: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 675: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 677: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 679: loc
  ```
  ! allocate nteach vars
  ```
- Line 680: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 681: loc
  ```
  allocate(wstat_vary(nteach))
  ```
- Line 682: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 691: district
  ```
  !figure out index of district j
  ```
- Line 705: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 764: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 775: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 777: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 809: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 819: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 822: loc
  ```
  !dealloc nteach vars
  ```
- Line 823: loc
  ```
  deallocate(Ht_Vary, waget_vary, wstat_vary)
  ```
- Line 824: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 829: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 831: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 851: loc
  ```
  double precision, allocatable, dimension(:,:) :: v
  ```
- Line 853: loc
  ```
  integer, allocatable, dimension(:,:) :: match_eqm
  ```
- Line 862: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 864: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 867: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_match_eqm
  ```
- Line 874: loc
  ```
  !allocate nteach vars
  ```
- Line 875: loc
  ```
  allocate(v(nteach,ndist))
  ```
- Line 876: loc
  ```
  allocate(match_eqm(nteach,ndist))
  ```
- Line 877: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 878: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 879: loc
  ```
  allocate(pr_match_eqm(nteach,ndist))
  ```
- Line 880: loc
  ```
  allocate(offer_eqm_glob(nteach,ndist))
  ```
- Line 881: loc
  ```
  allocate(wage_eqm_glob(nteach,ndist))
  ```
- Line 882: loc
  ```
  allocate(H(nteach,ndist), udist(nteach,ndist), uteach(nteach,ndist), exp_uteach(nteach,ndist))
  ```
- Line 896: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 898: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 902: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 906: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 920: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 931: district
  ```
  !udist       = utility_district(beta_in)
  ```
- Line 934: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 936: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 950: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1061: district
  ```
  call utility_district(beta_in, udist)
  ```
- Line 1074: lat
  ```
  !     sure theta0 gets reset for this bug cehck, so remove the below 21/22 later.
  ```
- Line 1124: loc
  ```
  !  (if I reinstitute this, need to allocate these here instead of in eqbm obj)
  ```
- Line 1293: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1317: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1320: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1342: loc
  ```
  call deallocate_variables()
  ```
- Line 1349: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1361: loc
  ```
  !dealloc nteach vars
  ```
- Line 1362: loc
  ```
  deallocate(v, match_eqm, wage_eqm_glob_wedge, subsidy, pr_match_eqm)
  ```
- Line 1363: loc
  ```
  deallocate(offer_eqm_glob)
  ```
- Line 1364: loc
  ```
  deallocate(wage_eqm_glob)
  ```
- Line 1365: loc
  ```
  deallocate(H, udist, uteach, exp_uteach)
  ```
- Line 1367: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1377: loc
  ```
  double precision, allocatable, dimension(:) :: denom
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: mean_target, sd_target
  ```
- Line 1383: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 1390: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_eqm_glob_wedge
  ```
- Line 1392: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 1397: loc
  ```
  integer, allocatable, dimension(:, :) :: tc_ind
  ```
- Line 1399: loc
  ```
  double precision, allocatable, dimension(:,:) :: reward_tot, reward_dist, reward_state
  ```
- Line 1401: loc
  ```
  integer, allocatable, dimension(:,:) :: reward_tot_ind, reward_dist_ind, reward_state_ind
  ```
- Line 1414: loc
  ```
  double precision, allocatable, dimension(:,:) :: H_target
  ```
- Line 1416: loc
  ```
  !allocate nteach vars
  ```
- Line 1417: loc
  ```
  allocate(denom(nteach))
  ```
- Line 1418: loc
  ```
  allocate(mean_target(nteach), sd_target(nteach))
  ```
- Line 1419: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 1420: loc
  ```
  allocate(wage_eqm_glob_wedge(nteach,ndist))
  ```
- Line 1421: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 1422: loc
  ```
  allocate(tc_ind(nteach,ndist))
  ```
- Line 1423: loc
  ```
  allocate(reward_tot(nteach,ndist), reward_dist(nteach,ndist), reward_state(nteach,ndist))
  ```
- Line 1424: loc
  ```
  allocate(reward_tot_ind(nteach,ndist), reward_dist_ind(nteach,ndist), reward_state_ind(nteach,ndist)
  ```
- Line 1425: loc
  ```
  allocate(mean_eqm_glob(nteach), sd_eqm_glob(nteach))
  ```
- Line 1426: loc
  ```
  allocate(H_eqm_glob(nteach,ndist))
  ```
- Line 1427: loc
  ```
  allocate(H_target(nteach,ndist))
  ```
- Line 1439: lat
  ```
  !   and fix if need be later
  ```
- Line 1553: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1576: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1589: son
  ```
  ! XXX temp printing for comparison
  ```
- Line 1694: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1781: district
  ```
  ! teachers rewarded: district
  ```
- Line 1833: loc
  ```
  !deallocate
  ```
- Line 1834: loc
  ```
  deallocate(denom, mean_target, sd_target, C_matrix, wage_eqm_glob_wedge, subsidy)
  ```
- Line 1835: loc
  ```
  deallocate(tc_ind, reward_tot, reward_dist, reward_state)
  ```
- Line 1836: loc
  ```
  deallocate(reward_tot_ind, reward_dist_ind, reward_state_ind)
  ```
- Line 1837: loc
  ```
  deallocate(mean_eqm_glob, sd_eqm_glob)
  ```
- Line 1838: loc
  ```
  deallocate(H_eqm_glob)
  ```
- Line 1839: loc
  ```
  deallocate(H_target)
  ```
- Line 1852: loc
  ```
  double precision, allocatable, dimension(:) :: wstat_vary
  ```
- Line 1854: loc
  ```
  double precision, allocatable, dimension(:,:) :: waget_eqm, waget_vary, Ht_vary, Ht_eqm
  ```
- Line 1856: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_omegast_eqm
  ```
- Line 1858: loc
  ```
  double precision, allocatable, dimension(:,:)      :: omegast_eqm
  ```
- Line 1860: loc
  ```
  integer,          allocatable, dimension(:,:) :: offert_eqm
  ```
- Line 1867: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1871: loc
  ```
  integer, allocatable, dimension(:) :: noninc_offert
  ```
- Line 1873: loc
  ```
  !allocate nteach vars
  ```
- Line 1874: loc
  ```
  allocate(waget_eqm(nteach,rec_count/nteach), waget_vary(nteach,rec_count/nteach))
  ```
- Line 1875: loc
  ```
  allocate(Ht_vary(nteach,rec_count/nteach), Ht_eqm(nteach,rec_count/nteach))
  ```
- Line 1876: loc
  ```
  allocate(pr_omegast_eqm(NWgrid1,NWgrid2,rec_count/nteach))
  ```
- Line 1877: loc
  ```
  allocate(omegast_eqm(2,rec_count/nteach))
  ```
- Line 1878: loc
  ```
  allocate(offert_eqm(nteach,rec_count/nteach))
  ```
- Line 1879: loc
  ```
  allocate(noninc_offert(nteach))
  ```
- Line 1891: district
  ```
  !figure out index of district j
  ```
- Line 2044: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 2054: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 2072: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 2091: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 2093: loc
  ```
  !dealloc nteach vars
  ```
- Line 2094: loc
  ```
  deallocate(waget_eqm, waget_vary, Ht_vary, Ht_eqm, pr_omegast_eqm, omegast_eqm, offert_eqm)
  ```
- Line 2095: loc
  ```
  deallocate(noninc_offert)
  ```
- Line 2105: loc
  ```
  double precision, allocatable, dimension(:)      :: var_eqm
  ```
- Line 2109: loc
  ```
  !allocate nteach vars
  ```
- Line 2110: loc
  ```
  allocate(var_eqm(nteach))
  ```
- Line 2181: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 2183: loc
  ```
  !dealloc nteach vars
  ```
- Line 2184: loc
  ```
  deallocate(var_eqm)
  ```
- Line 2190: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 2192: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 2193: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 2194: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 2196: loc
  ```
  end subroutine deallocate_variables
  ```
- Line 2268: loc
  ```
  !allocate(send_counts(ntasks),send_starts(ntasks))
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: tval_nEntrant !prob_exit,
  ```
- Line 29: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 34: loc
  ```
  double precision, allocatable, dimension(:,:)   :: C1_prev, C2_prev
  ```
- Line 35: loc
  ```
  integer,          allocatable, dimension(:,:)   :: exp_prev, age_prev, master_prev, inc_prev
  ```
- Line 36: loc
  ```
  !integer,          allocatable, dimension(:,:) :: inc_temp
  ```
- Line 39: loc
  ```
  integer,          allocatable, dimension(:) :: t_exit, t_entry
  ```
- Line 40: loc
  ```
  double precision, allocatable, dimension(:) ::  wage_baseline_entrant! prob_entry,
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_baseline_entrant_tmp, prob_entry, prob_exit
  ```
- Line 42: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: omega_baseline
  ```
- Line 44: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach, ones_nEntrant
  ```
- Line 46: loc
  ```
  double precision, allocatable, dimension(:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 47: loc
  ```
  !double precision, allocatable, dimension(:,:) :: baseline_wage_tmp, wage_tmp, wage_entrant_tmp
  ```
- Line 48: loc
  ```
  double precision, allocatable, dimension(:,:) :: wage_tmp_tmp, wage_entrant_tmp_tmp
  ```
- Line 49: loc
  ```
  double precision, allocatable, dimension(:) :: wage_change, wage_change_entrant
  ```
- Line 72: loc
  ```
  ! Before reading data we need to allocate stuffs
  ```
- Line 74: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 76: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 98: loc
  ```
  !   allocate(prob_exit(nteach))
  ```
- Line 108: loc
  ```
  !   deallocate(prob_exit)
  ```
- Line 113: loc, location
  ```
  !allocations we need
  ```
- Line 115: loc
  ```
  ! allocate shock variables
  ```
- Line 116: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 117: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 118: loc
  ```
  !allocate global sim_omega
  ```
- Line 119: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 122: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 127: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 196: lat
  ```
  !         e) generate all the new teacher variables based on our new population
  ```
- Line 198: loc
  ```
  !      2. For t=1, if doing iteration: allocate a couple vars we will need
  ```
- Line 226: lat
  ```
  if (mpi_id == 0) print*, 'moving population forward, period:', t_period, 'of, ', T_max
  ```
- Line 237: loc
  ```
  allocate(prob_entry(nEntrant, nEconomy))
  ```
- Line 238: loc
  ```
  allocate(prob_exit(nteach, nEconomy))
  ```
- Line 278: lat
  ```
  !need these to calc prob exit, and exp also used to calculate wagesched
  ```
- Line 293: loc
  ```
  !allocate(prob_exit(nteach))
  ```
- Line 298: city
  ```
  ! Now we adjust prob exit based on wage elasticity !
  ```
- Line 302: loc
  ```
  allocate(wage_change(nteach))
  ```
- Line 303: loc
  ```
  allocate(baseline_wage_tmp(nteach))!,nEconomy))
  ```
- Line 304: loc
  ```
  allocate(wage_tmp(nteach))!, nEconomy))
  ```
- Line 305: loc
  ```
  allocate(wage_tmp_tmp(nteach, ndist))
  ```
- Line 306: loc
  ```
  allocate(ones_nteach(nteach, 1))
  ```
- Line 324: loc
  ```
  !deallocate(ones_nteach, wage_tmp_tmp)
  ```
- Line 344: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 356: second
  ```
  ! Second, adjust exit prob b/c of wage_change
  ```
- Line 364: loc
  ```
  deallocate(baseline_wage_tmp, wage_tmp, wage_tmp_tmp)
  ```
- Line 366: city
  ```
  ! Restrict that those in age group 4 or higher aren't affected by subsidy (wage elasticity)
  ```
- Line 375: loc
  ```
  deallocate(wage_change)
  ```
- Line 380: second
  ```
  ! Second, draw entrants ---------------------------------
  ```
- Line 381: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 384: lat
  ```
  !First,  calc change in wage relative to baseline, for each economy
  ```
- Line 385: loc
  ```
  allocate(wage_entrant_tmp(nEntrant))!, nEconomy))
  ```
- Line 386: loc
  ```
  allocate(wage_entrant_tmp_tmp(nEntrant, ndist))
  ```
- Line 387: loc
  ```
  allocate(ones_nEntrant(nEntrant,1))
  ```
- Line 397: district
  ```
  !   ! avg across districts
  ```
- Line 416: loc
  ```
  deallocate(ones_nEntrant)
  ```
- Line 418: loc
  ```
  allocate(wage_change_entrant(nEntrant))
  ```
- Line 422: city, second
  ```
  !Second, adjust prob_entry accordingly, multiply change by elasticity
  ```
- Line 443: loc
  ```
  deallocate(wage_change_entrant)
  ```
- Line 445: loc
  ```
  deallocate(wage_entrant_tmp)
  ```
- Line 446: loc
  ```
  deallocate(wage_entrant_tmp_tmp)
  ```
- Line 455: lat
  ```
  ! Here we calculate the expected number of entrants/exits across economies
  ```
- Line 486: loc
  ```
  allocate(t_exit(nteach_prev))
  ```
- Line 487: loc
  ```
  allocate(t_entry(nEntrant))
  ```
- Line 490: loc
  ```
  ! save incumbents in the data b/c we will need to reallocate these arrays
  ```
- Line 491: loc
  ```
  allocate(C1_prev(nEconomy, nteach_prev), C2_prev(nEconomy, nteach_prev))
  ```
- Line 492: loc
  ```
  allocate(exp_prev(nEconomy, nteach_prev), age_prev(nEconomy, nteach_prev), &
  ```
- Line 502: loc
  ```
  ! deallocate all our variables, so can reallocate with new nteach
  ```
- Line 504: loc
  ```
  call deallocate_nteach_vars()
  ```
- Line 505: loc
  ```
  call allocate_nteach_vars()
  ```
- Line 531: city
  ```
  print*, 'nteach less than total capacity'
  ```
- Line 536: district
  ```
  ! And we also have to save the district info... b/c these get expanded as well
  ```
- Line 641: loc
  ```
  if (mpi_id == 0) deallocate(prob_exit, prob_entry)
  ```
- Line 643: lon
  ```
  ! no longer need these:
  ```
- Line 644: loc
  ```
  deallocate(C1_prev, C2_prev, exp_prev, age_prev, master_prev, inc_prev)
  ```
- Line 645: loc
  ```
  deallocate(t_exit, t_entry)
  ```
- Line 648: loc
  ```
  deallocate(tshock)
  ```
- Line 649: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 664: loc
  ```
  ! allocate these to keep track of beliefs by economy
  ```
- Line 666: loc
  ```
  allocate(theta_period(nEconomy, ntheta))
  ```
- Line 667: loc
  ```
  allocate(wage_theta_period(nEconomy, ntheta_wage))
  ```
- Line 677: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 725: city
  ```
  ! Every economy-teacher needs an average wage    (to consider exit elasticity)
  ```
- Line 726: city
  ```
  ! As well as every entrant needs an average wage (to consider entry elasticity)
  ```
- Line 728: loc
  ```
  allocate(ones_nteach(nteach, 1), ones_nEntrant(nEntrant,1))
  ```
- Line 733: loc
  ```
  allocate(omega_baseline(nEconomy,2,ndist))
  ```
- Line 737: lat
  ```
  ! for entrants, b/c pop is static, we can calculate a base wage now.
  ```
- Line 738: loc
  ```
  allocate(wage_baseline_entrant(nEntrant))
  ```
- Line 739: loc
  ```
  allocate(wage_baseline_entrant_tmp(nEntrant, nEconomy))
  ```
- Line 755: loc
  ```
  deallocate(ones_nteach, ones_nEntrant, wage_baseline_entrant_tmp)
  ```
- Line 762: loc
  ```
  deallocate(param0)
  ```
- Line 763: loc
  ```
  deallocate(tshock)
  ```
- Line 765: loc
  ```
  deallocate(dshock)
  ```
- Line 766: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 769: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 782: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 786: loc
  ```
  double precision, allocatable, dimension(:,:) :: mean_wage, sd_wage
  ```
- Line 788: loc
  ```
  double precision, allocatable, dimension(:) :: var_wage
  ```
- Line 790: loc
  ```
  !allocate
  ```
- Line 791: loc
  ```
  allocate(mean_wage(nteach,ndist), sd_wage(nteach,ndist))
  ```
- Line 792: loc
  ```
  allocate(var_wage(nteach))
  ```
- Line 803: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 813: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 824: loc
  ```
  !deallocate, even tho it will automatically
  ```
- Line 825: loc
  ```
  deallocate(mean_wage, sd_wage)
  ```
- Line 826: loc
  ```
  deallocate(var_wage)
  ```
- Line 837: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 840: loc
  ```
  allocate(subsidy(nteach))
  ```
- Line 935: loc
  ```
  deallocate(subsidy)
  ```
- Line 946: loc
  ```
  double precision, allocatable, dimension(:) :: subsidy
  ```
- Line 949: loc
  ```
  allocate(subsidy(nEntrant))
  ```
- Line 1044: loc
  ```
  deallocate(subsidy)
  ```
- Line 1103: loc
  ```
  double precision, allocatable, dimension(:) :: temp_prob
  ```
- Line 1105: loc
  ```
  allocate(temp_prob(size(probability)))
  ```
- Line 1112: name
  ```
  n = sampleSize ! shorter name for sample size
  ```
- Line 1128: lat
  ```
  ! do cumulative sum until we are greater than u
  ```
- Line 1152: loc
  ```
  deallocate(temp_prob)
  ```
- Line 1157: lat
  ```
  ! note that wstat and read_data needs to be calculated before this is run...
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 41: loc
  ```
  double precision, allocatable, dimension(:,:) :: pr_model_match
  ```
- Line 43: loc
  ```
  integer, allocatable, dimension(:,:,:)    :: model_match_copies
  ```
- Line 46: loc
  ```
  integer, allocatable, dimension(:,:,:) :: sim_matches
  ```
- Line 50: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_dbl
  ```
- Line 52: loc
  ```
  integer, allocatable, dimension(:,:,:)   :: match_copies
  ```
- Line 64: loc
  ```
  !allocate nteach vars
  ```
- Line 65: loc
  ```
  allocate(pr_model_match(nteach,ndist))
  ```
- Line 66: loc
  ```
  allocate(model_match_copies(ncopyT, nteach, ndist))
  ```
- Line 67: loc
  ```
  allocate(sim_matches(nEconomy, nteach, ndist))
  ```
- Line 68: loc
  ```
  allocate(match_dbl(nteach, ndist))
  ```
- Line 69: loc
  ```
  allocate(match_copies(ncopyT, nteach, ndist))
  ```
- Line 76: lat
  ```
  !translate parameters
  ```
- Line 162: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 170: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 173: lat
  ```
  print*, '--------- Equilibrium Simulation Finished ----------', t_period
  ```
- Line 177: district
  ```
  ! inc_period saves the index of the incumbant district, i.e. one number for each teacher instead of 
  ```
- Line 178: loc
  ```
  inc_period(:,i) = maxloc(sim_matches(:,i,:),2)
  ```
- Line 180: lat
  ```
  ! XX can comment out later
  ```
- Line 195: lat
  ```
  ! Calculate our moments
  ```
- Line 196: lat
  ```
  !    (with inc being economy-by-economy for foward iter, can't calclate this stuff anyways, and we d
  ```
- Line 213: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 261: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 312: lat
  ```
  !call write_latest_theta()
  ```
- Line 314: lat
  ```
  call write_latest_theta()
  ```
- Line 318: loc
  ```
  !dealloc nteach vars
  ```
- Line 319: loc
  ```
  deallocate(pr_model_match, model_match_copies, sim_matches, match_dbl, match_copies)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/read_data_params.f90**

- Line 18: loc
  ```
  allocate_nteach_vars, deallocate_nteach_vars, &
  ```
- Line 27: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 57: lat
  ```
  r_vec = 0 !this is set later, b/c we need to know lambda (share_low)
  ```
- Line 61: lat
  ```
  s_vec = 0 !this is set later, needs to be zero for t_period=1 if forward iter
  ```
- Line 87: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 96: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 107: lat
  ```
  !translate into index
  ```
- Line 121: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 180: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 182: loc
  ```
  if (allocated(param0)) then
  ```
- Line 183: loc
  ```
  deallocate(param0)
  ```
- Line 186: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 187: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 201: loc
  ```
  allocate(param0(np))
  ```
- Line 243: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 256: loc
  ```
  double precision, allocatable, dimension(:,:) :: match_double
  ```
- Line 258: loc
  ```
  allocate(match_double(nteach,ndist))
  ```
- Line 369: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 370: name
  ```
  !   but that was not a good name)
  ```
- Line 371: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 376: second
  ```
  !second, calc the moms
  ```
- Line 381: loc
  ```
  deallocate(match_double)
  ```
- Line 386: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 393: district
  ```
  !assign districts to quintiles
  ```
- Line 400: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 405: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 418: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 430: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 520: name
  ```
  !skip first line b/c it is varname
  ```
- Line 531: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 553: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 661: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 662: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 668: lat
  ```
  ! ++  Age - cts variable for forward simulation
  ```
- Line 680: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 682: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 684: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 686: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 689: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 691: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 693: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 700: loc
  ```
  integer, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 708: loc
  ```
  double precision, allocatable, dimension(:) :: prob_entry
  ```
- Line 710: loc
  ```
  !allocate nteach vars
  ```
- Line 711: loc
  ```
  allocate(twageschedule(ndist, nteach), tlogdist(ndist, nteach))
  ```
- Line 712: loc
  ```
  allocate(tsamecz(ndist, nteach), tmatch(ndist, nteach), tinc(ndist, nteach))
  ```
- Line 713: loc
  ```
  allocate(texp(6, nteach))
  ```
- Line 714: loc
  ```
  allocate(tva(4,nteach))
  ```
- Line 715: loc
  ```
  allocate(tmpmaster(nteach,1), tmpexp_con(nteach,1))
  ```
- Line 716: loc
  ```
  allocate(v1(nteach,1), v2(nteach,1), xb1(nteach,1), xb2(nteach,1))
  ```
- Line 717: loc
  ```
  allocate(va(nteach,4))
  ```
- Line 772: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 779: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 785: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 791: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 825: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 828: lat
  ```
  ! New for forward iter, we need to load in more granular wageschedule so can use it w/ new teacher p
  ```
- Line 854: district, loc
  ```
  allocate(logdist_btwn_districts(ndist,ndist))
  ```
- Line 856: district
  ```
  open(1, file = trim(filepath)//'/model_data/logdist_btwn_districts.csv', status = 'old')
  ```
- Line 857: district
  ```
  read(1,*) logdist_btwn_districts
  ```
- Line 888: district
  ```
  ! cz ids, so know which districts are in the same cz. Will generate czten in generate_teacher_data()
  ```
- Line 916: lat, loc
  ```
  ! alloc entrant population vars
  ```
- Line 917: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 925: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 931: lat
  ```
  ! And b/c these probs are population * 3, lets devide them by 3
  ```
- Line 938: lat
  ```
  ! we are instead doing a different entrant population
  ```
- Line 941: lat, loc
  ```
  ! allocate entrant population vars
  ```
- Line 942: loc
  ```
  allocate(exp_entrant(nentrant), &
  ```
- Line 948: lat
  ```
  ! and fill these variables as they are for our t=1 population
  ```
- Line 980: loc
  ```
  allocate(baseline_prob_entry(nentrant))
  ```
- Line 993: loc
  ```
  !allocate(prob_entry(nentrant))
  ```
- Line 1014: loc
  ```
  allocate(wageschedule_entrant(nEntrant, ndist))
  ```
- Line 1015: loc
  ```
  allocate(contribution_entrant(nEntrant, ndist))
  ```
- Line 1049: loc
  ```
  !deallocate nteach vars
  ```
- Line 1050: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1051: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1052: loc
  ```
  deallocate(texp)
  ```
- Line 1053: loc
  ```
  deallocate(tva)
  ```
- Line 1054: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1055: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1056: loc
  ```
  deallocate(va)
  ```
- Line 1073: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach!, logdist_tmp
  ```
- Line 1074: loc
  ```
  !integer,          allocatable, dimension(:,:) :: samecz_tmp
  ```
- Line 1077: district
  ```
  ! start with some district vars that are stretched across nteach
  ```
- Line 1078: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1083: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1089: lat
  ```
  ! teacher contribution related vars
  ```
- Line 1105: lat
  ```
  ! inc related vars
  ```
- Line 1106: lat
  ```
  ! Note: this will actually get re-run seperately for t>1 later, when we do inc economy-by-economy, w
  ```
- Line 1111: district
  ```
  ! logdist (logdist_btwn_districts)
  ```
- Line 1118: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 1121: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 1128: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1137: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 1144: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 1201: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1212: district
  ```
  !      logdist_btwn_districts:
  ```
- Line 1213: loc
  ```
  !allocate(logdist_tmp(nteach,ndist))
  ```
- Line 1218: district, loc
  ```
  logdist(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1219: district, loc
  ```
  !logdist_tmp(i:i,:) = logdist_btwn_districts(maxloc(inc(i,:)),:)
  ```
- Line 1238: loc
  ```
  !allocate(samecz_tmp(nteach,ndist))
  ```
- Line 1244: loc
  ```
  incdist = maxloc(inc(i,:),1)
  ```
- Line 1255: son
  ```
  ! we match as of 10/13/23, except for one person, i=1870, who is an entrant, but somehow had a samec
  ```
- Line 1259: loc
  ```
  !   incdist = maxloc(inc(i,:),1)
  ```
- Line 1284: lat
  ```
  ! experience related vars
  ```
- Line 1380: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1381: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1385: loc
  ```
  !alloc nteach vars
  ```
- Line 1386: loc
  ```
  allocate(age1(nteach), age2(nteach), age3(nteach), age4(nteach), age5(nteach))
  ```
- Line 1387: loc
  ```
  allocate(exp1(nteach), exp2(nteach), exp3(nteach), exp4(nteach), exp5(nteach), exp6(nteach))
  ```
- Line 1493: lat
  ```
  ! now translate through the logit:
  ```
- Line 1498: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1507: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1508: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1524: loc
  ```
  double precision, allocatable, dimension(:) :: age1, age2, age3, age4, age5
  ```
- Line 1525: loc
  ```
  double precision, allocatable, dimension(:) :: exp1, exp2, exp3, exp4, exp5, exp6
  ```
- Line 1531: loc
  ```
  !alloc nteach vars
  ```
- Line 1532: loc
  ```
  allocate(age1(m), age2(m), age3(m), age4(m), age5(m))
  ```
- Line 1533: loc
  ```
  allocate(exp1(m), exp2(m), exp3(m), exp4(m), exp5(m), exp6(m))
  ```
- Line 1655: lat
  ```
  ! now translate through the logit:
  ```
- Line 1660: lat
  ```
  !   if checking match, make sure exit prob gets run on t=0 population before moving forward c1/c2, a
  ```
- Line 1669: loc
  ```
  deallocate(age1, age2, age3, age4, age5)
  ```
- Line 1670: loc
  ```
  deallocate(exp1, exp2, exp3, exp4, exp5, exp6)
  ```
- Line 1683: loc
  ```
  double precision, allocatable, dimension(:,:) :: twageschedule, tlogdist
  ```
- Line 1685: loc
  ```
  integer, allocatable, dimension(:,:) :: tsamecz,tmatch, tinc
  ```
- Line 1687: loc
  ```
  integer, allocatable, dimension(:,:) :: texp
  ```
- Line 1689: loc
  ```
  double precision, allocatable, dimension(:,:) :: tva
  ```
- Line 1692: loc
  ```
  integer, allocatable, dimension(:,:) :: tmpmaster, tmpexp_con
  ```
- Line 1694: loc
  ```
  double precision, allocatable, dimension(:,:) :: v1, v2, xb1, xb2
  ```
- Line 1696: loc
  ```
  double precision, allocatable, dimension(:, :) :: va
  ```
- Line 1706: loc
  ```
  double precision, allocatable, dimension(:,:) :: ones_nteach
  ```
- Line 1716: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 1721: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 1731: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 1736: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 1741: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 1761: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 1772: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 1789: loc
  ```
  allocate(ones_nteach(nteach,1))
  ```
- Line 1793: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1796: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1799: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1802: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1805: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1808: lon
  ```
  ! this also occurs in generate_teacher_data, so if that is called alone it still happens (i.e. when 
  ```
- Line 1815: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1819: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1823: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1839: loc
  ```
  deallocate(ones_nteach)
  ```
- Line 1850: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1875: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1882: lat, second
  ```
  !    second we can calculate
  ```
- Line 1886: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1891: loc
  ```
  !deallocate nteach vars
  ```
- Line 1892: loc
  ```
  deallocate(twageschedule, tlogdist)
  ```
- Line 1893: loc
  ```
  deallocate(tsamecz, tmatch, tinc)
  ```
- Line 1894: loc
  ```
  deallocate(texp)
  ```
- Line 1895: loc
  ```
  deallocate(tva)
  ```
- Line 1896: loc
  ```
  deallocate(tmpmaster, tmpexp_con)
  ```
- Line 1897: loc
  ```
  deallocate(v1, v2, xb1, xb2)
  ```
- Line 1898: loc
  ```
  deallocate(va)
  ```
- Line 1903: lat
  ```
  !calculate median of a vector
  ```
- Line 1927: loc
  ```
  ! allocate all of our nteach variables
  ```
- Line 1928: loc
  ```
  subroutine allocate_nteach_vars()
  ```
- Line 1930: loc
  ```
  if (mpi_id == 0) print*, '(re)allocating nteach vars, nteach=', nteach
  ```
- Line 1932: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1933: loc
  ```
  allocate(wage(nteach, ndist), wageschedule(nteach, ndist), contribution(nteach, ndist))
  ```
- Line 1939: loc
  ```
  allocate(match(nteach, ndist), inc(nteach, ndist), logdist(nteach, ndist))
  ```
- Line 1940: loc
  ```
  allocate(tenured(nteach, ndist), entrant(nteach, ndist), milwauk(nteach, ndist))
  ```
- Line 1941: loc
  ```
  allocate(samecz(nteach, ndist), master(nteach, ndist), exp_con(nteach, ndist))
  ```
- Line 1942: loc
  ```
  allocate(C1(nteach, 1), C2(nteach, 1), exp_dum(nteach, 6), exp_gr(nteach,1))
  ```
- Line 1943: loc
  ```
  allocate(exp_dum1(nteach,ndist), exp_dum2(nteach,ndist), exp_dum3(nteach,ndist))
  ```
- Line 1944: loc
  ```
  allocate(exp_dum4(nteach,ndist), exp_dum5(nteach,ndist), exp_dum6(nteach,ndist))
  ```
- Line 1945: loc
  ```
  allocate(age(nteach))
  ```
- Line 1947: loc
  ```
  allocate(c1_period(nEconomy, nteach), C2_period(nEconomy, nteach))
  ```
- Line 1948: loc
  ```
  allocate(exp_period(nEconomy, nteach), age_period(nEconomy, nteach))
  ```
- Line 1949: loc
  ```
  allocate(master_period(nEconomy, nteach))
  ```
- Line 1950: loc
  ```
  allocate(inc_period(nEconomy,nteach))
  ```
- Line 1952: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1953: loc
  ```
  allocate(share_low(nteach,ndist), urban(nteach,ndist), suburban(nteach,ndist))
  ```
- Line 1956: loc
  ```
  allocate(subsidized(nteach),subsidized_c1(nteach),subsidized_c2(nteach))
  ```
- Line 1960: loc
  ```
  allocate(wstat(nteach,ndist), meanW(nteach), sdW(nteach))
  ```
- Line 1963: loc
  ```
  end subroutine allocate_nteach_vars
  ```
- Line 1965: loc
  ```
  ! deallocate all of our nteach variables
  ```
- Line 1966: loc
  ```
  subroutine deallocate_nteach_vars()
  ```
- Line 1968: lat
  ```
  !wage related, wage wageschedule contribution
  ```
- Line 1969: loc
  ```
  deallocate(wage, wageschedule, contribution)
  ```
- Line 1972: loc
  ```
  deallocate(match, inc, logdist, tenured, entrant, milwauk, samecz, master, exp_con)
  ```
- Line 1973: loc
  ```
  deallocate(C1, C2, exp_dum, exp_gr, age)
  ```
- Line 1974: loc
  ```
  deallocate(exp_dum1, exp_dum2, exp_dum3, exp_dum4, exp_dum5, exp_dum6)
  ```
- Line 1976: loc
  ```
  deallocate(c1_period, C2_period)
  ```
- Line 1977: loc
  ```
  deallocate(exp_period, age_period)
  ```
- Line 1978: loc
  ```
  deallocate(master_period)
  ```
- Line 1979: loc
  ```
  deallocate(inc_period)
  ```
- Line 1981: district
  ```
  !district that have been expanded, share_low, urban, suburban
  ```
- Line 1982: loc
  ```
  deallocate(share_low, urban, suburban)
  ```
- Line 1985: loc
  ```
  deallocate(subsidized, subsidized_c1, subsidized_c2)
  ```
- Line 1986: loc
  ```
  deallocate(wstat, meanw, sdw)
  ```
- Line 1988: loc
  ```
  end subroutine deallocate_nteach_vars
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 105: district
  ```
  subroutine utility_district(beta,u)
  ```
- Line 140: district
  ```
  end subroutine utility_district
  ```
- Line 146: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/6_forward/forward_new2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_15/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/7_higherbonus/new1_25/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new1/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new2/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new3/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  if (purpose >= 10) wage_tmp = wage !save wage data for later moments
  ```
- Line 82: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 83: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 89: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 122: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 125: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 132: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 169: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 170: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 188: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 189: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 191: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 193: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 194: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 202: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 206: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 207: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 208: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 209: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 213: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 219: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 252: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 258: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 273: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 275: loc
  ```
  deallocate(X_big)
  ```
- Line 278: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 279: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 282: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 285: loc
  ```
  deallocate(y_big)
  ```
- Line 286: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 288: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 294: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 314: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 322: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 335: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 337: city
  ```
  X(:,2) = free_capacity
  ```
- Line 343: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 348: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 349: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 350: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 389: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 396: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 401: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 402: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 403: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 420: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 437: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 442: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 443: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 444: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 461: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 510: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 535: second
  ```
  !second set of vars
  ```
- Line 538: city
  ```
  !capacity
  ```
- Line 559: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 590: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 598: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 667: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 718: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 734: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 737: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 752: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 769: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 771: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 781: district
  ```
  !across all district
  ```
- Line 795: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 796: city
  ```
  excess_capacity = 0
  ```
- Line 799: city
  ```
  excess_capacity(j) = 1
  ```
- Line 802: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 803: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 805: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 806: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 812: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 820: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 884: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 888: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 896: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 918: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 940: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/8_noRcost/new4/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 277: district
  ```
  write(44,*) "district_id ", "o2_0 ", "o2_10 ",  "o2_30 ", "o2_50 ", "o2_75 ", "o2_100 ", "o2_200 ", 
  ```
- Line 288: lat
  ```
  !call write_latest_theta()
  ```
- Line 290: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/estimation_result/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```
- Line 106: lat
  ```
  ! ** Variables needs for se calculation **
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 182: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 203: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 207: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 217: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 230: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 246: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 248: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 251: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 254: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 317: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 321: loc
  ```
  call deallocate_variables()
  ```
- Line 332: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 333: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 379: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 382: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 386: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 400: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 453: district
  ```
  !solve knapsack for given districts
  ```
- Line 466: loc
  ```
  call deallocate_variables()
  ```
- Line 478: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 497: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 501: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 513: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 517: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 521: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 525: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 534: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 535: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 536: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 537: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 573: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 576: district
  ```
  !    which district nums each thread is working on
  ```
- Line 584: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 593: district
  ```
  !figure out index of district j
  ```
- Line 607: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 662: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 673: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 675: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 707: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 717: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 722: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 724: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 767: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 769: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 772: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 775: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 786: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 792: lat
  ```
  ! Late addition
  ```
- Line 800: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 802: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 816: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1017: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1040: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1043: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1062: loc
  ```
  call deallocate_variables()
  ```
- Line 1067: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1075: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1117: lat
  ```
  !   and fix if need be later
  ```
- Line 1241: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1346: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1431: district
  ```
  ! teachers rewarded: district
  ```
- Line 1458: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1483: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1497: district
  ```
  !figure out index of district j
  ```
- Line 1597: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1640: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1650: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1667: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1686: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1767: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1772: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1774: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1775: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1776: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1778: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 31: loc
  ```
  double precision, allocatable, dimension(:) :: initial_param_se, param_se, perturbation
  ```
- Line 32: loc
  ```
  !double precision, allocatable, dimension(:,:) :: W_se, G_se, parammat, auxmat, bigOmega, outer, inn
  ```
- Line 33: loc
  ```
  double precision, allocatable, dimension(:,:) :: parammat, auxmat
  ```
- Line 34: loc
  ```
  !double precision, allocatable, dimension(:,:) :: Gt_se, outert, innert, vart, sematt
  ```
- Line 35: loc
  ```
  !double precision, allocatable, dimension(:,:) :: Gd_se, outerd, innerd, vard, sematd
  ```
- Line 36: loc
  ```
  !double precision, allocatable, dimension(:,:) :: G2_se, outer2, inner2, var2, semat2
  ```
- Line 52: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 69: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 70: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 87: loc
  ```
  !allocate global sim_omega
  ```
- Line 88: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 91: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 103: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 116: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 142: lat
  ```
  else if (purpose == -1) then !calculating standard error
  ```
- Line 150: loc
  ```
  allocate(initial_param_se(nparam), param_se(nparam), perturbation(nparam))
  ```
- Line 151: loc
  ```
  allocate(parammat(nparam,sum(incl)+1))
  ```
- Line 152: loc
  ```
  allocate(auxmat(naux+naux_omega+naux_other,sum(incl)+1))
  ```
- Line 154: loc
  ```
  !allocate(semat(sum(incl),size(r_se_list)+1))
  ```
- Line 155: loc
  ```
  !allocate(semat2(sum(incl)-1,size(r_se_list)+1))
  ```
- Line 157: district, loc
  ```
  !also now need to find dimensions to allocate teacher and district subsets
  ```
- Line 166: loc
  ```
  !allocate(sematt(ntparam,size(r_se_list)+1))
  ```
- Line 168: district
  ```
  !districts
  ```
- Line 175: lat
  ```
  !      !save index of the teacher sigma term in incl terms so we can use later
  ```
- Line 180: loc
  ```
  !allocate(sematd(ndparam,size(r_se_list)+1))
  ```
- Line 224: lat
  ```
  !set up perturbation amounts as SEs calculated in paper
  ```
- Line 268: district
  ```
  !district and teacher subsets
  ```
- Line 293: district
  ```
  !respect bounds for exp parameters in district utility
  ```
- Line 394: loc
  ```
  deallocate(initial_param_se, param_se)
  ```
- Line 405: loc
  ```
  deallocate(param0)
  ```
- Line 419: loc
  ```
  deallocate(tshock)
  ```
- Line 421: loc
  ```
  deallocate(dshock)
  ```
- Line 422: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 425: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 438: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 453: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 463: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 36: lat
  ```
  !these three need to be global variables for the SE calculation
  ```
- Line 65: lat
  ```
  !translate parameters
  ```
- Line 156: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 164: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 169: lat
  ```
  ! Calculate our moments
  ```
- Line 185: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 235: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 286: lat
  ```
  !call write_latest_theta()
  ```
- Line 288: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 79: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 83: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 94: lat
  ```
  !translate into index
  ```
- Line 108: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 156: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 158: loc
  ```
  if (allocated(param0)) then
  ```
- Line 159: loc
  ```
  deallocate(param0)
  ```
- Line 162: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 163: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 177: loc
  ```
  allocate(param0(np))
  ```
- Line 224: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 347: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 348: name
  ```
  !   but that was not a good name)
  ```
- Line 349: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 354: second
  ```
  !second, calc the moms
  ```
- Line 362: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 369: district
  ```
  !assign districts to quintiles
  ```
- Line 376: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 381: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 394: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 406: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 496: name
  ```
  !skip first line b/c it is varname
  ```
- Line 507: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 529: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 640: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 641: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 828: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 835: lat, second
  ```
  !    second we can calculate
  ```
- Line 839: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 848: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 851: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 856: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 865: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 870: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 923: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 928: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 938: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 943: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 948: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 979: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 997: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1002: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1007: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1016: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1020: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1024: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1050: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1075: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1082: lat, second
  ```
  !    second we can calculate
  ```
- Line 1086: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1094: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/perturbation/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/run_all_local.sh**

- Line 3: loc
  ```
  # Portable local compiler/runner for the 31 Fortran model scenarios.
  ```
- Line 5: lat, lon
  ```
  # without starting the potentially long model calculations.
  ```
- Line 9: name
  ```
  script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  ```
- Line 17: loc
  ```
  run_all_local.sh [--compile-only] [--run] [--ranks N] [--scenario PATH]
  ```
- Line 24: lat
  ```
  --scenario PATH  Limit work to one path relative to code/fortran_model,
  ```
- Line 91: name
  ```
  elif [[ "$(uname -s)" == "Darwin" ]]; then
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```
- Line 106: lat
  ```
  ! ** Variables needs for se calculation **
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 182: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 203: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 207: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 217: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 230: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 246: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 248: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 251: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 254: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 264: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 317: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 321: loc
  ```
  call deallocate_variables()
  ```
- Line 332: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 333: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 379: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 382: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 386: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 400: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 453: district
  ```
  !solve knapsack for given districts
  ```
- Line 466: loc
  ```
  call deallocate_variables()
  ```
- Line 478: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 497: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 501: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 513: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 517: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 521: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 525: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 534: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 535: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 536: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 537: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 573: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 576: district
  ```
  !    which district nums each thread is working on
  ```
- Line 584: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 593: district
  ```
  !figure out index of district j
  ```
- Line 607: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 662: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 673: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 675: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 707: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 717: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 722: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 724: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 767: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 769: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 772: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 775: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 786: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 792: lat
  ```
  ! Late addition
  ```
- Line 800: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 802: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 816: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1017: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1040: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1043: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1062: loc
  ```
  call deallocate_variables()
  ```
- Line 1067: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1075: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1117: lat
  ```
  !   and fix if need be later
  ```
- Line 1241: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1346: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1431: district
  ```
  ! teachers rewarded: district
  ```
- Line 1458: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1483: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1497: district
  ```
  !figure out index of district j
  ```
- Line 1597: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1640: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1650: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1667: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1686: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1767: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1772: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1774: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1775: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1776: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1778: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/main.f90**

- Line 27: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 31: loc
  ```
  double precision, allocatable, dimension(:) :: initial_param_se, param_se, r_se_list, perturb_min_li
  ```
- Line 32: loc
  ```
  double precision, allocatable, dimension(:,:) :: W_se, G_se, parammat, auxmat, bigOmega, outer, inne
  ```
- Line 33: loc
  ```
  !double precision, allocatable, dimension(:,:) :: Gt_se, outert, innert, vart, sematt
  ```
- Line 34: loc
  ```
  !double precision, allocatable, dimension(:,:) :: Gd_se, outerd, innerd, vard, sematd
  ```
- Line 35: loc
  ```
  !double precision, allocatable, dimension(:,:) :: G2_se, outer2, inner2, var2, semat2
  ```
- Line 36: loc
  ```
  double precision, allocatable, dimension(:,:) :: wmat, twmat
  ```
- Line 52: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 69: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 70: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 87: loc
  ```
  !allocate global sim_omega
  ```
- Line 88: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 91: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 103: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 116: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 142: lat
  ```
  else if (purpose == -1) then !calculating standard error
  ```
- Line 147: loc
  ```
  !allocate(r_se_list(6))
  ```
- Line 150: loc
  ```
  !allocate(perturb_min_list(6))
  ```
- Line 151: loc
  ```
  allocate(r_se_list(1))
  ```
- Line 153: loc
  ```
  allocate(perturb_min_list(1))
  ```
- Line 156: loc
  ```
  !allocate(r_se_list(2))
  ```
- Line 157: loc
  ```
  !allocate(perturb_min_list(2))
  ```
- Line 165: loc
  ```
  allocate(initial_param_se(nparam), param_se(nparam))
  ```
- Line 166: loc
  ```
  allocate(parammat(nparam,sum(incl)+1))
  ```
- Line 167: loc
  ```
  allocate(auxmat(naux+naux_omega+naux_other,sum(incl)+1))
  ```
- Line 169: loc
  ```
  allocate(semat(sum(incl),size(r_se_list)+1))
  ```
- Line 170: loc
  ```
  !allocate(semat2(sum(incl)-1,size(r_se_list)+1))
  ```
- Line 172: district, loc
  ```
  !also now need to find dimensions to allocate teacher and district subsets
  ```
- Line 181: loc
  ```
  !allocate(sematt(ntparam,size(r_se_list)+1))
  ```
- Line 183: district
  ```
  !districts
  ```
- Line 190: lat
  ```
  !      !save index of the teacher sigma term in incl terms so we can use later
  ```
- Line 195: loc
  ```
  !allocate(sematd(ndparam,size(r_se_list)+1))
  ```
- Line 249: district
  ```
  !district and teacher subsets
  ```
- Line 281: district
  ```
  !respect bounds for exp parameters in district utility
  ```
- Line 369: lat
  ```
  !calculate std errors
  ```
- Line 371: loc
  ```
  ! sum W==0 and then allocate new matrices
  ```
- Line 383: loc
  ```
  !allocate with correct size, removing weight zero aux params, and incl=0 params
  ```
- Line 384: loc
  ```
  allocate(W_se(nauxparams,nauxparams), G_se(nauxparams,sum(incl)), bigOmega(nauxparams,nauxparams))
  ```
- Line 385: loc
  ```
  allocate(outer(sum(incl),sum(incl)), inner(sum(incl),sum(incl)))
  ```
- Line 386: loc
  ```
  allocate(var(sum(incl),sum(incl)))
  ```
- Line 441: block, loc
  ```
  ! Trying block diagonal for big Omega (partly "hardcoded", b/c W_se could change b/c of what has inc
  ```
- Line 446: loc
  ```
  !allocate(twmat(nalpha+2,nalpha+2),wmat(nalpha+1,nalpha+1))
  ```
- Line 459: loc
  ```
  !deallocate(twmat, wmat)
  ```
- Line 461: loc
  ```
  !allocate(twmat(16,16), wmat(16, 16))
  ```
- Line 473: loc
  ```
  !deallocate(twmat, wmat)
  ```
- Line 501: loc
  ```
  !           allocate(G2_se(nauxparams,sum(incl)-1))
  ```
- Line 502: loc
  ```
  !           allocate(outer2(sum(incl)-1,sum(incl)-1), inner2(sum(incl)-1,sum(incl)-1))
  ```
- Line 503: loc
  ```
  !           allocate(var2(sum(incl)-1,sum(incl)-1))
  ```
- Line 520: loc
  ```
  !           deallocate(G2_se, outer2, inner2, var2)
  ```
- Line 523: district
  ```
  !           ! Also alternatively, lets compute SEs for teacher side and district side separately to 
  ```
- Line 526: loc
  ```
  !           !allocate(W_se(nauxparams,nauxparams), G_se(nauxparams,sum(incl)), bigOmega(nauxparams,n
  ```
- Line 527: loc
  ```
  !           !allocate(outer(sum(incl),sum(incl)), inner(sum(incl),sum(incl)))
  ```
- Line 528: loc
  ```
  !           !allocate(var(sum(incl),sum(incl)))
  ```
- Line 530: loc
  ```
  !           allocate(Gt_se(nauxparams,ntparam))
  ```
- Line 531: loc
  ```
  !           allocate(outert(ntparam, ntparam), innert(ntparam, ntparam))
  ```
- Line 532: loc
  ```
  !           allocate(vart(ntparam,ntparam))
  ```
- Line 548: loc
  ```
  !           deallocate(Gt_se, outert, innert, vart)
  ```
- Line 550: district
  ```
  !           !districts
  ```
- Line 551: loc
  ```
  !           allocate(Gd_se(nauxparams,ndparam))
  ```
- Line 552: loc
  ```
  !           allocate(outerd(ndparam, ndparam), innerd(ndparam, ndparam))
  ```
- Line 553: loc
  ```
  !           allocate(vard(ndparam,ndparam))
  ```
- Line 571: loc
  ```
  !deallocate(Gd_se, outerd, innerd, vard)
  ```
- Line 573: loc
  ```
  deallocate(W_se, G_se, bigOmega, outer, inner, var)
  ```
- Line 592: district
  ```
  !        !write out se for just teacher and district subsets
  ```
- Line 604: loc
  ```
  deallocate(initial_param_se, param_se)
  ```
- Line 615: loc
  ```
  deallocate(param0)
  ```
- Line 629: loc
  ```
  deallocate(tshock)
  ```
- Line 631: loc
  ```
  deallocate(dshock)
  ```
- Line 632: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 635: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 648: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 663: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 673: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```
- Line 687: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 36: lat
  ```
  !these three need to be global variables for the SE calculation
  ```
- Line 65: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 229: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 280: lat
  ```
  !call write_latest_theta()
  ```
- Line 282: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 79: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 83: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 94: lat
  ```
  !translate into index
  ```
- Line 108: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 156: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 158: loc
  ```
  if (allocated(param0)) then
  ```
- Line 159: loc
  ```
  deallocate(param0)
  ```
- Line 162: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 163: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 177: loc
  ```
  allocate(param0(np))
  ```
- Line 224: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 347: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 348: name
  ```
  !   but that was not a good name)
  ```
- Line 349: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 354: second
  ```
  !second, calc the moms
  ```
- Line 362: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 369: district
  ```
  !assign districts to quintiles
  ```
- Line 376: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 381: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 394: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 406: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 496: name
  ```
  !skip first line b/c it is varname
  ```
- Line 507: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 529: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 640: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 641: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 828: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 835: lat, second
  ```
  !    second we can calculate
  ```
- Line 839: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 848: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 851: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 856: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 865: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 870: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 923: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 928: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 938: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 943: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 948: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 979: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 997: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 1002: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 1007: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1016: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1020: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1024: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1050: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1075: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1082: lat, second
  ```
  !    second we can calculate
  ```
- Line 1086: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1094: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/stderr/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/aux_model.f90**

- Line 4: lat
  ```
  ! Calculates aux model parameters
  ```
- Line 18: lat
  ```
  ! calculate aux model parameters
  ```
- Line 21: district
  ```
  ! OLS - regress district chars. on average teacher chars.:
  ```
- Line 36: loc
  ```
  double precision, dimension(:,:), allocatable :: X_big, y_big, X_small, X_small_stacked, &
  ```
- Line 40: loc
  ```
  double precision, dimension(:,:), allocatable :: X_new, X_2
  ```
- Line 42: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_new, M_master_new, M_C1_new, M_C2_new
  ```
- Line 43: loc
  ```
  double precision, dimension(:,:), allocatable :: M_exp_2, M_master_2, M_C1_2, M_C2_2
  ```
- Line 44: city
  ```
  double precision, dimension(ndist)        :: free_wage, free_capacity
  ```
- Line 72: lat
  ```
  ! start by calculating sub-offer set
  ```
- Line 73: district
  ```
  ! identify seed teacher-district offers
  ```
- Line 79: district
  ```
  ! tenured who move districts (i.e. also non-entrants)
  ```
- Line 112: district
  ```
  !also add in 'match==1 district' for the matched teachers
  ```
- Line 115: loc
  ```
  l = maxloc(model_match_copies(cp,i,:))
  ```
- Line 122: district
  ```
  ! also add into our offer set: inc district for tenured teachers who moved
  ```
- Line 159: loc
  ```
  allocate(X_small(sum(model_offer_sub),13))
  ```
- Line 160: loc
  ```
  allocate(y_small(sum(model_offer_sub),1))
  ```
- Line 178: loc
  ```
  if (allocated(X_small_stacked)) deallocate(X_small_stacked)
  ```
- Line 179: loc
  ```
  if (allocated(y_small_stacked)) deallocate(y_small_stacked)
  ```
- Line 181: loc
  ```
  if (allocated(X_tmp) .and. allocated(y_tmp)) then !not first 'copy'
  ```
- Line 183: loc
  ```
  allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 184: loc
  ```
  allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 192: loc
  ```
  deallocate(X_tmp, y_tmp)
  ```
- Line 193: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 196: loc
  ```
  !allocate(X_small_stacked(size(X_tmp,1)+size(X_small,1),13))
  ```
- Line 197: loc
  ```
  !allocate(y_small_stacked(size(y_tmp,1)+size(y_small,1),1))
  ```
- Line 198: loc
  ```
  allocate(X_small_stacked(size(X_small,1),13))
  ```
- Line 199: loc
  ```
  allocate(y_small_stacked(size(y_small,1),1))
  ```
- Line 203: loc
  ```
  deallocate(y_small,X_small)
  ```
- Line 209: loc
  ```
  allocate(X_tmp(size(x_small_stacked,1),13), y_tmp(size(y_small_stacked,1),1))
  ```
- Line 242: loc
  ```
  deallocate(y_small_stacked, X_small_stacked)
  ```
- Line 248: loc
  ```
  allocate(X_big(sum(model_offer_all),13))
  ```
- Line 263: loc
  ```
  allocate(X_big_stacked(size(X_big,1)*nCopy,13))
  ```
- Line 265: loc
  ```
  deallocate(X_big)
  ```
- Line 268: loc
  ```
  allocate(y_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 269: loc
  ```
  if (purpose >= 10) allocate(x_wage_big_stacked(size(X_big_stacked,1),1))
  ```
- Line 272: loc
  ```
  allocate(y_big(sum(model_offer_all),1))
  ```
- Line 275: loc
  ```
  deallocate(y_big)
  ```
- Line 276: lat
  ```
  !if simulation, will have different wages, need to stack
  ```
- Line 278: loc
  ```
  allocate(x_wage_big(sum(model_offer_all),1))
  ```
- Line 284: loc
  ```
  deallocate(x_wage_big)
  ```
- Line 304: loc
  ```
  deallocate(y_big_stacked, X_big_stacked)
  ```
- Line 312: district
  ```
  !set up objects needed for OLS (probability of matches used for district regs)
  ```
- Line 324: city
  ```
  free_capacity = T(1,:) - sum(ten_inc_stay,1)
  ```
- Line 326: city
  ```
  X(:,2) = free_capacity
  ```
- Line 332: district
  ```
  !first figure out how many districts do indeed have new teachers
  ```
- Line 337: loc
  ```
  !then allocate and assign the matrices to only contain the new teachers
  ```
- Line 338: loc
  ```
  allocate(M_exp_new(new_ct,1), M_master_new(new_ct,1))
  ```
- Line 339: loc
  ```
  allocate(M_C1_new(new_ct,1), M_C2_new(new_ct,1), X_new(new_ct,4))
  ```
- Line 378: loc
  ```
  deallocate(M_exp_new,M_master_new,M_C1_new,M_C2_new,X_new)
  ```
- Line 385: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 390: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 391: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 392: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 409: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 426: lon
  ```
  if ( minval(sum(pr_model_match,1)) < epsilon(0d0)) then
  ```
- Line 431: loc
  ```
  allocate(M_exp_2(non_nan_ct,1), M_master_2(non_nan_ct,1))
  ```
- Line 432: loc
  ```
  allocate(M_C1_2(non_nan_ct,1), M_C2_2(non_nan_ct,1))
  ```
- Line 433: loc
  ```
  allocate(X_2(non_nan_ct,4))
  ```
- Line 450: loc
  ```
  deallocate(M_exp_2, M_master_2, M_C1_2, M_C2_2, X_2)
  ```
- Line 499: district
  ```
  ! first set of vars are average in district for exp!=1 and incumbents
  ```
- Line 524: second
  ```
  !second set of vars
  ```
- Line 527: city
  ```
  !capacity
  ```
- Line 548: lon
  ```
  if (denom(j) .lt. epsilon(denom(j))) then
  ```
- Line 579: lon
  ```
  !            pr_model_Wgrid(a1,a2,j) = epsilon(1d0)
  ```
- Line 587: district
  ```
  ! _big denotes contains repetitions of each district for each omega
  ```
- Line 656: district
  ```
  !fraction of districts choosing each omega
  ```
- Line 707: city
  ```
  integer,          dimension(ndist)        :: excess_capacity
  ```
- Line 723: lat
  ```
  ! this needs quintiles to already be calculated, which happens in read_data so should
  ```
- Line 726: lat
  ```
  print*, ' Did not calculate quintiles before running calc_aux_other_parameters '
  ```
- Line 741: district
  ```
  !set nans to zero (i.e. if district has no teacher matches...)
  ```
- Line 758: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 760: district
  ```
  !which districts only have tenured incumbents (i.e. inverse have some new hires)
  ```
- Line 770: district
  ```
  !across all district
  ```
- Line 784: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 785: city
  ```
  excess_capacity = 0
  ```
- Line 788: city
  ```
  excess_capacity(j) = 1
  ```
- Line 791: city
  ```
  mom10(1) = sum(inc_only * excess_capacity)     / real(sum(excess_capacity))
  ```
- Line 792: city
  ```
  mom10(2) = sum(inc_only * (1-excess_capacity)) / real(sum(1-excess_capacity))
  ```
- Line 794: district
  ```
  ! *** districts w/ new hires: new hires/T and charac. of new hires ***
  ```
- Line 795: district
  ```
  ! districts w/ new hires is 1 - inc_only or model_inc_only
  ```
- Line 801: city
  ```
  !average fraction of new hires over capacity
  ```
- Line 809: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 873: loc
  ```
  double precision, allocatable, dimension(:,:) :: sortedvec
  ```
- Line 877: district, lat
  ```
  !calculate mean of bottom pth percentile teachers separately for each district
  ```
- Line 885: loc
  ```
  allocate(sortedvec(nmatch,2))
  ```
- Line 907: lon
  ```
  if (abs(Wsum - sortedvec(indL,2) - pp) .lt. epsilon(pp)) then
  ```
- Line 929: loc
  ```
  deallocate(sortedvec)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/belief_functions.f90**

- Line 4: lat
  ```
  ! Functions related to computation and estimation of 'h' beliefs
  ```
- Line 76: district
  ```
  ! If we only want to get h for ONE district, not all ndist,
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/global_variables.f90**

- Line 32: district
  ```
  ! ** number of teachers and districts **
  ```
- Line 47: loc
  ```
  integer,          allocatable :: eqbm_omega2(:,:)
  ```
- Line 48: loc
  ```
  double precision, allocatable :: sim_omegas(:,:,:)
  ```
- Line 49: district
  ```
  !teacher and district specific (wage, match, incumbent, commuting zone)
  ```
- Line 52: degree
  ```
  !teacher specific (masters degree, cts experience, exp dummies, value added)
  ```
- Line 54: district
  ```
  double precision, dimension(nteach, 1) :: C1, C2 !identical for each district
  ```
- Line 62: district
  ```
  !district specific (% low achieving, urban, suburban)
  ```
- Line 66: city
  ```
  !budget and capacity and subsidized status
  ```
- Line 70: loc
  ```
  double precision, allocatable :: tshock(:,:,:) !(sim_nteach, ndist)
  ```
- Line 71: loc
  ```
  double precision, allocatable :: dshock(:,:,:,:) !(sim_ndist,  NWgrid1*NWgrid2)
  ```
- Line 94: lat
  ```
  !helpers for calculating aux params
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/inner_loop_one.f90**

- Line 5: district
  ```
  !  iterate over district beliefs to find h, and offers
  ```
- Line 25: loc
  ```
  integer, allocatable, dimension(:) :: send_counts, send_starts
  ```
- Line 26: loc
  ```
  double precision, allocatable, dimension(:,:) :: Ht, udistt, waget, Bt, Tt
  ```
- Line 27: loc
  ```
  integer, allocatable, dimension(:,:) :: tenuredt
  ```
- Line 28: loc
  ```
  double precision, allocatable, dimension(:,:,:) :: pr_model_Wgridt
  ```
- Line 30: loc
  ```
  integer, allocatable, dimension(:,:) :: inct, model_offert
  ```
- Line 35: loc
  ```
  double precision, allocatable, dimension(:,:) :: best_H_target
  ```
- Line 49: son
  ```
  !temp variable for printing mean approx comparison
  ```
- Line 52: lat
  ```
  public :: run_inner_loop_one, run_slave_inner_loop_one, simulate_equilibrium
  ```
- Line 70: loc
  ```
  double precision, allocatable, dimension(:,:) :: C_matrix
  ```
- Line 79: loc
  ```
  allocate(best_H_target(nteach,ndist))
  ```
- Line 80: loc
  ```
  allocate(C_matrix(nteach,ndist))
  ```
- Line 90: district
  ```
  udist  = utility_district(beta_in)
  ```
- Line 109: lon
  ```
  !      if (abs(exp_uteach(i,j) - 0.0d0) .lt. epsilon(exp_uteach(i,j))) then
  ```
- Line 176: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 197: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 201: loc
  ```
  model_match_copies(k,i,maxloc(v(k,i,:))) = 1
  ```
- Line 211: district
  ```
  !  each thread will solve for each alpha of their assigned districts
  ```
- Line 224: district
  ```
  !       !if not solving district wage decision, set wage choice as seen in data:
  ```
- Line 240: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 242: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 245: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 248: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 258: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 311: loc
  ```
  deallocate(best_H_target, C_matrix)
  ```
- Line 315: loc
  ```
  call deallocate_variables()
  ```
- Line 326: loc
  ```
  aa(j,1) = minloc(abs(WW(j,1)-omega1_grid),dim=1)
  ```
- Line 327: loc
  ```
  aa(j,2) = minloc(abs(WW(j,2)-omega2_grid),dim=1)
  ```
- Line 373: district
  ```
  !   !teacher i picks district with highest utility
  ```
- Line 376: loc
  ```
  !      model_match(i, maxloc(v(i,:))) = 1
  ```
- Line 380: lat
  ```
  !calculate 'true' beliefs which are the target. Note exp_uteach contains sigma
  ```
- Line 394: lon
  ```
  !if (abs(H_target(i,j) - 1.0d0) < epsilon(0d0)) H_target(i,j) = 0.99d0
  ```
- Line 447: district
  ```
  !solve knapsack for given districts
  ```
- Line 460: loc
  ```
  call deallocate_variables()
  ```
- Line 472: loc
  ```
  allocate(send_counts(ntasks),send_starts(ntasks))
  ```
- Line 491: loc
  ```
  allocate(Bt(1,rec_count))
  ```
- Line 495: loc
  ```
  allocate(Tt(1,rec_count))
  ```
- Line 507: loc
  ```
  allocate(udistt(nteach,rec_count/nteach))
  ```
- Line 511: loc
  ```
  allocate(waget(nteach,rec_count/nteach))
  ```
- Line 515: loc
  ```
  allocate(inct(nteach,rec_count/nteach))
  ```
- Line 519: loc
  ```
  allocate(tenuredt(nteach, rec_count/nteach))
  ```
- Line 528: loc
  ```
  !also allocate matrices we will use in h loop
  ```
- Line 529: loc
  ```
  allocate(Ht(nteach,rec_count/nteach))
  ```
- Line 530: loc
  ```
  allocate(model_offert(nteach,rec_count/nteach))
  ```
- Line 531: loc
  ```
  if(wage_decision == 1) allocate(pr_model_Wgridt(nWgrid1,nWgrid2,rec_count/nteach))
  ```
- Line 567: district
  ```
  !    about the districts they are working on. (t suffixes).
  ```
- Line 570: district
  ```
  !    which district nums each thread is working on
  ```
- Line 578: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 587: district
  ```
  !figure out index of district j
  ```
- Line 601: city
  ```
  !     'excess_budget     ', 'excess_capacity     ','num_offers     '
  ```
- Line 656: district
  ```
  !    print*, 'j, a1, a2, district pi, pi-resist, #offers, expected hires'
  ```
- Line 667: lon
  ```
  !if (abs(exp(profit(a1,a2) / sigmad_glob) - 0.0d0) .lt. epsilon(profit(a1,a2))) then
  ```
- Line 669: lon
  ```
  !   profit(a1,a2) = log(epsilon(sigmad_glob))!-30 * sigmad_glob !for now just set essentially to exp
  ```
- Line 701: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 711: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 716: lat
  ```
  !subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 718: lat
  ```
  subroutine simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in, sigmaD_in,&
  ```
- Line 761: district
  ```
  write(1,*) ' economy_id', ' district_id', ' avg_expect_cont', ' avg_C1', ' avg_C2',&
  ```
- Line 763: city
  ```
  ' budg_per_teach', ' budget', ' capacity', ' share_tenured'
  ```
- Line 766: city, district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_hires', ' capacity', ' belief_expected_hires'
  ```
- Line 769: district
  ```
  write(1,*) ' economy_id', ' district_id', ' expected_wage_bill', ' budget', ' belief_expected_Wage_b
  ```
- Line 780: district
  ```
  ' rewarded_district', ' rewarded_state', ' rewarded_tot', &
  ```
- Line 786: lat
  ```
  ! Late addition
  ```
- Line 794: district
  ```
  udist       = utility_district(beta_in)
  ```
- Line 796: lat, lon
  ```
  !! ZZZ no longer can pre-calculate teacher utility
  ```
- Line 810: loc
  ```
  !  tenuredt, sigmad_glob, gamma_glob. allocates(Ht,model_offert)
  ```
- Line 1011: lat
  ```
  !Now calculate probabilities based on solved offers (note exp_uteach contains sigma)
  ```
- Line 1034: district
  ```
  !teacher pick district which gives highest utility
  ```
- Line 1037: loc
  ```
  match_eqm(i,maxloc(v(i,:))) = 1
  ```
- Line 1056: loc
  ```
  call deallocate_variables()
  ```
- Line 1061: district
  ```
  write(1,*) ' economy_id ', 'district_id ', 'omega1 ', 'omega2 '
  ```
- Line 1069: lat
  ```
  end subroutine simulate_equilibrium
  ```
- Line 1111: lat
  ```
  !   and fix if need be later
  ```
- Line 1235: district, lat
  ```
  !calc stat for each teacher across districts, later can use matmul to duplicate across dists
  ```
- Line 1340: district
  ```
  !finally save the cost to the state by district
  ```
- Line 1425: district
  ```
  ! teachers rewarded: district
  ```
- Line 1452: district
  ```
  ! sum teacher utility, across all offers/probability of being in that district
  ```
- Line 1477: loc
  ```
  integer, allocatable, dimension(:) :: send_counts_omega, send_starts_omega
  ```
- Line 1491: district
  ```
  !figure out index of district j
  ```
- Line 1591: district
  ```
  !   but it is outside of district budgets otherwise
  ```
- Line 1634: lat
  ```
  !calculate prob of choosing each omega
  ```
- Line 1644: city, district
  ```
  !temporary, just print out, district, and capacity vs. expected
  ```
- Line 1661: loc
  ```
  allocate(send_starts_omega(ntasks),send_counts_omega(ntasks))
  ```
- Line 1680: loc
  ```
  deallocate(send_starts_omega, send_counts_omega)
  ```
- Line 1761: lon
  ```
  sd_eqm = min(sqrt(max(var_eqm,epsilon(var_eqm))), wageupper-wagelower)
  ```
- Line 1766: loc
  ```
  subroutine deallocate_variables()
  ```
- Line 1768: loc
  ```
  deallocate(send_counts, send_starts, Ht, udistt, waget, Bt, Tt)
  ```
- Line 1769: loc
  ```
  deallocate(inct, model_offert, tenuredt)
  ```
- Line 1770: loc
  ```
  if(wage_decision == 1) deallocate(pr_model_Wgridt)
  ```
- Line 1772: loc
  ```
  end subroutine deallocate_variables
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/knapsack_problem.f90**

- Line 4: district, school
  ```
  ! Solves the knapsack problem for a school district, finds optimal
  ```
- Line 37: loc
  ```
  double precision, allocatable, dimension(:,:) :: knapsack_data, knapsack_data_sorted
  ```
- Line 44: city
  ```
  ! Remaining budget and capacity
  ```
- Line 48: city
  ```
  !if open budget/capacity, figure out how many offers to make
  ```
- Line 54: loc
  ```
  allocate(knapsack_data(n_non_inc,4),knapsack_data_sorted(n_non_inc,4) )
  ```
- Line 68: city
  ```
  !if lambda=0 does not bind capacity constraint, then it is optimal
  ```
- Line 77: lon
  ```
  if (abs(highval) < epsilon(highval)) then
  ```
- Line 90: lat
  ```
  !commence interpolation algorithm to find crossing point
  ```
- Line 94: lon
  ```
  if ( abs(midval) < epsilon(midval)) then
  ```
- Line 142: loc
  ```
  deallocate(knapsack_data,knapsack_data_sorted)
  ```
- Line 149: city
  ```
  !since no excess capacity, no extra offers can be made
  ```
- Line 159: lat
  ```
  ! lambda_objective calculates a 'likelihood' to help find
  ```
- Line 160: city
  ```
  ! optimal value of the lagrange multiplier on capacity constraint
  ```
- Line 162: city
  ```
  ! the likelihood L returned is the slack in the capacity constraint
  ```
- Line 177: lat
  ```
  ! calculate ordering statistic
  ```
- Line 206: district
  ```
  tval = 1 !corner case where districts offer to all teachers
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/main.f90**

- Line 28: loc
  ```
  double precision, allocatable, dimension(:) :: param0, param0_big
  ```
- Line 45: district
  ```
  ! read the teacher/district data, needed in all threads so just reading in instead of mpi
  ```
- Line 62: loc
  ```
  allocate(dshock(nEconomy,ndist,NWgrid1,NWgrid2))
  ```
- Line 63: loc
  ```
  allocate(tshock(nEconomy,nteach,ndist))
  ```
- Line 80: loc
  ```
  !allocate global sim_omega
  ```
- Line 81: loc
  ```
  allocate(sim_omegas(nEconomy,2,ndist))
  ```
- Line 84: loc
  ```
  allocate(tshock(nCopyT,nteach,ndist))
  ```
- Line 96: city
  ```
  !set budget and capacity constraint vectors
  ```
- Line 109: loc
  ```
  allocate(param0_big(size(param0) + ntheta))
  ```
- Line 144: loc
  ```
  deallocate(param0)
  ```
- Line 158: loc
  ```
  deallocate(tshock)
  ```
- Line 160: loc
  ```
  deallocate(dshock)
  ```
- Line 161: loc
  ```
  deallocate(sim_omegas)
  ```
- Line 164: loc
  ```
  deallocate(eqbm_omega2)
  ```
- Line 177: district, lat
  ```
  ! calculate wage statistic, used to help with district beliefs on teach acceptance
  ```
- Line 192: district
  ```
  do j=2, ndist !apply mean wage across all districts
  ```
- Line 202: district
  ```
  do j=1, ndist !take sd and apply sd across all districts
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/nrutil.f90**

- Line 22: loc
  ```
  INTERFACE reallocate
  ```
- Line 23: loc
  ```
  MODULE PROCEDURE reallocate_rv,reallocate_rm,reallocate_iv,reallocate_im,reallocate_hv
  ```
- Line 25: loc
  ```
  INTERFACE imaxloc
  ```
- Line 26: loc
  ```
  MODULE PROCEDURE imaxloc_r,imaxloc_i
  ```
- Line 148: loc
  ```
  FUNCTION reallocate_rv(p,n)
  ```
- Line 149: loc
  ```
  REAL(8), DIMENSION(:), POINTER :: p, reallocate_rv
  ```
- Line 152: loc
  ```
  allocate(reallocate_rv(n),stat=ierr)
  ```
- Line 154: loc
  ```
  nrerror('reallocate_rv: problem in attempt to allocate memory')
  ```
- Line 157: loc
  ```
  reallocate_rv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 158: loc
  ```
  deallocate(p)
  ```
- Line 159: loc
  ```
  END FUNCTION reallocate_rv
  ```
- Line 161: loc
  ```
  FUNCTION reallocate_iv(p,n)
  ```
- Line 162: loc
  ```
  INTEGER, DIMENSION(:), POINTER :: p, reallocate_iv
  ```
- Line 165: loc
  ```
  allocate(reallocate_iv(n),stat=ierr)
  ```
- Line 167: loc
  ```
  nrerror('reallocate_iv: problem in attempt to allocate memory')
  ```
- Line 170: loc
  ```
  reallocate_iv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 171: loc
  ```
  deallocate(p)
  ```
- Line 172: loc
  ```
  END FUNCTION reallocate_iv
  ```
- Line 174: loc
  ```
  FUNCTION reallocate_hv(p,n)
  ```
- Line 175: loc
  ```
  CHARACTER(1), DIMENSION(:), POINTER :: p, reallocate_hv
  ```
- Line 178: loc
  ```
  allocate(reallocate_hv(n),stat=ierr)
  ```
- Line 180: loc
  ```
  nrerror('reallocate_hv: problem in attempt to allocate memory')
  ```
- Line 183: loc
  ```
  reallocate_hv(1:min(nold,n))=p(1:min(nold,n))
  ```
- Line 184: loc
  ```
  deallocate(p)
  ```
- Line 185: loc
  ```
  END FUNCTION reallocate_hv
  ```
- Line 187: loc
  ```
  FUNCTION reallocate_rm(p,n,m)
  ```
- Line 188: loc
  ```
  REAL(8), DIMENSION(:,:), POINTER :: p, reallocate_rm
  ```
- Line 191: loc
  ```
  allocate(reallocate_rm(n,m),stat=ierr)
  ```
- Line 193: loc
  ```
  nrerror('reallocate_rm: problem in attempt to allocate memory')
  ```
- Line 197: loc
  ```
  reallocate_rm(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 199: loc
  ```
  deallocate(p)
  ```
- Line 200: loc
  ```
  END FUNCTION reallocate_rm
  ```
- Line 202: loc
  ```
  FUNCTION reallocate_im(p,n,m)
  ```
- Line 203: loc
  ```
  INTEGER, DIMENSION(:,:), POINTER :: p, reallocate_im
  ```
- Line 206: loc
  ```
  allocate(reallocate_im(n,m),stat=ierr)
  ```
- Line 208: loc
  ```
  nrerror('reallocate_im: problem in attempt to allocate memory')
  ```
- Line 212: loc
  ```
  reallocate_im(1:min(nold,n),1:min(mold,m))=&
  ```
- Line 214: loc
  ```
  deallocate(p)
  ```
- Line 215: loc
  ```
  END FUNCTION reallocate_im
  ```
- Line 217: loc
  ```
  FUNCTION ifirstloc(mask)
  ```
- Line 219: loc
  ```
  INTEGER :: ifirstloc
  ```
- Line 220: loc
  ```
  INTEGER, DIMENSION(1) :: loc
  ```
- Line 221: loc
  ```
  loc=maxloc(merge(1,0,mask))
  ```
- Line 222: loc
  ```
  ifirstloc=loc(1)
  ```
- Line 223: loc
  ```
  if (.not. mask(ifirstloc)) ifirstloc=size(mask)+1
  ```
- Line 224: loc
  ```
  END FUNCTION ifirstloc
  ```
- Line 226: loc
  ```
  FUNCTION imaxloc_r(arr)
  ```
- Line 228: loc
  ```
  INTEGER :: imaxloc_r
  ```
- Line 230: loc
  ```
  imax=maxloc(arr(:))
  ```
- Line 231: loc
  ```
  imaxloc_r=imax(1)
  ```
- Line 232: loc
  ```
  END FUNCTION imaxloc_r
  ```
- Line 234: loc
  ```
  FUNCTION imaxloc_i(iarr)
  ```
- Line 237: loc
  ```
  INTEGER :: imaxloc_i
  ```
- Line 238: loc
  ```
  imax=maxloc(iarr(:))
  ```
- Line 239: loc
  ```
  imaxloc_i=imax(1)
  ```
- Line 240: loc
  ```
  END FUNCTION imaxloc_i
  ```
- Line 242: loc
  ```
  FUNCTION iminloc(arr)
  ```
- Line 245: loc
  ```
  INTEGER :: iminloc
  ```
- Line 246: loc
  ```
  imin=minloc(arr(:))
  ```
- Line 247: loc
  ```
  iminloc=imin(1)
  ```
- Line 248: loc
  ```
  END FUNCTION iminloc
  ```
- Line 558: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: vec
  ```
- Line 569: loc
  ```
  allocate(vec(n+1))
  ```
- Line 581: loc
  ```
  deallocate(vec)
  ```
- Line 757: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 787: lon
  ```
  if (abs(x - 0.0d0) < epsilon(x)) then
  ```
- Line 836: loc
  ```
  REAL(8), DIMENSION(:), ALLOCATABLE :: xt
  ```
- Line 837: loc
  ```
  ALLOCATE(xt(ncom))
  ```
- Line 840: loc
  ```
  DEALLOCATE(xt)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/objective.f90**

- Line 6: lat
  ```
  ! Also will write latest best parameters to txt file
  ```
- Line 64: lat
  ```
  !translate parameters
  ```
- Line 150: district
  ```
  ! Solve district problem at observed wages (also sets global theta)
  ```
- Line 158: lat
  ```
  call simulate_equilibrium(alpha_in, beta_in, gamma_in, sigma_in,&
  ```
- Line 163: lat
  ```
  ! Calculate our moments
  ```
- Line 179: lat
  ```
  ! If running validation, we need to calculate the data moments as well, as bb doesn't have saves
  ```
- Line 227: district
  ```
  !          !tmp write out prob of choosing each omega for each district
  ```
- Line 278: lat
  ```
  !call write_latest_theta()
  ```
- Line 280: lat
  ```
  call write_latest_theta()
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/random.f90**

- Line 3: lat
  ```
  ! Edited slightly to remove warnings in compilation
  ```
- Line 21: lat
  ```
  ! Latest version - February 3 2004
  ```
- Line 44: loc
  ```
  CALL SYSTEM_CLOCK(seed)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/read_data_params.f90**

- Line 23: loc
  ```
  integer, allocatable  :: eqbm_omega2_tmp(:,:)
  ```
- Line 73: son
  ```
  write(*,*) "you can't run offer eqbm purpose when teacher/offer deicison = 0"
  ```
- Line 77: loc
  ```
  allocate(eqbm_omega2(nEconomy,ndist), eqbm_omega2_tmp(nEconomy,ndist))
  ```
- Line 88: lat
  ```
  !translate into index
  ```
- Line 102: loc
  ```
  deallocate(eqbm_omega2_tmp)
  ```
- Line 150: loc
  ```
  double precision, allocatable, dimension(:), intent(out) :: param0
  ```
- Line 152: loc
  ```
  if (allocated(param0)) then
  ```
- Line 153: loc
  ```
  deallocate(param0)
  ```
- Line 156: lat
  ```
  !  parameters, just copy latest_param_guess to the initial file.
  ```
- Line 157: lat
  ```
  !  This is because latest_param_guess is overwritten each iter
  ```
- Line 171: loc
  ```
  allocate(param0(np))
  ```
- Line 216: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 339: lat
  ```
  ! now calculate other aux params, (note, used to call this 'sim' moments or 'calibration' moments,
  ```
- Line 340: name
  ```
  !   but that was not a good name)
  ```
- Line 341: city, district
  ```
  !   i.e. teacher chars by district quintiles, new hires, capacity, movers
  ```
- Line 346: second
  ```
  !second, calc the moms
  ```
- Line 354: lat
  ```
  ! generates quintiles (global vars) which are helpful in calculating moments
  ```
- Line 361: district
  ```
  !assign districts to quintiles
  ```
- Line 368: district, second
  ```
  !sort on first column of matrix, second column is district index number
  ```
- Line 373: district
  ```
  !quintile 1-4 has 82 districts, 5 has 83 (5*82=410, but we have 411 districts)
  ```
- Line 386: district
  ```
  ! add the last district to quintile 5. This is matching our stata code.
  ```
- Line 398: city
  ```
  integer, parameter :: nauxd = 4 !intercept budget capacity share_low
  ```
- Line 488: name
  ```
  !skip first line b/c it is varname
  ```
- Line 499: city
  ```
  !weights for fraction over/under capacity
  ```
- Line 521: loc
  ```
  !omega12 variances are located in same moments file, not separate '_var.csv'
  ```
- Line 629: district
  ```
  ! 1.  Matches   (district t+1)
  ```
- Line 630: district
  ```
  ! 2.  Incumbent (district t)
  ```
- Line 817: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 824: lat, second
  ```
  !    second we can calculate
  ```
- Line 828: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 837: district
  ```
  ! need tc for a district match to be > 0
  ```
- Line 840: district
  ```
  ! if all districts are doing bonuses, then we just need some match
  ```
- Line 845: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 854: district
  ```
  ! if all districts are doing bonuses, then if C1 is positive for a
  ```
- Line 859: district
  ```
  print*, "logic isn't set up right now for having some districts unsubsidized"
  ```
- Line 912: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/match_pre_NEW.csv', status = 'old')
  ```
- Line 917: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/incumbent_pre_NEW.csv', status = 'old')
  ```
- Line 927: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/samecz_pre_NEW.csv', status = 'old')
  ```
- Line 932: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/master_pre_NEW.csv', status = 'old')
  ```
- Line 937: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experience_pre_NEW.csv', status = 'old')
  ```
- Line 957: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/experiencecont_pre_NEW.csv', status = 'old')
  ```
- Line 968: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/contributions_pre_NEW.csv', status = 'old')
  ```
- Line 986: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lambda_pre_NEW.csv', status = 'old')
  ```
- Line 991: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/urban_pre_NEW.csv', status = 'old')
  ```
- Line 996: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/suburban_pre_NEW.csv', status = 'old')
  ```
- Line 1005: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha1.csv', status = 'old')
  ```
- Line 1009: lat
  ```
  !open(1, file = trim(filepath)//'/simulation_data/alpha2.csv', status = 'old')
  ```
- Line 1013: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/wageschedule_pre_NEW.csv', status = 'old')
  ```
- Line 1039: lat
  ```
  open(1, file = trim(filepath)//'/simulation_data/lndist_pre.csv', status = 'old')
  ```
- Line 1064: district
  ```
  !    First need to get cz ids, so know which districts are in the same cz.
  ```
- Line 1071: lat, second
  ```
  !    second we can calculate
  ```
- Line 1075: lat
  ```
  !for dist j, calculate avg share tenured among others in their cz
  ```
- Line 1083: lat
  ```
  !calculate median of a vector
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/simplex_salvador.f90**

- Line 7: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 69: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 71: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 74: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 83: loc
  ```
  call system_clock(count=tout)
  ```
- Line 90: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 91: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/simplex_salvador_equilibrium.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 113: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 119: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 122: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 131: loc
  ```
  call system_clock(count=tout)
  ```
- Line 138: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 139: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/simplex_salvador_equilibrium_w.f90**

- Line 10: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/simplex_salvador_inner.f90**

- Line 11: loc
  ```
  use nrutil, only : assert_eq,imaxloc,iminloc,nrerror,swap
  ```
- Line 82: loc
  ```
  ilo=iminloc(y(:))
  ```
- Line 88: loc
  ```
  ihi=imaxloc(y(:))
  ```
- Line 91: loc
  ```
  inhi=imaxloc(y(:))
  ```
- Line 100: loc
  ```
  call system_clock(count=tout)
  ```
- Line 107: loc
  ```
  call system_clock(count_rate=hz)
  ```
- Line 108: loc
  ```
  call system_clock(count=tin)
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/statistical_models.f90**

- Line 42: lat
  ```
  ! Returns the inverse of a matrix calculated by finding the LU
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/timer.f90**

- Line 5: loc
  ```
  integer :: cr, cm, clock1, clock2
  ```
- Line 16: loc
  ```
  !! First initialize the system_clock
  ```
- Line 17: loc
  ```
  !CALL system_clock(count_rate=cr)
  ```
- Line 18: loc
  ```
  !CALL system_clock(count_max=cm)
  ```
- Line 20: loc
  ```
  !if (mpi_id ==0) WRITE(*,*) "system_clock rate ",rate
  ```
- Line 22: loc
  ```
  !CALL SYSTEM_CLOCK(clock1)
  ```
- Line 36: loc
  ```
  !! system clock timer
  ```
- Line 37: loc
  ```
  !CALL SYSTEM_CLOCK(clock2)
  ```
- Line 39: loc
  ```
  !if (mpi_id ==0) print*, (clock2 - clock1)/rate, num
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/utility_functions.f90**

- Line 4: district, school
  ```
  ! Utility functions for both school districts and teachers
  ```
- Line 13: district
  ```
  public :: utility_teacher, utility_teacher_eqm, utility_district, Rcost
  ```
- Line 131: lon
  ```
  if (abs(w1s-1) < epsilon(0d0) .and. abs(w2s-0) < epsilon(0d0)) then
  ```

**/replication-package/replication_delivery/code/fortran_model/validation_result/write_moments.f90**

- Line 15: lat
  ```
  public write_best_param, write_latest_theta, write_all_aux_params, write_moment_weight_factors
  ```
- Line 26: lat
  ```
  double precision :: latestval
  ```
- Line 37: district
  ```
  param_desc(6)  = '  alpha 6,  same district * exp1   '
  ```
- Line 38: district
  ```
  param_desc(7)  = '  alpha 7,  same district * exp2   '
  ```
- Line 39: district
  ```
  param_desc(8)  = '  alpha 8,  same district * exp3   '
  ```
- Line 40: district
  ```
  param_desc(9)  = '  alpha 9,  same district * exp4   '
  ```
- Line 41: district
  ```
  param_desc(10) = '  alpha 10, same district * exp5   '
  ```
- Line 42: district
  ```
  param_desc(11) = '  alpha 11, same district * exp6   '
  ```
- Line 43: district
  ```
  param_desc(12) = '  alpha 12, same district * logdist'
  ```
- Line 78: lat
  ```
  !latestvalue is akin to the 'initials' which are a transformation, and what is read into the program
  ```
- Line 80: lat
  ```
  open(1, file = 'latest_param_guess.txt', status = 'replace')
  ```
- Line 87: lat
  ```
  latestval = initial_param(i) * step(i) * param0_in(n)
  ```
- Line 89: lat
  ```
  latestval = initial_param(i)
  ```
- Line 91: lat
  ```
  write(1,format2) incl(i), latestval, step(i), best_param2(i), n, param_desc(i)
  ```
- Line 99: lat
  ```
  !also save our latest belief params for this best guess
  ```
- Line 100: lat
  ```
  call write_latest_theta()
  ```
- Line 105: lat
  ```
  subroutine write_latest_theta()
  ```
- Line 111: lat
  ```
  open(1, file = 'latest_theta.txt', status = 'replace')
  ```
- Line 122: lat
  ```
  end subroutine write_latest_theta
  ```
- Line 197: city
  ```
  write(1,f2) aux_omega(10,1), aux_omega_model(10,1), 'capacity      '
  ```
- Line 216: city
  ```
  write(1,f2) aux_omega(27,1), aux_omega_model(27,1), 'capacity      '
  ```
- Line 255: city
  ```
  aux_beta(36,1), aux_beta_model(36,1), aux_beta(40,1), aux_beta_model(40,1), 'Capacity Left'
  ```
- Line 265: city
  ```
  aux_beta(52,1), aux_beta_model(52,1), aux_beta(56,1), aux_beta_model(56,1), 'Capacity Left'
  ```
- Line 275: city
  ```
  aux_beta(68,1), aux_beta_model(68,1), aux_beta(72,1), aux_beta_model(72,1), 'Capacity Left'
  ```
- Line 298: city
  ```
  integer, dimension(ndist) :: excess_capacity
  ```
- Line 352: son
  ```
  !print all teachers moment comparison to text file
  ```
- Line 385: district
  ```
  ! *** % of districts with only tenured incumbents ***
  ```
- Line 387: district
  ```
  !now look between districts that have more slots than incumbents
  ```
- Line 388: city
  ```
  excess_capacity = 0
  ```
- Line 391: city
  ```
  excess_capacity(j) = 1
  ```
- Line 397: district
  ```
  !   write(2,*) '**** Fraction of districts who only employ tenured incumbents ****'
  ```
- Line 399: district
  ```
  !   write(2,*) 'Across all districts'
  ```
- Line 432: city, district
  ```
  !   write(2,*) '  Note: ',sum(excess_capacity)/real(ndist)*100,'% of districts have slots > tenured-
  ```
- Line 435: city
  ```
  mom11desc(1) = 'new hires/capacity'
  ```
- Line 442: district
  ```
  write(2,*) '**** New Hires (including only districts with new hires) ****'
  ```
- Line 450: city, district
  ```
  ! adding information on over/under capacity for districts
  ```
- Line 452: city, district
  ```
  !write(2,*) '**** Fraction of districts over/under capacity in MODEL ****'
  ```
- Line 455: city
  ```
  !write(2,f6) 'capacity diff > 2       ', mom12_model(1,1), mom12_model(1,2)
  ```
- Line 456: city
  ```
  !write(2,f6) 'capacity diff > 20%     ', mom12_model(2,1), mom12_model(2,2)
  ```
- Line 513: city
  ```
  desc_aux(28)        = '       Non-ten, E(exp), Capacity Left             '
  ```
- Line 517: city
  ```
  desc_aux(32)        = '       Non-ten, E(master), Capacity Left          '
  ```
- Line 521: city
  ```
  desc_aux(36)        = '       Non-ten, E(C1), Capacity Left              '
  ```
- Line 525: city
  ```
  desc_aux(40)        = '       Non-ten, E(C2), Capacity Left              '
  ```
- Line 529: city
  ```
  desc_aux(44)        = '       Bottom-ten, E(exp), Capacity Left          '
  ```
- Line 533: city
  ```
  desc_aux(48)        = '       Bottom-ten, E(master), Capacity Left       '
  ```
- Line 537: city
  ```
  desc_aux(52)        = '       Bottom-ten, E(C1), Capacity Left           '
  ```
- Line 541: city
  ```
  desc_aux(56)        = '       Bottom-ten, E(C2), Capacity Left           '
  ```
- Line 545: city
  ```
  desc_aux(60)        = '       All teach, E(exp), Capacity Left           '
  ```
- Line 549: city
  ```
  desc_aux(64)        = '       All teach, E(master), Capacity Left        '
  ```
- Line 553: city
  ```
  desc_aux(68)        = '       All teach, E(C1), Capacity Left            '
  ```
- Line 557: city
  ```
  desc_aux(72)        = '       All teach, E(C2), Capacity Left            '
  ```
- Line 570: city
  ```
  desc_aux_omega(10)  = '       omega1 reg, capacity                       '
  ```
- Line 587: city
  ```
  desc_aux_omega(27)  = '       omega2 reg, capacity                       '
  ```
- Line 638: city
  ```
  desc_aux_other(21)  = '       budget/capacity (low):  exp                '
  ```
- Line 639: city
  ```
  desc_aux_other(22)  = '       budget/capacity:        exp                '
  ```
- Line 640: city
  ```
  desc_aux_other(23)  = '       budget/capacity:        exp                '
  ```
- Line 641: city
  ```
  desc_aux_other(24)  = '       budget/capacity:        exp                '
  ```
- Line 642: city
  ```
  desc_aux_other(25)  = '       budget/capacity (high): exp                '
  ```
- Line 643: city
  ```
  desc_aux_other(26)  = '       budget/capacity (low):  v1                 '
  ```
- Line 644: city
  ```
  desc_aux_other(27)  = '       budget/capacity:        v1                 '
  ```
- Line 645: city
  ```
  desc_aux_other(28)  = '       budget/capacity:        v1                 '
  ```
- Line 646: city
  ```
  desc_aux_other(29)  = '       budget/capacity:        v1                 '
  ```
- Line 647: city
  ```
  desc_aux_other(30)  = '       budget/capacity (high): v1                 '
  ```
- Line 648: city
  ```
  desc_aux_other(31)  = '       budget/capacity (low):  v2                 '
  ```
- Line 649: city
  ```
  desc_aux_other(32)  = '       budget/capacity:        v2                 '
  ```
- Line 650: city
  ```
  desc_aux_other(33)  = '       budget/capacity:        v2                 '
  ```
- Line 651: city
  ```
  desc_aux_other(34)  = '       budget/capacity:        v2                 '
  ```
- Line 652: city
  ```
  desc_aux_other(35)  = '       budget/capacity (high): v2                 '
  ```
- Line 653: city
  ```
  desc_aux_other(36)  = '       budget/capacity (low):  masters            '
  ```
- Line 654: city
  ```
  desc_aux_other(37)  = '       budget/capacity:        masters            '
  ```
- Line 655: city
  ```
  desc_aux_other(38)  = '       budget/capacity:        masters            '
  ```
- Line 656: city
  ```
  desc_aux_other(39)  = '       budget/capacity:        masters            '
  ```
- Line 657: city
  ```
  desc_aux_other(40)  = '       budget/capacity (high): masters            '
  ```
- Line 680: city
  ```
  desc_aux_other(63)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 681: city
  ```
  desc_aux_other(64)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 682: city
  ```
  desc_aux_other(65)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 683: city
  ```
  desc_aux_other(66)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 684: city
  ```
  desc_aux_other(67)  = '       fraction empl only ten-inc: budget/capacity'
  ```
- Line 691: city
  ```
  desc_aux_other(74)  = '       dist w/ new hires: new/capacity            '
  ```
- Line 696: city
  ```
  desc_aux_other(79)  = '       Fraction >2   over capacity                '
  ```
- Line 697: city
  ```
  desc_aux_other(80)  = '       Fraction >20% over capacity                '
  ```
- Line 698: city
  ```
  desc_aux_other(81)  = '       Fraction >2   under capacity               '
  ```
- Line 699: city
  ```
  desc_aux_other(82)  = '       Fraction >20% under capacity               '
  ```

**/replication-package/replication_delivery/code/master.do**

- Line 3: school
  ```
  *			Equilibrium in the Market for Public School Teachers	(Biasi, Fu, Stromme JPE)			*
  ```
- Line 25: loc
  ```
  * Locate the replication root
  ```
- Line 30: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 36: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 41: loc
  ```
  display as error "Could not locate the replication root."
  ```
- Line 53: lat
  ```
  global sim = "$rootpath/simulation_data/"
  ```
- Line 76: lon
  ```
  + va_teacherside_math.dta - contains list of teachers and grade assignments, in long form
  ```
- Line 77: lat
  ```
  + va_highlow_math_expdisc.dta - contains main teacher contribution measures. Constructed in valueadd
  ```
- Line 78: lat
  ```
  + va_nocomparativeadv.dta - contains teacher contribution w/o comparative advantage. Constructed in 
  ```
- Line 80: district
  ```
  + alphagrid_8groups_lambdad.dta - generates the omegas for each district. Constructed in gen_omegas.
  ```
- Line 89: lon
  ```
  + alphagrid_12.dta, budget_past_long.dta, budget_past.dta, budget_long.dta, budget.dta, info_all.dta
  ```
- Line 91: lat
  ```
  *---> Simulation data
  ```
- Line 92: lon
  ```
  + budget_past_long_pre_NEW.dta, budget_past_pre_NEW.dta, budget_long_pre_NEW.dta, budget_NEW.dta, in
  ```
- Line 114: lat
  ```
  do $code/build/valueadded_calculation.do // Model 1
  ```
- Line 115: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do // Model 2
  ```
- Line 116: lat
  ```
  do $code/build/valueadded_calculation_stockstaiger_nocomparativeadv.do // No comparative advantage
  ```
- Line 129: district
  ```
  do $code/auxiliary_regressions/auxiliary_models_districts.do
  ```
- Line 143: lat
  ```
  * Generate model simulation data files (2011)
  ```
- Line 145: lat
  ```
  do $code/build/build_simulation_lndist.do
  ```
- Line 178: lat
  ```
  //do $code/build/valueadded_calculation.do
  ```
- Line 187: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do
  ```
- Line 221: lat
  ```
  do $code/descriptives/summary_simulation.do
  ```

**/replication-package/replication_delivery/code/master_synth.do**

- Line 3: school
  ```
  *			Equilibrium in the Market for Public School Teachers	(Biasi, Fu, Stromme JPE)			*
  ```
- Line 29: loc
  ```
  * Locate the replication root
  ```
- Line 34: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 40: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 45: loc
  ```
  display as error "Could not locate the replication root."
  ```
- Line 57: lat
  ```
  global sim = "$rootpath/simulation_data/"
  ```
- Line 88: lon
  ```
  + va_teacherside_math.dta - contains list of teachers and grade assignments, in long form
  ```
- Line 89: lat
  ```
  + va_highlow_math_expdisc.dta - contains main teacher contribution measures. Constructed in valueadd
  ```
- Line 90: lat
  ```
  + va_nocomparativeadv.dta - contains teacher contribution w/o comparative advantage. Constructed in 
  ```
- Line 92: district
  ```
  + alphagrid_8groups_lambdad.dta - generates the omegas for each district. Constructed in gen_omegas.
  ```
- Line 101: lon
  ```
  + alphagrid_12.dta, budget_past_long.dta, budget_past.dta, budget_long.dta, budget.dta, info_all.dta
  ```
- Line 103: lat
  ```
  *---> Simulation data
  ```
- Line 104: lon
  ```
  + budget_past_long_pre_NEW.dta, budget_past_pre_NEW.dta, budget_long_pre_NEW.dta, budget_NEW.dta, in
  ```
- Line 126: lat
  ```
  do $code/build/valueadded_calculation.do // Model 1
  ```
- Line 127: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do // Model 2
  ```
- Line 128: lat
  ```
  do $code/build/valueadded_calculation_stockstaiger_nocomparativeadv.do // No comparative advantage
  ```
- Line 143: district
  ```
  do $code/auxiliary_regressions/auxiliary_models_districts.do
  ```
- Line 157: lat
  ```
  * Generate model simulation data files (2011)
  ```
- Line 159: lat
  ```
  do $code/build/build_simulation_lndist.do
  ```
- Line 192: lat
  ```
  //do $code/build/valueadded_calculation.do
  ```
- Line 201: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do
  ```
- Line 235: lat
  ```
  do $code/descriptives/summary_simulation.do
  ```
- Line 274: loc
  ```
  local synth_logfiles : dir "$out/tables" files "*.log"
  ```
- Line 275: loc
  ```
  foreach synth_log of local synth_logfiles {
  ```
- Line 276: loc
  ```
  local clean_synth_log "$out/tables/__clean_`synth_log'"
  ```
- Line 281: loc
  ```
  local skip_log_path_continuation = 0
  ```
- Line 284: loc
  ```
  local normalized_line = ustrregexra(`"`macval(line)'"', ///
  ```
- Line 286: loc
  ```
  local write_synth_line = 1
  ```
- Line 289: loc
  ```
  local write_synth_line = 0
  ```
- Line 291: loc
  ```
  local skip_log_path_continuation = 0
  ```
- Line 297: loc
  ```
  local skip_log_path_continuation = 1
  ```

**/replication-package/replication_delivery/code/produce_exhibits.do**

- Line 3: school
  ```
  *			Equilibrium in the Market for Public School Teachers	(Biasi, Fu, Stromme JPE)			*
  ```
- Line 17: loc
  ```
  * Locate the replication root
  ```
- Line 22: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 28: loc
  ```
  local launch_dir `"`c(pwd)'"'
  ```
- Line 33: loc
  ```
  display as error "Could not locate the replication root."
  ```
- Line 47: lat
  ```
  * Generating Exhibits That Rely on Generated model_data & simulation_data
  ```
- Line 75: district
  ```
  * Table A2 (Model Fit: OLS of Teacher-District Match)
  ```
- Line 78: district
  ```
  * Table A3 (Model Fit: District Wage Schedules)
  ```
- Line 85: loc
  ```
  * Table B15 (Allocating Teachers at Will)
  ```
- Line 103: district
  ```
  * Table B22 (Model Fit: Avg District Employee Characteristics)
  ```
- Line 106: district
  ```
  * Table B23 (Model Fit: OLS of District Wage Schedule)
  ```
- Line 109: district
  ```
  * Table B24 (Pre-Act 10 Model Validation: OLS of Teacher-District Match)
  ```
- Line 112: district
  ```
  * Table B25 (Pre-Act 10 Model Validation: Avg District Employee Characteristics)
  ```

**/replication-package/replication_delivery/out_synth/tables/Table_B3.tex**

- Line 12: school
  ```
  School-by-year FE   &         Yes         &         Yes         &         Yes         &         Yes 
  ```
- Line 15: school
  ```
  \# school-grades    &          38         &          39         &          38         &          39 
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all1_JPE.tex**

- Line 11: city
  ```
  capacity            &     -0.0276         &     -0.2164         &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all2_JPE.tex**

- Line 11: city
  ```
  capacity            &      0.0000         &      0.0000\sym{**} &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all3_JPE.tex**

- Line 11: city
  ```
  capacity            &      0.0000         &      0.0000\sym{**} &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all_mean1_JPE.tex**

- Line 11: city
  ```
  capacity            &      0.0498         &     -0.1761         &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all_mean2_JPE.tex**

- Line 11: city
  ```
  capacity            &     -0.0000         &     -0.0000\sym{*}  &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_all_mean3_JPE.tex**

- Line 11: city
  ```
  capacity            &     -0.0000         &     -0.0000\sym{**} &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_noninc1_JPE.tex**

- Line 11: city
  ```
  free capacity       &      0.2964\sym{***}&      0.3821         &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_noninc2_JPE.tex**

- Line 11: city
  ```
  free capacity       &     -0.0000\sym{*}  &     -0.0000\sym{**} &                     &             
  ```

**/replication-package/replication_delivery/out_synth/tables/aux_district_noninc3_JPE.tex**

- Line 11: city
  ```
  free capacity       &     -0.0000\sym{**} &     -0.0000\sym{**} &                     &             
  ```

**/replication-package/replication_delivery/paper_appendix/Draft_final.tex**

- Line 17: lat
  ```
  %TCIDATA{OutputFilter=latex2.dll}
  ```
- Line 20: lat
  ```
  %TCIDATA{CSTFile=40 LaTeX article.cst}
  ```
- Line 23: name
  ```
  %TCIDATA{<META NAME="GraphicsSave" CONTENT="32">}
  ```
- Line 24: name
  ```
  %TCIDATA{<META NAME="SaveForMode" CONTENT="3">}
  ```
- Line 26: lat, name
  ```
  %TCIDATA{<META NAME="DocumentShell" CONTENT="Standard LaTeX\Blank - Standard LaTeX Article">}
  ```
- Line 76: district, school
  ```
  \title{Equilibrium in the Market for Public School Teachers: District Wage Strategies
  ```
- Line 81: lon
  ```
  Barcelona GSE Summer Forum, and International Symposium on Labor Economics
  ```
- Line 83: school
  ```
  \author{Barbara Biasi, Chao Fu and John Stromme\thanks{Biasi: Yale School of
  ```
- Line 91: district, school
  ```
  We study the equity-efficiency implication of giving school districts control
  ```
- Line 94: district, school
  ```
  high-achieving students. School districts, which serve different student
  ```
- Line 97: district
  ```
  districts gained control over teacher pay in 2011. We find that, all else
  ```
- Line 98: district
  ```
  equal, giving districts control over teacher pay would lead to more efficient
  ```
- Line 99: district
  ```
  teacher-district sorting but larger educational inequality. Teacher bonus
  ```
- Line 101: district
  ```
  bonus rates favoring districts with more low-achieving students, could improve
  ```
- Line 125: lat
  ```
  some teachers may be better at stimulating high-achieving students, while
  ```
- Line 130: school
  ```
  However, the typical salary structure for U.S. public school teachers
  ```
- Line 134: district
  ```
  rigid-pay\textquotedblright\ regime. Under rigid pay, districts cannot use
  ```
- Line 136: district
  ```
  the substantial cross-district variation in students'
  ```
- Line 137: district
  ```
  preparedness.\footnote{For example, in 2021, the share of a district's 5th
  ```
- Line 139: district
  ```
  10-80\% across districts in Wisconsin, 10-85\% in California, and 10-90\% in
  ```
- Line 140: district
  ```
  Texas \citep{eddataexpress}.} Associated with rigid pay, teachers deemed better by various measures 
  ```
- Line 145: district
  ```
  one where districts have the flexibility to design their own teacher pay
  ```
- Line 147: district
  ```
  which districts can choose their own teacher pay schemes; it does not
  ```
- Line 148: district
  ```
  necessarily mean that all districts will choose to reward teacher
  ```
- Line 151: district, loc, location
  ```
  teacher-district allocations is not obvious ex ante: It depends on how
  ```
- Line 152: district
  ```
  districts would choose teacher pay if allowed to do so and how both sides of
  ```
- Line 154: district
  ```
  These choices and interactions, in turn, depend on teachers' and districts'
  ```
- Line 156: district
  ```
  similar and rigid pay schedules are often imposed on all districts within a
  ```
- Line 161: district
  ```
  teacher salaries and gave districts full autonomy over teacher pay. Using
  ```
- Line 162: lat
  ```
  post-Act 10 Wisconsin as a platform, we study the implications of flexible pay
  ```
- Line 165: lat
  ```
  high-achieving students. The correlation between the two VA measures is 0.67,
  ```
- Line 169: district
  ```
  teacher cares about their wage and the characteristics of the district they
  ```
- Line 170: district
  ```
  work in, including its student composition. A district cares about a teacher's
  ```
- Line 172: district
  ```
  a teacher's experience and education. Given its budget, the goal of a district
  ```
- Line 173: city
  ```
  is to fill its capacity with teachers it prefers the most by setting a wage
  ```
- Line 175: district
  ```
  rewarded for their contribution to the achievement of the district's students,
  ```
- Line 176: district
  ```
  and for their experience and education. Districts simultaneously make wage and
  ```
- Line 180: district
  ```
  district, net of moving costs.\footnote{We focus on teachers' job choices and
  ```
- Line 182: district
  ```
  districts' beliefs to be consistent with decisions by all districts and teachers.
  ```
- Line 185: district
  ```
  the one hand, given that student bodies differ across districts and teachers
  ```
- Line 187: district
  ```
  teacher-district sorting is not necessarily a zero-sum game. Giving districts
  ```
- Line 190: district
  ```
  hand, districts make choices to maximize their own objectives. With teacher
  ```
- Line 191: district
  ```
  pay at their disposal, advantaged districts may find it even easier to attract
  ```
- Line 193: district
  ```
  would weaken comparative advantage-based sorting and exacerbate cross-district
  ```
- Line 194: second
  ```
  inequality. When this second force is strong, policy interventions favoring
  ```
- Line 195: district
  ```
  disadvantaged districts can be justified on grounds of both equity \emph{and }efficiency.
  ```
- Line 201: district
  ```
  preferences (which govern districts' hiring decisions and, if given the
  ```
- Line 203: district
  ```
  argument is as follows: First, under Act 10, districts have control over
  ```
- Line 204: district
  ```
  teacher pay; the extent to which a district's wage schedule favors different
  ```
- Line 205: second
  ```
  teachers contains information about its preferences. Second,\ with the mild
  ```
- Line 206: district
  ```
  assumption that district preferences for teachers are weakly increasing in
  ```
- Line 212: district
  ```
  teacher preferences. Given teachers' preferences, districts' preference
  ```
- Line 213: district
  ```
  parameters, which govern districts' offer decisions, must rationalize the
  ```
- Line 214: district
  ```
  realized teacher-district matches.
  ```
- Line 218: district
  ```
  and district level. The data allow us to track a teacher's employment history
  ```
- Line 219: school
  ```
  within the state's public school system, including their salaries and job
  ```
- Line 220: lat
  ```
  characteristics. We use post-Act 10 data to estimate the model. With the estimated parameters, we si
  ```
- Line 224: district
  ```
  districts control over teacher pay. Compared to the rigid-pay equilibrium,
  ```
- Line 226: district
  ```
  features more efficient teacher-district matching, with a 0.04\% improvement
  ```
- Line 229: district
  ```
  districts with high fractions of low-achieving students.
  ```
- Line 238: district
  ```
  when their comparative advantage better matches a district's student
  ```
- Line 239: second
  ```
  composition. The second component rewards a teacher additionally for their
  ```
- Line 241: district
  ```
  good at teaching low-achieving students to teach in districts with more
  ```
- Line 242: loc, location
  ```
  students of this type, incentivizing a more equitable allocation. The third
  ```
- Line 243: district
  ```
  component ties the bonus to a district's wage schedule such that districts are
  ```
- Line 247: lat
  ```
  At a relatively mild cost compared to programs implemented in other states, we
  ```
- Line 250: district
  ```
  equilibrium, we find that programs that favor districts with more
  ```
- Line 262: lat
  ```
  \paragraph{Related Literature}
  ```
- Line 264: loc, location
  ```
  A proper allocation of public servants across local employers can have
  ```
- Line 265: social
  ```
  important implications for both efficiency and equity. In reality, a socially
  ```
- Line 266: loc, location
  ```
  optimal allocation is hampered by various institutional
  ```
- Line 269: school
  ```
  of the labor market for public school teachers, our paper contributes toward a
  ```
- Line 271: loc, location
  ```
  institutional friction---impacts the allocation of workers to employers and
  ```
- Line 280: son
  ```
  efficiency is part of the reason why many OECD countries have been
  ```
- Line 281: lat
  ```
  increasingly introducing performance-related pay for government employees \citep{cardona2006performa
  ```
- Line 291: school
  ```
  down the teaching option into teaching in one of three types of schools. Using
  ```
- Line 294: school
  ```
  study teachers' preferences for schools and find that teachers prefer schools
  ```
- Line 299: school
  ```
  \citet{ederer2023labor} study teacher-school sorting in a centralized
  ```
- Line 300: school
  ```
  application-assignment environment. Assuming that the observed teacher-school
  ```
- Line 302: school
  ```
  model to disentangle teacher and school preferences. While
  ```
- Line 303: district
  ```
  \citet{boyd2013analyzing} study a context with rigid pay, districts in our
  ```
- Line 305: district
  ```
  competition among districts, which choose both wage and hiring strategies.
  ```
- Line 307: school
  ```
  representative private school sets teacher wages and tuition; workers choose
  ```
- Line 308: school
  ```
  among teaching in the public school (which is passive in her model), teaching
  ```
- Line 309: house, school
  ```
  in the private school, and non-teaching; and households choose between public
  ```
- Line 310: school
  ```
  and private schools. Our paper and \citet{tincani2020teacher} well complement
  ```
- Line 313: house
  ```
  affect teachers' and households' choices between public and private sectors.
  ```
- Line 315: district, school
  ```
  study how public school districts use wage and hiring strategies to compete
  ```
- Line 320: son
  ```
  reviews]{neal2011design,jackson2014teacher}, and more specifically on
  ```
- Line 323: school
  ```
  retain teachers in disadvantaged schools
  ```
- Line 330: lat
  ```
  find that teacher mobility is more related to student composition than salary,
  ```
- Line 332: lat
  ```
  teachers' labor market is also mixed. \citet{burgess2022deregulating} find
  ```
- Line 333: school
  ```
  that schools became better at retaining their teachers after a reform that
  ```
- Line 334: school
  ```
  compelled schools in England to replace centralized pay scales with a
  ```
- Line 335: school
  ```
  school-level performance pay regime. A similar reform in Sweden, which
  ```
- Line 339: district
  ```
  Wisconsin implemented Act 10, higher-quality teachers moved to districts that
  ```
- Line 344: district
  ```
  we develop and estimate an equilibrium model to understand districts' and
  ```
- Line 346: district
  ```
  counterfactual policies affect districts' wage and hiring decisions and
  ```
- Line 347: district
  ```
  equilibrium teacher-district matches.
  ```
- Line 349: lat
  ```
  An innovation of our study relative to the works mentioned above is that we
  ```
- Line 351: district
  ```
  of students, which leaves open the possibility that changing teacher-district
  ```
- Line 354: son
  ```
  student composition. For example, \citet{jackson2013match} demonstrates the
  ```
- Line 355: school
  ```
  importance of match quality between teachers and schools.
  ```
- Line 360: lon, son
  ```
  teacher effectiveness by race \citep{dee2004teachers,gershenson2022long} and
  ```
- Line 363: district, loc, location, school
  ```
  study teacher-school allocation within a district, allowing teacher valued
  ```
- Line 365: school
  ```
  data on teacher and school decisions (application, interview, offer, and
  ```
- Line 367: school
  ```
  model student achievement and teacher and school decisions, accounting for
  ```
- Line 370: school
  ```
  students test scores by reassigning teachers across schools. In contrast, we
  ```
- Line 372: district
  ```
  induce more efficient teacher-district sorting in a market equilibrium setting
  ```
- Line 383: district, school
  ```
  Most US public school districts pay teachers according to \textquotedblleft
  ```
- Line 386: lon
  ```
  Movements along the \textquotedblleft steps\textquotedblright\ (experience
  ```
- Line 387: degree
  ```
  levels) and \textquotedblleft lanes\textquotedblright\ (education degrees) of
  ```
- Line 391: district, school
  ```
  school districts and teachers' unions. CB agreements usually prevent districts
  ```
- Line 396: school
  ```
  gained considerable power and have been involved in negotiations with school
  ```
- Line 397: district
  ```
  districts over key aspects of a teaching job. Until 2011, unions negotiated
  ```
- Line 398: district
  ```
  all teacher salary schedules, which were included in each district's CB agreement.
  ```
- Line 402: lat
  ```
  legislature, aimed at reducing benefits for public-sector employees and
  ```
- Line 412: school
  ```
  bargaining. For public school teachers, who account for approximately 45\% of
  ```
- Line 414: district
  ```
  salary negotiations between districts and unions up to the base pay (i.e., the
  ```
- Line 417: lat
  ```
  also capped to the rate of inflation. Above and beyond base salaries, Act 10
  ```
- Line 418: district, school
  ```
  gave school districts ample flexibility to design teacher pay. For example,
  ```
- Line 419: district
  ```
  the 2015 employee handbook of the Mequon-Thiensville District states that
  ```
- Line 420: district
  ```
  \textquotedblleft The District, in its sole discretion, may place employees at
  ```
- Line 426: son
  ```
  for law enforcement and fire department personnel, through an increase in
  ```
- Line 428: district, school
  ```
  2011 the state also passed Act 32, which reduced state aid to school districts
  ```
- Line 429: district
  ```
  and decreased districts' revenue limits (the maximum revenue a district can
  ```
- Line 430: loc
  ```
  raise through general state aid and local property taxes).}
  ```
- Line 433: school
  ```
  public school teachers through multiple provisions. Most of these provisions,
  ```
- Line 435: district, school
  ```
  among school districts. Changes in pay schedules, instead, occurred at the
  ```
- Line 436: district
  ```
  discretion of each district. The model we develop in Section 3 will allow us
  ```
- Line 437: lat
  ```
  to isolate the equilibrium effect of changes in pay regimes on the teachers'
  ```
- Line 443: district
  ```
  linked data sets at the teacher, student, and district level, respectively. We
  ```
- Line 450: district
  ```
  of wages on district-by-year and seniority-by-education fixed effects. Figure
  ```
- Line 451: district
  ```
  \ref{fig:cvar} shows that, prior to Act 10, teacher wage variation was rather small within each dist
  ```
- Line 452: district
  ```
  education. After Act 10, wage variation increased as districts gained control
  ```
- Line 468: district
  ```
  \ref{fig:moves} shows that movements of teachers across districts are rare,
  ```
- Line 469: district
  ```
  but their frequency, i.e., the fraction of teachers employed in a district
  ```
- Line 479: district
  ```
  \caption{Rates of Teacher Movements Across Districts}%
  ```
- Line 485: district
  ```
  changing district on year fixed effects, clustering standard errors at the
  ```
- Line 486: district
  ```
  district level. The right panel shows estimates and confidence intervals of
  ```
- Line 488: district
  ```
  teacher fixed effects and we cluster standard errors at the district level; we
  ```
- Line 493: district
  ```
  \noindent\textbf{Teacher-District Sorting:} Prior to Act 10, teachers with
  ```
- Line 495: district
  ```
  were significantly less likely to work in districts with a larger share of
  ```
- Line 497: district, lat
  ```
  average teacher experience of each district was negatively correlated with the
  ```
- Line 500: district, lat
  ```
  district. These relationships became much weaker after Act 10. Before Act 10,
  ```
- Line 501: lat
  ```
  there was barely any (positive) correlation between average teacher experience
  ```
- Line 502: district, lat
  ```
  of each district and the district's budget; after Act 10, this correlation
  ```
- Line 504: district
  ```
  hand, districts\ with larger budgets found it even easier to attract teachers
  ```
- Line 511: lat
  ```
  \includegraphics[width=\textwidth]{figures/sorting_correlations_lambda_overtime.png}
  ```
- Line 512: district, lat
  ```
  \caption*{\footnotesize Notes: Year-specific correlations between district
  ```
- Line 513: district
  ```
  characteristics and the average experience of teachers in the district.
  ```
- Line 514: district, lat
  ```
  Correlations are obtained weighting districts by student enrollment.}%
  ```
- Line 519: district
  ```
  districts used wage strategies to compete for teachers and teacher-district
  ```
- Line 521: district
  ```
  versus post-Act 10 differences as the effect of giving districts control over
  ```
- Line 523: lat
  ```
  eras. To isolate the equilibrium impact of replacing rigid pay with flexible
  ```
- Line 529: school
  ```
  We model a static equilibrium in the market for public school teachers, with a
  ```
- Line 530: district, school
  ```
  distribution of teachers and $D$ school districts. Districts compete for their
  ```
- Line 538: district
  ```
  high-achieving students; $d_{0}$ is the district one works in at the beginning
  ```
- Line 544: district
  ```
  \newline\textbf{Districts: }District $d$ is
  ```
- Line 546: district
  ```
  $q_{d}$ is a vector of district characteristics, $\lambda_{d}$ is the fraction
  ```
- Line 548: city, district
  ```
  state median), $\kappa_{d}$ is district $d$'s capacity (number of teaching
  ```
- Line 549: district
  ```
  slots), and $M_{d}$ is its budget. The sum of slots across districts $\sum
  ```
- Line 552: district
  ```
  in district $d$\textbf{ }is given by\
  ```
- Line 557: district
  ```
  which, for the same teacher, varies across districts with student composition
  ```
- Line 559: district
  ```
  follows:\newline1. Districts simultaneously choose their wage schedules
  ```
- Line 568: district
  ```
  district may want to pay incumbent teachers less than non-incumbent teachers
  ```
- Line 580: lon
  ```
  $d$ is given by\newline$V_{d}\left(  x,c,d_{0}\right)  +\epsilon_{d}\equiv$%
  ```
- Line 584: lon
  ```
  ,x_{1}\right)  +\epsilon_{d}, \label{ut1}%
  ```
- Line 586: lon
  ```
  where\ $\epsilon_{d}$ is\ an i.i.d. Type 1 extreme-value distributed taste
  ```
- Line 587: lon
  ```
  shock with a scale parameter $\sigma_{\epsilon}.$ Wage enters with a
  ```
- Line 589: district
  ```
  \$1,000. Teachers' preferences for district characteristics $q_{d}$ are
  ```
- Line 595: district
  ```
  disproportionately rare to see teachers move into districts with a high
  ```
- Line 616: district
  ```
  moving costs varies with (log) physical distance between two districts, and
  ```
- Line 617: district
  ```
  $\delta_{2}$ is the additional cost if the new district is in a different
  ```
- Line 618: district
  ```
  commuting zone than one's current district. For\ teachers new to the market
  ```
- Line 619: district
  ```
  $\left(  d_{0}=0\right)  ,$ who are not attached to any district at the
  ```
- Line 621: district
  ```
  district $d$ is the same, given by $\Gamma_{0}\left(  x\right)  $%
  ```
- Line 624: district
  ```
  Given that $\Gamma_{0}\left(  x\right)  $ is constant across districts, we set
  ```
- Line 626: district
  ```
  choice over different districts.}
  ```
- Line 634: lon
  ```
  \right)  +\epsilon_{d}\right\}  . \label{teacher}%
  ```
- Line 636: lon
  ```
  Let $d^{\ast}\left(  x,c,d_{0},\epsilon\right)  $ be the teacher's optimal choice.
  ```
- Line 638: district
  ```
  \subsection{District's Problem}
  ```
- Line 640: district
  ```
  \subsubsection{District Preferences}
  ```
- Line 642: district
  ```
  A teacher's (gross) value to district $d$ is given by
  ```
- Line 647: district
  ```
  where $b_{0}$ allows for the possibility that districts may directly care
  ```
- Line 649: district
  ```
  much a district cares about a teacher's contribution to its low- and
  ```
- Line 652: district
  ```
  districts' direct preference over teachers' origins $d_{0}.$ As such, we have
  ```
- Line 653: district, lat
  ```
  assumed the latter away.} We assume that $b\geq0$, i.e., district preferences
  ```
- Line 657: district
  ```
  i.e., a district values a teacher only for their total contribution to its
  ```
- Line 658: lat
  ```
  students. More generally, if $b_{1}$ and $b_{2}$ are large relative to $b_{0}%
  ```
- Line 659: district
  ```
  $, districts would rank teachers differently depending on their student
  ```
- Line 660: district
  ```
  compositions $\lambda_{d};$ if $b_{0}$ is dominant, districts would largely
  ```
- Line 666: district
  ```
  infinite dimensions. To keep the model tractable, we assume that a district's
  ```
- Line 684: district
  ```
  Under $\left(  \ref{wrule}\right)  ,$ a district's wage strategy boils down to
  ```
- Line 692: district, school
  ```
  the case in most U.S. school districts; if $\omega_{2}>0,$ teachers are
  ```
- Line 694: lat
  ```
  show in Section \ref{wage_emp}, wages calculated under $\left(  \ref{wrule}%
  ```
- Line 701: district
  ```
  \subsubsection{District's Optimal Decisions}
  ```
- Line 703: district
  ```
  Taking all the other districts' policies and teachers' decision rules as
  ```
- Line 704: city, district
  ```
  given, a district fills its capacity with its most preferred teachers by
  ```
- Line 706: district
  ```
  district's problem can\ be solved in two steps: First, a district chooses a
  ```
- Line 707: second
  ```
  wage schedule $\omega=\left(  \omega_{1},\omega_{2}\right)  $; and second, it
  ```
- Line 708: district
  ```
  makes job offers conditional on $\omega$. We solve a district's problem via
  ```
- Line 713: district
  ```
  For a given wage schedule $\omega$, district $d$'s job offers $\left\{
  ```
- Line 732: district
  ```
  teacher would accept the job \emph{if} district $d$ made them an offer
  ```
- Line 734: district
  ```
  probability that the teacher prefers $d$ over all the other districts that
  ```
- Line 739: lon
  ```
  x,c,d_{0}\right)  }{\sigma_{\epsilon}}\right)  }{\exp\left(  \frac
  ```
- Line 740: lon
  ```
  {V_{d}\left(  x,c,d_{0}\right)  }{\sigma_{\epsilon}}\right)  +\sum_{d^{\prime
  ```
- Line 742: lon
  ```
  \frac{V_{d^{\prime}}\left(  x,c,d_{0}\right)  }{\sigma_{\epsilon}}\right)  }.
  ```
- Line 745: city
  ```
  The first two constraints in $\left(  \ref{pi}\right)  $ are for capacity and
  ```
- Line 750: district
  ```
  solution to $\left(  \ref{pi}\right)  $. In particular, district $d$ would
  ```
- Line 756: district
  ```
  tenured incumbents, district $d$ would make offers to its $n$ top-ranked
  ```
- Line 757: city
  ```
  teachers, where $n$ is the maximum number of offers allowed by its capacity
  ```
- Line 762: district
  ```
  District $d$ chooses $\omega$ to solve the following problem%
  ```
- Line 770: city, district
  ```
  is normalized by district capacity to make the scale comparable across
  ```
- Line 771: district
  ```
  districts with different capacities$.$ Given the highly controversial nature
  ```
- Line 783: district
  ```
  plausible that districts with a larger share of Democratic voters may be more
  ```
- Line 786: district
  ```
  controlling for factors that enter a district's payoff $\pi_{d}\left(
  ```
- Line 787: district
  ```
  \omega\right)  ,$ the share of Democratic votes among district $d$'s residents
  ```
- Line 789: lat
  ```
  rigid wage schedule; this correlation remains when we additionally control for
  ```
- Line 790: district
  ```
  the characteristics of a district's residents.\footnote{We exclude $dem_{d}$
  ```
- Line 794: district
  ```
  variable does not explain teacher-district sorting.} We therefore allow the
  ```
- Line 803: lon
  ```
  }\left(  x,c,d_{0},\epsilon\right)  \right\}  ,\left\{  \omega_{d}^{\ast
  ```
- Line 809: lon
  ```
  ,\epsilon\right)  $ solves the teacher's problem, for all $\left(
  ```
- Line 810: lon
  ```
  x,c,d_{0},\epsilon\right)  $.\newline2) For all $d,$ given $\left\{
  ```
- Line 820: district
  ```
  To solve its problem, it is sufficient for a district to know teachers'
  ```
- Line 823: district
  ```
  about other districts' strategies is redundant. An equilibrium requires a
  ```
- Line 829: district
  ```
  the numbers of teachers, districts and potential wage levels, respectively).
  ```
- Line 830: district
  ```
  Alternatively, a district can derive $\left\{  h_{d}\left(  \cdot\right)
  ```
- Line 832: district
  ```
  about other districts' strategies. With 411 districts in the market, forming
  ```
- Line 833: district
  ```
  the exact belief about other districts' wage strategies $\left\{  \left(
  ```
- Line 835: district
  ```
  daunting task.} As a feasible alternative, we assume that districts make their
  ```
- Line 857: district, lat
  ```
  relate to the overall desirability of the teacher: A district should expect
  ```
- Line 859: district
  ```
  probability. The next term captures the idea that a district offering a more
  ```
- Line 862: district
  ```
  cross-district average and standard deviation of wages for a teacher with
  ```
- Line 864: district
  ```
  districts in the equilibrium. We measure the competitiveness of a wage offer
  ```
- Line 867: district
  ```
  \ref{f2}\right)  $ mirror teachers' preferences over districts'
  ```
- Line 877: district
  ```
  consistency between districts' belief $\widetilde{h}_{d}\left(  \cdot\right)
  ```
- Line 892: school
  ```
  their competition against teachers' outside options (e.g., private schools,
  ```
- Line 893: school, son
  ```
  public schools in other states, and other occupations). For the same reason,
  ```
- Line 894: school
  ```
  although new teachers who ended up working in Wisconsin public schools are
  ```
- Line 903: school
  ```
  suggest that performance pay in public schools may improve the quality of the
  ```
- Line 904: school
  ```
  overall supply of teachers in both public and private schools
  ```
- Line 905: lat
  ```
  \citep[e.g.,][]{tincani2020teacher}. If the latter is true, the efficiency
  ```
- Line 908: district, second
  ```
  Second, because wage schedules are set at the district level, we focus on the
  ```
- Line 910: district, school
  ```
  across schools within a district. Appendix B3 shows that the
  ```
- Line 911: district
  ```
  cross-district variation in teacher wages and student bodies clearly dominate
  ```
- Line 912: district
  ```
  the within-district variation.\footnote{Of the 411 districts in Wisconsin, 173
  ```
- Line 913: school
  ```
  only have one public elementary school.} Moreover, implementing the tests
  ```
- Line 915: school
  ```
  find no evidence of non-random sorting of teachers across grade-schools within
  ```
- Line 916: district
  ```
  a district (Appendix B.3.3). That being said, focusing on
  ```
- Line 917: district
  ```
  cross-district movement and competition limits our ability to conduct
  ```
- Line 918: district
  ```
  counterfactual policies that introduce within-district pay variation to induce
  ```
- Line 919: district, loc, location
  ```
  within-district teacher reallocation. On the one hand, such policies can be
  ```
- Line 920: district
  ```
  fruitful, because teachers' within-district moving costs are presumbaly lower
  ```
- Line 921: district
  ```
  than their cross-district moving costs. On the other hand, districts appear to
  ```
- Line 922: district
  ```
  be averse to within-district pay variation (Appendix B3); such policies
  ```
- Line 924: loc, location
  ```
  and teacher reallocation into our framework would allow for a more complete
  ```
- Line 928: district
  ```
  Third, we take a district's student composition $\lambda_{d}$ as given. In
  ```
- Line 929: district, house
  ```
  particular, we assume away potential households re-sorting across districts in
  ```
- Line 931: district
  ```
  moving across districts was very small and similarly so before and after Act
  ```
- Line 932: district
  ```
  10; this is true for moves between any two districts and for moves between a
  ```
- Line 933: district
  ```
  district that rewarded teacher effectiveness under Act 10 and one that did
  ```
- Line 935: district
  ```
  districts between two adjacent years on average. This fraction was
  ```
- Line 937: district
  ```
  4.2\% in 2012 and 4.3\% in 2013. Labeling districts as adopting and
  ```
- Line 945: house
  ```
  expect our counterfactual policies to significantly affect households'
  ```
- Line 946: loc, location
  ```
  location choices. However, readers should still be aware of this limitation
  ```
- Line 959: son
  ```
  \citep{muralidharan2011teacher,duflo2012incentives,lavy2002evaluating,atkinson2009evaluating,glewwe2
  ```
- Line 965: district
  ```
  efficient teacher-district matching. To the extent that teachers may improve
  ```
- Line 975: lat
  ```
  repeatedly simulate data with the structural model, compute corresponding
  ```
- Line 991: district
  ```
  vector $\Theta$, which consists of teachers' and districts' preference
  ```
- Line 993: lat
  ```
  (detailed in Appendix B2). In our counterfactual policy simulations, we
  ```
- Line 1012: district
  ```
  teacher-district matches. This makes it hard to separate teachers' preferences
  ```
- Line 1023: school
  ```
  centralized student-school matching system, \citet{fack2019beyond} define a
  ```
- Line 1024: school
  ```
  student's feasible choice set as the set of schools whose observed ex post
  ```
- Line 1026: district
  ```
  students' preferences assuming stability.} We take a different approach: As we argue below, the obse
  ```
- Line 1028: district
  ```
  \subsubsection{Wage Schedule and District Pre-Determined Conditions\label{id2}%
  ```
- Line 1031: district
  ```
  Under Act 10, districts can choose how to reward teachers. Therefore, the
  ```
- Line 1035: district, lat
  ```
  x,c\right)  $ and how wage schedules relate to districts' pre-determined
  ```
- Line 1038: district
  ```
  attributes $\left(  x,c\right)  ,$ a district should choose a wage schedule
  ```
- Line 1040: district
  ```
  teachers are not district incumbents, because moving is costly for teachers.
  ```
- Line 1041: district
  ```
  Meanwhile, although a district cannot dismiss its tenured incumbents with
  ```
- Line 1044: district
  ```
  $. Notice that district $d$ can avoid teachers with $\left(  x^{\prime
  ```
- Line 1048: district
  ```
  district has more tenured incumbents with $\left(  x^{\prime},c^{\prime
  ```
- Line 1051: district
  ```
  However, district preferences over teachers may not be sufficient to explain
  ```
- Line 1053: district
  ```
  of districts kept their pre-Act 10 wage schedules and 50\% of districts\ chose
  ```
- Line 1055: district
  ```
  points as optimal wage schedules chosen by districts purely to hire their
  ```
- Line 1056: district
  ```
  preferred teachers. Districts' choices that are not explained by their
  ```
- Line 1062: district, second
  ```
  The observed teacher-district matches provide the second major source of
  ```
- Line 1067: district
  ```
  However, combining the observed matches with districts' optimal offer
  ```
- Line 1069: district
  ```
  and thus to identify teachers' preferences. Specifically, for district $d,$
  ```
- Line 1071: district
  ```
  contribution to district $d$'s low-achieving students $\lambda_{d}c_{1}$ and
  ```
- Line 1078: district
  ```
  preference rather than on the non-dismissal constraint), then for any district
  ```
- Line 1083: district
  ```
  $\left(  x_{1}\right)  $ enters district preferences as ordered categorical
  ```
- Line 1084: son
  ```
  variables (0-2, 3-4, 5-9, 10-14, 15 years or more). Therefore, the comparison
  ```
- Line 1094: lon
  ```
  therefore as long as $O_{i}^{s}$ is not a singleton (which is true for 5,170
  ```
- Line 1096: district
  ```
  $O_{i}^{s}$ identifies their preferences--- how much teachers value districts'
  ```
- Line 1097: lat
  ```
  characteristics relative to wages and how dispersed their preference shocks
  ```
- Line 1102: lat
  ```
  \emph{unrestricted correlation} between choice sets and preferences and
  ```
- Line 1107: district
  ```
  Observed matches are also informative of district preferences. First, as the
  ```
- Line 1108: district
  ```
  number of teachers per district grows large, the lowest $\left(  x,c\right)  $
  ```
- Line 1110: district
  ```
  district $d$ is willing to hire. This is because teachers are subject to
  ```
- Line 1112: district
  ```
  some teacher. The lowest $\left(  x,c\right)  $ within each district
  ```
- Line 1113: district
  ```
  identifies districts' preferences over teachers.
  ```
- Line 1115: district
  ```
  In practice, many districts are small, therefore, our identification also
  ```
- Line 1118: district
  ```
  the probability that a teacher would choose to work in each district if they
  ```
- Line 1121: district
  ```
  teacher preference shocks, this predicted distribution of teacher-district
  ```
- Line 1124: district
  ```
  is, given teachers' preferences, districts' offer decisions---which are
  ```
- Line 1125: district
  ```
  governed by districts' preferences---must rationalize the realized match distribution.
  ```
- Line 1130: district
  ```
  and $j$ both prefer district $1$ over district $2$. If we observe $i$ working
  ```
- Line 1131: district
  ```
  in district $1$ and $j$ working in district $2$, it must be the case that
  ```
- Line 1132: district
  ```
  district $1$ prefers $i$ over $j.$ The same argument applies when teachers
  ```
- Line 1133: district
  ```
  have preference shocks: If teachers systematically prefer district $1$ over
  ```
- Line 1134: district
  ```
  district $2$, then district $1$ must prefer their hires over (most) teachers
  ```
- Line 1135: district, lon
  ```
  working in district $2.$ As long as the distribution of $\left(  x,c\right)  $
  ```
- Line 1136: district
  ```
  in district $1$ does not systematically dominate the distribution of $\left(
  ```
- Line 1137: district
  ```
  x,c\right)  $ in district $2$ in all dimensions, we can infer how much
  ```
- Line 1138: district, lat
  ```
  district $1$ cares about $x$ and $c_{2}$ relative to $c_{1}$ (the coefficient
  ```
- Line 1147: district
  ```
  \noindent \textbf{Step 1:} We construct subsets of offers faced by each teacher, under two types of 
  ```
- Line 1150: lon
  ```
  \cdot\right)  $ is irrelevant here as long as $B\left(  \cdot\right)  $ is
  ```
- Line 1151: lon
  ```
  weakly increasing in every dimension of teacher trait. Moreover, as long as
  ```
- Line 1152: district
  ```
  the researcher observes the same measures of teacher traits as districts do, be them exact or noisy 
  ```
- Line 1161: district
  ```
  Appendix B4: Instead of $\left(  c_{1},c_{2}\right)  ,$ districts observe
  ```
- Line 1170: district
  ```
  \noindent \textbf{A2}: Districts cannot discriminate among
  ```
- Line 1172: son
  ```
  made for reasons other than $\left(  x,c\right)  $, then the inferred
  ```
- Line 1175: lon
  ```
  However, as long as most job offers are based on $\left(  x,c\right)  $, the
  ```
- Line 1180: district
  ```
  models but do not use observed teacher-district $\left(  i,d\right)  $ matches
  ```
- Line 1183: son
  ```
  ineffective teachers may indeed have been hired for other reasons. Doing so
  ```
- Line 1187: district
  ```
  \noindent \textbf{A3}: The cost of posting jobs is zero. This assumption is plausible because in rea
  ```
- Line 1188: lat
  ```
  publicly on online platforms.\footnote{See, e.g.,
  ```
- Line 1189: network
  ```
  https://wecan.education.wisc.edu (Wisconsin Education Career Access Network).}
  ```
- Line 1192: district
  ```
  following two cases would both imply that district $d$ was not attractive
  ```
- Line 1195: district
  ```
  teachers to apply for jobs (more so for jobs in districts other than one's
  ```
- Line 1196: district
  ```
  initial district), then these costs would be absorbed in teachers' moving
  ```
- Line 1203: second
  ```
  likely to be chosen by a given agent. The second condition is exchangeability,
  ```
- Line 1208: lat
  ```
  coefficient models violate this condition), but two agents with only slightly
  ```
- Line 1210: second
  ```
  The second condition holds for any collection of i.i.d. random variables.}
  ```
- Line 1214: lat
  ```
  two offers in $O_{i}^{s}$; we rely on extrapolation to identify preferences
  ```
- Line 1217: district
  ```
  \noindent\textbf{Step 3: } Given teachers' preferences, we identify districts'
  ```
- Line 1219: district, lon
  ```
  district's choice set---the entire sample of teachers. Given A1-A3, as long as a district
  ```
- Line 1220: district
  ```
  does not make offers to all teachers, its preference is revealed by its offer decisions. Moreover, p
  ```
- Line 1222: district
  ```
  current (parsimonious) specification.\footnote{For example, we can allow for any interaction of teac
  ```
- Line 1227: district, lat
  ```
  statistics on the relationship between districts' wage schedules and their
  ```
- Line 1230: district
  ```
  offer subset $O_{i}^{s}$ and 2) the realized distribution of teacher-district
  ```
- Line 1257: lon
  ```
  _{6}^{m}c_{1i}\lambda_{d}+\psi_{i}+\varepsilon_{id}^{m},
  ```
- Line 1259: district
  ```
  where $y_{id}=1$ if teacher $i$ is matched with district $d,$ and $0$
  ```
- Line 1262: district
  ```
  _{d}\right)  ,$ the wage $i$ would be paid by district $d$ under wage rule
  ```
- Line 1263: lat
  ```
  $\left(  \ref{wrule}\right)  $. $\psi_{i}$ is a teacher dummy that relates all
  ```
- Line 1269: district
  ```
  idea that the same teacher is choosing one district out of a given set of
  ```
- Line 1270: district
  ```
  districts.} The two regressions differ in the number of observations,
  ```
- Line 1276: district
  ```
  teacher-district pair in these inferred subsets.
  ```
- Line 1278: district, second
  ```
  \item[Aux 1b] The second regression includes every possible teacher-district
  ```
- Line 1279: district
  ```
  pair, which serves to summarize observed teacher-district matches.
  ```
- Line 1282: district
  ```
  \item[Aux 2] Moments of district-level teacher characteristics $\left(
  ```
- Line 1283: district
  ```
  x,c_{1},c_{2}\right)  $ by district groups (quintiles of $\lambda_{d},$
  ```
- Line 1287: district
  ```
  $\omega_{dn},$ $n=1,2$, on district's pre-determined conditions, reflecting
  ```
- Line 1294: lon
  ```
  _{z_{d}}+\beta_{10n}^{w}dem_{d}+\varepsilon_{dn}^{w},
  ```
- Line 1297: district
  ```
  district characteristics and constraints, and $\beta_{5n}^{w}$ to $\beta
  ```
- Line 1298: district
  ```
  _{7n}^{w}$ are associated with the composition of district incumbents. In
  ```
- Line 1301: district
  ```
  average $TC$ among the district's tenured incumbents $\left(  d_{0i}=d\text{
  ```
- Line 1303: district
  ```
  working in other districts within $d$'s commuting zone (i.e., $d_{0i}\neq d,$
  ```
- Line 1306: district
  ```
  Democratic votes in the district.
  ```
- Line 1308: district
  ```
  \item[Aux 4] Cross-district wage schedule moments, which supplement Aux 3:
  ```
- Line 1319: district
  ```
  and districts respectively. All of our data are reported by academic year and
  ```
- Line 1326: district, school
  ```
  experience, total wages, full-time equivalency units, school and district
  ```
- Line 1330: district, school
  ```
  \nocite{wdpi_school_districts_2024}
  ```
- Line 1331: district, school
  ```
  \nocite{nces_school_district_boundaries}
  ```
- Line 1337: school
  ```
  state standardized test scores for all public school students in Grades 3 to 8
  ```
- Line 1340: district
  ```
  \medskip\noindent\textit{\textbf{District-level Information: }}Using student
  ```
- Line 1341: lat
  ```
  test score data, we calculate $\lambda_{d}$, the fraction of students in
  ```
- Line 1342: district
  ```
  district $d$ whose prior math scores were below the grade-specific state
  ```
- Line 1345: census, district
  ```
  based on the 2010 Census classification. Each district is assigned to one of
  ```
- Line 1357: district
  ```
  10; by then, all the CB agreements pre-dating Act 10 had expired and districts
  ```
- Line 1360: lat
  ```
  validate the estimated model, we simulate the market equilibrium under rigid
  ```
- Line 1364: school, son
  ```
  school math teachers in Grades 4-6, for the following reasons. We exclude the
  ```
- Line 1366: school
  ```
  contracts than regular, full-time teachers.\footnote{Among all public school
  ```
- Line 1369: school, second
  ```
  secondary-school teachers because they often teach multiple grades, making it
  ```
- Line 1371: school
  ```
  \citep{kane2008estimating,chetty2014measuring}. Among elementary-school
  ```
- Line 1375: lat
  ```
  teachers.\footnote{The achievement models used to calculate teachers'
  ```
- Line 1387: district
  ```
  sample contains 411 districts and 6,600 teachers; the validation sample
  ```
- Line 1388: district
  ```
  contains 411 districts and 6,741 teachers.
  ```
- Line 1391: city, district
  ```
  district's capacity and budget constraints for this subgroup do not interact
  ```
- Line 1393: district
  ```
  district commits certain resources for the math education of its Grade 4-6
  ```
- Line 1395: district
  ```
  district's budget spent on Grade 4-6 math teachers is stable over time.
  ```
- Line 1412: lon
  ```
  \right)  +\varepsilon_{kt}, \label{achievement}%
  ```
- Line 1416: school
  ```
  $A_{kt-1})$, a school-grade fixed effect, and a year fixed effect;
  ```
- Line 1417: lon
  ```
  $\varepsilon_{kt}$ is an i.i.d. idiosyncratic component. In the summation,
  ```
- Line 1418: school
  ```
  $SG_{kt}$ ($SG_{it}^{T})$ denotes the school-grade student $k$ (teacher $i)$
  ```
- Line 1419: lon
  ```
  belongs to in year $t$; $\tau_{k}$ denotes a student's type ($\tau_{k}=1$ if
  ```
- Line 1425: school
  ```
  Assuming that student-teacher sorting is random across school-grades
  ```
- Line 1439: district
  ```
  that all teachers have a (weakly) positive contribution to a district's
  ```
- Line 1440: district
  ```
  objective value $\left(  \ref{pi}\right)  $ and that a district would not want
  ```
- Line 1446: district
  ```
  affect how a district ranks teachers.}
  ```
- Line 1457: second
  ```
  Second, besides modeling $c$ as being two-dimensional, we also allow $c$ to
  ```
- Line 1461: school
  ```
  contributes to all students in their school-grade in $\left(
  ```
- Line 1463: school
  ```
  to the school-grade level. In an alternative model where a teacher contributes
  ```
- Line 1466: school
  ```
  Identification of both models exploits teacher turnover across school-grades
  ```
- Line 1467: lat, lon
  ```
  and the assumption that $\varepsilon_{kt}$ and $v_{in}$ are uncorrelated.
  ```
- Line 1468: district
  ```
  Notice that this assumption allows for endogeneous district-teacher sorting
  ```
- Line 1470: school
  ```
  includes school-grade fixed effects and year fixed effects. As we show in
  ```
- Line 1472: lat
  ```
  two achievement models are highly correlated; more importantly, auxiliary
  ```
- Line 1478: lat
  ```
  our counterfactual policy implications should be interpreted with due caveat.\footnote{Relatedly, gi
  ```
- Line 1480: district
  ```
  \medskip\noindent\textit{\textbf{Teacher's Origin District}}: For the
  ```
- Line 1486: district
  ```
  \subsubsection{Wage Schedules and District Constraints\label{wage_emp}}
  ```
- Line 1493: district
  ```
  regression coefficients are allowed to differ across districts$.$
  ```
- Line 1500: district
  ```
  set $\Omega\supset\Omega^{o}$, to allow for the possibility that district
  ```
- Line 1505: district
  ```
  \medskip\noindent\textit{\textbf{District Wage Schedules}}: For each district,
  ```
- Line 1516: district
  ```
  ^{o}\right)  $ is treated as district $d$'s wage schedule in the realized
  ```
- Line 1525: city, district
  ```
  \medskip\noindent\textit{\textbf{District Capacity and Budget Constraints:}}
  ```
- Line 1537: degree
  ```
  percent of all teachers have a graduate degree; this share is 6\% among
  ```
- Line 1541: lat
  ```
  correlation between experience $\left(  x_{1}\right)  $ and either $c_{1}$ or
  ```
- Line 1544: lat
  ```
  shows that the correlation between $c_{1}$ and $c_{2}$ is $0.67$, which
  ```
- Line 1548: district
  ```
  Panel B of Table 1 summarizes districts' characteristics and the composition
  ```
- Line 1549: district
  ```
  of a district's incumbent teachers ($d_{0i}=d)$. We present statistics for all
  ```
- Line 1550: district, school
  ```
  the 411 school districts in the estimation sample and separately for districts
  ```
- Line 1551: lon
  ```
  belonging to the 1st and 4th quartiles of the distribution of $\lambda_{d}$
  ```
- Line 1552: district
  ```
  (the fraction of low-achieving students). Districts with fewer low-achieving
  ```
- Line 1553: loc
  ```
  students are more likely to be located in suburban areas and have larger
  ```
- Line 1554: city
  ```
  capacity and per teacher budgets (throughout the paper, all dollar values are
  ```
- Line 1555: district
  ```
  in 2014 dollars). Incumbent teachers in these districts are more likely to be highly-educated.
  ```
- Line 1558: district
  ```
  \caption{Teacher and District Characteristics (2014)}
  ```
- Line 1573: district
  ```
  \textbf{B. District Characteristics} &  & All &  & $\lambda_{d}$ 1st Quartile &  & $\lambda_{d}$ 4th
  ```
- Line 1578: city
  ```
  Capacity &  & 16.9 (30.5) &  & 18.4 (15.9) &  &
  ```
- Line 1580: city
  ```
  Budget/Capacity (\$1,000) &  & 50.9 (6.6) &  &
  ```
- Line 1582: district
  ```
  \multicolumn{7}{l}{\emph{Characteristics of District Incumbent Teachers ($d_{0}=d$)}}\\
  ```
- Line 1591: district
  ```
  \# Districts & \ \ \ \ \ \ \ \ \ \ \ \  & 411 &
  ```
- Line 1595: district
  ```
  parentheses) of teacher (Panel A) and district (Panel B) characteristics.}}}
  ```
- Line 1601: district
  ```
  of offers $O_{i}^{s}$.\footnote{Controlling for district-level shares of
  ```
- Line 1603: city
  ```
  with the corresponding indicators for teachers' race and ethnicity barely
  ```
- Line 1611: district
  ```
  low-achieving students are more willing to teach in districts with higher
  ```
- Line 1612: lat
  ```
  fractions of these students (Row 3). However, neither of these relationships
  ```
- Line 1617: district
  ```
  teachers chose low-wage districts over high-wage districts. This example
  ```
- Line 1618: district
  ```
  illustrates our identification argument. In general, districts' preference
  ```
- Line 1623: district
  ```
  \caption{OLS of Teacher-District Match (2014)}
  ```
- Line 1630: district
  ```
  & \multicolumn{2}{c}{All Districts} \\
  ```
- Line 1671: district
  ```
  Panel A of Table 3 summarizes districts' wage schedules. Districts' choices of
  ```
- Line 1673: district
  ```
  choices of $\omega_{1}$. Although given the flexibility, 24\% of districts
  ```
- Line 1675: district
  ```
  $) and only 50\% of districts chose to reward teacher contribution $\left(
  ```
- Line 1677: son
  ```
  teacher effectiveness is largely driven by political reasons. Two other
  ```
- Line 1680: district
  ```
  reward them accordingly, and 2) districts care a lot about teachers'
  ```
- Line 1685: district
  ```
  summarizes wages in the realized district-teacher matches. On average, more
  ```
- Line 1686: district
  ```
  experienced teachers are paid more. Panel C compares districts'
  ```
- Line 1687: district
  ```
  characteristics and the composition of each district's incumbent teachers
  ```
- Line 1688: district
  ```
  among districts that did not reward teacher contribution and those that did.
  ```
- Line 1689: district
  ```
  Districts with $\omega_{2}>0$ are more likely to be in rural areas and have
  ```
- Line 1692: lat
  ```
  Figure~\ref{fig:omega2_cdf} further illustrates the correlation between
  ```
- Line 1693: lat
  ```
  $\omega_{2}$ and student composition $\lambda_{d}$ by plotting the cumulative
  ```
- Line 1696: second
  ```
  the first group first-order stochastically dominates that in the second group,
  ```
- Line 1697: district
  ```
  i.e., districts with more low-achieving students are more likely to reward
  ```
- Line 1699: district
  ```
  disadvantaged districts to compete for experienced and effective teachers,
  ```
- Line 1704: district
  ```
  \caption{District Wage Schedules (2014) }
  ```
- Line 1741: district
  ```
  \multicolumn{2}{l}{\textbf{C. District characteristics by $\omega_{2}$} }
  ```
- Line 1753: city
  ```
  \multicolumn{2}{l}{Budget/Capacity (\$1{,}000)}
  ```
- Line 1757: district
  ```
  \multicolumn{2}{l}{\# Districts}
  ```
- Line 1765: district
  ```
  \caption*{ {\footnotesize {Notes: Summary statistics of districts' wage
  ```
- Line 1767: district, lat
  ```
  $\omega_{2}$, their correlation, and the fractions of districts with
  ```
- Line 1770: district
  ```
  teachers' wages in the realized matches, given districts' wage schedules, for
  ```
- Line 1772: district
  ```
  characteristics of districts with $\omega_{2}=0$ (left column) and $\omega
  ```
- Line 1779: lat
  ```
  \caption{Cumulative Distribution Function of $\omega_{2}$ by Groups of
  ```
- Line 1784: district
  ```
  \caption*{\footnotesize Notes: This Figure plots the CDF of $\omega_{2}$ for districts
  ```
- Line 1787: district, second
  ```
  second group, i.e., districts with more low-achieving students are more likely
  ```
- Line 1792: district
  ```
  To further investigate districts' wage schedule choices, in Table 4 we use OLS
  ```
- Line 1793: district, lat
  ```
  regressions to relate a district's reward for teacher contribution
  ```
- Line 1796: district
  ```
  county $\left(  dem_{d}\right)  \citep{dailykos_statewide_results_2021};$ 2) district $d$'s student 
  ```
- Line 1797: city
  ```
  capacity, and urbanicity; 3) the characteristics of teachers initially working
  ```
- Line 1798: district
  ```
  in $d$ and in $d$'s commuting zone; and 4) the characteristics of district
  ```
- Line 1801: district
  ```
  that districts with a larger share of Democratic votes and districts with
  ```
- Line 1802: city
  ```
  larger capacity tend to offer lower rewards for teacher effectiveness, i.e.,
  ```
- Line 1804: lat
  ```
  find that $\omega_{d2}$ is (insignificantly) positively correlated with the
  ```
- Line 1806: lat
  ```
  (insignificantly) negatively correlated with the effectiveness of tenured
  ```
- Line 1808: district
  ```
  estimate is intuitive: If tenured incumbents, whom the district cannot
  ```
- Line 1809: district
  ```
  dismiss, are ineffective, a district can set higher $\omega_{d_{2}}$ (higher
  ```
- Line 1811: district
  ```
  controls of the income distribution in the district; in Column (3), we further
  ```
- Line 1814: district
  ```
  a district's wage schedule choice is explained, to a large extent, by the
  ```
- Line 1818: district
  ```
  \caption{Explaining District Reward for Teacher Contribution $\omega_{d2}$
  ```
- Line 1835: city
  ```
  Capacity
  ```
- Line 1866: city, district
  ```
  District urbanicity
  ```
- Line 1882: lat
  ```
  % restore default spacing for later tables
  ```
- Line 1888: house
  ```
  households with incomes higher than \$200,000. The distribution of residents'
  ```
- Line 1899: lon
  ```
  Table 5 shows estimated model parameters, along with their standard errors (in
  ```
- Line 1901: district
  ```
  parameters governing teachers' preferences. For most teachers, districts with
  ```
- Line 1904: district
  ```
  \$4,100 on a district with ${\small \lambda}_{d}=0.3$ over an otherwise
  ```
- Line 1905: district
  ```
  identical district with ${\small \lambda}_{d^{\prime}}=0.7$. However, teachers
  ```
- Line 1907: district
  ```
  teach in these districts: The coefficient of $c_{1}\times{\small \lambda}_{d}$
  ```
- Line 1908: district
  ```
  is positive, although imprecisely estimated. Rural districts are less
  ```
- Line 1912: lat
  ```
  experienced teachers throughout our simulations. For example, in the baseline
  ```
- Line 1916: lon
  ```
  preference shocks (governed by ${\small \sigma}_{\epsilon})$. High moving
  ```
- Line 1918: son
  ```
  experienced teachers, while preference shocks absorb idiosyncratic reasons for
  ```
- Line 1953: lon
  ```
  $\sigma_{\epsilon}$ & 18.73 & (0.94)
  ```
- Line 1959: district
  ```
  \multicolumn{6}{l}{\textbf{B. District Preferences}}\\
  ```
- Line 1983: lat
  ```
  (in parentheses) are calculated numerically via the Delta Method.}}
  ```
- Line 1987: district
  ```
  Panel B shows district preference estimates. Districts significantly value a
  ```
- Line 1989: district
  ```
  experience and education per se. In addition, districts value a teacher's
  ```
- Line 1993: district
  ```
  consistent with Table 4, the estimates indicate that districts with a larger
  ```
- Line 1995: district
  ```
  explore the implications of both teachers' moving costs and districts' costs
  ```
- Line 2001: school
  ```
  teacher-school sorting as summarized by the two regressions specified in
  ```
- Line 2009: district
  ```
  dispersion of $\omega_{2}\ $and the fraction of districts choosing $\omega
  ```
- Line 2010: district
  ```
  _{2}=0$. The lower panel shows model fits for district characteristics by
  ```
- Line 2013: district
  ```
  predicts that districts with $\omega_{2}>0$ are slightly more disadvantaged.
  ```
- Line 2025: lat
  ```
  consider teachers' welfare in our efficiency-equity calculations.}
  ```
- Line 2038: district
  ```
  {TC}_{D^{\prime}}$ among teachers working in a given group of districts
  ```
- Line 2044: district
  ```
  cross-district educational equity if it increases $\overline{TC}_{D^{\prime}}$
  ```
- Line 2045: district
  ```
  more for high-$\lambda_{d}$ districts, i.e., districts with higher fractions
  ```
- Line 2047: district
  ```
  districts.\newline2. Average teacher contribution to the state's low-achieving
  ```
- Line 2060: district
  ```
  $\omega=\left(  1,0\right)  $ is imposed on all districts.
  ```
- Line 2074: district
  ```
  All Districts & 0.04 & -0.01 & 0.10 & 0.50 (0.12) \\
  ```
- Line 2084: district
  ```
  \caption*{ {\footnotesize {Notes: Columns 1--3 report the percent difference in $\overline{TC}$, $\o
  ```
- Line 2094: district
  ```
  standard deviation of $\lambda_{d}$ among districts in each row.
  ```
- Line 2098: district
  ```
  \noindent (1) Flexible pay allows districts to directly
  ```
- Line 2106: district
  ```
  (2) As districts'
  ```
- Line 2107: district
  ```
  competition intensifies under flexible pay, districts in the top quartile of the
  ```
- Line 2109: district
  ```
  of students losing effective teachers to other districts. This is because, all
  ```
- Line 2110: district
  ```
  else being equal, most teachers prefer districts with fewer low-achieving
  ```
- Line 2113: district, second
  ```
  (3)\ Perhaps surprisingly, districts in the second-highest quartile of $\lambda_{d}$
  ```
- Line 2116: district
  ```
  teachers move out of highest-$\lambda_{d}$ districts, they are more likely to
  ```
- Line 2117: district
  ```
  move to nearby districts, which are less likely to be low-$\lambda_{d}$
  ```
- Line 2118: district
  ```
  districts due to the fact that student bodies tend to be similar among nearby
  ```
- Line 2119: district
  ```
  districts (Appendix Figure B10). In general, teachers' unwillingness to move explains why the equity
  ```
- Line 2122: district
  ```
  districts to compete more effectively for teachers better suited for their
  ```
- Line 2123: district
  ```
  students; as a result, a district's majority student group---low- or
  ```
- Line 2125: district
  ```
  versus Column 3). Most evidently, in districts where a large majority of
  ```
- Line 2128: district
  ```
  students, widening the within-district achievement gap between the two groups
  ```
- Line 2133: district
  ```
  it also widens\ achievement gaps across districts and between low- and
  ```
- Line 2145: district
  ```
  an obstacle to equity. On the demand side, a large fraction of districts
  ```
- Line 2151: district
  ```
  for the demand side, it gives a direct incentive for districts to reward
  ```
- Line 2153: district
  ```
  c_{1},c_{2}\right)  $ teaching in district $d\ $would obtain a state-funded
  ```
- Line 2167: district
  ```
  advantage better matches a district's student composition, this component
  ```
- Line 2172: district
  ```
  low-achievement districts, this component directly favors these districts,
  ```
- Line 2179: district
  ```
  ties a teacher's bonus to the rate at which the district rewards teacher
  ```
- Line 2180: district
  ```
  contribution. Since a higher $\omega_{d2}$ would enable district $d$ to obtain
  ```
- Line 2181: district
  ```
  more state bonuses for its teachers, this feature incentivizes districts to
  ```
- Line 2204: lat
  ```
  details and additional simulation results are reported in Appendix
  ```
- Line 2209: lat
  ```
  Table 7). These amounts are comparable to relatively mild bonus programs
  ```
- Line 2222: lat
  ```
  at the state level. Among the simulations we have conducted, the vector
  ```
- Line 2229: district
  ```
  bonus schemes that favor districts with more low-achieving students outperform
  ```
- Line 2230: lat
  ```
  a purely TC-based bonus scheme: At the same total cost, the latter leads to a
  ```
- Line 2234: district
  ```
  the gains go to districts serving fewer low-achieving students, at the expense
  ```
- Line 2235: district
  ```
  of districts serving more low-achieving students. In sum, New1 succeeds in its
  ```
- Line 2270: district
  ```
  $\overline{TC}$ in 4th quartile $\lambda_{d}$ districts
  ```
- Line 2273: district
  ```
  $\overline{TC}$ in 3rd quartile $\lambda_{d}$ districts
  ```
- Line 2276: district
  ```
  $\overline{TC}$ in 2nd quartile $\lambda_{d}$ districts
  ```
- Line 2279: district
  ```
  $\overline{TC}$ in 1st quartile $\lambda_{d}$ districts
  ```
- Line 2311: district
  ```
  teachers to teach in districts with more low-achieving students. New2 leads to
  ```
- Line 2313: district
  ```
  for high-achieving students. Cross-district achievement gaps also narrow as
  ```
- Line 2325: district
  ```
  districts than both New1 and New2.
  ```
- Line 2330: district
  ```
  achievement gap across districts ($\overline{{\small TC}}$ in high- versus
  ```
- Line 2331: district
  ```
  low-$\lambda_{d}$ districts). A program that improves the average achievement
  ```
- Line 2332: district
  ```
  of a student group or a district group is not guaranteed to benefit all
  ```
- Line 2335: district
  ```
  in the lowest-$\lambda_{d}$ district group (Appendix Table B18).
  ```
- Line 2343: lat
  ```
  Our counterfactual simulations demonstrate that it is possible to design bonus
  ```
- Line 2349: loc, location
  ```
  equitable allocations. However, the magnitudes of our counterfactual policy
  ```
- Line 2353: son
  ```
  some possible reasons.
  ```
- Line 2355: school, son
  ```
  One possible reason is that the current equilibrium teacher-school sorting is
  ```
- Line 2359: district, loc
  ```
  teachers' preferences and dictatorially allocate teachers to districts. An
  ```
- Line 2360: lat
  ```
  efficiency-seeking dictator can improve efficiency by 31\% relative to the
  ```
- Line 2366: district
  ```
  and districts' preferences and constraints, which ultimately govern
  ```
- Line 2369: district
  ```
  \cdot\right)  $ and districts' resistance costs $R\left(  \cdot\right)  $. To
  ```
- Line 2373: district
  ```
  districts face zero resistance costs, i.e., $R\left(  \cdot\right)
  ```
- Line 2374: lat
  ```
  =0$.\newline For each of these cases, we simulate the new flexible-pay
  ```
- Line 2377: lat
  ```
  The results are shown in the first two columns of Table 8. Relative to the
  ```
- Line 2384: district
  ```
  districts' resistance costs (Column 2), TC increases further and equity
  ```
- Line 2386: lon
  ```
  zero, Case 2 can provide a sense of the long-run equilibrium outcomes under
  ```
- Line 2428: district
  ```
  high-achieving students would be larger. Districts' resistance costs for
  ```
- Line 2437: district
  ```
  districts are more willing to reward teacher contribution; without moving
  ```
- Line 2442: district
  ```
  move, districts' willingness to change their wage schedules, and the
  ```
- Line 2445: lat
  ```
  \subsection{Additional Simulations}
  ```
- Line 2447: lat
  ```
  We have conducted additional simulations in Appendix B6, the results of
  ```
- Line 2455: lon
  ```
  policy impacts in the long run with teachers' extensive-margin responses is
  ```
- Line 2458: lat
  ```
  conduct additional simulations in a static setting and in a setting where the
  ```
- Line 2470: city
  ```
  elasticity with respect to pay is one and that the static market equilibrium
  ```
- Line 2471: lat
  ```
  is played repeatedly for 5 years under a given bonus program, we simulate a
  ```
- Line 2473: district
  ```
  intensive margin (the history of districts they work in during these 5 years).
  ```
- Line 2474: lat
  ```
  Five years later, our program New1 (New2) would lead to an efficiency gain of
  ```
- Line 2479: lat
  ```
  To illustrate the role of program generosity, we simulate three bonus programs
  ```
- Line 2481: lat
  ```
  $(2.3,3.1),$ the latter being the bonus rates used in New1. These three
  ```
- Line 2487: lat
  ```
  expect a simple linear relationship between program costs and their impacts.
  ```
- Line 2492: district
  ```
  To study the role of districts' resistance cost $R\left(  \cdot\right)  $ by
  ```
- Line 2493: lat
  ```
  itself, we re-simulated all our three bonus programs by setting $R\left(
  ```
- Line 2495: district
  ```
  values. Perhaps not surprisingly, without resistance costs, districts are more
  ```
- Line 2503: loc, location
  ```
  Proper allocation of public servants across local employers can have important
  ```
- Line 2507: school
  ```
  market for public school teachers. To that end, we have developed an
  ```
- Line 2514: district
  ```
  Our estimated model implies that, ceteris paribus, giving districts control
  ```
- Line 2516: district
  ```
  teachers across districts. Efficiency improves because districts are allowed
  ```
- Line 2519: district
  ```
  (most) teachers prefer working in districts with more high-achieving students
  ```
- Line 2520: district
  ```
  and flexible pay makes it even easier for these districts to attract teachers.
  ```
- Line 2523: son
  ```
  effectiveness of these policy interventions and the efficiency-equity comparison across pay regimes 
  ```
- Line 2524: district
  ```
  move and districts' willingness to change their wage schedules.
  ```
- Line 2527: lon
  ```
  extending our framework along these lines is worth pursuing. The first
  ```
- Line 2530: district, school, second
  ```
  school districts, but also between public and private sectors. The second
  ```
- Line 2531: house
  ```
  extension is to incorporate household sorting
  ```
- Line 2532: school
  ```
  \citep[e.g.,][]{epple1999estimating,epple2003neighborhood,ferreyra2007estimating,epple2008school}.
  ```
- Line 2538: loc, social
  ```
  allocate socially optimal levels of effort. Finally, our static equilibrium
  ```
- Line 2539: lon
  ```
  model is better suited to study short-run rather than long-run policy impacts;
  ```
- Line 2542: district
  ```
  teachers' life-cycle concerns; on the demand side, districts would consider
  ```
- Line 2545: lon
  ```
  of long-run policy impacts.
  ```
- Line 2563: district
  ```
  \textbf{A1. Districts' Optimal Decisions}
  ```
- Line 2565: district
  ```
  Given $\omega$, district $d$'s job offers $o_{d}\left(  x,c,d_{0}%
  ```
- Line 2590: city
  ```
  with the adjusted capacity and budget constraints. The capacity (budget) is
  ```
- Line 2595: district
  ```
  If the district makes an offer to $\left(  x,c\right)  $ and the offer is
  ```
- Line 2628: district
  ```
  For a given $\omega,$ a district's job offer decision can be derived by the
  ```
- Line 2633: city
  ```
  top-ranked downwards, until the expected capacity or budget is filled, i.e.,
  ```
- Line 2635: district, lat
  ```
  4) Calculate the district's value associated with this $\nu_{M},$ and optimize
  ```
- Line 2638: district
  ```
  $\omega.$\ \newline In the outer loop, the district searches over $\omega$ to
  ```
- Line 2641: lat
  ```
  throughout our simulations.
  ```
- Line 2647: district
  ```
  We model the resistance cost a district faces $R_{d}\left(  \omega\right)  $
  ```
- Line 2660: district
  ```
  the political views of district residents, as measured by $dem_{d}.$ To impose
  ```
- Line 2695: district
  ```
  \caption{Model Fit: OLS of Teacher-District Match (post-Act 10)}%
  ```
- Line 2701: district
  ```
  \multicolumn{2}{c}{All Districts} \\
  ```
- Line 2719: district
  ```
  (\emph{Inferred Offer set}) and Aux 1b (\emph{All Districts}), obtained
  ```
- Line 2726: district
  ```
  \caption{Model Fit: District Wage Schedules}%
  ```
- Line 2743: district
  ```
  \multicolumn{6}{l}{\textbf{B. District Characteristics by $\omega_{2}$ }} \\
  ```
- Line 2753: city
  ```
  Budget/Capacity (\$1,000) & 51.2 & 51.0 &  & &
  ```
- Line 2758: district
  ```
  and district characteristics by $\omega_{2}$: data vs model, post-Act 10.}}
  ```

**/replication-package/replication_delivery/paper_appendix/Online_Appendix_Sept2025.tex**

- Line 3: lat
  ```
  %reference relative to PaperDraft folder so all computers work
  ```
- Line 16: lat
  ```
  %TCIDATA{OutputFilter=latex2.dll}
  ```
- Line 19: lat
  ```
  %TCIDATA{CSTFile=40 LaTeX article.cst}
  ```
- Line 22: name
  ```
  %TCIDATA{<META NAME="GraphicsSave" CONTENT="32">}
  ```
- Line 23: name
  ```
  %TCIDATA{<META NAME="SaveForMode" CONTENT="3">}
  ```
- Line 25: lat, name
  ```
  %TCIDATA{<META NAME="DocumentShell" CONTENT="Standard LaTeX\Blank - Standard LaTeX Article">}
  ```
- Line 75: district, school
  ```
  \title{Equilibrium in the Market for Public School Teachers: District Wage Strategies
  ```
- Line 77: school
  ```
  \author{Barbara Biasi, Chao Fu and John Stromme\thanks{Biasi: Yale School of
  ```
- Line 95: school
  ```
  focus on full-time Grades 4-6 math teachers employed in Wisconsin school
  ```
- Line 96: district
  ```
  districts in 2014 (411 districts and 6,625 individuals).\footnote{Wisconsin
  ```
- Line 97: district, school
  ```
  had 424 school districts in 2014, 11 of which did not have any elementary
  ```
- Line 98: school
  ```
  school, and 2 of which did not have any full-time Grades 4-6 math teachers.}
  ```
- Line 101: district
  ```
  experience. This leaves us with 6,600 teachers and 411 districts in the final
  ```
- Line 103: district
  ```
  full-time Grades 4-6 math teachers employed in 411 districts in 2010. We
  ```
- Line 105: district
  ```
  leaves us with 6,741 teachers and 411 districts in the final validation sample.
  ```
- Line 107: district
  ```
  \subsection{Teacher's Previous District}
  ```
- Line 109: district
  ```
  Our model requires identifying the district where each teacher was working at
  ```
- Line 112: district
  ```
  moved or moved only once between 2011 and 2014, $d_{i0}$ is the district where
  ```
- Line 115: district
  ```
  example, if teacher $i$ worked in District A in 2011 and 2012, and District C
  ```
- Line 116: district
  ```
  in 2013 and 2014, then $d_{i0}=C$. If teacher $i$ worked in District A in 2011
  ```
- Line 117: district
  ```
  and 2012, in District B in 2013, and in District C in 2014, then $d_{i0}%
  ```
- Line 128: school
  ```
  on their math scores. The WKCE was administered in November of each school
  ```
- Line 133: city, gender
  ```
  as gender, race and ethnicity, socioeconomic (SES) status, migration status,
  ```
- Line 136: school
  ```
  Our data allow us to link students and teachers up to the school-grade level,
  ```
- Line 146: lat
  ```
  are strongly correlated and that our auxiliary models used in our structural
  ```
- Line 156: lon
  ```
  ^{2}I\left(  \tau_{k}=n\right)  (\rho_{n}x_{it}+v_{in})+\varepsilon
  ```
- Line 165: school
  ```
  cohort in the school, interacted with grade fixed effects; a set of student
  ```
- Line 166: city, gender
  ```
  characteristics, including gender, race and ethnicity, disability status,
  ```
- Line 168: school
  ```
  characteristics for student $k$'s cohort; cohort size; grade-by-school fixed
  ```
- Line 169: lon
  ```
  effects; and year fixed effects. The variable $\varepsilon_{kt}$ is an i.i.d.
  ```
- Line 171: school
  ```
  $SG_{kt}$ ($SG_{it}^{T})$ denotes the school-grade of student $k$ (teacher
  ```
- Line 181: school
  ```
  in a given school-grade contribute to the achievement of all students in the
  ```
- Line 182: school
  ```
  same school-grade. We make this choice to be able to allow $x_{it}$ to
  ```
- Line 192: lon
  ```
  two assumptions: \newline A1. $\varepsilon_{kt}$ is i.i.d. with mean 0 and
  ```
- Line 193: lon
  ```
  variance $\sigma_{\varepsilon}^{2}$.\newline A2. $Cov(\varepsilon_{kt}%
  ```
- Line 195: school
  ```
  no sorting on unobservables of teachers across school-grades within a
  ```
- Line 196: district
  ```
  district. Although there is no direct test of this assumption, in Section
  ```
- Line 221: lon
  ```
  ^{2}v_{i^{\prime}n}I(\tau_{k}=n)+\varepsilon_{kt}.
  ```
- Line 224: lon, school
  ```
  school-grade that $i$ belongs to. For each teacher $i$ we define, for
  ```
- Line 233: lon
  ```
  {1}{K_{SG_{it}^{T}n}}\sum_{k:SG_{kt}=SG_{it}^{T}}\varepsilon_{kt}.
  ```
- Line 236: school
  ```
  students in teacher $i$'s school-grade in year $t$, conditional on observables
  ```
- Line 238: school
  ```
  school-grade in $t$.
  ```
- Line 262: school
  ```
  school-grade over time has to approach $0$ as the number of periods grows
  ```
- Line 265: school
  ```
  grade-by-school fixed effects (which implies that, across time, the mean of
  ```
- Line 266: school
  ```
  $v_{in}$ is zero within each school-grade) and 2) Assumption A2 guarantees
  ```
- Line 267: school
  ```
  that there is no sorting of teachers on unobservables across school-grades
  ```
- Line 287: school
  ```
  are in the same school-grade as $i$ in both $t$ and $t-1$. The estimator $\hat{v}_{in}$ is forecast-
  ```
- Line 288: school
  ```
  %When classroom assignments are observable and $\hat{v}_{int}$ is defined based on classroom (rather
  ```
- Line 296: school
  ```
  across school-grades over time. Our identifying assumption is that turnover of
  ```
- Line 297: district, lat, school
  ```
  teachers across school-grades, within a district, is unrelated to $v_{in}$
  ```
- Line 299: district
  ```
  sorting of teachers across districts based on $v_{i1}$ and $v_{i2},$ as is the
  ```
- Line 301: school
  ```
  accounted for by the school-grade fixed effects included in $Z_{kt}^{s}.$
  ```
- Line 313: school
  ```
  no two teachers who teach the same school-grade in all $t$. When this is the
  ```
- Line 328: son
  ```
  \citep[e.g.][]{rockoff2004impact,aaronson2007teachers,kane2008estimating}.\footnote{Besides
  ```
- Line 334: lon
  ```
  v_{i(kt)n}+\varepsilon_{kt}\label{achievement_ols_A2}\\
  ```
- Line 341: school
  ```
  \newline A3.\ Assumptions about within school-grade sorting: 1) The
  ```
- Line 345: school
  ```
  teachers in school-grade $SG_{it}^{T}$ in $t$. \newline2) $Cov(j_{int}%
  ```
- Line 347: lat, school
  ```
  unrelated to teacher effectiveness within each school-grade.\newline3) There
  ```
- Line 349: school
  ```
  cross-school-grade teacher turnovers.
  ```
- Line 363: lon
  ```
  which is an estimate for $\sum_{n=1}^{2}v_{i(kt)n}I(\tau_{k}=n)+\varepsilon
  ```
- Line 371: lon
  ```
  ^{T}n}}\sum_{k:SG_{kt}=SG_{it}^{T}}\varepsilon_{kt}%
  ```
- Line 405: school
  ```
  weighted average of the effects of all teachers in $i$'s school-grade over
  ```
- Line 407: school
  ```
  and the fact that we are conditioning on school-grade fixed effects guarantees
  ```
- Line 427: district
  ```
  The identification of this alternative model also relies on within-district
  ```
- Line 428: school
  ```
  school-grade turnover as in Model 1. Equation (\ref{unshrunk2}) represents a
  ```
- Line 440: district
  ```
  An attractive property of our estimator is that it is forecast-unbiased. As we discuss in Section \r
  ```
- Line 441: lat
  ```
  \citet{chetty2014measuring} and estimate the slope of the relationship between
  ```
- Line 444: school
  ```
  $c_{2}$. As\ in \citet{chetty2014measuring}, we control for school-by-grade
  ```
- Line 445: school
  ```
  and school-by-year fixed effects. These tests, shown in columns 1 and 2,
  ```
- Line 455: school
  ```
  the assumption of random sorting of teachers across school-grades within each
  ```
- Line 456: district, school
  ```
  district and school, conditional on all the covariates described at the beginning of Section
  ```
- Line 458: lat
  ```
  relationship between changes in $(c_{1},c_{2})$ and changes in students' \emph{lagged}
  ```
- Line 461: school
  ```
  they would indicate non-random sorting of teachers across grade-schools.
  ```
- Line 471: lat
  ```
  \paragraph{Correlation of Teacher Effectiveness Measures}
  ```
- Line 473: lat
  ```
  Table \ref{tab:model1_2} displays the correlations between $\left(
  ```
- Line 485: district
  ```
  the observed teacher-district matches (we denote these as $O_{i}^{s}$). To
  ```
- Line 495: district
  ```
  of teachers, there are some districts in $O_{i}^{s}\ $but not in
  ```
- Line 496: district
  ```
  $\widetilde{O}_{i}^{s}$ and some districts in $\widetilde{O}_{i}^{s}$ but not
  ```
- Line 506: district
  ```
  1b that characterize teacher-district matches via regressions,
  ```
- Line 522: lon
  ```
  _{6}^{m}c_{1i}\lambda_{d}+\psi_{i}+\varepsilon_{id}^{m},
  ```
- Line 525: district
  ```
  $O_{i}^{s}$ contain more than one district, and an observation $(i,d)$ is a
  ```
- Line 526: district
  ```
  teacher-district pair in these inferred subsets$.$ In Aux 1b, an observation
  ```
- Line 527: district
  ```
  is any teacher-district pair, with $I\times D$ total observations.
  ```
- Line 540: son
  ```
  data with classroom links. We perform this comparison in Table \ref{snr}.
  ```
- Line 553: son
  ```
  we believe these values to be reasonably smaller than that in row 1, since
  ```
- Line 556: son
  ```
  perform reasonably well.
  ```
- Line 573: son
  ```
  student's race can matter for achievement. In comparison, we focus on
  ```
- Line 575: son
  ```
  achievement types. We make this choice for two reasons. First, as shown in
  ```
- Line 579: gender, second
  ```
  Second, if we add a teacher's race and gender and their interactions with the
  ```
- Line 580: district, gender
  ```
  district's racial and gender composition of students to our Aux 1a (Column 1
  ```
- Line 591: lon
  ```
  ^{m}+\varepsilon_{it}, \label{w0}%
  ```
- Line 596: degree
  ```
  $[15,+\infty)$; and $MA_{it}$ is an indicator for having a Master's degree
  ```
- Line 597: degree
  ```
  (MA) or a higher degree. The parameter $\delta_{0}$ can be interpreted as the
  ```
- Line 601: lat
  ```
  higher experience category, relative to those with zero experience with the
  ```
- Line 604: district
  ```
  We estimate the intercept $\delta_{d}^{0}$ separately for each district.
  ```
- Line 606: district
  ```
  coefficients $\delta^{e}$ and $\delta^{m}$ by groups of districts, defined as follows:
  ```
- Line 609: district
  ```
  \item For the 35 large districts (i.e., those with at least 10 teachers in
  ```
- Line 610: district
  ```
  each experience and education category), each group corresponds to a district.
  ```
- Line 612: district
  ```
  \item For the remaining 356 districts, we construct groups based on the
  ```
- Line 616: district, lat
  ```
  \item For each district, we calculate the following summary statistics for
  ```
- Line 624: district
  ```
  \item We check whether each district is above or below the median of the
  ```
- Line 625: district
  ```
  cross-districts distribution for each of the three statistics.
  ```
- Line 628: district
  ```
  and assign each district to a group as follows:%
  ```
- Line 652: district
  ```
  \ref{w0}\right)  $. In particular, it reports the cross-district means and
  ```
- Line 655: lat
  ```
  $w_{it}^{0}$ in 2010. The former predicts the latter remarkably well, with a
  ```
- Line 656: lat
  ```
  correlation coefficient of 0.93 (significant at 1 percent).
  ```
- Line 658: district
  ```
  \subsubsection{Districts' Choice Set of Wage Schedules\label{wages}}
  ```
- Line 660: district
  ```
  A district's wage rule is given by
  ```
- Line 672: district
  ```
  _{d1},\widetilde{\omega}_{d2}\right)  \geq0$ separately for each district
  ```
- Line 676: lon
  ```
  }_{d2}TC\left(  c_{i},\lambda_{d}\right)  +\varepsilon_{i}^{w},\text{ for
  ```
- Line 679: district
  ```
  where $w_{i}$ is the observed 2014 wage for teacher $i$ working in district
  ```
- Line 699: district
  ```
  \item We assign each district the wage schedule $(\omega_{d1}^{o},\omega
  ```
- Line 701: district
  ```
  wages in that district $\left\{  i:d(i)=d\right\}  $, i.e.,
  ```
- Line 733: district
  ```
  district choices may go out of the empirical range in counterfactual
  ```
- Line 759: district
  ```
  summarizes the observed distribution of teacher wages in each district $d.$
  ```
- Line 768: lat
  ```
  We have also tested for the possibility that the relationship between teacher
  ```
- Line 784: lat
  ```
  (y-axis) and the alternative schedule shown above (x-axis). The relationship
  ```
- Line 785: degree
  ```
  is close to a 45-degree line, indicating that the predicted wages are very
  ```
- Line 790: lat
  ```
  Next, we test whether the relationship between teacher contribution and wages
  ```
- Line 795: degree, lat
  ```
  above (x-axis). As before, the relationship is close to a 45-degree line,
  ```
- Line 803: district
  ```
  Teachers' decision rule implies that if District $d$ makes an offer to the
  ```
- Line 807: lon
  ```
  x,c,d_{0}\right)  }{\sigma\epsilon}\right)  }{\exp\left(  \frac{V_{d}\left(
  ```
- Line 808: lon
  ```
  x,c,d_{0}\right)  }{\sigma_{\epsilon}}\right)  +\sum_{d^{\prime}\in
  ```
- Line 810: lon
  ```
  \frac{V_{d^{\prime}}\left(  x,c,d_{0}\right)  }{\sigma\epsilon}\right)  }.
  ```
- Line 813: district
  ```
  We assume that districts make decisions based on a simplified belief, given
  ```
- Line 828: district
  ```
  are the mean and standard deviation of wages across all districts for a
  ```
- Line 842: district
  ```
  _{x,c}\ $, to be consistent with decisions made by teachers and districts.
  ```
- Line 875: district
  ```
  the observed $\omega_{d}^{o}$ for each district $d$.
  ```
- Line 879: lat
  ```
  calculate each teacher's acceptance probabilities $h_{d}\left(  \cdot\right)
  ```
- Line 897: district
  ```
  received offers. The implied teacher-district matches will be compared with
  ```
- Line 902: district
  ```
  \right)  $, each district makes optimal decisions on its wage schedule
  ```
- Line 904: district
  ```
  single district's wage for Teacher $i$ has a negligible effect on wage
  ```
- Line 907: district
  ```
  deviation of Teacher $i$'s wage across the 411 districts in our sample.}$ The
  ```
- Line 918: district
  ```
  However, notice that districts' wages are given by
  ```
- Line 950: district
  ```
  Similarly, the cross-district wage standard deviation for a teacher will be
  ```
- Line 968: lat
  ```
  and are policy dependent. We now describe the algorithm we use to simulate the
  ```
- Line 973: district
  ```
  We draw $M$ economies, each with $D$ districts and $N$ teachers. All economies
  ```
- Line 974: district
  ```
  share the same observable teacher and district characteristics as those in the
  ```
- Line 979: lat
  ```
  are calculated as the average outcomes across $M$ economies. For each economy
  ```
- Line 992: district
  ```
  \right\}  ,$ each district $d$ chooses its optimal wage and offer policies
  ```
- Line 1002: lat
  ```
  \item Calculate the distance between $\left\{  \widetilde{\overline{w}}\left(
  ```
- Line 1015: district
  ```
  decisions made by districts and teachers under this consistent belief.
  ```
- Line 1021: district
  ```
  \section{Across-District vs Within-District Variation}
  ```
- Line 1024: district
  ```
  focusing on competition across districts. Here we show that cross-district
  ```
- Line 1025: district, school
  ```
  variation clearly dominates within-district, cross-school variation in terms
  ```
- Line 1033: district, second
  ```
  and MSE change as we sequentially add district fixed effects (second row) and
  ```
- Line 1034: district, school
  ```
  school fixed effects (third row). Adding district fixed effects reduces the
  ```
- Line 1035: district
  ```
  root MSE by 31.3\%; this implies that differences across districts explain
  ```
- Line 1037: school
  ```
  characteristics. Adding school fixed effects instead only explains an
  ```
- Line 1039: district, school
  ```
  variation in wages is across districts, not across schools within districts.
  ```
- Line 1043: district
  ```
  The cross-district variation in the share of low-achieving students ($\lambda$
  ```
- Line 1044: district, school
  ```
  in our model) largely dominates the within-district, cross-school variation.
  ```
- Line 1047: district, school
  ```
  which we progressively add district and school fixed effects, indicates that
  ```
- Line 1048: district, school
  ```
  districts explain 8.7\% of the variation in this probability whereas schools
  ```
- Line 1050: district, school
  ```
  regression of the school-level share of low-achieving students on district
  ```
- Line 1052: district, school
  ```
  the school-level share is explained by the district. \newline3. For each
  ```
- Line 1053: lat, school
  ```
  school, we calculate the absolute difference between the school-level and the
  ```
- Line 1054: district
  ```
  district-level shares of low-achieving students. This absolute difference has
  ```
- Line 1071: district
  ```
  Under the information assumption we have made, districts and the researcher see $\widehat{c},$ a for
  ```
- Line 1072: district
  ```
  decisions based on $\widehat{c}$. Suppose instead that districts realize that
  ```
- Line 1074: lat
  ```
  bias our estimates of $\left(  b_{1},b_{2}\right)  $ downward relative to
  ```
- Line 1075: district
  ```
  $b_{0}$ in the districts' preference function (Equation (\ref{dist}))?
  ```
- Line 1080: district
  ```
  \newline Q2: Will this cause districts to reduce wage rewards ($\omega_{2}$)
  ```
- Line 1095: district
  ```
  that the researcher and the district see the same $\widehat{c},$ the answer is
  ```
- Line 1096: district
  ```
  no; whether or not districts know what they see is noisy would not affect their actions. If they do 
  ```
- Line 1103: district
  ```
  district observes is%
  ```
- Line 1113: district
  ```
  the case in our original model. Given that the researcher and the districts
  ```
- Line 1122: lon
  ```
  \textbf{For Q2:} As long as $\widehat{c}$ is a forecast-unbiased estimate of $c,$ the
  ```
- Line 1124: district
  ```
  notice that in our model, districts reward $TC$ in their wage schedule not
  ```
- Line 1128: city
  ```
  $). Accounting for the constraints it faces (budget, capacity, and resistance
  ```
- Line 1129: district
  ```
  cost), the district uses the most effective combination of $\left(  \omega
  ```
- Line 1131: district
  ```
  combination, as a best response to other districts' strategies, can
  ```
- Line 1139: district
  ```
  which is the input that enters the districts' wage function (rewarded at the rate of $\omega_{2}$). 
  ```
- Line 1141: district
  ```
  for a district to attract high-value teachers in a complete information
  ```
- Line 1143: district
  ```
  where the district observes $\widehat{c}$ (a noisy but unbiased forecast of
  ```
- Line 1147: district
  ```
  the district in these two cases. In other words, districts would not reduce
  ```
- Line 1150: district
  ```
  direct implication of districts' optimal decisions: It holds for any
  ```
- Line 1151: district
  ```
  districts' preference parameters $b.\footnote{Of course, one can come up with
  ```
- Line 1152: district
  ```
  very different models of districts' behaviors and informational environment
  ```
- Line 1158: lon
  ```
  In this case, the researcher no longer observes the same teacher traits that
  ```
- Line 1159: district
  ```
  the district uses to make offer decisions. The information asymmetry between
  ```
- Line 1160: district
  ```
  the district and the researcher would break our identification strategy. To
  ```
- Line 1161: district
  ```
  separate teachers' and districts' preferences, we rely on our ability to
  ```
- Line 1164: district
  ```
  that are used by districts to make offer decisions. Therefore, in our approach, it is a necessary id
  ```
- Line 1171: district
  ```
  x,c\right)  $ are observable to all districts. \newline A2: Districts cannot
  ```
- Line 1176: district
  ```
  districts observe $\left(  c_{1}+err_{1},c_{2}+err_{2}\right)  $ and make wage
  ```
- Line 1185: district
  ```
  and districts' decisions are based on $\left(  c_{1}+err_{1},c_{2}%
  ```
- Line 1187: district
  ```
  $. Columns 5-7 show the corresponding estimates assuming that districts'
  ```
- Line 1192: lat
  ```
  To investigate robustness to a violation of A2, we consider the possibility
  ```
- Line 1193: son
  ```
  that some ineffective teachers may have been hired for reasons other than
  ```
- Line 1195: district
  ```
  its counterpart that does not use observed teacher-district $\left(
  ```
- Line 1202: lat
  ```
  simple violations of A1 and A2 may not seriously affect our inference, they do
  ```
- Line 1208: district
  ```
  \subsection{Districts' Preferences: Illustrating The Identification}
  ```
- Line 1213: district, lon
  ```
  districts.} As long as at least some districts are selective (i.e., they do
  ```
- Line 1215: district
  ```
  this predicted distribution of teacher-district matches will be systematically
  ```
- Line 1218: district
  ```
  preferences, districts' offer decisions---which are governed by districts'
  ```
- Line 1222: district
  ```
  shocks and suppose that two teachers $i$ and $j$ both prefer district $1$ over
  ```
- Line 1223: district
  ```
  district $2$. If we observe $i$ working in district $1$ and $j$ working in
  ```
- Line 1224: district
  ```
  district $2$, it must be the case that district $1$ prefers $i$ over $j.$ The
  ```
- Line 1226: district
  ```
  systematically prefer district $1$ over district $2$, then district $1$ must
  ```
- Line 1227: district, lon
  ```
  prefer their hires over (most) teachers working in district $2.$ As long as
  ```
- Line 1228: district
  ```
  the distribution of $\left(  x,c\right)  $ in district $1$ does not
  ```
- Line 1229: district
  ```
  systematically dominate the distribution of $\left(  x,c\right)  $ in district
  ```
- Line 1230: district
  ```
  $2$ in all dimensions, we can infer how much district $1$ cares about $x$ and
  ```
- Line 1231: lat
  ```
  $c_{2}$ relative to $c_{1}$ (the coefficient for $c_{1}$ is normalized to $1$).
  ```
- Line 1234: district
  ```
  simple example. There are two districts, $d_{1}$ and $d_{2}$, that make offer
  ```
- Line 1237: city, district
  ```
  $c_{2}$). Both districts have the same capacity, $0.5,$ and identical
  ```
- Line 1242: lat
  ```
  where $b$ is the importance of $c_{2}$ relative to $c_{1}.$ Teachers'
  ```
- Line 1243: district
  ```
  preferences for district $d=d_{1},d_{2}$ are given by
  ```
- Line 1245: lon
  ```
  I\left(  d=d_{1}\right)  +\epsilon_{d},
  ```
- Line 1247: lon
  ```
  where $\epsilon_{d}$'s are type-1 extreme-value preference shocks that are
  ```
- Line 1248: district
  ```
  i.i.d. across district-teacher pairs with mean $0$ and a scale parameter of
  ```
- Line 1253: district
  ```
  $d_{1}$, the more desirable district, extends offers to its favored teachers,
  ```
- Line 1254: city
  ```
  those with higher $B\left(  c\right)  ,$ until it reaches its capacity;
  ```
- Line 1255: district
  ```
  $d_{2},$ the less desirable district, extends offers to every teacher. Panels
  ```
- Line 1259: city, district
  ```
  given districts' preferences and capacity constraints, $d_{1}$ only makes
  ```
- Line 1261: district
  ```
  teachers who end up working in district $d_{1},$ and the solid blue squares
  ```
- Line 1286: lat
  ```
  re-simulate our model $N$ times. In the $n^{th}$ simulation, we use the
  ```
- Line 1295: block, loc
  ```
  within sub-blocks of this matrix. Specifically, we split the auxiliary models
  ```
- Line 1298: district
  ```
  district preference parameters, and wage-setting resistance cost parameters).
  ```
- Line 1302: block, loc
  ```
  perturbations within a given sub-block.
  ```
- Line 1306: district
  ```
  1a and Aux1b), as well as the regression coefficients that link districts'
  ```
- Line 1308: lat
  ```
  that Aux 1a and Aux 1b are closely related to teachers' preferences, as these
  ```
- Line 1310: district
  ```
  choices. Additionally, as teachers' preferences change, districts change their
  ```
- Line 1314: district
  ```
  Column 2 shows that district preference parameters mostly affect the
  ```
- Line 1315: district
  ```
  regression coefficients that link wages to districts' pre-determined
  ```
- Line 1318: district
  ```
  districts' preferences as districts can use wage choice to push or pull
  ```
- Line 1320: district
  ```
  informative of districts' preferences.
  ```
- Line 1323: district
  ```
  affect the wage regressions and cross-district wage moments (Aux 3 and Aux 4).
  ```
- Line 1335: district, loc, location
  ```
  allocations of teachers to districts. First, we attempt to improve efficiency
  ```
- Line 1336: loc
  ```
  or equity by allocating teachers at will, regardless of their preferences.
  ```
- Line 1337: second
  ```
  Second, we re-examine the effects of teacher bonus programs under additional
  ```
- Line 1341: lat
  ```
  additional simulations.
  ```
- Line 1343: loc
  ```
  \subsection{Allocating Teachers at Will}
  ```
- Line 1349: loc
  ```
  market. To do so, we allocate teachers with the largest comparative advantage
  ```
- Line 1350: district
  ```
  in teaching low-achieving students (measured as $(c_{1}-c_{2})$) to districts
  ```
- Line 1352: district, loc, location
  ```
  allocation, we sort districts by $\lambda_{d}$ (fraction of low-achieving
  ```
- Line 1354: district
  ```
  _{d}$ district with the highest-$\left(  c_{1}-c_{2}\right)  $ teachers until
  ```
- Line 1355: city, district
  ```
  the district's capacity is filled. We then move to the next district and fill
  ```
- Line 1356: city
  ```
  its capacity with the highest-$\left(  c_{1}-c_{2}\right)  $ teachers among
  ```
- Line 1357: city, district
  ```
  those yet to be assigned. We proceed by filling the capacity of all districts
  ```
- Line 1362: district
  ```
  highest-$\lambda_{d}$ district with the highest-$c_{1}$ teachers until the
  ```
- Line 1363: city, district
  ```
  district's capacity if filled; then, we move to the next district to fill its
  ```
- Line 1364: city
  ```
  capacity with the highest-$c_{1}$ teachers among those yet to be assigned. We
  ```
- Line 1365: city, district
  ```
  proceed by filling the capacity of all districts according to this rule, in
  ```
- Line 1372: loc, location
  ```
  student lose 55.8\%. These estimates indicate that there exist reallocations
  ```
- Line 1380: lon
  ```
  A rigorous analysis of policy impacts in the long run with teachers'
  ```
- Line 1384: lat
  ```
  conduct further counterfactual policy simulations under additional assumptions
  ```
- Line 1395: district
  ```
  they are the pool of teachers who are available for districts to hire. After
  ```
- Line 1396: district
  ```
  Stage 0, districts and participant teachers make decisions as specified in our
  ```
- Line 1424: degree
  ```
  group fixed effects; an indicator for holding a Master's degree; the
  ```
- Line 1434: city
  ```
  variation. We therefore cannot credibly estimate an entry elasticity with
  ```
- Line 1443: city
  ```
  an assumed level of elasticity (in Step 4).
  ```
- Line 1461: lat
  ```
  \item Given 1 and 2, we can simulate the baseline equilibrium with entry and
  ```
- Line 1464: district
  ```
  districts and participating teachers make decisions as specified in our main
  ```
- Line 1472: lat
  ```
  \item We then additionally simulate the model under a given counterfactual
  ```
- Line 1477: lat
  ```
  \item Using the baseline equilibrium, we calculate each teacher's expected pay
  ```
- Line 1481: city
  ```
  \item Assuming a given entry/exit elasticity with respect to pay that is
  ```
- Line 1482: lat
  ```
  consistent with the literature, we can calculate the change in a teacher's
  ```
- Line 1483: lat
  ```
  entry/exit probability in response to the pay change calculated in 4 (a). This
  ```
- Line 1488: lat
  ```
  \item Given this new pool of participants, we simulate the equilibrium
  ```
- Line 1489: district
  ```
  interaction between districts and participating teachers as specified in our
  ```
- Line 1494: district
  ```
  happens, some participants will end up without a district match.
  ```
- Line 1506: district
  ```
  and districts play the static game (as described in Section \ref{oneshot})
  ```
- Line 1507: lat
  ```
  repeatedly over $T$ years.\footnote{In particular, when simulating entry/exit
  ```
- Line 1508: lat
  ```
  decisions, we assume that an individual teacher calculates their expected pay
  ```
- Line 1513: district
  ```
  probability accordingly. For example, if a teacher works in district $d$ in
  ```
- Line 1515: district
  ```
  (whether or not to exit and which district's offer to accept if they stay); if
  ```
- Line 1518: loc, location
  ```
  time, the location of an incumbent teacher is updated. This path of
  ```
- Line 1519: district
  ```
  cross-district movements, together with exits and entries, will determine the
  ```
- Line 1520: district, loc, location
  ```
  allocation of teachers across districts over time.
  ```
- Line 1522: lat
  ```
  To simplify the simulation exercise, we assume that the state uses the
  ```
- Line 1523: district, lat
  ```
  baseline district characteristics, including $\lambda_{d}$, to calculate
  ```
- Line 1524: son
  ```
  teacher bonuses. As a caveat, this assumption is reasonable when we consider
  ```
- Line 1525: lon
  ```
  shorter $T$, but is not well suited for very long $T$.\footnote{State
  ```
- Line 1526: district, school
  ```
  transfers to school districts tend to be stable over time as functions of
  ```
- Line 1527: district
  ```
  district-level observables (e.g., property values) that get reassessed very
  ```
- Line 1528: lat, school
  ```
  infrequently \citep{biasi2023school}} Therefore, we consider a relatively
  ```
- Line 1529: lat
  ```
  short $T=5$ in our following simulation.
  ```
- Line 1533: lat
  ```
  For illustration, we simulate the results for our counterfactual bonus
  ```
- Line 1547: district
  ```
  i.e., when teachers and districts play a static game (as in our main model),
  ```
- Line 1549: block, loc
  ```
  contrasts its impacts in four cases. Within each block of columns, the first
  ```
- Line 1559: district
  ```
  allowing teachers and districts to repeatedly play the static game. For each
  ```
- Line 1561: block, loc
  ```
  each block of columns, the first column refers to the setting in our main text
  ```
- Line 1562: second
  ```
  (a static game without teachers' extensive-margin responses); the second
  ```
- Line 1564: city
  ```
  repeatedly for 5 years) with an entry/exit elasticity of 1.0.\footnote{The
  ```
- Line 1574: lon
  ```
  responses are non-trivial and if the programs last longer, the
  ```
- Line 1578: city
  ```
  with additional assumptions, including externally set entry/exit elasticity
  ```
- Line 1581: lat
  ```
  \subsection{Further Details and Additional Simulations}
  ```
- Line 1588: lat
  ```
  Relative to Table 7 in the main text, this table shows more detailed impacts:
  ```
- Line 1589: district
  ```
  for each student group within a district group.
  ```
- Line 1594: lat
  ```
  impacts, we simulate three bonus programs with bonus rates $\left(
  ```
- Line 1595: lat
  ```
  r_{0},r_{1}\right)  $ set at 1.5, 2.0, and 2.5 times $(2.3,3.1),$ (the latter
  ```
- Line 1602: lat
  ```
  one should not expect a simple linear relationship between program costs and
  ```
- Line 1608: district
  ```
  To study the role of districts' resistance cost $R\left(  \cdot\right)  $ by
  ```
- Line 1609: lat
  ```
  itself, we re-simulated our bonus programs by setting $R\left(  \cdot\right)
  ```
- Line 1611: district
  ```
  \ref{tab:XXX3}). Perhaps not surprisingly, without resistance costs, districts
  ```
- Line 1619: district
  ```
  pre-Act 10 era, when districts were forced to use the rigid wage schedule. We
  ```
- Line 1676: school
  ```
  \caption{Test for Forecast Unbiasedness (Chetty et al., 2014) and Non-Random Teacher Sorting Across 
  ```
- Line 1692: school
  ```
  School-by-year FE & Yes & Yes & Yes & Yes\\\hline
  ```
- Line 1694: school
  ```
  \# school-grades & 1950 & 694 & 582 & 174\\\hline\hline
  ```
- Line 1703: lat
  ```
  \caption{Correlation of Teacher Effectiveness between Model 1 and Model 2}%
  ```
- Line 1780: son
  ```
  \caption{Comparison of Signal-to-Noise Ratios with Estimates of Math Teacher
  ```
- Line 1785: lat
  ```
  Estimate & signal-to- & \multicolumn{1}{c}{calculated as}\\
  ```
- Line 1800: school, son
  ```
  Aaronson et al. (2007) high-school VA & 0.70 & ratio between adjusted and\\
  ```
- Line 1828: city
  ```
  Students' Race/Ethnicity}%
  ```
- Line 1861: city
  ```
  obtained controlling for teachers'\ (T) and students' (S) race/ethnicity
  ```
- Line 1865: district
  ```
  \caption{Cross-District Summary of Pre-Reform Wage Schedules}%
  ```
- Line 1870: district
  ```
  &  & {\small Cross-District Mean} & {\small Cross-District Std Dev.}\\\hline
  ```
- Line 1891: district
  ```
  \caption{Variation in Salaries Across and Within Districts, 2013-2016}%
  ```
- Line 1900: district
  ```
  {\small + District FE} & {\small 4,711} & {\small 0.86} & {\small 31.3\%}\\
  ```
- Line 1901: school
  ```
  {\small + School FE} & {\small 4,523} & {\small 0.87} & {\small 34.0\%}%
  ```
- Line 1921: district
  ```
  & Baseline$^{a}$ &  & \multicolumn{3}{c}{For teachers and districts} &  &
  ```
- Line 1922: district
  ```
  \multicolumn{3}{c}{For districts only} & \\\cline{2-2}\cline{4-6}\cline{8-10}
  ```
- Line 1986: district
  ```
  districts, with various variances; and columns 5-7 assume noise in the
  ```
- Line 1987: district
  ```
  measures of teacher effectiveness only for districts, with various variances.
  ```
- Line 1991: district
  ```
  \caption{OLS of Teacher-District Matches (Aux 1a): Baseline and Excluding
  ```
- Line 2028: district
  ```
  estimates; column 2 shows estimates obtained ignoring teacher-district matches
  ```
- Line 2035: son
  ```
  \caption{Teachers With One vs Many Offers: Comparison}
  ```
- Line 2067: district
  ```
  Auxiliary Model: & Teacher Preferences & District Preferences & Wage-Setting
  ```
- Line 2085: loc
  ```
  \caption{Allocating Teachers at Will}%
  ```
- Line 2100: loc
  ```
  \caption*{{\footnotesize Notes: Policy impacts when teachers are allocated
  ```
- Line 2101: district
  ```
  to districts at will.}}\end{table}
  ```
- Line 2112: city
  ```
  {\small Entry/Exit Elasticity} & {\small -} & {\small 0.5} & {\small 1.0} &
  ```
- Line 2138: city
  ```
  {\small Entry/Exit Elasiticity} & {\small -} & {\small 1.0} &  &  &  &
  ```
- Line 2153: district
  ```
  and assuming districts play a repeated static game.}}\end{table}
  ```
- Line 2168: district
  ```
  {\small 4th quartile }$\lambda_{d}$ {\small districts} & $\overline
  ```
- Line 2172: district
  ```
  {\small 3rd quartile }$\lambda_{d}$ {\small districts} & $\overline
  ```
- Line 2176: district
  ```
  {\small 2nd quartile }$\lambda_{d}$ {\small districts} & $\overline
  ```
- Line 2180: district
  ```
  {\small 1st quartile }$\lambda_{d}$ {\small districts} & $\overline
  ```
- Line 2210: district
  ```
  {\small districts} & -0.33 & -0.59 & -1.04 & -1.26\\
  ```
- Line 2212: district
  ```
  {\small districts} & -0.34 & -0.61 & -0.76 & -0.81\\
  ```
- Line 2214: district
  ```
  {\small districts} & 0.41 & 0.63 & 0.64 & 0.86\\
  ```
- Line 2216: district
  ```
  {\small districts} & 1.08 & 1.50 & 2.00 & 2.05\\\hline
  ```
- Line 2242: district
  ```
  {\small districts} & 0.46 & 1.02 & 0.69 & 0.05\\
  ```
- Line 2244: district
  ```
  {\small districts} & 0.45 & 0.78 & 0.94 & 0.27\\
  ```
- Line 2246: district
  ```
  {\small districts} & 0.52 & 0.003 & 0.66 & 0.39\\
  ```
- Line 2248: district
  ```
  {\small districts} & 0.02 & -0.92 & -0.85 & 0.42\\\hline
  ```
- Line 2263: district
  ```
  \caption{Teacher and District Characteristics (2010)}
  ```
- Line 2280: district
  ```
  {\small B. District Characteristics} & {\small All} & $\lambda_{d}%
  ```
- Line 2286: city
  ```
  {\small Capacity} & {\small 16.4 (30.7)} & {\small 18.4 (16.2)} & {\small 15.1
  ```
- Line 2288: city
  ```
  {\small Budget/Capacity} {\small (\$1,000)} & {\small 52.4 (6.1)} &
  ```
- Line 2290: district
  ```
  \multicolumn{4}{l}{{\small Characteristics of District Incumbent Teachers
  ```
- Line 2300: district
  ```
  {\small \# Districts} & {\small 411} & {\small 103} & {\small 103}%
  ```
- Line 2306: district
  ```
  of teacher (Panel A) and district (Panel B) characteristics.}}\end{table}
  ```
- Line 2311: district
  ```
  \caption{Model Fit: Average District Employee Characteristics $(d^{\ast
  ```
- Line 2319: district
  ```
  \multicolumn{2}{l}{District Group \ \ \ \ \ \ } & Data & Model & Data &
  ```
- Line 2329: city
  ```
  $\frac{\text{Budget}}{\text{Capacity}}$ & Quintile 1 & 11.5 & 11.8 & 0.29 &
  ```
- Line 2348: district
  ```
  \caption{Model Fit: OLS of District Wage Schedule}
  ```
- Line 2372: district
  ```
  {\small \emph{District Characteristics}} &  &  &  &  &  & \\
  ```
- Line 2377: city
  ```
  {\small capacity} & {\small -0.00002} & {\small 0.0001} &  &  & {\small -0.35}
  ```
- Line 2387: district
  ```
  \multicolumn{7}{l}{{\small \emph{Teachers in nearby districts} (}$z_{d_{0}%
  ```
- Line 2400: district
  ```
  \caption{Model Validation: OLS of Teacher-District Match (pre-Act 10)}%
  ```
- Line 2406: district
  ```
  \multicolumn{2}{c}{{\small All Districts}$^{b}$}\\\cline{3-4}\cline{6-9}
  ```
- Line 2439: district
  ```
  Aux 1a (\emph{Inferred Offer set}) and Aux 1b (\emph{All Districts}), obtained
  ```
- Line 2443: district
  ```
  \caption{Model Validation: Average District Employee Characteristics (pre-Act
  ```
- Line 2452: district
  ```
  \multicolumn{2}{l}{{\small District Group \ \ \ \ \ \ }} & {\small Data} &
  ```
- Line 2467: city
  ```
  $\frac{\text{{\small Budget}}}{\text{{\small Capacity}}}$ & {\small Quintile
  ```
- Line 2504: lat
  ```
  \caption{Relationship Between $c_{1}$ and $c_{2}$}%
  ```
- Line 2511: lat
  ```
  \caption{Relationship Between $W_{d}^{0}\left(  x_{i}\right)  $ and
  ```
- Line 2577: district
  ```
  \caption{Share of Districts' Budgets Spent on Teacher Salaries, by Grade and
  ```
- Line 2582: loc
  ```
  \includegraphics[width=\textwidth]{figures/budget_allocated_math}
  ```
- Line 2586: loc
  ```
  \includegraphics[width=\textwidth]{figures/budget_allocated_lang}
  ```
- Line 2590: loc
  ```
  \includegraphics[width=\textwidth]{figures/budget_allocated_grades_4_6}
  ```
- Line 2594: loc
  ```
  \includegraphics[width=\textwidth]{figures/budget_allocated_grades_1_3}
  ```
- Line 2597: lat
  ```
  the share of total wages paid to teachers in each subgroup, calculated for
  ```
- Line 2598: district
  ```
  each district. }\end{figure}
  ```
- Line 2624: district
  ```
  \caption{Distribution of $\lambda$ Across Wisconsin Districts}
  ```
- Line 2630: district, school
  ```
  \nocite{wdpi_school_districts_2024}
  ```
- Line 2631: district, school
  ```
  \nocite{nces_school_district_boundaries}
  ```

**/replication-package/replication_delivery/synthetic_data/code/00_project_globals.do**

- Line 4: lat
  ```
  Public synthetic/simulated-data globals.
  ```
- Line 9: lat
  ```
  do synthetic_data/code/make_simulated_data.do
  ```
- Line 12: loc
  ```
  local pwd "`c(pwd)'"
  ```

**/replication-package/replication_delivery/synthetic_data/code/02_make_synthetic_data.do**

- Line 8: lat
  ```
  Public simulated/synthetic-data generator.
  ```
- Line 43: loc
  ```
  local data_label_start = 10
  ```
- Line 44: loc
  ```
  local data_label_end = `data_label_start' + 81 - 1
  ```
- Line 45: loc
  ```
  local variable_label_start = ///
  ```
- Line 48: loc
  ```
  local variable_label_end = `variable_label_start' + 81 * `nvars' - 1
  ```
- Line 62: loc
  ```
  local staff_years_txt_old "2006 2007 2008 2009 2010 2011"
  ```
- Line 63: loc
  ```
  local staff_years_txt_new "2012 2013 2014"
  ```
- Line 64: loc
  ```
  local staff_years_csv "2015 2016"
  ```
- Line 65: loc
  ```
  local dist_codes "413 422 665 2793 714 721 1015 1253 1376 1890 1900 2058 2177 2184 2217 2296 2303 24
  ```
- Line 66: loc
  ```
  local n_dist_codes : word count `dist_codes'
  ```
- Line 67: loc
  ```
  local area_codes "50 400 412 300 600 312 550 800"
  ```
- Line 68: loc
  ```
  local exp3_mover_seqs "30 38 47 57 68 80"
  ```
- Line 87: loc
  ```
  foreach s of local exp3_mover_seqs {
  ```
- Line 95: school
  ```
  gen int va_cluster_school = .
  ```
- Line 96: loc
  ```
  local va_lo = 1
  ```
- Line 98: loc
  ```
  local va_hi = `va_lo' + `k' - 1
  ```
- Line 100: school
  ```
  replace va_cluster_school = 1900 + `k' if inrange(seq, `va_lo', `va_hi')
  ```
- Line 101: loc
  ```
  local va_lo = `va_hi' + 1
  ```
- Line 117: lon
  ```
  gen long filenum_n = 600000 + staff_idx
  ```
- Line 125: name
  ```
  gen str20 lastname = "S" + string(staff_idx, "%04.0f") + "-Unit X"
  ```
- Line 126: name
  ```
  gen str16 firstname = "P" + string(mod(staff_idx, 10000), "%04.0f") + "-Giv X"
  ```
- Line 127: gender
  ```
  gen str1 gender = cond(mod(staff_idx, 2) == 0, "F", "M")
  ```
- Line 132: birth
  ```
  gen float birth = 1958 + mod(staff_idx, 28)
  ```
- Line 133: birth
  ```
  replace birth = 1960 if inlist(seq, 1, 7, 11, 16, 22, 29)
  ```
- Line 143: loc
  ```
  foreach s of local exp3_mover_seqs {
  ```
- Line 147: lat
  ```
  * These rows survive the model-data restrictions and are later expanded
  ```
- Line 148: district
  ```
  * across the 411-district synthetic model universe.
  ```
- Line 158: loc
  ```
  gen float localexp = max(1, exp_eff - mod(staff_idx, 4))
  ```
- Line 160: loc
  ```
  replace localexp = localexp * 10 if year <= 2014
  ```
- Line 169: school
  ```
  gen byte school_slot = mod(staff_idx + assign_n + year, `n_dist_codes') + 1
  ```
- Line 170: school
  ```
  replace school_slot = va_cluster_size if va_cluster_size < .
  ```
- Line 171: school
  ```
  gen float workagencycode = real(word("`dist_codes'", school_slot))
  ```
- Line 172: school
  ```
  gen float schoolcode = 1000 + school_slot
  ```
- Line 174: school
  ```
  replace schoolcode = 1000 + assign_n if seq == 10
  ```
- Line 176: school
  ```
  replace schoolcode = 1100 + assign_n if seq == 20
  ```
- Line 178: school
  ```
  replace schoolcode = 1800 + assign_n if seq == 410
  ```
- Line 180: school
  ```
  replace schoolcode = 1700 + assign_n if seq == 420
  ```
- Line 185: school
  ```
  replace schoolcode = va_cluster_school if va_cluster_size < .
  ```
- Line 189: school
  ```
  replace schoolcode = 2500 + (seq - 394) * 20 + (year - 2006) ///
  ```
- Line 223: district, name
  ```
  gen str30 workagencyname = "Synthetic District " + string(workagencycode, "%04.0f")
  ```
- Line 224: lname, name, school
  ```
  gen str30 schoolname = "Synthetic School " + string(schoolcode, "%04.0f")
  ```
- Line 226: school
  ```
  gen str2 cesa = string(1 + mod(school_slot, 12), "%02.0f")
  ```
- Line 227: school
  ```
  gen float county = 1 + mod(school_slot, 20)
  ```
- Line 228: school, zip
  ```
  gen str10 zipcode = "53" + string(100 + mod(school_slot, 800), "%03.0f")
  ```
- Line 237: birth, gender, name
  ```
  order id lastname firstname filenumber gender race birth highedu dayscontract ///
  ```
- Line 238: loc
  ```
  localexp totalexp salary fringe staffcat hireagencycode workagencycode ///
  ```
- Line 239: school
  ```
  hireagencytype schoolcode position area lowgrade highgrade lowgradecode ///
  ```
- Line 240: lname, name, school
  ```
  highgradecode bilingual fte workagencyname schoolname gradelevel cesa ///
  ```
- Line 241: zip
  ```
  county workagencytype zipcode lt_substitute subcontracted year
  ```
- Line 244: loc
  ```
  local staff_file_rows = r(N)
  ```
- Line 247: birth, gender, name
  ```
  keep id lastname firstname filenum_n gender race birth workagencycode year
  ```
- Line 249: name
  ```
  rename filenum_n filenumber
  ```
- Line 250: district, name
  ```
  rename workagencycode district_code
  ```
- Line 253: birth, district, gender, name
  ```
  order id lastname firstname filenumber gender race birth district_code year ///
  ```
- Line 255: name
  ```
  label variable lastname "Last Name"
  ```
- Line 256: name
  ```
  label variable firstname "First Name"
  ```
- Line 258: gender
  ```
  label variable gender "Gndr"
  ```
- Line 260: birth
  ```
  label variable birth "Birth Year"
  ```
- Line 261: district
  ```
  label variable district_code "Work Agncy Cd"
  ```
- Line 262: loc
  ```
  local dta_ts_before "`c(current_date)' `=substr("`c(current_time)'", 1, 5)'"
  ```
- Line 264: loc
  ```
  local dta_ts_after "`c(current_date)' `=substr("`c(current_time)'", 1, 5)'"
  ```
- Line 272: loc
  ```
  local staff_temp_rows = _N
  ```
- Line 279: loc
  ```
  foreach yr of local staff_years_txt_old {
  ```
- Line 280: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 287: name
  ```
  (substr(lastname[`i'] + "                    ", 1, 20)) ///
  ```
- Line 288: name
  ```
  (substr(firstname[`i'] + "                ", 1, 16)) ///
  ```
- Line 289: birth, gender
  ```
  (gender[`i']) (race[`i']) %4.0f (birth[`i']) (highedu[`i'])
  ```
- Line 291: loc
  ```
  file write fw _column(66) %3.0f (localexp[`i']) %3.0f (totalexp[`i']) ///
  ```
- Line 296: school
  ```
  %4.0f (schoolcode[`i']) %2.0f (position[`i']) %4.0f (area[`i']) ///
  ```
- Line 300: name
  ```
  (substr(workagencyname[`i'] + "                              ", 1, 30)) ///
  ```
- Line 301: lname, name, school
  ```
  (substr(schoolname[`i'] + "                              ", 1, 30)) ///
  ```
- Line 304: zip
  ```
  file write fw _column(443) (substr(zipcode[`i'] + "          ", 1, 10))
  ```
- Line 311: loc
  ```
  foreach yr of local staff_years_txt_new {
  ```
- Line 312: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 319: name
  ```
  (substr(lastname[`i'] + "                    ", 1, 20)) ///
  ```
- Line 320: name
  ```
  (substr(firstname[`i'] + "                ", 1, 16)) ///
  ```
- Line 321: birth, gender
  ```
  (filenumber[`i']) (gender[`i']) (race[`i']) %4.0f (birth[`i']) ///
  ```
- Line 324: loc
  ```
  %4.0f (localexp[`i']) %3.0f (totalexp[`i']) ///
  ```
- Line 329: school
  ```
  %4.0f (schoolcode[`i']) %2.0f (position[`i']) %4.0f (area[`i']) ///
  ```
- Line 333: name
  ```
  (substr(workagencyname[`i'] + "                                            ", 1, 44)) ///
  ```
- Line 334: lname, name, school
  ```
  (substr(schoolname[`i'] + "                              ", 1, 30)) ///
  ```
- Line 337: zip
  ```
  file write fw _column(450) (substr(zipcode[`i'] + "          ", 1, 10))
  ```
- Line 348: loc
  ```
  foreach yr of local staff_years_csv {
  ```
- Line 349: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 353: lon
  ```
  gen long IDNbr = id
  ```
- Line 354: name
  ```
  gen str20 LastName = lastname
  ```
- Line 355: name
  ```
  gen str16 FirstName = firstname
  ```
- Line 358: gender
  ```
  gen str1 Gndr = gender
  ```
- Line 360: birth
  ```
  gen int BirthYear = birth
  ```
- Line 361: degree
  ```
  gen str1 HighDegree = highedu
  ```
- Line 362: degree
  ```
  replace HighDegree = "X" in 1
  ```
- Line 365: loc
  ```
  gen float LocalExp = localexp
  ```
- Line 367: lon
  ```
  gen long TotSalary = salary
  ```
- Line 369: lon
  ```
  gen long TotFringe = fringe
  ```
- Line 375: school
  ```
  gen str4 SchoolCd = string(schoolcode, "%04.0f")
  ```
- Line 386: loc, location, name
  ```
  gen str30 WorkLocationName = workagencyname
  ```
- Line 387: lname, name, school
  ```
  gen str30 SchoolName = schoolname
  ```
- Line 392: name
  ```
  gen str30 CntyName = "Synthetic County " + string(county, "%02.0f")
  ```
- Line 394: address, school
  ```
  gen str30 SchoolMailingAddress1 = "Synthetic Address"
  ```
- Line 395: address, school
  ```
  gen str30 SchoolMailingAddress2 = ""
  ```
- Line 396: address, school
  ```
  gen str30 SchoolShippingAddress1 = "Synthetic Address"
  ```
- Line 397: address, school
  ```
  gen str30 SchoolShippingAddress2 = ""
  ```
- Line 398: city
  ```
  gen str17 MailCity = "Synthetic City"
  ```
- Line 400: zip
  ```
  gen str10 MailZipCd = zipcode
  ```
- Line 401: zip
  ```
  replace MailZipCd = "ZIP" in 1
  ```
- Line 402: city
  ```
  gen str17 ShipCity = "Synthetic City"
  ```
- Line 404: zip
  ```
  gen str10 ShipZipCd = zipcode
  ```
- Line 405: zip
  ```
  replace ShipZipCd = "ZIP" in 1
  ```
- Line 406: phone
  ```
  gen str12 Phone = "000-000-0000"
  ```
- Line 407: name
  ```
  gen str30 AdminName = "Synthetic Admin"
  ```
- Line 412: birth, name
  ```
  keep IDNbr LastName FirstName FileNumber Gndr RaceEthn BirthYear ///
  ```
- Line 413: degree, loc
  ```
  HighDegree YearSession CntrctDays LocalExp TotalExp TotSalary ///
  ```
- Line 414: school
  ```
  TotFringe StaffCat HireAgncyCd WorkAgncyCd HireAgncyTyp SchoolCd ///
  ```
- Line 416: lname, loc, location, name, school
  ```
  AssgnFTE WorkLocationName SchoolName GrdLevel CESANumber CntyNbr ///
  ```
- Line 417: address, name, school
  ```
  CntyName WorkAgncyTyp SchoolMailingAddress1 SchoolMailingAddress2 ///
  ```
- Line 418: address, city, school
  ```
  SchoolShippingAddress1 SchoolShippingAddress2 MailCity MailSt ///
  ```
- Line 419: city, name, phone, zip
  ```
  MailZipCd ShipCity ShipSt ShipZipCd Phone AdminName FormerLastNm ///
  ```
- Line 430: birth, gender, name
  ```
  keep lastname firstname filenumber year race birth gender workagencycode id
  ```
- Line 432: last_name, name
  ```
  rename lastname last_name
  ```
- Line 433: first_name, name
  ```
  rename firstname first_name
  ```
- Line 435: birth
  ```
  gen str8 birth_year = string(birth, "%04.0f")
  ```
- Line 436: gender, sex
  ```
  gen str1 sex = gender
  ```
- Line 439: birth
  ```
  replace birth_year = "NA" in 1
  ```
- Line 441: birth, first_name, last_name, name, sex
  ```
  keep last_name first_name file_num year race birth_year sex distidworksite
  ```
- Line 442: birth, first_name, last_name, name, sex
  ```
  order last_name first_name file_num year race birth_year sex distidworksite
  ```
- Line 447: loc
  ```
  local linked_rows = _N
  ```
- Line 463: lon
  ```
  gen long STUDENT_KEY = 500000 + student_idx
  ```
- Line 467: school
  ```
  gen byte school_slot = mod(student_idx + `yr', `n_dist_codes') + 1
  ```
- Line 468: school
  ```
  gen int DIST_ACCTBL_CODE = real(word("`dist_codes'", school_slot))
  ```
- Line 469: school
  ```
  gen int schoolcode = 1000 + school_slot
  ```
- Line 473: school
  ```
  replace schoolcode = 1900 + va_student_cluster if va_student_cluster < .
  ```
- Line 474: school
  ```
  gen str9 SCH_ACCTBL_CODE = "SCH00" + string(schoolcode, "%04.0f")
  ```
- Line 475: school
  ```
  gen str9 SCHOOL_YEAR = string(year, "%04.0f") + "-" + ///
  ```
- Line 477: gender
  ```
  gen str1 GENDER = cond(mod(student_idx, 2) == 0, "F", "M")
  ```
- Line 478: city
  ```
  gen str12 RACE_ETHNICITY = "White"
  ```
- Line 479: city
  ```
  replace RACE_ETHNICITY = "Black" if mod(student_idx, 5) == 1
  ```
- Line 480: city
  ```
  replace RACE_ETHNICITY = "Hispanic" if mod(student_idx, 5) == 2
  ```
- Line 481: city
  ```
  replace RACE_ETHNICITY = "Asian" if mod(student_idx, 5) == 3
  ```
- Line 482: city
  ```
  replace RACE_ETHNICITY = "Amer Indian" if mod(student_idx, 5) == 4
  ```
- Line 486: school
  ```
  keep SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 487: city, gender
  ```
  GENDER RACE_ETHNICITY DISAB_STATUS EL_STATUS ECON_DISADVANTAGE
  ```
- Line 493: school
  ```
  order SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 494: city, gender
  ```
  GENDER RACE_ETHNICITY DISAB_STATUS EL_STATUS ECON_DISADVANTAGE
  ```
- Line 495: school
  ```
  sort STUDENT_KEY SCHOOL_YEAR
  ```
- Line 496: loc
  ```
  local dem_rows = _N
  ```
- Line 500: school
  ```
  gen int year = real(substr(SCHOOL_YEAR, 1, 4))
  ```
- Line 501: gender
  ```
  gen float Sfemale = GENDER == "F"
  ```
- Line 502: city
  ```
  gen float Sblack = RACE_ETHNICITY == "Black"
  ```
- Line 503: city
  ```
  gen float Shispanic = RACE_ETHNICITY == "Hispanic"
  ```
- Line 504: city
  ```
  gen float Sasian = RACE_ETHNICITY == "Asian"
  ```
- Line 505: city
  ```
  gen float Sam_ind = RACE_ETHNICITY == "Amer Indian"
  ```
- Line 506: name
  ```
  rename STUDENT_KEY student_key
  ```
- Line 507: name
  ```
  rename DISAB_STATUS disab_status
  ```
- Line 508: name
  ```
  rename EL_STATUS el_status
  ```
- Line 509: name
  ```
  rename ECON_DISADVANTAGE econ_disadvantage
  ```
- Line 518: loc
  ```
  local dta_ts_before "`c(current_date)' `=substr("`c(current_time)'", 1, 5)'"
  ```
- Line 520: loc
  ```
  local dta_ts_after "`c(current_date)' `=substr("`c(current_time)'", 1, 5)'"
  ```
- Line 532: school
  ```
  bysort STUDENT_KEY SCHOOL_YEAR: gen byte subj_n = _n
  ```
- Line 536: name
  ```
  gen str7 TEST_NAME = "SYNTEST"
  ```
- Line 537: school
  ```
  gen int base_year = real(substr(SCHOOL_YEAR, 1, 4))
  ```
- Line 554: school
  ```
  keep SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 555: name
  ```
  TEST_NAME TEST_SUBJECT TEST_SCALED_SCORE TEST_STDERR TEST_PROF_LVL
  ```
- Line 556: school
  ```
  order SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 557: name
  ```
  TEST_NAME TEST_SUBJECT TEST_SCALED_SCORE TEST_STDERR TEST_PROF_LVL
  ```
- Line 558: school
  ```
  sort STUDENT_KEY SCHOOL_YEAR TEST_SUBJECT
  ```
- Line 559: loc
  ```
  local testscores_rows = _N
  ```
- Line 568: lat
  ```
  file write mf "source_folder_number,relative_file_path,file_format,synthetic_rows,number_variables,b
  ```
- Line 569: loc
  ```
  foreach yr of local staff_years_txt_old {
  ```
- Line 570: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 571: district, school
  ```
  file write mf "1,raw/staff/`yy'staff.txt,txt,`staff_file_rows',34,staff district-school-position-yea
  ```
- Line 573: loc
  ```
  foreach yr of local staff_years_txt_new {
  ```
- Line 574: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 575: district, school
  ```
  file write mf "1,raw/staff/`yy'staff.txt,txt,`staff_file_rows',35,staff district-school-position-yea
  ```
- Line 577: loc
  ```
  foreach yr of local staff_years_csv {
  ```
- Line 578: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 579: district, school
  ```
  file write mf "1,raw/staff/`yy'staff.csv,csv,`staff_file_rows',49,staff district-school-position-yea
  ```
- Line 581: district
  ```
  file write mf "1,raw/staff/temp.dta,dta,`staff_temp_rows',11,staff-year,id filenumber year district_
  ```
- Line 584: school
  ```
  file write mf "2,raw/testscores_indlev/student_demographics.csv,csv,`dem_rows',10,student-year,STUDE
  ```
- Line 585: school
  ```
  file write mf "2,raw/testscores_indlev/testscores.csv,csv,`testscores_rows',10,student-subject-year,
  ```
- Line 592: district, school
  ```
  file write notes "- Unit: individual working in a district-school-position-year." _n
  ```
- Line 594: district, school, son
  ```
  file write notes "- About 90 percent of staff person-years have one district-school-position record;
  ```
- Line 595: district, school
  ```
  file write notes "- Position, assignment-area, district, and school codes are synthetic code-support
  ```
- Line 596: lat, loc
  ```
  file write notes "- Total and local experience generally increase mechanically within staff historie
  ```
- Line 599: school
  ```
  file write notes "- Synthetic school-grade-year clusters cover exactly 1 through 26 eligible teacher
  ```
- Line 602: district
  ```
  file write notes "- A small subset of those VA-cluster teachers enter in 2011 and move districts by 
  ```
- Line 604: district
  ```
  file write notes "- A small cohort of synthetic non-VA teachers changes districts across years so mo
  ```
- Line 605: school
  ```
  file write notes "- VA-supporting student cells span grades 3 through 6 within school-years, with lo
  ```
- Line 609: school
  ```
  file write notes "- Fake student IDs repeat across years for about 97 percent of students in consecu
  ```
- Line 610: school
  ```
  file write notes "- Demographic and test-score rows fully match on synthetic student ID and school y
  ```
- Line 611: district, lat, school
  ```
  file write notes "- Grade, subject, school, and district cells are populated so the standardization 
  ```
- Line 613: name
  ```
  file write notes "- The raw 2008-2010 staff files are written with lowercase filenames to match the 
  ```
- Line 614: gender
  ```
  file write notes "- Generic race, gender, subject, and proficiency labels are included only where th
  ```

**/replication-package/replication_delivery/synthetic_data/code/03_validate_synthetic_structure.do**

- Line 10: lat
  ```
  Public validation for simulated/synthetic raw files.
  ```
- Line 13: block, loc
  ```
  alignment, broad panel properties, and a smoke run of the required build block
  ```
- Line 28: loc
  ```
  local REPL_ROOT "$SYNTH_ROOT/.."
  ```
- Line 31: loc
  ```
  local REPL_ROOT "$REPL_ROOT"
  ```
- Line 34: loc
  ```
  local SMOKE_ROOT "`c(tmpdir)'/wisconsin_competition_synth_stage2"
  ```
- Line 35: loc
  ```
  local SMOKE_INT "`SMOKE_ROOT'/intermediate_data"
  ```
- Line 36: loc
  ```
  local SMOKE_OUT "`SMOKE_ROOT'/out_synth"
  ```
- Line 37: loc
  ```
  local SMOKE_EST "`SMOKE_ROOT'/model_data"
  ```
- Line 38: lat, loc
  ```
  local SMOKE_SIM "`SMOKE_ROOT'/simulation_data"
  ```
- Line 51: loc
  ```
  local yy = substr("`year'", 3, 2)
  ```
- Line 54: name
  ```
  str lastname        10  -   29  ///
  ```
- Line 55: name
  ```
  str firstname       30  -   45  ///
  ```
- Line 56: gender
  ```
  str gender          46  -   46  ///
  ```
- Line 58: birth
  ```
  birth           48  -   51  ///
  ```
- Line 61: loc
  ```
  localexp        66  -   68  ///
  ```
- Line 69: school
  ```
  schoolcode      111 -   114 ///
  ```
- Line 78: name
  ```
  str workagencyname  147 -   176 ///
  ```
- Line 79: lname, name, school
  ```
  str schoolname      177 -   206 ///
  ```
- Line 84: zip
  ```
  str zipcode         443 -   452 ///
  ```
- Line 89: loc
  ```
  local nvars : word count `r(varlist)'
  ```
- Line 101: loc
  ```
  local yy = substr("`year'", 3, 2)
  ```
- Line 104: name
  ```
  str lastname        11-30   ///
  ```
- Line 105: name
  ```
  str firstname       31-46   ///
  ```
- Line 107: gender
  ```
  str gender          53-53   ///
  ```
- Line 109: birth
  ```
  birth           55-58   ///
  ```
- Line 112: loc
  ```
  localexp        72-75   ///
  ```
- Line 120: school
  ```
  schoolcode      118-121 ///
  ```
- Line 129: name
  ```
  str workagencyname  140-183 ///
  ```
- Line 130: lname, name, school
  ```
  str schoolname      184-213 ///
  ```
- Line 135: zip
  ```
  str zipcode         450-459 ///
  ```
- Line 140: loc
  ```
  local nvars : word count `r(varlist)'
  ```
- Line 175: loc
  ```
  local actual_cols = c(k)
  ```
- Line 178: name
  ```
  rename v`j' urban`j'
  ```
- Line 193: loc
  ```
  local rural_cells = 0
  ```
- Line 197: loc
  ```
  local rural_cells = `rural_cells' + r(N)
  ```
- Line 248: loc
  ```
  local yy = substr("`yr'", 3, 2)
  ```
- Line 251: loc
  ```
  local nvars : word count `r(varlist)'
  ```
- Line 253: birth, name
  ```
  foreach v in IDNbr LastName FirstName FileNumber Gndr RaceEthn BirthYear ///
  ```
- Line 254: loc
  ```
  CntrctDays LocalExp TotalExp TotSalary TotFringe StaffCat ///
  ```
- Line 255: school
  ```
  HireAgncyCd WorkAgncyCd HireAgncyTyp SchoolCd PositionCd ///
  ```
- Line 290: lon
  ```
  gen long staff_idx = id - 100000
  ```
- Line 309: loc
  ```
  local both = r(N)
  ```
- Line 311: loc
  ```
  local prev = r(N)
  ```
- Line 317: birth, district, gender, name
  ```
  foreach v in id lastname firstname filenumber gender race birth district_code ///
  ```
- Line 337: school
  ```
  foreach v in SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 338: city, gender
  ```
  GENDER RACE_ETHNICITY DISAB_STATUS EL_STATUS ECON_DISADVANTAGE {
  ```
- Line 341: school
  ```
  isid STUDENT_KEY SCHOOL_YEAR
  ```
- Line 342: school
  ```
  gen int year = real(substr(SCHOOL_YEAR, 1, 4))
  ```
- Line 362: loc
  ```
  local both = r(N)
  ```
- Line 364: loc
  ```
  local prev = r(N)
  ```
- Line 370: school
  ```
  foreach v in SCHOOL_YEAR DIST_ACCTBL_CODE SCH_ACCTBL_CODE STUDENT_KEY GRADE ///
  ```
- Line 371: name
  ```
  TEST_NAME TEST_SUBJECT TEST_SCALED_SCORE TEST_STDERR TEST_PROF_LVL {
  ```
- Line 379: school
  ```
  bysort SCHOOL_YEAR GRADE TEST_SUBJECT: assert _N >= 2
  ```
- Line 382: block, loc
  ```
  * Smoke Run Required Build Block in a Temporary Intermediate Folder
  ```
- Line 387: block, loc
  ```
  display as error "Required package carryforward is not installed; cannot smoke-run build block."
  ```
- Line 421: lat
  ```
  do $code/build/valueadded_calculation.do
  ```
- Line 422: lat
  ```
  do $code/build/valueadded_calculation_classmodel.do
  ```
- Line 423: lat
  ```
  do $code/build/valueadded_calculation_stockstaiger_nocomparativeadv.do
  ```
- Line 428: district
  ```
  isid district_code
  ```
- Line 447: loc
  ```
  local nmatch : word count `r(varlist)'
  ```
- Line 496: district
  ```
  egen tag_dist = tag(District)
  ```
- Line 529: district
  ```
  egen tag_dist = tag(District)
  ```
- Line 539: loc
  ```
  local nmode : word count `r(varlist)'
  ```
- Line 555: district
  ```
  collapse Match wage d C0 C1 urban suburban, by(id District)
  ```
- Line 602: loc
  ```
  local actual_cols = c(k)
  ```
- Line 605: name
  ```
  rename v`j' urban`j'
  ```
- Line 620: loc
  ```
  local rural_cells = 0
  ```
- Line 624: loc
  ```
  local rural_cells = `rural_cells' + r(N)
  ```
- Line 642: loc
  ```
  local nmatch : word count `r(varlist)'
  ```
- Line 679: lat
  ```
  do $code/descriptives/summary_simulation.do
  ```
- Line 692: loc
  ```
  local delimiter comma
  ```
- Line 694: loc
  ```
  local delimiter tab
  ```
- Line 703: loc
  ```
  local aux_other_lines = 0
  ```
- Line 706: loc
  ```
  local aux_other_lines = `aux_other_lines' + 1
  ```
- Line 714: district
  ```
  do $code/auxiliary_regressions/auxiliary_models_districts.do
  ```

**/replication-package/replication_delivery/synthetic_data/code/make_simulated_data.do**

- Line 8: lat
  ```
  Public simulated-data generator.
  ```
- Line 16: lat
  ```
  do synthetic_data/code/make_simulated_data.do
  ```
- Line 20: lat
  ```
  do code/make_simulated_data.do
  ```
- Line 23: loc
  ```
  local pwd "`c(pwd)'"
  ```
- Line 56: lat
  ```
  display as text "Public simulated-data generation and validation complete."
  ```
- Line 57: lat
  ```
  display as text "Simulated raw inputs: $SYNTH_ROOT/raw"
  ```

